openapi: 3.0.0
servers:
  - url: https://art19.com
info:
  contact:
    email: support@art19.com
    name: ART19 Engineering Team
    x-twitter: art19co
  description: |
    The ART19 Content API conforms to the [JSON:API specification](http://jsonapi.org).

    API requests **MUST** use the HTTP Accept header:

    `Accept: application/vnd.api+json`

    API requests **MUST** be authenticated using the HTTP Authorization header:

    `Authorization: Token token="your-token", credential="your-credential"`

    ## General Notes

    Some query parameters use unencoded [ and ] characters simply for readability. Defaults, examples, and
    possible values are additionally rendered in double quotes for readability. In practice, query parameters should
    not have quotes around the values (e.g., `foo=bar` is valid, not `foo="bar"`), and both query parameter keys
    and values must be percent-encoded, per the requirements in [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).

    ## Rate Limiting

    In order to provide a fair distribution of available resources, all API calls are subject to rate limits.
    If you exceed the number of API calls per minute granted to your credential, a `429 Too Many Requests`
    error response will be returned.

    In that case, a `Retry-After` header MAY be included in the response, describing the number of seconds
    after which a request can be retried.

    If you run into a high number of 429 errors, please reach out to ART19 Support to adjust your rate limit.

    ### Example

    In the following example the request can be retried after waiting for 21 seconds:

        HTTP/1.1 429 Too Many Requests
        Content-Type: text/html
        Retry-After: 21

    ## Pagination

    Requests to collection endpoints **SHOULD** provide pagination parameters.
    Some endpoints **REQUIRE** pagination parameters to be provided.
    Whenever pagination is provided, it **MUST** be valid.
    Failing to provide pagination when it is required or providing wrong or incomplete pagination
    always results in a `400 Bad Request` error response.

    The page numbering starts with `1` and the maximum page size (if not otherwise documented
    on an endpoint) is `100`. Pagination **MUST NOT** be specified if requesting a list of IDs (using an `ids[]` parameter).

    Providing invalid values for page number or page size, as well as providing only a page number or only a page size,
    is considered an error. Pagination is provided like this:

    `page[number]=1&page[size]=25`

    Responses conform to the [JSON:API specification's pagination section](https://jsonapi.org/format/#fetching-pagination)
    by including pagination links. Your requested page size will be carried into the pagination links.

    ## Sorting

    Requests to collection endpoints usually accept a `sort` parameter. Please refer to the
    [JSON:API Specification's sorting section](https://jsonapi.org/format/#fetching-sorting) for further details.

    ## Relationship Linking

    Currently, resources return all of their relationships, in no particular order, pursuant to how relationships
    should be returned [according to the JSON:API specification](https://jsonapi.org/format/#document-resource-object-relationships). Consumers of this API
    **MUST NOT** make assumptions about the order of these collections. Even though this data is not currently paginated, consumers **MUST** support
    paginating relationships per the JSON:API specification if this data is important for their application.
  termsOfService: https://art19.com/business-terms
  title: ART19 Content API Documentation
  version: 1.0.0
  x-apisguru-categories:
    - media
  x-logo:
    url: https://api.apis.guru/v2/cache/logo/https_twitter.com_art19co_profile_image.jpeg
  x-origin:
    - format: openapi
      url: https://art19.com/swagger_json/external/content.json
      version: "3.0"
  x-providerName: art19.com
security:
  - api_key: []
paths:
  /classification_inclusions:
    get:
      description: |
        Classification Inclusions connect classifications with entities like series, episodes, or campaigns, amongst others.

        In order to retrieve a set of classification inclusions, at least one of the following filter parameters must
        be provided. Failing to do so renders a `400 Bad Request` response.

        - `ids[]`
        - `classified_id` and `classified_type`
        - `classified_id` and `classification_type`
        - `classification_id` and `classified_type`
      parameters:
        - description: |
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.
            The brackets *MUST* be percent-encoded, per the requirements in
            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: |+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: |+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: Limit the result to classification inclusions linked to a classification with this ID.
          example: a90609c0-9f0c-48fd-8a41-83eb8414bedb
          in: query
          name: classification_id
          schema:
            format: uuid
            type: string
        - description: |
            The type of classification that the classification inclusions are linked to.
            Depending on the permissions of your credential, only a subset of these types are usable.
          in: query
          name: classification_type
          schema:
            enum:
              - AlternateFeedType
              - Genre
              - Industry
              - Language
              - MediaRating
            type: string
        - description: |
            Limit the result to classification inclusions linked to a resource with this ID.
            You will have to use this filter together with either `classified_type` or `classification_type`.
          example: fee1976e-f1bc-48a9-9ee8-58800f4d3462
          in: query
          name: classified_id
          schema:
            format: uuid
            type: string
        - description: Limit the result to classification inclusions linked to this type of entity.
          in: query
          name: classified_type
          schema:
            enum:
              - Series
              - Season
              - Episode
            type: string
        - description: |
            Limit the result to classification inclusions linked to a classification containing
            the parameter value disregarding case.
          in: query
          name: q
          schema:
            type: string
        - description: |
            Specify how to sort the result. Please refer to either the top section or the
            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: created_at
            items:
              enum:
                - classification_value
                - created_at
                - position
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/ClassificationInclusion"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  errors:
                    description: |
                      An error object describing the problem. Please check out the
                      [JSON:API Error document reference](https://jsonapi.org/format/#error-objects) for a full
                      description of the returned object.
                    items:
                      properties:
                        code:
                          description: An application-specific error code, expressed as a string value
                          type: string
                        detail:
                          description: |
                            A human-readable explanation specific to this occurrence of the problem. Like title, this field’s value can be localized.
                          example: Required query parameter “series_id” is missing
                          type: string
                        source:
                          description: |
                            An object containing references to the source of the error, optionally including any of the following members.
                          nullable: true
                          properties:
                            parameter:
                              description: A string indicating which URI query parameter caused the error.
                              example: series_id
                              type: string
                            pointer:
                              description: |
                                A JSON Pointer [RFC6901](https://tools.ietf.org/html/rfc6901) to the associated entity in the request document (e.g. `/data` for a primary data object, or `/data/attributes/title` for a specific attribute).
                              example: /data/attributes/name
                              type: string
                          type: object
                        status:
                          description: "The HTTP status code applicable to this problem, expressed as a string value: 400"
                          example: "400"
                          type: string
                        title:
                          description: |
                            A short, human-readable summary of the problem that SHOULD NOT change from occurrence to occurrence of the
                            problem, except for purposes of localization.
                          example: Bad Request
                          type: string
                      type: object
                    type: array
                type: object
          description: |
            Bad Request

            Parameters requiring a specific format must adhere to that format.
            Pagination must have a positive page number and a page size no larger than 100. At least one filter parameter is required.
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get ClassificationInclusion records
      tags:
        - ClassificationInclusion
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/classification_inclusions",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="390087cf-b76d-47a0-a57d-cef05199824c"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/classification_inclusions?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="8c678eef-8c71-4405-85bc-5b76eb9e6451"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/classification_inclusions');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="d485d205-1888-468a-bcc7-63204f59442c"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/classification_inclusions?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="4b866157-04d6-414f-89bc-0fd22157c8c4"' \
              --compressed
  "/classification_inclusions/{id}":
    get:
      parameters:
        - description: The ID of the classification inclusion resource to load.
          example: d1d90de9-c143-42e7-9e00-eca75e984847
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/ClassificationInclusion"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a specific classification inclusion
      tags:
        - ClassificationInclusion
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/classification_inclusions/a640cf0d-582e-40d1-a0ce-99aca12387ad",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="7f5cb2aa-e1b7-49e1-8a75-b73e08c6db93"'
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/classification_inclusions/a640cf0d-582e-40d1-a0ce-99aca12387ad', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="5a85ca02-b02a-41ca-adbe-dc19ccd75dac"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/classification_inclusions/a640cf0d-582e-40d1-a0ce-99aca12387ad');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="5d754aff-89aa-41cd-8d8e-cf77937aa418"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/classification_inclusions/a640cf0d-582e-40d1-a0ce-99aca12387ad' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="f294168a-5198-408b-8041-6d11d842ae79"' \
              --compressed
  /classifications:
    get:
      parameters:
        - description: |
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.
            The brackets *MUST* be percent-encoded, per the requirements in
            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: |+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: |+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: |
            Limit the result to classifications of this type. The possible values for types
            depends on the privileges of the provided credential.
          example: Genre
          in: query
          name: type
          schema:
            enum:
              - AlternateFeedType
              - Genre
              - Industry
              - Language
              - MediaRating
            type: string
        - description: |
            Limit the result to classifications with a value containing the parameter value
            disregarding case.
          in: query
          name: q
          schema:
            type: string
        - description: |
            If this parameter is provided and not empty, limit the result to classifications
            of type `Geography` representing countries. The credential must have at least one
            privilege with a network, series, or advertiser profile.
          example: "true"
          in: query
          name: is_country
          schema:
            type: string
        - description: |
            Specify how to sort the result. Please refer to either the top section or the
            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: value
            items:
              enum:
                - created_at
                - updated_at
                - value
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Classification"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  errors:
                    description: |
                      An error object describing the problem. Please check out the
                      [JSON:API Error document reference](https://jsonapi.org/format/#error-objects) for a full
                      description of the returned object.
                    items:
                      properties:
                        code:
                          description: An application-specific error code, expressed as a string value
                          type: string
                        detail:
                          description: |
                            A human-readable explanation specific to this occurrence of the problem. Like title, this field’s value can be localized.
                          example: Required query parameter “series_id” is missing
                          type: string
                        source:
                          description: |
                            An object containing references to the source of the error, optionally including any of the following members.
                          nullable: true
                          properties:
                            parameter:
                              description: A string indicating which URI query parameter caused the error.
                              example: series_id
                              type: string
                            pointer:
                              description: |
                                A JSON Pointer [RFC6901](https://tools.ietf.org/html/rfc6901) to the associated entity in the request document (e.g. `/data` for a primary data object, or `/data/attributes/title` for a specific attribute).
                              example: /data/attributes/name
                              type: string
                          type: object
                        status:
                          description: "The HTTP status code applicable to this problem, expressed as a string value: 400"
                          example: "400"
                          type: string
                        title:
                          description: |
                            A short, human-readable summary of the problem that SHOULD NOT change from occurrence to occurrence of the
                            problem, except for purposes of localization.
                          example: Bad Request
                          type: string
                      type: object
                    type: array
                type: object
          description: |
            Bad Request

            Parameters requiring a specific format must adhere to that format.
            Pagination must have a positive page number and a page size no larger than 100. Filtering by type for AudienceSegment, AudienceTag, ListeningPlatform, and UserDevice is no longer supported.
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a list of classifications
      tags:
        - Classification
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/classifications",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="9c5a51f7-21f6-4f94-81fb-5e3e7cc2f12f"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/classifications?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="aa7710fd-f5aa-4287-bb3e-947589e6f1d0"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/classifications');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="4567e7f5-93ce-4d31-8131-9c7342e27de3"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/classifications?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="db9812ce-83d0-439c-9c4f-117f1a856ada"' \
              --compressed
  "/classifications/{id}":
    get:
      parameters:
        - description: The ID of the classification resource to load.
          example: 6fb61aa8-e221-4caf-b166-30b7ccda7114
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Classification"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a specific classification
      tags:
        - Classification
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/classifications/b39be1a9-b7c3-4c64-8cc6-3c5bd6bc86e7",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="eff18ee3-6886-47e6-97eb-bba23f83ebf8"'
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/classifications/b39be1a9-b7c3-4c64-8cc6-3c5bd6bc86e7', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="27de0bf8-fc55-4f79-972c-ed56b03abba0"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/classifications/b39be1a9-b7c3-4c64-8cc6-3c5bd6bc86e7');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="3c7ac501-3b8d-4f09-a85f-0a7afe46d710"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/classifications/b39be1a9-b7c3-4c64-8cc6-3c5bd6bc86e7' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="1d2fb50f-86f5-484e-bd24-8472ed9cc456"' \
              --compressed
  /credits:
    get:
      parameters:
        - description: |
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.
            The brackets *MUST* be percent-encoded, per the requirements in
            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: |+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: |+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: |
            Limit the result to credits for the entity with this ID only. If the entity is not
            published, the credential needs to have the right privilege to list the credits for it.
          example: 76654e16-76ce-4945-92e9-e0a381917853
          in: query
          name: creditable_id
          schema:
            format: uuid
            type: string
        - description: |
            Limit the result to credits linked to this type of entity only.
            Depending on the privileges of the credential, this list might be further reduced to
            only published entities.
          in: query
          name: creditable_type
          schema:
            enum:
              - Series
              - Season
              - Episode
            type: string
        - description: |
            Specify how to sort the result. Please refer to either the top section or the
            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: position
            items:
              enum:
                - created_at
                - position
                - updated_at
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Credit"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  errors:
                    description: |
                      An error object describing the problem. Please check out the
                      [JSON:API Error document reference](https://jsonapi.org/format/#error-objects) for a full
                      description of the returned object.
                    items:
                      properties:
                        code:
                          description: An application-specific error code, expressed as a string value
                          type: string
                        detail:
                          description: |
                            A human-readable explanation specific to this occurrence of the problem. Like title, this field’s value can be localized.
                          example: Required query parameter “series_id” is missing
                          type: string
                        source:
                          description: |
                            An object containing references to the source of the error, optionally including any of the following members.
                          nullable: true
                          properties:
                            parameter:
                              description: A string indicating which URI query parameter caused the error.
                              example: series_id
                              type: string
                            pointer:
                              description: |
                                A JSON Pointer [RFC6901](https://tools.ietf.org/html/rfc6901) to the associated entity in the request document (e.g. `/data` for a primary data object, or `/data/attributes/title` for a specific attribute).
                              example: /data/attributes/name
                              type: string
                          type: object
                        status:
                          description: "The HTTP status code applicable to this problem, expressed as a string value: 400"
                          example: "400"
                          type: string
                        title:
                          description: |
                            A short, human-readable summary of the problem that SHOULD NOT change from occurrence to occurrence of the
                            problem, except for purposes of localization.
                          example: Bad Request
                          type: string
                      type: object
                    type: array
                type: object
          description: |
            Bad Request

            Parameters requiring a specific format must adhere to that format.
            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a list of credits
      tags:
        - Credit
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/credits",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="3bd3f88e-fba4-4a73-a2c5-731321843136"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/credits?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="211f169e-cae9-43ea-9c1e-5c0c2ffda295"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/credits');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="fa68535a-f792-4bae-ad9a-c5b054cc74d6"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/credits?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="9d05ac66-cd62-4878-8c36-ced1f920981b"' \
              --compressed
  "/credits/{id}":
    get:
      parameters:
        - description: The ID of the credit resource to load.
          example: e345d7b6-cc47-437c-b06e-032a04a2c8cf
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Credit"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a specific credit
      tags:
        - Credit
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/credits/df6147ef-51a8-45e9-a910-7f646d87eb5c",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="e3c4c74d-e30a-4483-9a80-b3a388c95fe9"'
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/credits/df6147ef-51a8-45e9-a910-7f646d87eb5c', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="b4aa295b-e290-4460-ae5c-ca0c3974c161"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/credits/df6147ef-51a8-45e9-a910-7f646d87eb5c');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="948ae066-4773-44ab-a2bc-5d006df9feea"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/credits/df6147ef-51a8-45e9-a910-7f646d87eb5c' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="b22e22ad-5553-4c35-9a53-18cc1443e918"' \
              --compressed
  /episodes:
    get:
      description: |
        One initial filter must be provided (`ids`, `series_id`, or `season_id`), otherwise a `400 Bad Request` response
        will be returned. Additional filters are allowed.

        This API will only return episodes that your credential has permission to access, which may not be exclusive to
        your account, depending on the filter(s) being used. Be careful to filter the results as needed.
      parameters:
        - description: |
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.
            The brackets *MUST* be percent-encoded, per the requirements in
            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: |+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          required: true
          schema:
            minimum: 1
            type: integer
        - description: |+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          required: true
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: Limit the result to episodes usable with an inventory management calendar for the provided AdDeal ID.
          example: b97239ef-1776-489a-a1df-5f70a8407148
          in: query
          name: included_in_inventory_calendar
          schema:
            format: uuid
            type: string
        - description: Limit the result to episodes released in this month.
          in: query
          name: month
          schema:
            type: string
        - description: |
            If `true`, limit the result to published episodes only.
            If `false`, limit the result to _unpublished_ episodes only.
          in: query
          name: published
          schema:
            type: boolean
        - description: Limit the result to episodes with a title containing this parameter in a case-insensitive way.
          in: query
          name: q
          schema:
            type: string
        - description: Limit the result to episodes released after this timestamp.
          in: query
          name: released_after
          schema:
            format: date-time
            type: string
        - description: Limit the result to episodes released before this timestamp.
          in: query
          name: released_before
          schema:
            format: date-time
            type: string
        - description: |
            If `true`, limit the result to published and released episodes holding an available media file.
            If this is `true`, it also changes the default sort order to `-released_at`.
          in: query
          name: rss
          schema:
            type: boolean
        - description: Limit result to episodes linked to this season.
          example: 30e7b568-d099-486b-9a8e-51b7594cab7d
          in: query
          name: season_id
          schema:
            format: uuid
            type: string
        - description: Limit the result to episodes linked to this series.
          example: d0ceb1ff-b95b-4c1e-a7e5-a36ed7b4d3b7
          in: query
          name: series_id
          schema:
            format: uuid
            type: string
        - description: |
            Specify how to sort the result. Please refer to either the top section or the
            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: sort_title
            items:
              enum:
                - created_at
                - earliest_released_at
                - released_at
                - released_or_created_at
                - sort_title
                - title
                - updated_at
              type: string
            type: array
          style: form
        - description: Limit the result to episodes released in this year.
          in: query
          name: year
          schema:
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Episode"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  errors:
                    description: |
                      An error object describing the problem. Please check out the
                      [JSON:API Error document reference](https://jsonapi.org/format/#error-objects) for a full
                      description of the returned object.
                    items:
                      properties:
                        code:
                          description: An application-specific error code, expressed as a string value
                          type: string
                        detail:
                          description: |
                            A human-readable explanation specific to this occurrence of the problem. Like title, this field’s value can be localized.
                          example: Required query parameter “series_id” is missing
                          type: string
                        source:
                          description: |
                            An object containing references to the source of the error, optionally including any of the following members.
                          nullable: true
                          properties:
                            parameter:
                              description: A string indicating which URI query parameter caused the error.
                              example: series_id
                              type: string
                            pointer:
                              description: |
                                A JSON Pointer [RFC6901](https://tools.ietf.org/html/rfc6901) to the associated entity in the request document (e.g. `/data` for a primary data object, or `/data/attributes/title` for a specific attribute).
                              example: /data/attributes/name
                              type: string
                          type: object
                        status:
                          description: "The HTTP status code applicable to this problem, expressed as a string value: 400"
                          example: "400"
                          type: string
                        title:
                          description: |
                            A short, human-readable summary of the problem that SHOULD NOT change from occurrence to occurrence of the
                            problem, except for purposes of localization.
                          example: Bad Request
                          type: string
                      type: object
                    type: array
                type: object
          description: |
            Bad Request

            Parameters requiring a specific format must adhere to that format.
            Pagination must be present and have a positive page number and a page size no larger than 100. One initial filter must be provided (`ids`, `series_id`, or `season_id`).
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a list of episodes
      tags:
        - Episode
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/episodes",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="799e1415-2783-4751-b1a7-1b5b98bc2e63"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/episodes?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="d6a2e179-f982-49bc-876a-e417c2e78499"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/episodes');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="9f24a5f4-e0a9-447c-b552-7faa0cba08cf"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/episodes?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="38b3bf39-92e3-440d-9c23-ddb1499f2c25"' \
              --compressed
  "/episodes/{id}":
    get:
      parameters:
        - description: The ID of the episode resource to load.
          example: 00fb9e1b-88b3-43e5-a974-28551e8e4ca8
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Episode"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a specific episode
      tags:
        - Episode
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/episodes/203ad0de-c391-4f1b-9c03-6705611b1114",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="b18e997c-996a-41a6-9129-3bd52fd35af4"'
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/episodes/203ad0de-c391-4f1b-9c03-6705611b1114', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="6d8b0c23-e0b9-4447-a5d7-77085dd4dfb1"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/episodes/203ad0de-c391-4f1b-9c03-6705611b1114');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="fd6d266a-a5b6-4983-9fb9-4842be856ca9"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/episodes/203ad0de-c391-4f1b-9c03-6705611b1114' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="94dccd04-27b3-48b8-8cc7-8f2c60b08072"' \
              --compressed
  "/episodes/{id}/next_sibling":
    get:
      parameters:
        - description: The ID of the episode to find the one episode released right after it.
          example: e9ac30cf-db68-4629-ac87-df8ba85d41ad
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
        - description: |
            If `true`, get the next released and published episode with an available media file.
          in: query
          name: rss
          schema:
            type: boolean
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Episode"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get the episode released right after the specified one
      tags:
        - Episode
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/episodes/33fbf42b-221a-4744-8de5-bb3903c976fb/next_sibling",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="b00a5c9b-78f1-4b4b-a121-13b3be05003b"'
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/episodes/33fbf42b-221a-4744-8de5-bb3903c976fb/next_sibling', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="b14df393-573f-4088-abce-6cfadc776cbd"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/episodes/33fbf42b-221a-4744-8de5-bb3903c976fb/next_sibling');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="c40de9e8-cc98-45d5-8809-c8a890eee8a9"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/episodes/33fbf42b-221a-4744-8de5-bb3903c976fb/next_sibling' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="b6b7c79b-7e3b-495c-a720-cd58f7ec1875"' \
              --compressed
  "/episodes/{id}/previous_sibling":
    get:
      parameters:
        - description: The ID of the episode to find the one episode released right before it.
          example: 7a8006b1-30d9-4d54-9484-e22532c298f4
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
        - description: |
            If `true`, get the previously released and published episode with an available media file.
          in: query
          name: rss
          schema:
            type: boolean
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Episode"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get the episode released right before the specified one
      tags:
        - Episode
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/episodes/49c57fbf-b0f3-4a57-ac47-87f7218a9ac7/previous_sibling",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="de6f1a55-35ba-48ba-919a-dbdd6c1d075a"'
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/episodes/49c57fbf-b0f3-4a57-ac47-87f7218a9ac7/previous_sibling', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="fe973310-932d-4f58-bfe2-9961933feda4"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/episodes/49c57fbf-b0f3-4a57-ac47-87f7218a9ac7/previous_sibling');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="1b5a85bf-ec7d-4b05-983e-06e01d732c60"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/episodes/49c57fbf-b0f3-4a57-ac47-87f7218a9ac7/previous_sibling' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="14f87d2e-6dfc-41e6-934a-86cfd4da7aaf"' \
              --compressed
  /images:
    get:
      description: |+
        An image represents a piece of artwork attached to some entity like a series, season, or episode,
        and is _owned_ by an entity called the `bucket`.
        An image is also a container for several `MediaAssets` representing the physical files for various
        styles used.

        ## Media Asset Styles for Images

        Most media assets use square images. You may upload and use a square image, or upload an image of any
        shape and crop it to a square by specifying the cropping area. This area – identified by a coordinate
        `x, y` and a `width` & `height` – is the portion of the image used for all cover art. If an image has
        cropping defined, the cropped version of the image will be used in any `regular` or `square` style of media
        asset. If the original file is rectangular and does not have cropping, then the system will use a squared
        version of the original file with the smaller of width or height as the square size.

        The original image as uploaded into the system is always retained unmodified and available through
        the style `original`. All media asset styles except `stripped-original` consist of the cropped image.

        An image has media assets with the following styles:

        - `original`: This is the original file provided. May not be available, depending on permissions
          and file type.
        - `stripped-original`: The original file with all metadata (EXIF, XMP, PNG metadata, etc.) removed.
          This should be used for any application needing the original, uncropped, image.
        - `regular`: If the image has cropping defined, this is the cropped image.
          If not, this is a square version of the original.
        - `thumb`: A square thumbnail of the image with a size of `100x100` pixels.
        - `square-400`: A square version of the image with a size of `400x400` pixels.
        - `square-640`: A square version of the image with a size of `640x640` pixels.
        - `square-888`: A square version of the image with a size of `888x888` pixels.
        - `square-3000`: A square version of the image with a size of `3000x3000` pixels.
          This variant is only created if the cropped width & height are each at least 3000.
        - `itunes`: A square version of the image with a size of `1400x1400` pixels.

        ### Preferred image used in feeds

        For the main series image used in feeds, it is ideal to use the `square-3000` version.
        If that is not available, the `itunes` version should be used instead.

      parameters:
        - description: |
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.
            The brackets *MUST* be percent-encoded, per the requirements in
            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: true
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Image"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  errors:
                    description: |
                      An error object describing the problem. Please check out the
                      [JSON:API Error document reference](https://jsonapi.org/format/#error-objects) for a full
                      description of the returned object.
                    items:
                      properties:
                        code:
                          description: An application-specific error code, expressed as a string value
                          type: string
                        detail:
                          description: |
                            A human-readable explanation specific to this occurrence of the problem. Like title, this field’s value can be localized.
                          example: Required query parameter “series_id” is missing
                          type: string
                        source:
                          description: |
                            An object containing references to the source of the error, optionally including any of the following members.
                          nullable: true
                          properties:
                            parameter:
                              description: A string indicating which URI query parameter caused the error.
                              example: series_id
                              type: string
                            pointer:
                              description: |
                                A JSON Pointer [RFC6901](https://tools.ietf.org/html/rfc6901) to the associated entity in the request document (e.g. `/data` for a primary data object, or `/data/attributes/title` for a specific attribute).
                              example: /data/attributes/name
                              type: string
                          type: object
                        status:
                          description: "The HTTP status code applicable to this problem, expressed as a string value: 400"
                          example: "400"
                          type: string
                        title:
                          description: |
                            A short, human-readable summary of the problem that SHOULD NOT change from occurrence to occurrence of the
                            problem, except for purposes of localization.
                          example: Bad Request
                          type: string
                      type: object
                    type: array
                type: object
          description: |
            Bad Request

            Parameters requiring a specific format must adhere to that format.
            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a list of images
      tags:
        - Image
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/images",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="728b506a-a113-4b99-936f-2be4df9d202e"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/images?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="b1b7afcf-d66c-4031-8803-c285600a7164"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/images');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="7571026f-ef81-4d34-8bca-1637ee3f4ffc"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/images?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="fc984163-d766-416a-b24b-bc9ae295cf41"' \
              --compressed
  "/images/{id}":
    get:
      description: |+
        An image represents a piece of artwork attached to some entity like a series, season, or episode,
        and is _owned_ by an entity called the `bucket`.
        An image is also a container for several `MediaAssets` representing the physical files for various
        styles used.

        ## Media Asset Styles for Images

        Most media assets use square images. You may upload and use a square image, or upload an image of any
        shape and crop it to a square by specifying the cropping area. This area – identified by a coordinate
        `x, y` and a `width` & `height` – is the portion of the image used for all cover art. If an image has
        cropping defined, the cropped version of the image will be used in any `regular` or `square` style of media
        asset. If the original file is rectangular and does not have cropping, then the system will use a squared
        version of the original file with the smaller of width or height as the square size.

        The original image as uploaded into the system is always retained unmodified and available through
        the style `original`. All media asset styles except `stripped-original` consist of the cropped image.

        An image has media assets with the following styles:

        - `original`: This is the original file provided. May not be available, depending on permissions
          and file type.
        - `stripped-original`: The original file with all metadata (EXIF, XMP, PNG metadata, etc.) removed.
          This should be used for any application needing the original, uncropped, image.
        - `regular`: If the image has cropping defined, this is the cropped image.
          If not, this is a square version of the original.
        - `thumb`: A square thumbnail of the image with a size of `100x100` pixels.
        - `square-400`: A square version of the image with a size of `400x400` pixels.
        - `square-640`: A square version of the image with a size of `640x640` pixels.
        - `square-888`: A square version of the image with a size of `888x888` pixels.
        - `square-3000`: A square version of the image with a size of `3000x3000` pixels.
          This variant is only created if the cropped width & height are each at least 3000.
        - `itunes`: A square version of the image with a size of `1400x1400` pixels.

        ### Preferred image used in feeds

        For the main series image used in feeds, it is ideal to use the `square-3000` version.
        If that is not available, the `itunes` version should be used instead.

      parameters:
        - description: The ID of the image resource to load.
          example: e63cb0b7-2a08-4cd9-bee7-cf34f54c1ce2
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Image"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a specific image
      tags:
        - Image
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/images/9df97f05-4506-49ef-943f-5864d085da8b",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="59382995-3c58-4f28-a52e-dab3da78dcae"'
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/images/9df97f05-4506-49ef-943f-5864d085da8b', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="ca57d0b7-0793-4b54-9df6-5100c2f985d1"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/images/9df97f05-4506-49ef-943f-5864d085da8b');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="e2092181-e8ca-4eae-8404-043888861416"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/images/9df97f05-4506-49ef-943f-5864d085da8b' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="365d8559-ff68-4e7c-bf32-3524b4ec979b"' \
              --compressed
  /media_assets:
    get:
      description: |+
        A media asset is part of a collection of assets or files representing an image or a piece of audio content
        like an episode or an ad. Images, for example, have differently sized versions for better rendering performance,
        and a piece of audio content usually comes in different encoding formats.

        The `style` attribute of a media asset describes the role an asset plays in the context of the collection.
        The collection is what an asset is attached to (`attachment_id|type`).

        ## Styles for Images

        Most media assets use square images. You may upload and use a square image, or upload an image of any
        shape and crop it to a square by specifying the cropping area. This area – identified by a coordinate
        `x, y` and a `width` & `height` – is the portion of the image used for all cover art. If an image has
        cropping defined, the cropped version of the image will be used in any `regular` or `square` style of media
        asset. If the original file is rectangular and does not have cropping, then the system will use a squared
        version of the original file with the smaller of width or height as the square size.

        The original image as uploaded into the system is always retained unmodified and available through
        the style `original`. All media asset styles except `stripped-original` consist of the cropped image.

        An image has media assets with the following styles:

        - `original`: This is the original file provided. May not be available, depending on permissions
          and file type.
        - `stripped-original`: The original file with all metadata (EXIF, XMP, PNG metadata, etc.) removed.
          This should be used for any application needing the original, uncropped, image.
        - `regular`: If the image has cropping defined, this is the cropped image.
          If not, this is a square version of the original.
        - `thumb`: A square thumbnail of the image with a size of `100x100` pixels.
        - `square-400`: A square version of the image with a size of `400x400` pixels.
        - `square-640`: A square version of the image with a size of `640x640` pixels.
        - `square-888`: A square version of the image with a size of `888x888` pixels.
        - `square-3000`: A square version of the image with a size of `3000x3000` pixels.
          This variant is only created if the cropped width & height are each at least 3000.
        - `itunes`: A square version of the image with a size of `1400x1400` pixels.

        ### Preferred image used in feeds

        For the main series image used in feeds, it is ideal to use the `square-3000` version.
        If that is not available, the `itunes` version should be used instead.


        ## Styles for Audio

        - `medium`: A medium-quality version of the audio asset in various formats.
        - `original`: This is the original file provided. May not be available, depending on permissions
          and file type.
        - `waveform_data`: The generated BBC Audiowaveform data in JSON or binary format.

      parameters:
        - description: |
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.
            The brackets *MUST* be percent-encoded, per the requirements in
            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: true
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/MediaAsset"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  errors:
                    description: |
                      An error object describing the problem. Please check out the
                      [JSON:API Error document reference](https://jsonapi.org/format/#error-objects) for a full
                      description of the returned object.
                    items:
                      properties:
                        code:
                          description: An application-specific error code, expressed as a string value
                          type: string
                        detail:
                          description: |
                            A human-readable explanation specific to this occurrence of the problem. Like title, this field’s value can be localized.
                          example: Required query parameter “series_id” is missing
                          type: string
                        source:
                          description: |
                            An object containing references to the source of the error, optionally including any of the following members.
                          nullable: true
                          properties:
                            parameter:
                              description: A string indicating which URI query parameter caused the error.
                              example: series_id
                              type: string
                            pointer:
                              description: |
                                A JSON Pointer [RFC6901](https://tools.ietf.org/html/rfc6901) to the associated entity in the request document (e.g. `/data` for a primary data object, or `/data/attributes/title` for a specific attribute).
                              example: /data/attributes/name
                              type: string
                          type: object
                        status:
                          description: "The HTTP status code applicable to this problem, expressed as a string value: 400"
                          example: "400"
                          type: string
                        title:
                          description: |
                            A short, human-readable summary of the problem that SHOULD NOT change from occurrence to occurrence of the
                            problem, except for purposes of localization.
                          example: Bad Request
                          type: string
                      type: object
                    type: array
                type: object
          description: |
            Bad Request

            Parameters requiring a specific format must adhere to that format.
            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a list of media assets
      tags:
        - MediaAsset
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/media_assets",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="698d4e31-2c7b-4227-949c-84204b516d02"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/media_assets?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="12c2b102-2149-47b1-82b7-930410f6264e"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/media_assets');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="98006cb7-e84b-4769-905a-b01edc343ff4"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/media_assets?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="1928c383-a24c-49dd-badd-f1657393832c"' \
              --compressed
  "/media_assets/{id}":
    get:
      description: |+
        A media asset is part of a collection of assets or files representing an image or a piece of audio content
        like an episode or an ad. Images, for example, have differently sized versions for better rendering performance,
        and a piece of audio content usually comes in different encoding formats.

        The `style` attribute of a media asset describes the role an asset plays in the context of the collection.
        The collection is what an asset is attached to (`attachment_id|type`).

        ## Styles for Images

        Most media assets use square images. You may upload and use a square image, or upload an image of any
        shape and crop it to a square by specifying the cropping area. This area – identified by a coordinate
        `x, y` and a `width` & `height` – is the portion of the image used for all cover art. If an image has
        cropping defined, the cropped version of the image will be used in any `regular` or `square` style of media
        asset. If the original file is rectangular and does not have cropping, then the system will use a squared
        version of the original file with the smaller of width or height as the square size.

        The original image as uploaded into the system is always retained unmodified and available through
        the style `original`. All media asset styles except `stripped-original` consist of the cropped image.

        An image has media assets with the following styles:

        - `original`: This is the original file provided. May not be available, depending on permissions
          and file type.
        - `stripped-original`: The original file with all metadata (EXIF, XMP, PNG metadata, etc.) removed.
          This should be used for any application needing the original, uncropped, image.
        - `regular`: If the image has cropping defined, this is the cropped image.
          If not, this is a square version of the original.
        - `thumb`: A square thumbnail of the image with a size of `100x100` pixels.
        - `square-400`: A square version of the image with a size of `400x400` pixels.
        - `square-640`: A square version of the image with a size of `640x640` pixels.
        - `square-888`: A square version of the image with a size of `888x888` pixels.
        - `square-3000`: A square version of the image with a size of `3000x3000` pixels.
          This variant is only created if the cropped width & height are each at least 3000.
        - `itunes`: A square version of the image with a size of `1400x1400` pixels.

        ### Preferred image used in feeds

        For the main series image used in feeds, it is ideal to use the `square-3000` version.
        If that is not available, the `itunes` version should be used instead.


        ## Styles for Audio

        - `medium`: A medium-quality version of the audio asset in various formats.
        - `original`: This is the original file provided. May not be available, depending on permissions
          and file type.
        - `waveform_data`: The generated BBC Audiowaveform data in JSON or binary format.

      parameters:
        - description: The ID of the media asset resource to load.
          example: 80803e71-1ebf-4a66-ac69-662eac0039b8
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/MediaAsset"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a specific media asset
      tags:
        - MediaAsset
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/media_assets/a4696094-e2fb-45a6-b07e-6e130cb7ad1d",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="ddc1c63e-0430-449d-af8f-e29ca8dce16a"'
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/media_assets/a4696094-e2fb-45a6-b07e-6e130cb7ad1d', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="0683a832-c0c4-49f5-892b-7f248102135c"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/media_assets/a4696094-e2fb-45a6-b07e-6e130cb7ad1d');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="9c812aed-aa68-4f26-92b4-3480869d191d"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/media_assets/a4696094-e2fb-45a6-b07e-6e130cb7ad1d' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="48acb3ab-ef10-4a97-8ac2-c819b09003b2"' \
              --compressed
  /networks:
    get:
      description: |
        ### Deprecations

        - The attribute `cover_image_id` has been replaced with the relationship `cover_image`
          and will be removed from the response in a future release.
      parameters:
        - description: |
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.
            The brackets *MUST* be percent-encoded, per the requirements in
            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: |+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: |+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: Limit the result to networks with a name containing this parameter in a case-insensitive way.
          in: query
          name: q
          schema:
            type: string
        - description: |
            Limit the result to networks containing at least one series with an ad deal of this Ad Ops profile.
          in: query
          name: ad_rep_account_id
          schema:
            format: uuid
            type: string
        - description: |
            Limit the result to networks containing at least one series with an ad deal matching one of the
            values in this parameter.
          explode: false
          in: query
          name: ad_deal_status
          schema:
            items:
              enum:
                - active
                - inactive
                - pending_active
                - pending_inactive
              type: string
            type: array
          style: form
        - description: |
            Specify how to sort the result. Please refer to either the top section or the
            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: name
            items:
              enum:
                - created_at
                - name
                - updated_at
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Network"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  errors:
                    description: |
                      An error object describing the problem. Please check out the
                      [JSON:API Error document reference](https://jsonapi.org/format/#error-objects) for a full
                      description of the returned object.
                    items:
                      properties:
                        code:
                          description: An application-specific error code, expressed as a string value
                          type: string
                        detail:
                          description: |
                            A human-readable explanation specific to this occurrence of the problem. Like title, this field’s value can be localized.
                          example: Required query parameter “series_id” is missing
                          type: string
                        source:
                          description: |
                            An object containing references to the source of the error, optionally including any of the following members.
                          nullable: true
                          properties:
                            parameter:
                              description: A string indicating which URI query parameter caused the error.
                              example: series_id
                              type: string
                            pointer:
                              description: |
                                A JSON Pointer [RFC6901](https://tools.ietf.org/html/rfc6901) to the associated entity in the request document (e.g. `/data` for a primary data object, or `/data/attributes/title` for a specific attribute).
                              example: /data/attributes/name
                              type: string
                          type: object
                        status:
                          description: "The HTTP status code applicable to this problem, expressed as a string value: 400"
                          example: "400"
                          type: string
                        title:
                          description: |
                            A short, human-readable summary of the problem that SHOULD NOT change from occurrence to occurrence of the
                            problem, except for purposes of localization.
                          example: Bad Request
                          type: string
                      type: object
                    type: array
                type: object
          description: |
            Bad Request

            Parameters requiring a specific format must adhere to that format.
            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a list of networks
      tags:
        - Network
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/networks",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="6298b4b3-46ec-4144-a04b-3ee1e9efdd9b"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/networks?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="e63dbb28-be79-4cd4-aeb2-faf6d8dfb2c1"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/networks');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="d2d51a92-5b6e-465e-8889-40ca05430655"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/networks?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="779c66b3-69a6-4b2a-8b81-1b51fa322d4d"' \
              --compressed
  "/networks/{id}":
    get:
      description: |
        ### Deprecations

        - The attribute `cover_image_id` has been replaced with the relationship `cover_image`
          and will be removed from the response in a future release.
      parameters:
        - description: The ID of the network resource to load.
          example: 04f32378-5ad0-422b-a271-c98f81a9e3fb
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Network"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a specific network
      tags:
        - Network
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/networks/d18c1895-6ef9-4bf8-95e7-09f2343ea08f",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="0eaa91c3-0f79-4286-a629-3580d9c7ceed"'
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/networks/d18c1895-6ef9-4bf8-95e7-09f2343ea08f', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="d1d8ec19-292b-47d8-b62c-a61c90391dcb"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/networks/d18c1895-6ef9-4bf8-95e7-09f2343ea08f');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="49bb9da7-9271-47c3-8f0b-8debe21cf41d"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/networks/d18c1895-6ef9-4bf8-95e7-09f2343ea08f' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="8a788a67-66ad-4807-ac88-785ebc2b16ef"' \
              --compressed
  /people:
    get:
      description: |
        Each series, season, and episode has a Credits section where you may add people and roles.
        This is an internal tool to recognize contributors. It is not related to ART19 users or account permissions.
        Each Person added will have no additional access or permissions granted as a result of being included in the Credits section.
      parameters:
        - description: |
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.
            The brackets *MUST* be percent-encoded, per the requirements in
            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: |+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: |+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: Limit the result to people with a full name containing this parameter in a case-insensitive way.
          in: query
          name: q
          schema:
            type: string
        - description: |
            Specify how to sort the result. Please refer to either the top section or the
            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: last_name,first_name
            items:
              enum:
                - created_at
                - first_name
                - last_name
                - updated_at
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Person"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  errors:
                    description: |
                      An error object describing the problem. Please check out the
                      [JSON:API Error document reference](https://jsonapi.org/format/#error-objects) for a full
                      description of the returned object.
                    items:
                      properties:
                        code:
                          description: An application-specific error code, expressed as a string value
                          type: string
                        detail:
                          description: |
                            A human-readable explanation specific to this occurrence of the problem. Like title, this field’s value can be localized.
                          example: Required query parameter “series_id” is missing
                          type: string
                        source:
                          description: |
                            An object containing references to the source of the error, optionally including any of the following members.
                          nullable: true
                          properties:
                            parameter:
                              description: A string indicating which URI query parameter caused the error.
                              example: series_id
                              type: string
                            pointer:
                              description: |
                                A JSON Pointer [RFC6901](https://tools.ietf.org/html/rfc6901) to the associated entity in the request document (e.g. `/data` for a primary data object, or `/data/attributes/title` for a specific attribute).
                              example: /data/attributes/name
                              type: string
                          type: object
                        status:
                          description: "The HTTP status code applicable to this problem, expressed as a string value: 400"
                          example: "400"
                          type: string
                        title:
                          description: |
                            A short, human-readable summary of the problem that SHOULD NOT change from occurrence to occurrence of the
                            problem, except for purposes of localization.
                          example: Bad Request
                          type: string
                      type: object
                    type: array
                type: object
          description: |
            Bad Request

            Parameters requiring a specific format must adhere to that format.
            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a list of people
      tags:
        - Person
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/people",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="387c645b-bb1b-4d39-a128-1ddf9ffccec5"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/people?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="64a22a4d-870c-44e0-a14b-1dc395f91bf2"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/people');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="d8849e21-05d6-4021-b52f-c61288a4d5b0"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/people?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="9a776d31-1ff3-482a-a2b9-4cd8ea10a850"' \
              --compressed
  "/people/{id}":
    get:
      description: |
        Each series, season, and episode has a Credits section where you may add people and roles.
        This is an internal tool to recognize contributors. It is not related to ART19 users or account permissions.
        Each Person added will have no additional access or permissions granted as a result of being included in the Credits section.
      parameters:
        - description: The ID of the person resource to load.
          example: b934651d-d80d-4565-a342-65ade270c804
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Person"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a specific person
      tags:
        - Person
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/people/93ef5d8a-1d28-471c-8827-aa0c4812030e",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="275fb9ae-28a5-4db3-b1e2-4dc5690e991c"'
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/people/93ef5d8a-1d28-471c-8827-aa0c4812030e', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="4a8a51cf-0545-450a-917c-172c4c876ace"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/people/93ef5d8a-1d28-471c-8827-aa0c4812030e');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="996438a8-0681-4e8e-810d-4ad6c56e7fd5"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/people/93ef5d8a-1d28-471c-8827-aa0c4812030e' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="16a05c38-7e8c-47bb-800e-3f80c3c80100"' \
              --compressed
  /seasons:
    get:
      description: |
        When retrieving a list of seasons, the result is automatically filtered depending on the
        privileges the used credential holds. If there are no specific privileges to a series or network,
        only active seasons for active series are included.
      parameters:
        - description: |
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.
            The brackets *MUST* be percent-encoded, per the requirements in
            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: |+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: |+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: Limit the result to seasons owned by this series.
          example: 7ebd702d-07c8-4da9-a7a8-cf18ec414d5c
          in: query
          name: series_id
          schema:
            format: uuid
            type: string
        - description: Limit the result to seasons with a title containing this parameter in a case-insensitive way.
          in: query
          name: q
          schema:
            type: string
        - description: |
            Specify how to sort the result. Please refer to either the top section or the
            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: sort_title
            items:
              enum:
                - created_at
                - sort_title
                - title
                - updated_at
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Season"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  errors:
                    description: |
                      An error object describing the problem. Please check out the
                      [JSON:API Error document reference](https://jsonapi.org/format/#error-objects) for a full
                      description of the returned object.
                    items:
                      properties:
                        code:
                          description: An application-specific error code, expressed as a string value
                          type: string
                        detail:
                          description: |
                            A human-readable explanation specific to this occurrence of the problem. Like title, this field’s value can be localized.
                          example: Required query parameter “series_id” is missing
                          type: string
                        source:
                          description: |
                            An object containing references to the source of the error, optionally including any of the following members.
                          nullable: true
                          properties:
                            parameter:
                              description: A string indicating which URI query parameter caused the error.
                              example: series_id
                              type: string
                            pointer:
                              description: |
                                A JSON Pointer [RFC6901](https://tools.ietf.org/html/rfc6901) to the associated entity in the request document (e.g. `/data` for a primary data object, or `/data/attributes/title` for a specific attribute).
                              example: /data/attributes/name
                              type: string
                          type: object
                        status:
                          description: "The HTTP status code applicable to this problem, expressed as a string value: 400"
                          example: "400"
                          type: string
                        title:
                          description: |
                            A short, human-readable summary of the problem that SHOULD NOT change from occurrence to occurrence of the
                            problem, except for purposes of localization.
                          example: Bad Request
                          type: string
                      type: object
                    type: array
                type: object
          description: |
            Bad Request

            Parameters requiring a specific format must adhere to that format.
            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a list of seasons
      tags:
        - Season
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/seasons",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="d11d081f-2df8-43ab-bf8a-b3dcf84b8408"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/seasons?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="8821d13a-2118-4ce7-8350-e175059a8279"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/seasons');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="ebc035e2-9b9b-4100-8c86-1b9b199ff024"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/seasons?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="3046ebb9-29ab-4d13-9370-2ac479ca9423"' \
              --compressed
  "/seasons/{id}":
    get:
      parameters:
        - description: The ID of the season resource to load.
          example: 8b92fc98-1e8c-4d1a-88ae-e40bd7d96dad
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Season"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a specific season
      tags:
        - Season
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/seasons/d643da6e-ee0d-4f79-8baa-04928cfc9d97",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="9655227e-043b-4bab-9c5b-44d07a89be41"'
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/seasons/d643da6e-ee0d-4f79-8baa-04928cfc9d97', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="d4ec84b8-1f39-4d07-b809-c51694a74a3a"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/seasons/d643da6e-ee0d-4f79-8baa-04928cfc9d97');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="e2dfb8e0-ed3c-4ae2-ba36-dc7f00fd21f4"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/seasons/d643da6e-ee0d-4f79-8baa-04928cfc9d97' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="2eaba3db-8697-496f-9df8-a4ce0560fe41"' \
              --compressed
  /series:
    get:
      description: |
        When retrieving a list of series, the result is automatically filtered depending on the
        privileges the used credential holds. All credentials will have access to active series
        with a public page enabled (on ART19). Utilizing a filter to limit the result to series
        associated with your account is recommended.
      parameters:
        - description: |
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.
            The brackets *MUST* be percent-encoded, per the requirements in
            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: |+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: |+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: |
            Limit the result to series with an ad deal of this Ad Ops profile.
          example: 35eb02fc-54db-4650-98e0-4695b101b71d
          in: query
          name: ad_rep_account_id
          schema:
            format: uuid
            type: string
        - description: Limit the result to series owned by this network.
          example: 5317358a-527e-4365-a343-361854286cc7
          in: query
          name: network_id
          schema:
            format: uuid
            type: string
        - description: Limit the result to series with a title or slug containing this parameter in a case-insensitive way.
          in: query
          name: q
          schema:
            type: string
        - description: |
            Specify how to sort the result. Please refer to either the top section or the
            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: sort_title
            items:
              enum:
                - created_at
                - episode_released_at
                - sort_title
                - title
                - updated_at
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Series"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  errors:
                    description: |
                      An error object describing the problem. Please check out the
                      [JSON:API Error document reference](https://jsonapi.org/format/#error-objects) for a full
                      description of the returned object.
                    items:
                      properties:
                        code:
                          description: An application-specific error code, expressed as a string value
                          type: string
                        detail:
                          description: |
                            A human-readable explanation specific to this occurrence of the problem. Like title, this field’s value can be localized.
                          example: Required query parameter “series_id” is missing
                          type: string
                        source:
                          description: |
                            An object containing references to the source of the error, optionally including any of the following members.
                          nullable: true
                          properties:
                            parameter:
                              description: A string indicating which URI query parameter caused the error.
                              example: series_id
                              type: string
                            pointer:
                              description: |
                                A JSON Pointer [RFC6901](https://tools.ietf.org/html/rfc6901) to the associated entity in the request document (e.g. `/data` for a primary data object, or `/data/attributes/title` for a specific attribute).
                              example: /data/attributes/name
                              type: string
                          type: object
                        status:
                          description: "The HTTP status code applicable to this problem, expressed as a string value: 400"
                          example: "400"
                          type: string
                        title:
                          description: |
                            A short, human-readable summary of the problem that SHOULD NOT change from occurrence to occurrence of the
                            problem, except for purposes of localization.
                          example: Bad Request
                          type: string
                      type: object
                    type: array
                type: object
          description: |
            Bad Request

            Parameters requiring a specific format must adhere to that format.
            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a list of series
      tags:
        - Series
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/series",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="b41fee0b-44de-4402-9529-c92e9f60739a"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/series?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="61ab529f-043d-4aa6-a323-741dc5869e97"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/series');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="a28eab2c-217e-4d0a-9454-7f42fb267f6c"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/series?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="90656198-958a-4cd4-88c8-8b2595f64401"' \
              --compressed
  "/series/{id}":
    get:
      parameters:
        - description: The ID of the series resource to load.
          example: c96cba77-1677-4053-a070-70c110f13d1b
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Series"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      security:
        - api_key: []
      summary: Get a specific series
      tags:
        - Series
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: |
            response = Typhoeus.get(
              "https://art19.com/series/8bc49bd4-3e5d-437b-b1d9-b1aec6f81e2e",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="3b3b7399-5195-48f2-98df-e44a4a65dd32"'
              },
              timeout: 10
            )
            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: |
            try {
              const response = await fetch('https://art19.com/series/8bc49bd4-3e5d-437b-b1d9-b1aec6f81e2e', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="ab5db3ba-8f34-4f69-b2a4-c08df1e18b75"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: |
            <?php
            // Initialize a cURL resource for a request
            $request = curl_init();

            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/series/8bc49bd4-3e5d-437b-b1d9-b1aec6f81e2e');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="f78e3684-ab0b-472d-9063-61b9ce11b5b8"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }
            ?>
        - label: cURL
          lang: shell
          source: |
            curl 'https://art19.com/series/8bc49bd4-3e5d-437b-b1d9-b1aec6f81e2e' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="af265b56-5ae3-423d-9697-18d037f01299"' \
              --compressed
components:
  responses:
    forbidden:
      description: |
        Forbidden

        The provided credential is not authorized to access the requested resource or collection.
        [See RFC 7231 § 6.5.3](https://tools.ietf.org/html/rfc7231#section-6.5.3)
    not_acceptable:
      description: |
        Not Acceptable

        The client requested a response format which cannot be satisfied. Ensure that you specify the
        correct `Accept` header. [See RFC 7231 § 6.5.6](https://tools.ietf.org/html/rfc7231#section-6.5.6).
    not_found:
      description: |
        Not Found

        The requested resource/collection does not exist. [See RFC 7231 § 6.5.4](https://tools.ietf.org/html/rfc7231#section-6.5.4).
    rate_limit_exceeded:
      description: Rate limit exceeded
      headers:
        Retry-After:
          description: |
            The number of seconds after which the request can be retried.
            Since the rate limit is per minute, this value ranges from 1 to 59.
            Conforms to [RFC 7231 § 7.1.3](https://tools.ietf.org/html/rfc7231#section-7.1.3).
          schema:
            type: integer
    unauthorized:
      description: |
        Unauthorized

        A valid credential was not supplied, but one is required.
        [See RFC 7235 § 3.1](https://tools.ietf.org/html/rfc7235#section-3.1).
        A `WWW-Authenticate` header is not currently returned.
  schemas:
    Classification:
      description: |
        A classification, in general, is something that can describe other resources, such as countries, genres or
        languages. How they're described (included or excluded, and whether they're the primary classification
        of this type) is handled using other resources. Different functions of ART19 rely on classifications differently,
        and could have an effect only on directory information, or may have an effect on ad decisioning.
      properties:
        attributes:
          properties:
            created_at:
              format: date-time
              type: string
            display_name:
              description: Human readable representation of the classification
              type: string
            metadata:
              description: |
                An object with more detailed information about the classification.
                The possible members are different per type of classification.
              type: object
            parent_id:
              deprecated: true
              description: |
                This will be replaced by the relationship `parent` in a future update.<br/>
                For hierarchical classifications, this is the ID of the parent classification
              format: uuid
              type: string
            tree_path:
              description: |
                For hierarchical classifications this is the list of IDs reflecting the hierarchy starting
                with the root level node.
              example:
                - cf6cfcd6-6221-4b63-b068-14d885193a8e
                - 6172c736-8d9e-4d40-8c43-8bf7070c0c94
              items:
                format: uuid
                type: string
              type: array
            type:
              description: |
                The type of classification. The type of retrievable classifications depends on the privileges
                of the credential being used.
              enum:
                - AlternateFeedType
                - Genre
                - Industry
                - Language
                - MediaRating
              example: Genre
              type: string
            updated_at:
              format: date-time
              type: string
            value:
              description: |
                The value the classification represents. The meaning of this attribute depends on the type of classification.
              type: string
            value_path:
              description: |
                For hierarchical classifications this is a concatenated string of all the classification
                values from the root to this classification using a `: ` as separator.
              example: "Comedy: Standup"
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        type:
          description: The type of resource. This is always `classifications`
          example: classifications
          type: string
      type: object
    ClassificationInclusion:
      description: |
        Connects a Classification with a classified item like a Series, Season or Episode.
      properties:
        attributes:
          properties:
            classification_id:
              deprecated: true
              description: |
                This will be replaced by the relationship `classification` in a future update.<br/>
                The ID of the classification linked to the entity
              example: 497aad13-1c96-41d8-80e2-9a5aa7bc96fa
              format: uuid
              type: string
            classification_type:
              deprecated: true
              description: This will be replaced by the relationship `classification` in a future update.<br/>
              enum:
                - AlternateFeedType
                - Genre
                - Industry
                - Language
                - MediaRating
              type: string
            classified_id:
              deprecated: true
              description: |
                This will be replaced by the relationship `classified` in a future update.<br/>
                The ID of the entity linked to the classification
              example: 8e9dc5d8-8a16-424e-ba96-6a7261a9a23b
              format: uuid
              type: string
            classified_type:
              deprecated: true
              description: This will be replaced by the relationship `classified` in a future update.<br/>
              enum:
                - Series
                - Season
                - Episode
              type: string
            created_at:
              format: date-time
              type: string
            position:
              description: The position or order of the linked classification within the classified resource
              type: integer
            primary:
              description: |
                When `true` it describes the best classification of a specific type for a given
                classified item
              type: boolean
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            classification:
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
          type: object
        type:
          description: The type of resource. This is always `classification_inclusion`
          example: classification_inclusion
          type: string
      type: object
    Credit:
      description: |
        A credit links a specific person to a series, season, or episode in a specific role.
      properties:
        attributes:
          properties:
            created_at:
              format: date-time
              type: string
            position:
              description: The order of this credit within all credits of the referenced creditable entity
              type: integer
            type:
              description: The type of credit the linked person has on the referenced entity
              enum:
                - AnchorCredit
                - AssociateProducerCredit
                - AuthorCredit
                - CastCredit
                - CoHostCredit
                - ComposerCredit
                - CreatorCredit
                - Credit
                - DirectorCredit
                - EditorCredit
                - EngineerCredit
                - ExecutiveProducerCredit
                - GuestCoHostCredit
                - GuestCredit
                - GuestHostCredit
                - HeadWriterCredit
                - HostCredit
                - ProducerCredit
                - ReporterCredit
                - SeniorProducerCredit
                - SidekickCredit
                - VideoProducerCredit
                - WriterCredit
              type: string
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            creditable:
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
            person:
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
          type: object
        type:
          description: The type of resource. This is always `credits`
          example: credits
          type: string
      type: object
    Episode:
      properties:
        attributes:
          properties:
            allow_user_comments:
              description: |
                Only visible to credentials having write privileges for the series.
              type: boolean
            cascaded_cover_image_id:
              deprecated: true
              description: |
                This will be replaced by the relationship `cascaded_cover_image` in a future update.<br/>
                The image ID of the cover art for this episode. This is the preferred image ID to use.
                If there is no specific cover for the episode, the image ID of the season or the
                series will be returned, whichever has one defined.
                The `/images` endpoint provides details to the image.
              example: ec8f13bf-77c0-40fe-933a-8c088c16bc17
              format: uuid
              type: string
            cover_image_id:
              deprecated: true
              description: |
                This will be replaced by the relationship `cover_image` in a future update.<br/>
                The image ID of the cover art for this episode, if an episode-specific image has been provided.
                The `/images` endpoint provideds details to the image.
              example: ec8f13bf-77c0-40fe-933a-8c088c16bc17
              format: uuid
              type: string
            created_at:
              format: date-time
              type: string
            description:
              description: The description of the episode, possibly in HTML format
              example: <p><strong>Some</strong> funny example.</p>
              type: string
            description_is_html:
              description: Determines whether `description` is in HTML format or plain.
              type: boolean
            description_plain:
              description: |
                The description with all HTML markup removed. If the description does not contain markup,
                this is identical to `description`.
              example: Some funny example.
              type: string
            file_name:
              description: The file name, without extension, to present users when downloading the episode
              type: string
            itunes_type:
              description: The type of the episode according to Apple's type categorization
              enum:
                - full
                - bonus
                - trailer
              type: string
            listen_count:
              description: |
                The number of total downloads all time for this episode.
                Only visible if the credential has the right privilege on the series.
              type: integer
            premium_status:
              description: The current premium status of the episode
              enum:
                - active
                - inactive
                - force-active
                - force-inactive
              type: string
            published:
              description: |
                If `true`, the episode will be available for download and included in the feed as soon as its
                release time is reached, provided it has a valid media file, and `release_end_at` is in the future (if applicable).
              type: boolean
            release_end_at:
              description: |
                The date and time when an episode will no longer be included in the feed, or available for download.
                `Published` will remain `true` unless the episode is manually unpublished.
              format: date-time
              type: string
            release_immediately:
              deprecated: true
              description: |
                If `true`, the episode's release time is set in a way it is considered released right away.
                Only visible if the credential has write privileges on the series.
              type: boolean
            released_at:
              description: |
                The date and time when an episode will be released. It will be available for download and included
                in the feed if this time is reached, `published` is set to `true`, and the episode has a media file.
              format: date-time
              type: string
            rss_guid:
              description: |
                A globally unique identifier describing the episode. This ID is commonly used by podcast applications
                to determine whether an episode has already been downloaded or not.
              type: string
            season_id:
              deprecated: true
              description: |
                Use the relationship `season` instead.<br />
                The ID of the season an episode belongs to. This can be `null`. A series can have episodes with and
                without seasons at the same time.
              example: eba5523d-e73a-43ec-bfd9-140ce9db76ac
              format: uuid
              type: string
            series_id:
              deprecated: true
              description: |
                Use the relationship `series` instead.<br />
                The ID of the series an episode belongs to.
              example: 9897f8cb-ffa7-4674-9529-c6037826c738
              format: uuid
              type: string
            sort_title:
              description: |
                The title of the episode modified for sorting. This converts all characters to
                lower case, removes an eventually leading `The` as well as leading and trailling whitespace characters.
              example: big hit
              type: string
            status:
              description: The status of an episode
              enum:
                - active
                - inactive
              type: string
            title:
              description: The title of the episode
              example: The Big Hit
              type: string
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            cascaded_cover_image:
              description: |
                The cover art for this episode. This is the preferred image to use.
                If there is no specific cover image for the episode, the cover of the season or the
                series will be returned, whichever has one defined.
                The `/images` endpoint provides details to the image.
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
            classification_inclusions:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of classifications associated with the episode
              type: object
            cover_image:
              description: |
                The cover art for this episode, if an episode-specific image has been provided.
                The `/images` endpoint provideds details to the image.
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
            credits:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of credits given to people in the episode
              type: object
            default_marker_points:
              description: Only visible if the credential has write privileges on the series
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of default (WarpFeed) marker points
              type: object
            episode_versions:
              description: Only visible if the credential has write privileges on the series
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of episode versions for this episode
              type: object
            images:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: Both the cover image and the cascaded cover image for the season
              type: object
            season:
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              title: The season the episode belongs to
              type: object
            segment_lists:
              description: Only visible if the credential has write privileges on the series
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of segment lists directly attached to the episode
              type: object
            series:
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              title: The series an episode belongs to
              type: object
            taggings:
              deprecated: true
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of associations linking tags to the episodes.
              type: object
            tags:
              deprecated: true
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of tags associated with the episodes
              type: object
          type: object
        type:
          description: The type of resource. This is always `episodes`
          example: episodes
          type: string
      type: object
    Image:
      properties:
        attributes:
          properties:
            bucket_id:
              deprecated: true
              description: |
                This will be replaced by the relationship `bucket` in a future update.<br/>
                The ID of the owning entity
              example: 687282a1-faf7-4d37-8f4a-cbddedf6db8f
              format: uuid
              type: string
            bucket_type:
              deprecated: true
              description: |
                This will be replaced by the relationship `bucket` in a future update.<br/>
                The type of the owning entity
              enum:
                - AdRepAccount
                - Agency
                - Network
                - Series
                - User
              type: string
            created_at:
              format: date-time
              type: string
            crop_data:
              properties:
                height:
                  description: The height of the crop area
                  example: 3000
                  format: float
                  type: number
                left:
                  description: The left coordinate of the crop area
                  example: 100
                  format: float
                  type: number
                top:
                  description: The top coordinate of the crop area
                  example: 150
                  format: float
                  type: number
                width:
                  description: The width of the crop area
                  example: 3000
                  format: float
                  type: number
              title: The crop area within the original media asset
              type: object
            status:
              description: |
                The current value of the image state machine. It starts with `uploaded` and
                transitions to `processing` once a worker has picked it up. If all variations and
                styles of an image have been successfully created, the status becomes `valid`. In
                case of a problem during the processing it changes to `invalid`.
              enum:
                - invalid
                - processing
                - uploaded
                - valid
              type: string
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            media_assets:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: All media assets available for this image
              type: object
          type: object
        type:
          description: The type of resource. This is always `images`
          example: images
          type: string
      type: object
    Links:
      properties:
        first:
          description: A URL to retrieve the first page of data keeping the provided page size
          type: string
        last:
          description: A URL to retrieve the last page of data keeping the provided page size
          type: string
        next:
          description: A URL to retrieve the next page of data keeping the provided page size
          type: string
        prev:
          description: A URL to retrieve the previous page of keeping using the provided page size
          type: string
        self:
          description: A URL to retrieve the collection as the primary data
          type: string
      type: object
    MediaAsset:
      properties:
        attributes:
          properties:
            attachment_id:
              deprecated: true
              description: |
                This will be replaced by the relationship `attachment` in a future update.<br/>
                The ID of the entity this media asset is attached to
              example: 09a93847-d5bf-47b4-90d6-854e95011951
              format: uuid
              type: string
            attachment_type:
              deprecated: true
              description: |
                This will be replaced by the relationship `attachment` in a future update.<br/>
                The type of the entity this media asset is attached to
              enum:
                - Image
                - Segment
                - SegmentList
              type: string
            cdn_url:
              description: The CDN-URL where the media asset is available
              type: string
            content_type:
              description: The MIME type of the media asset
              example: image/jpeg
              type: string
            created_at:
              format: date-time
              type: string
            file_name:
              description: |
                The filename of the media asset without any path components.
              type: string
            file_size:
              description: The size in bytes of the media asset
              example: 2544307
              type: integer
            size_height:
              description: The height in pixels of the media asset, if the media asset is an image
              example: 1839
              type: integer
            size_width:
              description: The width in pixels of the media asset, if the media asset is an image
              example: 1839
              type: integer
            style:
              description: |
                The style of the media asset. A media asset is part of a collection of related media files,
                for example an image or an episode (audio). Images usually have differently sized versions,
                and episodes can have different audio formats.
              enum:
                - info
                - itunes
                - medium
                - original
                - regular
                - square-400
                - square-640
                - square-888
                - square-3000
                - stripped-original
                - thumb
                - waveform_data
              example: regular
              type: string
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        type:
          description: The type of resource. This is always `media_assets`
          example: media_assets
          type: string
      type: object
    Network:
      properties:
        attributes:
          properties:
            copyright:
              description: The copyright of the network
              type: string
            created_at:
              format: date-time
              type: string
            description:
              description: The description of the network eventually in HTML format
              example: <p><strong>Some</strong> funny example.</p>
              type: string
            description_is_html:
              description: Determines whether `description` is in HTML format or plain.
              type: boolean
            description_plain:
              description: |
                The description with all HTML markup removed. If the description does not contain markup
                this is identitcal to `description`.
              example: Some funny example.
              type: string
            facebook_url:
              description: Where to find this network on Facebook
              type: string
            instagram_url:
              description: Where to find this network on Instagram
              type: string
            linkedin_url:
              description: Where to find the network on LinkedIn
              type: string
            name:
              description: The name of the network
              type: string
            public_email:
              description: A public point of contact through email
              type: string
            public_page_enabled:
              description: |
                If this is `true`, a public web page for this network is available under
                `https://art19.com/networks/{slug}`, where `slug` is the value of the corresponding attribute.
                This public page provides details about the network as well as descriptive links to all its
                series' public pages. If this is `false`, no such public page is available.
              type: boolean
            slug:
              description: |
                The unique slug for the network also used on its public page for example.
                This value is unique system-wide amongst all networks _and_ series.
              type: string
            tumblr_url:
              description: Where to find the network on Tumblr
              type: string
            twitter_url:
              description: Where to find the network on Twitter
              type: string
            updated_at:
              format: date-time
              type: string
            website_url:
              description: A public website for the network
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            cover_image:
              description: The cover image for the network
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
            images:
              description: |
                A collection of images linked to the network. This currently only includes
                the cover image for the network.
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              type: object
            parent:
              description: The parent network of the network
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
            series:
              description: List of series within the network which are accessible to you
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              type: object
          type: object
        type:
          description: The type of resource. This is always `networks`
          example: networks
          type: string
      type: object
    Person:
      properties:
        attributes:
          properties:
            avatar_id:
              deprecated: true
              description: |
                This will be replaced by the relationship `avatar` in a future update.<br/>
                The image ID of an avatar or head shot of the person. Details can be
                retrieved using the `/images` endpoint.
              example: 686ae786-6bbe-42f4-9a51-9e1cf207fc8b
              format: uuid
              type: string
            biography:
              description: The biography of the person without markup.
              type: string
            born:
              description: The birthday of the person
              format: date
              type: string
            created_at:
              format: date-time
              type: string
            died:
              description: The day of death of the person
              format: date
              type: string
            first_name:
              description: The first name of the person
              type: string
            from_country:
              description: The country the person is from
              type: string
            from_locality:
              description: The locality or city the person is from
              type: string
            from_region:
              description: The region/state/province the person is from
              type: string
            last_name:
              description: The last name of the person
              type: string
            public_email:
              description: The public email contact for the person
              type: string
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            avatar:
              description: |
                The avatar or head shot of the person. Details can be retrieved using the `/images` endpoint.
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
          type: object
        type:
          description: The type of resource. This is always `people`
          example: people
          type: string
      type: object
    ResourceIdentifier:
      properties:
        id:
          description: The ID of the resource
          example: 2d7e9be5-0b5a-43d3-9b75-04ec0c0edae8
          format: uuid
          type: string
        type:
          description: The type of the resource
          type: string
      type: object
    ResourceLink:
      description: Links related to the retrieved resource
      properties:
        self:
          description: A URL to retrieve the resource as the primary data
          type: string
      title: Links
      type: object
    Season:
      properties:
        attributes:
          properties:
            cascaded_cover_image_id:
              deprecated: true
              description: |
                This will be replaced by the relationship `cascaded_cover_image` in a future update.<br/>
                The image ID of the cover art for this season. If there is no specific cover for the season,
                the image ID of the series' cover art will be returned.
                The `/images` endpoint provideds details to the image.
              example: ec8f13bf-77c0-40fe-933a-8c088c16bc17
              format: uuid
              type: string
            cover_image_id:
              deprecated: true
              description: |
                This will be replaced by the relationship `cover_image` in a future update.<br/>
                The image ID of the cover art for this season. The `/images` endpoint provideds details to the image.
              example: ec8f13bf-77c0-40fe-933a-8c088c16bc17
              format: uuid
              type: string
            created_at:
              format: date-time
              type: string
            description:
              description: The description of the season eventually in HTML format
              example: <p><strong>Some</strong> funny example.</p>
              type: string
            description_is_html:
              description: Determines whether `description` is in HTML format or plain.
              type: boolean
            description_plain:
              description: |
                The description with all HTML markup removed. If the description does not contain markup
                this is identitcal to `description`.
              example: Some funny example.
              type: string
            first_released_episode_id:
              deprecated: true
              description: |
                This will be replaced by the relationship `first_released_episode` in a future update.<br/>
                The ID of the first released episode within the season
              example: 73a97f1a-5c3c-4226-8ab3-1d7f9e486e4d
              format: uuid
              type: string
            last_released_episode_id:
              deprecated: true
              description: |
                This will be replaced by the relationship `last_released_episode` in a future update.<br/>
                The ID of the most recently released episode within the season
              example: a50b7a22-2128-46d7-b3df-2b308df21bf0
              format: uuid
              type: string
            season_number:
              description: The number of the season. This could be `null` as well.
              example: 1
              type: integer
            sort_title:
              description: |
                The title of the season modified for sorting. This converts all characters to
                lower case, removes an eventually leading `The` as well as leading and trailling whitespace characters.
              example: awesome season
              type: string
            status:
              description: The current status of the season
              enum:
                - active
                - inactive
              type: string
            title:
              description: The title of the season
              example: The Aweseom Season
              type: string
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            cascaded_cover_image:
              description: |
                The cover art for this season. If there is no specific cover for the season,
                the cover image of the series' cover art will be returned.
                The `/images` endpoint provideds details to the image.
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
            classification_inclusions:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of classifications associated with the season
              type: object
            cover_image:
              description: The cover image for the season
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
            credits:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of credits given to people in the season
              type: object
            episodes:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of published and released episodes with an actual media file within the season
              type: object
            first_released_episode:
              description: The first released episode within the season
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
            images:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: Both the cover image and the cascaded cover image for the season
              type: object
            last_released_episode:
              description: The most recently released episode within the season
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
            series:
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              title: The series the season belongs to
              type: object
            taggings:
              deprecated: true
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of associations linking tags to the season.
              type: object
            tags:
              deprecated: true
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of tags associated with the season
              type: object
          type: object
        type:
          description: The type of resource. This is always `seasons`
          example: seasons
          type: string
      type: object
    Series:
      properties:
        attributes:
          properties:
            amazon_subscription_url:
              description: The Amazon Music subscription URL
              type: string
            castbox_subscription_url:
              description: The CastBox subscription URL
              type: string
            cover_image_id:
              deprecated: true
              description: |
                This will be replaced by the relationship `cover_image` in a future update.<br/>
                The image ID of the cover art for this series. The `/images` endpoint provideds details to the image.
              example: ec8f13bf-77c0-40fe-933a-8c088c16bc17
              format: uuid
              type: string
            created_at:
              format: date-time
              type: string
            description:
              description: The description of the series eventually in HTML format
              example: <p><strong>Some</strong> funny example.</p>
              type: string
            description_is_html:
              description: Determines whether `description` is in HTML format or plain.
              type: boolean
            description_plain:
              description: |
                The description with all HTML markup removed. If the description does not contain markup
                this is identitcal to `description`.
              example: Some funny example.
              type: string
            facebook_url:
              description: The URL of the series on Facebook
              type: string
            google_subscription_url:
              description: The Google subscription URL
              type: string
            iheart_subscription_url:
              description: The iHeartRadio subscription URL
              type: string
            instagram_url:
              description: The URL of the series on Instagram
              type: string
            itunes_subscription_badge_enabled:
              description: If `null` or `true`, the ART19 web player will render an iTunes subscription badge
              type: boolean
            itunes_subscription_url:
              description: The Apple iTunes subscription URL
              type: string
            linkedin_url:
              description: The URL of the series on LinkedIn
              type: string
            pocket_casts_subscription_url:
              description: The Pocket Casts subscription URL
              type: string
            podcast_subscription_url:
              description: The URL of the RSS feed
              example: https://rss.art19.com/greatest-show-ever
              type: string
            public_page_enabled:
              description: |
                If this is `true`, a public web page for this series is available under
                `https://art19.com/shows/{slug}`, where `slug` is the value of the corresponding attribute.
                This public page provides details about the series as well as descriptive links to all its
                episodes. If this is `false`, no such public page is available.
              type: boolean
            public_title:
              description: If present, this is the preferred title for the series in the RSS Feed
              type: string
            radio_public_subscription_url:
              description: The RadioPublic subscription URL
              type: string
            rss_author:
              description: The author responsible for the RSS feed
              type: string
            rss_copyright:
              description: The copyright disclaimer for the contents of the show, appearing after a © symbol, e.g. `2020 ART19, Inc.`
              type: string
            rss_email:
              description: The pubic point of contact for the show
              type: string
            rss_owner:
              description: The name of the contact for the show
              type: string
            slug:
              description: |
                This slug is unique over all series and networks system wide and is used to access the series' feed
                as well as its eventually enabled public page on ART19.
              example: greatest-show-ever
              type: string
            sort_title:
              description: |
                The title of the series modified for sorting. This converts all characters to
                lower case, removes an eventually leading `The` as well as leading and trailling whitespace characters.
              example: greatest show ever
              type: string
            spotify_subscription_badge_enabled:
              description: If `null` or `true`, the ART19 web player will render a Spotify subscription badge
              type: boolean
            spotify_subscription_url:
              description: The Spotify subscription URL
              type: string
            status:
              description: The status of the series
              enum:
                - active
                - inactive
              type: string
            stitcher_subscription_badge_enabled:
              description: If `null` or `true`, the ART19 web player will render a Stitcher subscription badge
              type: boolean
            stitcher_subscription_url:
              description: The Stitcher subscription URL
              type: string
            title:
              description: The title of the series
              example: The greatest show ever
              type: string
            tumblr_url:
              description: The URL of the series on Tumblr
              type: string
            tune_in_subscription_url:
              description: The TuneIn subscription URL
              type: string
            twitter_url:
              description: The URL of the series on Twitter
              type: string
            type:
              description: The type of the series
              enum:
                - Series
              type: string
            updated_at:
              format: date-time
              type: string
            website_url:
              description: The custom website URL for the series
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            classification_inclusions:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of classifications associated with the series
              type: object
            cover_image:
              description: The cover image for the series
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
            credits:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of credits given to people in the series
              type: object
            episodes:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of published and released episodes with an actual media file
              type: object
            images:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: The cover image for the series
              type: object
            network:
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              title: The network the series belongs to
              type: object
            seasons:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of seasons in the series
              type: object
            taggings:
              deprecated: true
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of associations linking tags to the series
              type: object
            tags:
              deprecated: true
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of tags associated with the series
              type: object
          type: object
        type:
          description: The type of resource. This is always `series`
          example: series
          type: string
      type: object
  securitySchemes:
    api_key:
      description: |
        Authentication is **REQUIRED** for all APIs using an API token, which is placed into an HTTP Authorization header.

        This token is generated by ART19 Support and consists of a shared secret placed in the `token`
        parameter and a credential ID placed in the `credential` parameter.

        The final Authorization header will look like this:

        ```
        Authorization: Token token="V2h5LCBoZWxsbyB0aGVyZSE=", credential="8f71320c-fdbc-4934-b3b9-f1eda4fb39a9"
        ```
      in: header
      name: Authorization
      type: apiKey
