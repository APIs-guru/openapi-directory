openapi: 3.0.3
servers:
  - url: https://kiprio.com/v1/hash
info:
  description: >-
    Hash strings using MD5, SHA-1, SHA-256, SHA-384, SHA-512, SHA3-256, SHA3-512.
    HMAC signing with secret keys, constant-time comparison, Base64 and hex
    encoding/decoding. Free demo tier, no API key required.
  title: Hash & Encode
  version: 1.0.0
  x-apisguru-categories:
    - developer_tools
    - security
  x-logo:
    url: https://kiprio.com/logo.png
  x-origin:
    - format: openapi
      url: https://kiprio.com/v1/hash/openapi.json
      version: '3.0'
  x-providerName: kiprio.com
  x-serviceName: hash
externalDocs:
  description: API documentation
  url: https://kiprio.com/docs/
paths:
  /:
    get:
      summary: Hash a string
      operationId: hashString
      parameters:
        - name: input
          in: query
          required: true
          schema:
            type: string
        - name: algo
          in: query
          schema:
            type: string
            enum: [md5, sha1, sha256, sha384, sha512, sha3-256, sha3-512]
            default: sha256
      responses:
        '200':
          description: Hash result
          content:
            application/json:
              schema:
                type: object
                required:
                  - input
                  - algo
                  - hash
                properties:
                  input:
                    type: string
                    example: hello
                  algo:
                    type: string
                    example: sha256
                  hash:
                    type: string
                    example: 2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824
  /hmac:
    post:
      summary: HMAC-sign a string
      operationId: hmacSign
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [input, secret]
              properties:
                input:
                  type: string
                secret:
                  type: string
                algo:
                  type: string
                  enum: [md5, sha1, sha256, sha384, sha512, sha3-256, sha3-512]
                  default: sha256
      responses:
        '200':
          description: HMAC result
          content:
            application/json:
              schema:
                type: object
                required:
                  - input
                  - algo
                  - hmac
                properties:
                  input:
                    type: string
                  algo:
                    type: string
                  hmac:
                    type: string
                    description: HMAC hex digest
