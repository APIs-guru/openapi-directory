# coding: utf-8

"""
    Redact API

    The [Redact API](/redact/overview) helps organisations meet their privacy compliance obligations. It provides controlled, on-demand redaction of private information from transactional records in the long-term storage. Note, Redact API does not have the capability to redact the short-lived server logs that are retained for a few weeks. For SMS customers that need immediate redaction, Vonage suggests using [Advanced Auto-redact](/redact/overview#auto-redact-vs-redact-api).

    The version of the OpenAPI document: 1.0.6
    Contact: devrel@vonage.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from openapi_client.models.error_premature_redaction import ErrorPrematureRedaction
from openapi_client.models.error_unprovisioned import ErrorUnprovisioned
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

REDACTMESSAGE403RESPONSE_ONE_OF_SCHEMAS = ["ErrorPrematureRedaction", "ErrorUnprovisioned"]

class RedactMessage403Response(BaseModel):
    """
    RedactMessage403Response
    """
    # data type: ErrorPrematureRedaction
    oneof_schema_1_validator: Optional[ErrorPrematureRedaction] = None
    # data type: ErrorUnprovisioned
    oneof_schema_2_validator: Optional[ErrorUnprovisioned] = None
    actual_instance: Optional[Union[ErrorPrematureRedaction, ErrorUnprovisioned]] = None
    one_of_schemas: Set[str] = { "ErrorPrematureRedaction", "ErrorUnprovisioned" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = RedactMessage403Response.model_construct()
        error_messages = []
        match = 0
        # validate data type: ErrorPrematureRedaction
        if not isinstance(v, ErrorPrematureRedaction):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ErrorPrematureRedaction`")
        else:
            match += 1
        # validate data type: ErrorUnprovisioned
        if not isinstance(v, ErrorUnprovisioned):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ErrorUnprovisioned`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in RedactMessage403Response with oneOf schemas: ErrorPrematureRedaction, ErrorUnprovisioned. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in RedactMessage403Response with oneOf schemas: ErrorPrematureRedaction, ErrorUnprovisioned. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into ErrorPrematureRedaction
        try:
            instance.actual_instance = ErrorPrematureRedaction.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ErrorUnprovisioned
        try:
            instance.actual_instance = ErrorUnprovisioned.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into RedactMessage403Response with oneOf schemas: ErrorPrematureRedaction, ErrorUnprovisioned. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into RedactMessage403Response with oneOf schemas: ErrorPrematureRedaction, ErrorUnprovisioned. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], ErrorPrematureRedaction, ErrorUnprovisioned]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


