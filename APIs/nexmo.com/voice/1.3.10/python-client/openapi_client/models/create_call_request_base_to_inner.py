# coding: utf-8

"""
    Voice API

    The Voice API lets you create outbound calls, control in-progress calls and get information about historical calls. More information about the Voice API can be found at <https://developer.nexmo.com/voice/voice-api/overview>.

    The version of the OpenAPI document: 1.3.10
    Contact: devrel@vonage.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from openapi_client.models.endpoint_phone_to import EndpointPhoneTo
from openapi_client.models.endpoint_sip import EndpointSip
from openapi_client.models.endpoint_vbc_extension import EndpointVBCExtension
from openapi_client.models.endpoint_websocket import EndpointWebsocket
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

CREATECALLREQUESTBASETOINNER_ONE_OF_SCHEMAS = ["EndpointPhoneTo", "EndpointSip", "EndpointVBCExtension", "EndpointWebsocket"]

class CreateCallRequestBaseToInner(BaseModel):
    """
    CreateCallRequestBaseToInner
    """
    # data type: EndpointPhoneTo
    oneof_schema_1_validator: Optional[EndpointPhoneTo] = None
    # data type: EndpointSip
    oneof_schema_2_validator: Optional[EndpointSip] = None
    # data type: EndpointWebsocket
    oneof_schema_3_validator: Optional[EndpointWebsocket] = None
    # data type: EndpointVBCExtension
    oneof_schema_4_validator: Optional[EndpointVBCExtension] = None
    actual_instance: Optional[Union[EndpointPhoneTo, EndpointSip, EndpointVBCExtension, EndpointWebsocket]] = None
    one_of_schemas: Set[str] = { "EndpointPhoneTo", "EndpointSip", "EndpointVBCExtension", "EndpointWebsocket" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = CreateCallRequestBaseToInner.model_construct()
        error_messages = []
        match = 0
        # validate data type: EndpointPhoneTo
        if not isinstance(v, EndpointPhoneTo):
            error_messages.append(f"Error! Input type `{type(v)}` is not `EndpointPhoneTo`")
        else:
            match += 1
        # validate data type: EndpointSip
        if not isinstance(v, EndpointSip):
            error_messages.append(f"Error! Input type `{type(v)}` is not `EndpointSip`")
        else:
            match += 1
        # validate data type: EndpointWebsocket
        if not isinstance(v, EndpointWebsocket):
            error_messages.append(f"Error! Input type `{type(v)}` is not `EndpointWebsocket`")
        else:
            match += 1
        # validate data type: EndpointVBCExtension
        if not isinstance(v, EndpointVBCExtension):
            error_messages.append(f"Error! Input type `{type(v)}` is not `EndpointVBCExtension`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in CreateCallRequestBaseToInner with oneOf schemas: EndpointPhoneTo, EndpointSip, EndpointVBCExtension, EndpointWebsocket. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in CreateCallRequestBaseToInner with oneOf schemas: EndpointPhoneTo, EndpointSip, EndpointVBCExtension, EndpointWebsocket. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into EndpointPhoneTo
        try:
            instance.actual_instance = EndpointPhoneTo.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into EndpointSip
        try:
            instance.actual_instance = EndpointSip.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into EndpointWebsocket
        try:
            instance.actual_instance = EndpointWebsocket.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into EndpointVBCExtension
        try:
            instance.actual_instance = EndpointVBCExtension.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into CreateCallRequestBaseToInner with oneOf schemas: EndpointPhoneTo, EndpointSip, EndpointVBCExtension, EndpointWebsocket. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into CreateCallRequestBaseToInner with oneOf schemas: EndpointPhoneTo, EndpointSip, EndpointVBCExtension, EndpointWebsocket. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], EndpointPhoneTo, EndpointSip, EndpointVBCExtension, EndpointWebsocket]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


