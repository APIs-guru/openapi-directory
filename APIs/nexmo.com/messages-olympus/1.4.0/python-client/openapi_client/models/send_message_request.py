# coding: utf-8

"""
    Messages API

    The Messages API consolidates and normalises exchanges across all messaging channels. It allows you to use a single API to interact with our various channels such as SMS, MMS, WhatsApp, Viber and Facebook Messenger

    The version of the OpenAPI document: 1.4.0
    Contact: devrel@nexmo.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from openapi_client.models.messenger import Messenger
from openapi_client.models.mms import MMS
from openapi_client.models.sms import SMS
from openapi_client.models.viber import Viber
from openapi_client.models.whats_app import WhatsApp
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

SENDMESSAGEREQUEST_ONE_OF_SCHEMAS = ["MMS", "Messenger", "SMS", "Viber", "WhatsApp"]

class SendMessageRequest(BaseModel):
    """
    SendMessageRequest
    """
    # data type: SMS
    oneof_schema_1_validator: Optional[SMS] = None
    # data type: MMS
    oneof_schema_2_validator: Optional[MMS] = None
    # data type: WhatsApp
    oneof_schema_3_validator: Optional[WhatsApp] = None
    # data type: Messenger
    oneof_schema_4_validator: Optional[Messenger] = None
    # data type: Viber
    oneof_schema_5_validator: Optional[Viber] = None
    actual_instance: Optional[Union[MMS, Messenger, SMS, Viber, WhatsApp]] = None
    one_of_schemas: Set[str] = { "MMS", "Messenger", "SMS", "Viber", "WhatsApp" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = SendMessageRequest.model_construct()
        error_messages = []
        match = 0
        # validate data type: SMS
        if not isinstance(v, SMS):
            error_messages.append(f"Error! Input type `{type(v)}` is not `SMS`")
        else:
            match += 1
        # validate data type: MMS
        if not isinstance(v, MMS):
            error_messages.append(f"Error! Input type `{type(v)}` is not `MMS`")
        else:
            match += 1
        # validate data type: WhatsApp
        if not isinstance(v, WhatsApp):
            error_messages.append(f"Error! Input type `{type(v)}` is not `WhatsApp`")
        else:
            match += 1
        # validate data type: Messenger
        if not isinstance(v, Messenger):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Messenger`")
        else:
            match += 1
        # validate data type: Viber
        if not isinstance(v, Viber):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Viber`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in SendMessageRequest with oneOf schemas: MMS, Messenger, SMS, Viber, WhatsApp. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in SendMessageRequest with oneOf schemas: MMS, Messenger, SMS, Viber, WhatsApp. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into SMS
        try:
            instance.actual_instance = SMS.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into MMS
        try:
            instance.actual_instance = MMS.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into WhatsApp
        try:
            instance.actual_instance = WhatsApp.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Messenger
        try:
            instance.actual_instance = Messenger.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Viber
        try:
            instance.actual_instance = Viber.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into SendMessageRequest with oneOf schemas: MMS, Messenger, SMS, Viber, WhatsApp. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into SendMessageRequest with oneOf schemas: MMS, Messenger, SMS, Viber, WhatsApp. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], MMS, Messenger, SMS, Viber, WhatsApp]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


