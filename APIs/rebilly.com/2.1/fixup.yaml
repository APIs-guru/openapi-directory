definitions:
  GatewayAccount:
    properties:
      monthlyLimit:
        type:
          - integer
          - number
  Payment:
    properties:
      amount:
        type:
          - integer
          - number
