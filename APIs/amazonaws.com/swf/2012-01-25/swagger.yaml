swagger: '2.0'
info:
  version: '2012-01-25'
  x-release: v4
  title: Amazon Simple Workflow Service
  description: '<fullname>Amazon Simple Workflow Service</fullname> <p>The Amazon Simple Workflow Service (Amazon SWF) makes it easy to build applications that use Amazon''s cloud to coordinate work across distributed components. In Amazon SWF, a <i>task</i> represents a logical unit of work that is performed by a component of your workflow. Coordinating tasks in a workflow involves managing intertask dependencies, scheduling, and concurrency in accordance with the logical flow of the application.</p> <p>Amazon SWF gives you full control over implementing tasks and coordinating them without worrying about underlying complexities such as tracking their progress and maintaining their state.</p> <p>This documentation serves as reference only. For a broader overview of the Amazon SWF programming model, see the <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/">Amazon SWF Developer Guide</a>.</p>'
  x-logo:
    url: 'https://media.amazonwebservices.com/blog/2007/big_pbaws_logo_300px.jpg'
    backgroundColor: '#FFFFFF'
  termsOfService: 'https://aws.amazon.com/service-terms/'
  contact:
    name: Mike Ralphson
    email: mike.ralphson@gmail.com
    url: 'https://github.com/mermade/aws2openapi'
  license:
    name: Apache 2.0 License
    url: 'http://www.apache.org/licenses/'
  x-providerName: amazonaws.com
  x-serviceName: swf
  x-origin:
    - contentType: application/json
      url: 'https://raw.githubusercontent.com/aws/aws-sdk-js/master/apis/swf-2012-01-25.normal.json'
      converter:
        url: 'https://github.com/mermade/aws2openapi'
        version: 1.0.0
      x-apisguru-direct: true
  x-apiClientRegistration:
    url: 'https://portal.aws.amazon.com/gp/aws/developer/registration/index.html?nc2=h_ct'
  x-apisguru-categories:
    - cloud
  x-preferred: true
externalDocs:
  description: Amazon Web Services documentation
  url: 'https://aws.amazon.com/swf/'
host: swf.amazonaws.com
basePath: /
x-hasEquivalentPaths: true
schemes:
  - http
consumes:
  - application/json
produces:
  - application/json
parameters:
  Action:
    name: Action
    in: header
    type: string
    required: true
  Version:
    name: Version
    in: header
    type: string
    required: true
  X-Amz-Content-Sha256:
    name: X-Amz-Content-Sha256
    in: header
    type: string
    required: false
  X-Amz-Date:
    name: X-Amz-Date
    in: header
    type: string
    required: false
  X-Amz-Algorithm:
    name: X-Amz-Algorithm
    in: header
    type: string
    required: false
  X-Amz-Credential:
    name: X-Amz-Credential
    in: header
    type: string
    required: false
  X-Amz-Security-Token:
    name: X-Amz-Security-Token
    in: header
    type: string
    required: false
  X-Amz-Signature:
    name: X-Amz-Signature
    in: header
    type: string
    required: false
  X-Amz-SignedHeaders:
    name: X-Amz-SignedHeaders
    in: header
    type: string
    required: false
securityDefinitions:
  hmac:
    type: apiKey
    name: Authorization
    in: header
    description: Amazon Signature authorization v4
    x-amazon-apigateway-authtype: awsSigv4
security:
  - hmac: []
paths:
  /:
    post:
      operationId: CountClosedWorkflowExecutions
      description: '<p>Returns the number of closed workflow executions within the given domain that meet the specified filtering criteria.</p> <note>This operation is eventually consistent. The results are best effort and may not exactly reflect recent updates and changes.</note> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li><code>tagFilter.tag</code>: String constraint. The key is <code>swf:tagFilter.tag</code>.</li> <li><code>typeFilter.name</code>: String constraint. The key is <code>swf:typeFilter.name</code>.</li> <li><code>typeFilter.version</code>: String constraint. The key is <code>swf:typeFilter.version</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/WorkflowExecutionCount'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/CountClosedWorkflowExecutionsInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#CountOpenWorkflowExecutions':
    post:
      operationId: CountOpenWorkflowExecutions
      description: '<p>Returns the number of open workflow executions within the given domain that meet the specified filtering criteria.</p> <note>This operation is eventually consistent. The results are best effort and may not exactly reflect recent updates and changes.</note> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li><code>tagFilter.tag</code>: String constraint. The key is <code>swf:tagFilter.tag</code>.</li> <li><code>typeFilter.name</code>: String constraint. The key is <code>swf:typeFilter.name</code>.</li> <li><code>typeFilter.version</code>: String constraint. The key is <code>swf:typeFilter.version</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/WorkflowExecutionCount'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/CountOpenWorkflowExecutionsInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#CountPendingActivityTasks':
    post:
      operationId: CountPendingActivityTasks
      description: '<p>Returns the estimated number of activity tasks in the specified task list. The count returned is an approximation and is not guaranteed to be exact. If you specify a task list that no activity task was ever scheduled in then 0 will be returned.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the <code>taskList.name</code> parameter by using a <b>Condition</b> element with the <code>swf:taskList.name</code> key to allow the action to access only certain task lists.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/PendingTaskCount'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/CountPendingActivityTasksInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#CountPendingDecisionTasks':
    post:
      operationId: CountPendingDecisionTasks
      description: '<p>Returns the estimated number of decision tasks in the specified task list. The count returned is an approximation and is not guaranteed to be exact. If you specify a task list that no decision task was ever scheduled in then 0 will be returned.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the <code>taskList.name</code> parameter by using a <b>Condition</b> element with the <code>swf:taskList.name</code> key to allow the action to access only certain task lists.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/PendingTaskCount'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/CountPendingDecisionTasksInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#DeprecateActivityType':
    post:
      operationId: DeprecateActivityType
      description: '<p>Deprecates the specified <i>activity type</i>. After an activity type has been deprecated, you cannot create new tasks of that activity type. Tasks of this type that were scheduled before the type was deprecated will continue to run.</p> <note>This operation is eventually consistent. The results are best effort and may not exactly reflect recent updates and changes.</note> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li><code>activityType.name</code>: String constraint. The key is <code>swf:activityType.name</code>.</li> <li><code>activityType.version</code>: String constraint. The key is <code>swf:activityType.version</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: TypeDeprecatedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/TypeDeprecatedFault'
        '482':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/DeprecateActivityTypeInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#DeprecateDomain':
    post:
      operationId: DeprecateDomain
      description: '<p>Deprecates the specified domain. After a domain has been deprecated it cannot be used to create new workflow executions or register new types. However, you can still use visibility actions on this domain. Deprecating a domain also deprecates all activity and workflow types registered in the domain. Executions that were started before the domain was deprecated will continue to run.</p> <note>This operation is eventually consistent. The results are best effort and may not exactly reflect recent updates and changes.</note> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: DomainDeprecatedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/DomainDeprecatedFault'
        '482':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/DeprecateDomainInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#DeprecateWorkflowType':
    post:
      operationId: DeprecateWorkflowType
      description: '<p>Deprecates the specified <i>workflow type</i>. After a workflow type has been deprecated, you cannot create new executions of that type. Executions that were started before the type was deprecated will continue to run. A deprecated workflow type may still be used when calling visibility actions.</p> <note>This operation is eventually consistent. The results are best effort and may not exactly reflect recent updates and changes.</note> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li><code>workflowType.name</code>: String constraint. The key is <code>swf:workflowType.name</code>.</li> <li><code>workflowType.version</code>: String constraint. The key is <code>swf:workflowType.version</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: TypeDeprecatedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/TypeDeprecatedFault'
        '482':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/DeprecateWorkflowTypeInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#DescribeActivityType':
    post:
      operationId: DescribeActivityType
      description: '<p>Returns information about the specified activity type. This includes configuration settings provided when the type was registered and other general information about the type.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li><code>activityType.name</code>: String constraint. The key is <code>swf:activityType.name</code>.</li> <li><code>activityType.version</code>: String constraint. The key is <code>swf:activityType.version</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/ActivityTypeDetail'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/DescribeActivityTypeInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#DescribeDomain':
    post:
      operationId: DescribeDomain
      description: '<p>Returns information about the specified domain, including description and status.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/DomainDetail'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/DescribeDomainInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#DescribeWorkflowExecution':
    post:
      operationId: DescribeWorkflowExecution
      description: '<p>Returns information about the specified workflow execution including its type and some statistics.</p> <note>This operation is eventually consistent. The results are best effort and may not exactly reflect recent updates and changes.</note> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/WorkflowExecutionDetail'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/DescribeWorkflowExecutionInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#DescribeWorkflowType':
    post:
      operationId: DescribeWorkflowType
      description: '<p>Returns information about the specified <i>workflow type</i>. This includes configuration settings specified when the type was registered and other information such as creation date, current status, and so on.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li><code>workflowType.name</code>: String constraint. The key is <code>swf:workflowType.name</code>.</li> <li><code>workflowType.version</code>: String constraint. The key is <code>swf:workflowType.version</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/WorkflowTypeDetail'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/DescribeWorkflowTypeInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#GetWorkflowExecutionHistory':
    post:
      operationId: GetWorkflowExecutionHistory
      description: '<p>Returns the history of the specified workflow execution. The results may be split into multiple pages. To retrieve subsequent pages, make the call again using the <code>nextPageToken</code> returned by the initial call.</p> <note>This operation is eventually consistent. The results are best effort and may not exactly reflect recent updates and changes.</note> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/History'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/GetWorkflowExecutionHistoryInput'
        - name: maximumPageSize
          type: string
          in: query
          description: Pagination limit
          required: false
        - name: nextPageToken
          type: string
          in: query
          description: Pagination token
          required: false
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#ListActivityTypes':
    post:
      operationId: ListActivityTypes
      description: '<p>Returns information about all activities registered in the specified domain that match the specified name and registration status. The result includes information like creation date, current status of the activity, etc. The results may be split into multiple pages. To retrieve subsequent pages, make the call again using the <code>nextPageToken</code> returned by the initial call.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/ActivityTypeInfos'
        '480':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
        '481':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/ListActivityTypesInput'
        - name: maximumPageSize
          type: string
          in: query
          description: Pagination limit
          required: false
        - name: nextPageToken
          type: string
          in: query
          description: Pagination token
          required: false
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#ListClosedWorkflowExecutions':
    post:
      operationId: ListClosedWorkflowExecutions
      description: '<p>Returns a list of closed workflow executions in the specified domain that meet the filtering criteria. The results may be split into multiple pages. To retrieve subsequent pages, make the call again using the nextPageToken returned by the initial call.</p> <note>This operation is eventually consistent. The results are best effort and may not exactly reflect recent updates and changes.</note> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li><code>tagFilter.tag</code>: String constraint. The key is <code>swf:tagFilter.tag</code>.</li> <li><code>typeFilter.name</code>: String constraint. The key is <code>swf:typeFilter.name</code>.</li> <li><code>typeFilter.version</code>: String constraint. The key is <code>swf:typeFilter.version</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/WorkflowExecutionInfos'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/ListClosedWorkflowExecutionsInput'
        - name: maximumPageSize
          type: string
          in: query
          description: Pagination limit
          required: false
        - name: nextPageToken
          type: string
          in: query
          description: Pagination token
          required: false
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#ListDomains':
    post:
      operationId: ListDomains
      description: '<p>Returns the list of domains registered in the account. The results may be split into multiple pages. To retrieve subsequent pages, make the call again using the nextPageToken returned by the initial call.</p> <note> This operation is eventually consistent. The results are best effort and may not exactly reflect recent updates and changes.</note> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains. The element must be set to <code>arn:aws:swf::AccountID:domain/*</code>, where <i>AccountID</i> is the account ID, with no dashes.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/DomainInfos'
        '480':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/ListDomainsInput'
        - name: maximumPageSize
          type: string
          in: query
          description: Pagination limit
          required: false
        - name: nextPageToken
          type: string
          in: query
          description: Pagination token
          required: false
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#ListOpenWorkflowExecutions':
    post:
      operationId: ListOpenWorkflowExecutions
      description: '<p>Returns a list of open workflow executions in the specified domain that meet the filtering criteria. The results may be split into multiple pages. To retrieve subsequent pages, make the call again using the nextPageToken returned by the initial call.</p> <note> This operation is eventually consistent. The results are best effort and may not exactly reflect recent updates and changes.</note> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li><code>tagFilter.tag</code>: String constraint. The key is <code>swf:tagFilter.tag</code>.</li> <li><code>typeFilter.name</code>: String constraint. The key is <code>swf:typeFilter.name</code>.</li> <li><code>typeFilter.version</code>: String constraint. The key is <code>swf:typeFilter.version</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/WorkflowExecutionInfos'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/ListOpenWorkflowExecutionsInput'
        - name: maximumPageSize
          type: string
          in: query
          description: Pagination limit
          required: false
        - name: nextPageToken
          type: string
          in: query
          description: Pagination token
          required: false
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#ListWorkflowTypes':
    post:
      operationId: ListWorkflowTypes
      description: '<p>Returns information about workflow types in the specified domain. The results may be split into multiple pages that can be retrieved by making the call repeatedly.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/WorkflowTypeInfos'
        '480':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
        '481':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/ListWorkflowTypesInput'
        - name: maximumPageSize
          type: string
          in: query
          description: Pagination limit
          required: false
        - name: nextPageToken
          type: string
          in: query
          description: Pagination token
          required: false
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#PollForActivityTask':
    post:
      operationId: PollForActivityTask
      description: '<p>Used by workers to get an <a>ActivityTask</a> from the specified activity <code>taskList</code>. This initiates a long poll, where the service holds the HTTP connection open and responds as soon as a task becomes available. The maximum time the service holds on to the request before responding is 60 seconds. If no task is available within 60 seconds, the poll will return an empty result. An empty result, in this context, means that an ActivityTask is returned, but that the value of taskToken is an empty string. If a task is returned, the worker should use its type to identify and process it correctly.</p> <important>Workers should set their client side socket timeout to at least 70 seconds (10 seconds higher than the maximum time service may hold the poll request).</important> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the <code>taskList.name</code> parameter by using a <b>Condition</b> element with the <code>swf:taskList.name</code> key to allow the action to access only certain task lists.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/ActivityTask'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
        '482':
          description: LimitExceededFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/LimitExceededFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/PollForActivityTaskInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#PollForDecisionTask':
    post:
      operationId: PollForDecisionTask
      description: '<p>Used by deciders to get a <a>DecisionTask</a> from the specified decision <code>taskList</code>. A decision task may be returned for any open workflow execution that is using the specified task list. The task includes a paginated view of the history of the workflow execution. The decider should use the workflow type and the history to determine how to properly handle the task.</p> <p>This action initiates a long poll, where the service holds the HTTP connection open and responds as soon a task becomes available. If no decision task is available in the specified task list before the timeout of 60 seconds expires, an empty result is returned. An empty result, in this context, means that a DecisionTask is returned, but that the value of <code>taskToken</code> is an empty string.</p> <important>Deciders should set their client-side socket timeout to at least 70 seconds (10 seconds higher than the timeout).</important> <important>Because the number of workflow history events for a single workflow execution might be very large, the result returned might be split up across a number of pages. To retrieve subsequent pages, make additional calls to <code>PollForDecisionTask</code> using the <code>nextPageToken</code> returned by the initial call. Note that you do <b>not</b> call <code>GetWorkflowExecutionHistory</code> with this <code>nextPageToken</code>. Instead, call <code>PollForDecisionTask</code> again.</important> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the <code>taskList.name</code> parameter by using a <b>Condition</b> element with the <code>swf:taskList.name</code> key to allow the action to access only certain task lists.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/DecisionTask'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
        '482':
          description: LimitExceededFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/LimitExceededFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/PollForDecisionTaskInput'
        - name: maximumPageSize
          type: string
          in: query
          description: Pagination limit
          required: false
        - name: nextPageToken
          type: string
          in: query
          description: Pagination token
          required: false
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#RecordActivityTaskHeartbeat':
    post:
      operationId: RecordActivityTaskHeartbeat
      description: '<p>Used by activity workers to report to the service that the <a>ActivityTask</a> represented by the specified <code>taskToken</code> is still making progress. The worker can also (optionally) specify details of the progress, for example percent complete, using the <code>details</code> parameter. This action can also be used by the worker as a mechanism to check if cancellation is being requested for the activity task. If a cancellation is being attempted for the specified task, then the boolean <code>cancelRequested</code> flag returned by the service is set to <code>true</code>.</p> <p>This action resets the <code>taskHeartbeatTimeout</code> clock. The <code>taskHeartbeatTimeout</code> is specified in <a>RegisterActivityType</a>.</p> <p>This action does not in itself create an event in the workflow execution history. However, if the task times out, the workflow execution history will contain a <code>ActivityTaskTimedOut</code> event that contains the information from the last heartbeat generated by the activity worker.</p> <note>The <code>taskStartToCloseTimeout</code> of an activity type is the maximum duration of an activity task, regardless of the number of <a>RecordActivityTaskHeartbeat</a> requests received. The <code>taskStartToCloseTimeout</code> is also specified in <a>RegisterActivityType</a>.</note> <note>This operation is only useful for long-lived activities to report liveliness of the task and to determine if a cancellation is being attempted. </note> <important>If the <code>cancelRequested</code> flag returns <code>true</code>, a cancellation is being attempted. If the worker can cancel the activity, it should respond with <a>RespondActivityTaskCanceled</a>. Otherwise, it should ignore the cancellation request.</important> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/ActivityTaskStatus'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/RecordActivityTaskHeartbeatInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#RegisterActivityType':
    post:
      operationId: RegisterActivityType
      description: '<p>Registers a new <i>activity type</i> along with its configuration settings in the specified domain.</p> <important>A <code>TypeAlreadyExists</code> fault is returned if the type already exists in the domain. You cannot change any configuration settings of the type after its registration, and it must be registered as a new version.</important> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li> <code>defaultTaskList.name</code>: String constraint. The key is <code>swf:defaultTaskList.name</code>.</li> <li> <code>name</code>: String constraint. The key is <code>swf:name</code>.</li> <li> <code>version</code>: String constraint. The key is <code>swf:version</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
        '480':
          description: TypeAlreadyExistsFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/TypeAlreadyExistsFault'
        '481':
          description: LimitExceededFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/LimitExceededFault'
        '482':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '483':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/RegisterActivityTypeInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#RegisterDomain':
    post:
      operationId: RegisterDomain
      description: '<p>Registers a new domain.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>You cannot use an IAM policy to control domain access for this action. The name of the domain being registered is available as the resource of this action.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
        '480':
          description: DomainAlreadyExistsFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/DomainAlreadyExistsFault'
        '481':
          description: LimitExceededFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/LimitExceededFault'
        '482':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/RegisterDomainInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#RegisterWorkflowType':
    post:
      operationId: RegisterWorkflowType
      description: '<p>Registers a new <i>workflow type</i> and its configuration settings in the specified domain.</p> <p>The retention period for the workflow history is set by the <a>RegisterDomain</a> action.</p> <important>If the type already exists, then a <code>TypeAlreadyExists</code> fault is returned. You cannot change the configuration settings of a workflow type once it is registered and it must be registered as a new version.</important> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li> <code>defaultTaskList.name</code>: String constraint. The key is <code>swf:defaultTaskList.name</code>.</li> <li> <code>name</code>: String constraint. The key is <code>swf:name</code>.</li> <li> <code>version</code>: String constraint. The key is <code>swf:version</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
        '480':
          description: TypeAlreadyExistsFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/TypeAlreadyExistsFault'
        '481':
          description: LimitExceededFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/LimitExceededFault'
        '482':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '483':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/RegisterWorkflowTypeInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#RequestCancelWorkflowExecution':
    post:
      operationId: RequestCancelWorkflowExecution
      description: '<p>Records a <code>WorkflowExecutionCancelRequested</code> event in the currently running workflow execution identified by the given domain, workflowId, and runId. This logically requests the cancellation of the workflow execution as a whole. It is up to the decider to take appropriate actions when it receives an execution history with this event.</p> <note>If the runId is not specified, the <code>WorkflowExecutionCancelRequested</code> event is recorded in the history of the current open workflow execution with the specified workflowId in the domain.</note> <note>Because this action allows the workflow to properly clean up and gracefully close, it should be used instead of <a>TerminateWorkflowExecution</a> when possible.</note> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/RequestCancelWorkflowExecutionInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#RespondActivityTaskCanceled':
    post:
      operationId: RespondActivityTaskCanceled
      description: '<p>Used by workers to tell the service that the <a>ActivityTask</a> identified by the <code>taskToken</code> was successfully canceled. Additional <code>details</code> can be optionally provided using the <code>details</code> argument.</p> <p>These <code>details</code> (if provided) appear in the <code>ActivityTaskCanceled</code> event added to the workflow history.</p> <important>Only use this operation if the <code>canceled</code> flag of a <a>RecordActivityTaskHeartbeat</a> request returns <code>true</code> and if the activity can be safely undone or abandoned.</important> <p>A task is considered open from the time that it is scheduled until it is closed. Therefore a task is reported as open while a worker is processing it. A task is closed after it has been specified in a call to <a>RespondActivityTaskCompleted</a>, RespondActivityTaskCanceled, <a>RespondActivityTaskFailed</a>, or the task has <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dg-basic.html#swf-dev-timeout-types">timed out</a>.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/RespondActivityTaskCanceledInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#RespondActivityTaskCompleted':
    post:
      operationId: RespondActivityTaskCompleted
      description: '<p>Used by workers to tell the service that the <a>ActivityTask</a> identified by the <code>taskToken</code> completed successfully with a <code>result</code> (if provided). The <code>result</code> appears in the <code>ActivityTaskCompleted</code> event in the workflow history.</p> <important> If the requested task does not complete successfully, use <a>RespondActivityTaskFailed</a> instead. If the worker finds that the task is canceled through the <code>canceled</code> flag returned by <a>RecordActivityTaskHeartbeat</a>, it should cancel the task, clean up and then call <a>RespondActivityTaskCanceled</a>.</important> <p>A task is considered open from the time that it is scheduled until it is closed. Therefore a task is reported as open while a worker is processing it. A task is closed after it has been specified in a call to RespondActivityTaskCompleted, <a>RespondActivityTaskCanceled</a>, <a>RespondActivityTaskFailed</a>, or the task has <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dg-basic.html#swf-dev-timeout-types">timed out</a>.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/RespondActivityTaskCompletedInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#RespondActivityTaskFailed':
    post:
      operationId: RespondActivityTaskFailed
      description: '<p>Used by workers to tell the service that the <a>ActivityTask</a> identified by the <code>taskToken</code> has failed with <code>reason</code> (if specified). The <code>reason</code> and <code>details</code> appear in the <code>ActivityTaskFailed</code> event added to the workflow history.</p> <p>A task is considered open from the time that it is scheduled until it is closed. Therefore a task is reported as open while a worker is processing it. A task is closed after it has been specified in a call to <a>RespondActivityTaskCompleted</a>, <a>RespondActivityTaskCanceled</a>, RespondActivityTaskFailed, or the task has <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dg-basic.html#swf-dev-timeout-types">timed out</a>.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/RespondActivityTaskFailedInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#RespondDecisionTaskCompleted':
    post:
      operationId: RespondDecisionTaskCompleted
      description: '<p>Used by deciders to tell the service that the <a>DecisionTask</a> identified by the <code>taskToken</code> has successfully completed. The <code>decisions</code> argument specifies the list of decisions made while processing the task.</p> <p>A <code>DecisionTaskCompleted</code> event is added to the workflow history. The <code>executionContext</code> specified is attached to the event in the workflow execution history.</p> <p><b>Access Control</b></p> <p>If an IAM policy grants permission to use <code>RespondDecisionTaskCompleted</code>, it can express permissions for the list of decisions in the <code>decisions</code> parameter. Each of the decisions has one or more parameters, much like a regular API call. To allow for policies to be as readable as possible, you can express permissions on decisions as if they were actual API calls, including applying conditions to some parameters. For more information, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/RespondDecisionTaskCompletedInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#SignalWorkflowExecution':
    post:
      operationId: SignalWorkflowExecution
      description: '<p>Records a <code>WorkflowExecutionSignaled</code> event in the workflow execution history and creates a decision task for the workflow execution identified by the given domain, workflowId and runId. The event is recorded with the specified user defined signalName and input (if provided).</p> <note> If a runId is not specified, then the <code>WorkflowExecutionSignaled</code> event is recorded in the history of the current open workflow with the matching workflowId in the domain.</note> <note> If the specified workflow execution is not open, this method fails with <code>UnknownResource</code>.</note> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/SignalWorkflowExecutionInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#StartWorkflowExecution':
    post:
      operationId: StartWorkflowExecution
      description: '<p>Starts an execution of the workflow type in the specified domain using the provided <code>workflowId</code> and input data.</p> <p>This action returns the newly started workflow execution.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li> <code>tagList.member.0</code>: The key is <code>swf:tagList.member.0</code>.</li> <li> <code>tagList.member.1</code>: The key is <code>swf:tagList.member.1</code>.</li> <li> <code>tagList.member.2</code>: The key is <code>swf:tagList.member.2</code>.</li> <li> <code>tagList.member.3</code>: The key is <code>swf:tagList.member.3</code>.</li> <li> <code>tagList.member.4</code>: The key is <code>swf:tagList.member.4</code>.</li> <li><code>taskList</code>: String constraint. The key is <code>swf:taskList.name</code>.</li> <li><code>workflowType.name</code>: String constraint. The key is <code>swf:workflowType.name</code>.</li> <li><code>workflowType.version</code>: String constraint. The key is <code>swf:workflowType.version</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/Run'
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: TypeDeprecatedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/TypeDeprecatedFault'
        '482':
          description: WorkflowExecutionAlreadyStartedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/WorkflowExecutionAlreadyStartedFault'
        '483':
          description: LimitExceededFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/LimitExceededFault'
        '484':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
        '485':
          description: DefaultUndefinedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/DefaultUndefinedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/StartWorkflowExecutionInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
  '/#TerminateWorkflowExecution':
    post:
      operationId: TerminateWorkflowExecution
      description: '<p>Records a <code>WorkflowExecutionTerminated</code> event and forces closure of the workflow execution identified by the given domain, runId, and workflowId. The child policy, registered with the workflow type or specified when starting this execution, is applied to any open child workflow executions of this workflow execution.</p> <important> If the identified workflow execution was in progress, it is terminated immediately.</important> <note> If a runId is not specified, then the <code>WorkflowExecutionTerminated</code> event is recorded in the history of the current open workflow with the matching workflowId in the domain.</note> <note> You should consider using <a>RequestCancelWorkflowExecution</a> action instead because it allows the workflow to gracefully close while <a>TerminateWorkflowExecution</a> does not.</note> <p><b>Access Control</b></p> <p>You can use IAM policies to control this action''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
      responses:
        '200':
          description: Success
        '480':
          description: UnknownResourceFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/UnknownResourceFault'
        '481':
          description: OperationNotPermittedFault
          x-aws-exception: true
          schema:
            $ref: '#/definitions/OperationNotPermittedFault'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/TerminateWorkflowExecutionInput'
    parameters:
      - $ref: '#/parameters/Action'
      - $ref: '#/parameters/Version'
      - $ref: '#/parameters/X-Amz-Content-Sha256'
      - $ref: '#/parameters/X-Amz-Date'
      - $ref: '#/parameters/X-Amz-Algorithm'
      - $ref: '#/parameters/X-Amz-Credential'
      - $ref: '#/parameters/X-Amz-Security-Token'
      - $ref: '#/parameters/X-Amz-Signature'
      - $ref: '#/parameters/X-Amz-SignedHeaders'
definitions:
  ActivityId:
    type: string
    minLength: 1
    maxLength: 256
  ActivityTask:
    type: object
    required:
      - taskToken
      - activityId
      - startedEventId
      - workflowExecution
      - activityType
    properties:
      taskToken:
        $ref: '#/definitions/TaskToken'
        description: The opaque string used as a handle on the task. This token is used by workers to communicate progress and response information back to the system about the task.
      activityId:
        $ref: '#/definitions/ActivityId'
        description: The unique ID of the task.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ActivityTaskStarted</code> event recorded in the history.
      workflowExecution:
        $ref: '#/definitions/WorkflowExecution'
        description: The workflow execution that started this activity task.
      activityType:
        $ref: '#/definitions/ActivityType'
        description: The type of this activity task.
      input:
        $ref: '#/definitions/Data'
        description: The inputs provided when the activity task was scheduled. The form of the input is user defined and should be meaningful to the activity implementation.
    description: Unit of work sent to an activity worker.
  ActivityTaskCancelRequestedEventAttributes:
    type: object
    required:
      - decisionTaskCompletedEventId
      - activityId
    properties:
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>RequestCancelActivityTask</code> decision for this cancellation request. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      activityId:
        $ref: '#/definitions/ActivityId'
        description: The unique ID of the task.
    description: Provides details of the <code>ActivityTaskCancelRequested</code> event.
  ActivityTaskCanceledEventAttributes:
    type: object
    required:
      - scheduledEventId
      - startedEventId
    properties:
      details:
        $ref: '#/definitions/Data'
        description: Details of the cancellation (if any).
      scheduledEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ActivityTaskScheduled</code> event that was recorded when this activity task was scheduled. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ActivityTaskStarted</code> event recorded when this activity task was started. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      latestCancelRequestedEventId:
        $ref: '#/definitions/EventId'
        description: 'If set, contains the ID of the last <code>ActivityTaskCancelRequested</code> event recorded for this activity task. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.'
    description: Provides details of the <code>ActivityTaskCanceled</code> event.
  ActivityTaskCompletedEventAttributes:
    type: object
    required:
      - scheduledEventId
      - startedEventId
    properties:
      result:
        $ref: '#/definitions/Data'
        description: The results of the activity task (if any).
      scheduledEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ActivityTaskScheduled</code> event that was recorded when this activity task was scheduled. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ActivityTaskStarted</code> event recorded when this activity task was started. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>ActivityTaskCompleted</code> event.
  ActivityTaskFailedEventAttributes:
    type: object
    required:
      - scheduledEventId
      - startedEventId
    properties:
      reason:
        $ref: '#/definitions/FailureReason'
        description: The reason provided for the failure (if any).
      details:
        $ref: '#/definitions/Data'
        description: The details of the failure (if any).
      scheduledEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ActivityTaskScheduled</code> event that was recorded when this activity task was scheduled. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ActivityTaskStarted</code> event recorded when this activity task was started. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>ActivityTaskFailed</code> event.
  ActivityTaskScheduledEventAttributes:
    type: object
    required:
      - activityType
      - activityId
      - taskList
      - decisionTaskCompletedEventId
    properties:
      activityType:
        $ref: '#/definitions/ActivityType'
        description: The type of the activity task.
      activityId:
        $ref: '#/definitions/ActivityId'
        description: The unique ID of the activity task.
      input:
        $ref: '#/definitions/Data'
        description: The input provided to the activity task.
      control:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Data attached to the event that can be used by the decider in subsequent workflow tasks. This data is not sent to the activity.
      scheduleToStartTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: The maximum amount of time the activity task can wait to be assigned to a worker.
      scheduleToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: The maximum amount of time for this activity task.
      startToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: The maximum amount of time a worker may take to process the activity task.
      taskList:
        $ref: '#/definitions/TaskList'
        description: The task list in which the activity task has been scheduled.
      taskPriority:
        $ref: '#/definitions/TaskPriority'
        description: '<p><i>Optional.</i> The priority to assign to the scheduled activity task. If set, this will override any default priority value that was assigned when the activity type was registered.</p> <p>Valid values are integers that range from Java''s <code>Integer.MIN_VALUE</code> (-2147483648) to <code>Integer.MAX_VALUE</code> (2147483647). Higher numbers indicate higher priority.</p> <p>For more information about setting task priority, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/programming-priority.html">Setting Task Priority</a> in the <i>Amazon Simple Workflow Developer Guide</i>.</p>'
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision that resulted in the scheduling of this activity task. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      heartbeatTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: 'The maximum time before which the worker processing this task must report progress by calling <a>RecordActivityTaskHeartbeat</a>. If the timeout is exceeded, the activity task is automatically timed out. If the worker subsequently attempts to record a heartbeat or return a result, it will be ignored.'
    description: Provides details of the <code>ActivityTaskScheduled</code> event.
  ActivityTaskStartedEventAttributes:
    type: object
    required:
      - scheduledEventId
    properties:
      identity:
        $ref: '#/definitions/Identity'
        description: Identity of the worker that was assigned this task. This aids diagnostics when problems arise. The form of this identity is user defined.
      scheduledEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ActivityTaskScheduled</code> event that was recorded when this activity task was scheduled. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>ActivityTaskStarted</code> event.
  ActivityTaskStatus:
    type: object
    required:
      - cancelRequested
    properties:
      cancelRequested:
        $ref: '#/definitions/Canceled'
        description: Set to <code>true</code> if cancellation of the task is requested.
    description: Status information about an activity task.
  ActivityTaskTimedOutEventAttributes:
    type: object
    required:
      - timeoutType
      - scheduledEventId
      - startedEventId
    properties:
      timeoutType:
        $ref: '#/definitions/ActivityTaskTimeoutType'
        description: The type of the timeout that caused this event.
      scheduledEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ActivityTaskScheduled</code> event that was recorded when this activity task was scheduled. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ActivityTaskStarted</code> event recorded when this activity task was started. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      details:
        $ref: '#/definitions/LimitedData'
        description: Contains the content of the <code>details</code> parameter for the last call made by the activity to <code>RecordActivityTaskHeartbeat</code>.
    description: Provides details of the <code>ActivityTaskTimedOut</code> event.
  ActivityTaskTimeoutType:
    type: string
    enum:
      - START_TO_CLOSE
      - SCHEDULE_TO_START
      - SCHEDULE_TO_CLOSE
      - HEARTBEAT
  ActivityType:
    type: object
    required:
      - name
      - version
    properties:
      name:
        $ref: '#/definitions/Name'
        description: <p>The name of this activity.</p> <note>The combination of activity type name and version must be unique within a domain.</note>
      version:
        $ref: '#/definitions/Version'
        description: <p>The version of this activity.</p> <note>The combination of activity type name and version must be unique with in a domain.</note>
    description: Represents an activity type.
  ActivityTypeConfiguration:
    type: object
    properties:
      defaultTaskStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: <p><i>Optional.</i> The default maximum duration for tasks of an activity type specified when registering the activity type. You can override this default when scheduling a task through the <code>ScheduleActivityTask</code> decision.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>
      defaultTaskHeartbeatTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p><i>Optional.</i> The default maximum time, in seconds, before which a worker processing a task must report progress by calling <a>RecordActivityTaskHeartbeat</a>.</p> <p>You can specify this value only when <i>registering</i> an activity type. The registered default value can be overridden when you schedule a task through the <code>ScheduleActivityTask</code> decision. If the activity worker subsequently attempts to record a heartbeat or returns a result, the activity worker receives an <code>UnknownResource</code> fault. In this case, Amazon SWF no longer considers the activity task to be valid; the activity worker should clean up the activity task.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>'
      defaultTaskList:
        $ref: '#/definitions/TaskList'
        description: <i>Optional.</i> The default task list specified for this activity type at registration. This default is used if a task list is not provided when a task is scheduled through the <code>ScheduleActivityTask</code> decision. You can override the default registered task list when scheduling a task through the <code>ScheduleActivityTask</code> decision.
      defaultTaskPriority:
        $ref: '#/definitions/TaskPriority'
        description: '<p><i>Optional.</i> The default task priority for tasks of this activity type, specified at registration. If not set, then "0" will be used as the default priority. This default can be overridden when scheduling an activity task.</p> <p>Valid values are integers that range from Java''s <code>Integer.MIN_VALUE</code> (-2147483648) to <code>Integer.MAX_VALUE</code> (2147483647). Higher numbers indicate higher priority.</p> <p>For more information about setting task priority, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/programming-priority.html">Setting Task Priority</a> in the <i>Amazon Simple Workflow Developer Guide</i>.</p>'
      defaultTaskScheduleToStartTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p><i>Optional.</i> The default maximum duration, specified when registering the activity type, that a task of an activity type can wait before being assigned to a worker. You can override this default when scheduling a task through the <code>ScheduleActivityTask</code> decision.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>'
      defaultTaskScheduleToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p><i>Optional.</i> The default maximum duration, specified when registering the activity type, for tasks of this activity type. You can override this default when scheduling a task through the <code>ScheduleActivityTask</code> decision.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>'
    description: Configuration settings registered with the activity type.
  ActivityTypeDetail:
    type: object
    required:
      - typeInfo
      - configuration
    properties:
      typeInfo:
        $ref: '#/definitions/ActivityTypeInfo'
        description: '<p>General information about the activity type.</p> <p>The status of activity type (returned in the ActivityTypeInfo structure) can be one of the following.</p> <ul> <li> <b>REGISTERED</b>: The type is registered and available. Workers supporting this type should be running. </li> <li> <b>DEPRECATED</b>: The type was deprecated using <a>DeprecateActivityType</a>, but is still in use. You should keep workers supporting this type running. You cannot create new tasks of this type. </li> </ul>'
      configuration:
        $ref: '#/definitions/ActivityTypeConfiguration'
        description: The configuration settings registered with the activity type.
    description: Detailed information about an activity type.
  ActivityTypeInfo:
    type: object
    required:
      - activityType
      - status
      - creationDate
    properties:
      activityType:
        $ref: '#/definitions/ActivityType'
        description: The <a>ActivityType</a> type structure representing the activity type.
      status:
        $ref: '#/definitions/RegistrationStatus'
        description: The current status of the activity type.
      description:
        $ref: '#/definitions/Description'
        description: The description of the activity type provided in <a>RegisterActivityType</a>.
      creationDate:
        $ref: '#/definitions/Timestamp'
        description: The date and time this activity type was created through <a>RegisterActivityType</a>.
      deprecationDate:
        $ref: '#/definitions/Timestamp'
        description: 'If DEPRECATED, the date and time <a>DeprecateActivityType</a> was called.'
    description: Detailed information about an activity type.
  ActivityTypeInfoList:
    type: array
    items:
      $ref: '#/definitions/ActivityTypeInfo'
  ActivityTypeInfos:
    type: object
    required:
      - typeInfos
    properties:
      typeInfos:
        $ref: '#/definitions/ActivityTypeInfoList'
        description: List of activity type information.
      nextPageToken:
        $ref: '#/definitions/PageToken'
        description: '<p>If a <code>NextPageToken</code> was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in <code>nextPageToken</code>. Keep all other arguments unchanged.</p> <p>The configured <code>maximumPageSize</code> determines how many results can be returned in a single call.</p>'
    description: Contains a paginated list of activity type information structures.
  Arn:
    type: string
    minLength: 1
    maxLength: 1224
  CancelTimerDecisionAttributes:
    type: object
    required:
      - timerId
    properties:
      timerId:
        $ref: '#/definitions/TimerId'
        description: <b>Required.</b> The unique ID of the timer to cancel.
    description: '<p>Provides details of the <code>CancelTimer</code> decision.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this decision''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
  CancelTimerFailedCause:
    type: string
    enum:
      - TIMER_ID_UNKNOWN
      - OPERATION_NOT_PERMITTED
  CancelTimerFailedEventAttributes:
    type: object
    required:
      - timerId
      - cause
      - decisionTaskCompletedEventId
    properties:
      timerId:
        $ref: '#/definitions/TimerId'
        description: The timerId provided in the <code>CancelTimer</code> decision that failed.
      cause:
        $ref: '#/definitions/CancelTimerFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>CancelTimer</code> decision to cancel this timer. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>CancelTimerFailed</code> event.
  CancelWorkflowExecutionDecisionAttributes:
    type: object
    properties:
      details:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> details of the cancellation.
    description: '<p>Provides details of the <code>CancelWorkflowExecution</code> decision.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this decision''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
  CancelWorkflowExecutionFailedCause:
    type: string
    enum:
      - UNHANDLED_DECISION
      - OPERATION_NOT_PERMITTED
  CancelWorkflowExecutionFailedEventAttributes:
    type: object
    required:
      - cause
      - decisionTaskCompletedEventId
    properties:
      cause:
        $ref: '#/definitions/CancelWorkflowExecutionFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>CancelWorkflowExecution</code> decision for this cancellation request. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>CancelWorkflowExecutionFailed</code> event.
  Canceled:
    type: boolean
  CauseMessage:
    type: string
    maxLength: 1728
  ChildPolicy:
    type: string
    enum:
      - TERMINATE
      - REQUEST_CANCEL
      - ABANDON
  ChildWorkflowExecutionCanceledEventAttributes:
    type: object
    required:
      - workflowExecution
      - workflowType
      - initiatedEventId
      - startedEventId
    properties:
      workflowExecution:
        $ref: '#/definitions/WorkflowExecution'
        description: The child workflow execution that was canceled.
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The type of the child workflow execution.
      details:
        $ref: '#/definitions/Data'
        description: Details of the cancellation (if provided).
      initiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>StartChildWorkflowExecutionInitiated</code> event corresponding to the <code>StartChildWorkflowExecution</code> decision to start this child workflow execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ChildWorkflowExecutionStarted</code> event recorded when this child workflow execution was started. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provide details of the <code>ChildWorkflowExecutionCanceled</code> event.
  ChildWorkflowExecutionCompletedEventAttributes:
    type: object
    required:
      - workflowExecution
      - workflowType
      - initiatedEventId
      - startedEventId
    properties:
      workflowExecution:
        $ref: '#/definitions/WorkflowExecution'
        description: The child workflow execution that was completed.
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The type of the child workflow execution.
      result:
        $ref: '#/definitions/Data'
        description: The result of the child workflow execution (if any).
      initiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>StartChildWorkflowExecutionInitiated</code> event corresponding to the <code>StartChildWorkflowExecution</code> decision to start this child workflow execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ChildWorkflowExecutionStarted</code> event recorded when this child workflow execution was started. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>ChildWorkflowExecutionCompleted</code> event.
  ChildWorkflowExecutionFailedEventAttributes:
    type: object
    required:
      - workflowExecution
      - workflowType
      - initiatedEventId
      - startedEventId
    properties:
      workflowExecution:
        $ref: '#/definitions/WorkflowExecution'
        description: The child workflow execution that failed.
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The type of the child workflow execution.
      reason:
        $ref: '#/definitions/FailureReason'
        description: The reason for the failure (if provided).
      details:
        $ref: '#/definitions/Data'
        description: The details of the failure (if provided).
      initiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>StartChildWorkflowExecutionInitiated</code> event corresponding to the <code>StartChildWorkflowExecution</code> decision to start this child workflow execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ChildWorkflowExecutionStarted</code> event recorded when this child workflow execution was started. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>ChildWorkflowExecutionFailed</code> event.
  ChildWorkflowExecutionStartedEventAttributes:
    type: object
    required:
      - workflowExecution
      - workflowType
      - initiatedEventId
    properties:
      workflowExecution:
        $ref: '#/definitions/WorkflowExecution'
        description: The child workflow execution that was started.
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: 'The type of the child workflow execution. '
      initiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>StartChildWorkflowExecutionInitiated</code> event corresponding to the <code>StartChildWorkflowExecution</code> decision to start this child workflow execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>ChildWorkflowExecutionStarted</code> event.
  ChildWorkflowExecutionTerminatedEventAttributes:
    type: object
    required:
      - workflowExecution
      - workflowType
      - initiatedEventId
      - startedEventId
    properties:
      workflowExecution:
        $ref: '#/definitions/WorkflowExecution'
        description: The child workflow execution that was terminated.
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The type of the child workflow execution.
      initiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>StartChildWorkflowExecutionInitiated</code> event corresponding to the <code>StartChildWorkflowExecution</code> decision to start this child workflow execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ChildWorkflowExecutionStarted</code> event recorded when this child workflow execution was started. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>ChildWorkflowExecutionTerminated</code> event.
  ChildWorkflowExecutionTimedOutEventAttributes:
    type: object
    required:
      - workflowExecution
      - workflowType
      - timeoutType
      - initiatedEventId
      - startedEventId
    properties:
      workflowExecution:
        $ref: '#/definitions/WorkflowExecution'
        description: The child workflow execution that timed out.
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The type of the child workflow execution.
      timeoutType:
        $ref: '#/definitions/WorkflowExecutionTimeoutType'
        description: The type of the timeout that caused the child workflow execution to time out.
      initiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>StartChildWorkflowExecutionInitiated</code> event corresponding to the <code>StartChildWorkflowExecution</code> decision to start this child workflow execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>ChildWorkflowExecutionStarted</code> event recorded when this child workflow execution was started. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>ChildWorkflowExecutionTimedOut</code> event.
  CloseStatus:
    type: string
    enum:
      - COMPLETED
      - FAILED
      - CANCELED
      - TERMINATED
      - CONTINUED_AS_NEW
      - TIMED_OUT
  CloseStatusFilter:
    type: object
    required:
      - status
    properties:
      status:
        $ref: '#/definitions/CloseStatus'
        description: <b>Required.</b> The close status that must match the close status of an execution for it to meet the criteria of this filter.
    description: Used to filter the closed workflow executions in visibility APIs by their close status.
  CompleteWorkflowExecutionDecisionAttributes:
    type: object
    properties:
      result:
        $ref: '#/definitions/Data'
        description: The result of the workflow execution. The form of the result is implementation defined.
    description: '<p>Provides details of the <code>CompleteWorkflowExecution</code> decision.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this decision''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
  CompleteWorkflowExecutionFailedCause:
    type: string
    enum:
      - UNHANDLED_DECISION
      - OPERATION_NOT_PERMITTED
  CompleteWorkflowExecutionFailedEventAttributes:
    type: object
    required:
      - cause
      - decisionTaskCompletedEventId
    properties:
      cause:
        $ref: '#/definitions/CompleteWorkflowExecutionFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>CompleteWorkflowExecution</code> decision to complete this execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>CompleteWorkflowExecutionFailed</code> event.
  ContinueAsNewWorkflowExecutionDecisionAttributes:
    type: object
    properties:
      input:
        $ref: '#/definitions/Data'
        description: The input provided to the new workflow execution.
      executionStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p>If set, specifies the total duration for this workflow execution. This overrides the <code>defaultExecutionStartToCloseTimeout</code> specified when registering the workflow type.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p> <note>An execution start-to-close timeout for this workflow execution must be specified either as a default for the workflow type or through this field. If neither this field is set nor a default execution start-to-close timeout was specified at registration time then a fault will be returned.</note>'
      taskList:
        $ref: '#/definitions/TaskList'
      taskPriority:
        $ref: '#/definitions/TaskPriority'
        description: '<p><i>Optional.</i> The task priority that, if set, specifies the priority for the decision tasks for this workflow execution. This overrides the defaultTaskPriority specified when registering the workflow type. Valid values are integers that range from Java''s <code>Integer.MIN_VALUE</code> (-2147483648) to <code>Integer.MAX_VALUE</code> (2147483647). Higher numbers indicate higher priority.</p> <p>For more information about setting task priority, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/programming-priority.html">Setting Task Priority</a> in the <i>Amazon Simple Workflow Developer Guide</i>.</p>'
      taskStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: <p>Specifies the maximum duration of decision tasks for the new workflow execution. This parameter overrides the <code>defaultTaskStartToCloseTimout</code> specified when registering the workflow type using <a>RegisterWorkflowType</a>.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p> <note>A task start-to-close timeout for the new workflow execution must be specified either as a default for the workflow type or through this parameter. If neither this parameter is set nor a default task start-to-close timeout was specified at registration time then a fault will be returned.</note>
      childPolicy:
        $ref: '#/definitions/ChildPolicy'
        description: '<p>If set, specifies the policy to use for the child workflow executions of the new execution if it is terminated by calling the <a>TerminateWorkflowExecution</a> action explicitly or due to an expired timeout. This policy overrides the default child policy specified when registering the workflow type using <a>RegisterWorkflowType</a>.</p> <p>The supported child policies are:</p> <ul> <li><b>TERMINATE:</b> the child executions will be terminated.</li> <li><b>REQUEST_CANCEL:</b> a request to cancel will be attempted for each child execution by recording a <code>WorkflowExecutionCancelRequested</code> event in its history. It is up to the decider to take appropriate actions when it receives an execution history with this event.</li> <li><b>ABANDON:</b> no action will be taken. The child executions will continue to run.</li> </ul> <note>A child policy for this workflow execution must be specified either as a default for the workflow type or through this parameter. If neither this parameter is set nor a default child policy was specified at registration time then a fault will be returned.</note>'
      tagList:
        $ref: '#/definitions/TagList'
        description: The list of tags to associate with the new workflow execution. A maximum of 5 tags can be specified. You can list workflow executions with a specific tag by calling <a>ListOpenWorkflowExecutions</a> or <a>ListClosedWorkflowExecutions</a> and specifying a <a>TagFilter</a>.
      workflowTypeVersion:
        $ref: '#/definitions/Version'
      lambdaRole:
        $ref: '#/definitions/Arn'
        description: '<p>The ARN of an IAM role that authorizes Amazon SWF to invoke AWS Lambda functions.</p> <note>In order for this workflow execution to invoke AWS Lambda functions, an appropriate IAM role must be specified either as a default for the workflow type or through this field.</note>'
    description: '<p>Provides details of the <code>ContinueAsNewWorkflowExecution</code> decision.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this decision''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li> <code>tag</code>: <i>Optional.</i>. A tag used to identify the workflow execution</li> <li><code>taskList</code>: String constraint. The key is <code>swf:taskList.name</code>.</li> <li><code>workflowType.version</code>: String constraint. The key is <code>swf:workflowType.version</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
  ContinueAsNewWorkflowExecutionFailedCause:
    type: string
    enum:
      - UNHANDLED_DECISION
      - WORKFLOW_TYPE_DEPRECATED
      - WORKFLOW_TYPE_DOES_NOT_EXIST
      - DEFAULT_EXECUTION_START_TO_CLOSE_TIMEOUT_UNDEFINED
      - DEFAULT_TASK_START_TO_CLOSE_TIMEOUT_UNDEFINED
      - DEFAULT_TASK_LIST_UNDEFINED
      - DEFAULT_CHILD_POLICY_UNDEFINED
      - CONTINUE_AS_NEW_WORKFLOW_EXECUTION_RATE_EXCEEDED
      - OPERATION_NOT_PERMITTED
  ContinueAsNewWorkflowExecutionFailedEventAttributes:
    type: object
    required:
      - cause
      - decisionTaskCompletedEventId
    properties:
      cause:
        $ref: '#/definitions/ContinueAsNewWorkflowExecutionFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>ContinueAsNewWorkflowExecution</code> decision that started this execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>ContinueAsNewWorkflowExecutionFailed</code> event.
  Count:
    type: integer
    minimum: 0
  CountClosedWorkflowExecutionsInput:
    type: object
    required:
      - domain
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain containing the workflow executions to count.
      startTimeFilter:
        $ref: '#/definitions/ExecutionTimeFilter'
        description: '<p>If specified, only workflow executions that meet the start time criteria of the filter are counted.</p> <note><code>startTimeFilter</code> and <code>closeTimeFilter</code> are mutually exclusive. You must specify one of these in a request but not both.</note>'
      closeTimeFilter:
        $ref: '#/definitions/ExecutionTimeFilter'
        description: '<p>If specified, only workflow executions that meet the close time criteria of the filter are counted.</p> <note><code>startTimeFilter</code> and <code>closeTimeFilter</code> are mutually exclusive. You must specify one of these in a request but not both.</note>'
      executionFilter:
        $ref: '#/definitions/WorkflowExecutionFilter'
        description: '<p>If specified, only workflow executions matching the <code>WorkflowId</code> in the filter are counted.</p> <note><code>closeStatusFilter</code>, <code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
      typeFilter:
        $ref: '#/definitions/WorkflowTypeFilter'
        description: '<p>If specified, indicates the type of the workflow executions to be counted.</p> <note><code>closeStatusFilter</code>, <code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
      tagFilter:
        $ref: '#/definitions/TagFilter'
        description: '<p>If specified, only executions that have a tag that matches the filter are counted.</p> <note><code>closeStatusFilter</code>, <code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
      closeStatusFilter:
        $ref: '#/definitions/CloseStatusFilter'
        description: '<p>If specified, only workflow executions that match this close status are counted. This filter has an affect only if <code>executionStatus</code> is specified as <code>CLOSED</code>.</p> <note><code>closeStatusFilter</code>, <code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
  CountOpenWorkflowExecutionsInput:
    type: object
    required:
      - domain
      - startTimeFilter
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain containing the workflow executions to count.
      startTimeFilter:
        $ref: '#/definitions/ExecutionTimeFilter'
        description: Specifies the start time criteria that workflow executions must meet in order to be counted.
      typeFilter:
        $ref: '#/definitions/WorkflowTypeFilter'
        description: '<p>Specifies the type of the workflow executions to be counted.</p> <note><code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
      tagFilter:
        $ref: '#/definitions/TagFilter'
        description: '<p>If specified, only executions that have a tag that matches the filter are counted.</p> <note><code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
      executionFilter:
        $ref: '#/definitions/WorkflowExecutionFilter'
        description: '<p>If specified, only workflow executions matching the <code>WorkflowId</code> in the filter are counted.</p> <note><code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
  CountPendingActivityTasksInput:
    type: object
    required:
      - domain
      - taskList
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain that contains the task list.
      taskList:
        $ref: '#/definitions/TaskList'
        description: The name of the task list.
  CountPendingDecisionTasksInput:
    type: object
    required:
      - domain
      - taskList
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain that contains the task list.
      taskList:
        $ref: '#/definitions/TaskList'
        description: The name of the task list.
  Data:
    type: string
    maxLength: 32768
  Decision:
    type: object
    required:
      - decisionType
    properties:
      decisionType:
        $ref: '#/definitions/DecisionType'
        description: Specifies the type of the decision.
      scheduleActivityTaskDecisionAttributes:
        $ref: '#/definitions/ScheduleActivityTaskDecisionAttributes'
        description: Provides details of the <code>ScheduleActivityTask</code> decision. It is not set for other decision types.
      requestCancelActivityTaskDecisionAttributes:
        $ref: '#/definitions/RequestCancelActivityTaskDecisionAttributes'
        description: Provides details of the <code>RequestCancelActivityTask</code> decision. It is not set for other decision types.
      completeWorkflowExecutionDecisionAttributes:
        $ref: '#/definitions/CompleteWorkflowExecutionDecisionAttributes'
        description: Provides details of the <code>CompleteWorkflowExecution</code> decision. It is not set for other decision types.
      failWorkflowExecutionDecisionAttributes:
        $ref: '#/definitions/FailWorkflowExecutionDecisionAttributes'
        description: Provides details of the <code>FailWorkflowExecution</code> decision. It is not set for other decision types.
      cancelWorkflowExecutionDecisionAttributes:
        $ref: '#/definitions/CancelWorkflowExecutionDecisionAttributes'
        description: Provides details of the <code>CancelWorkflowExecution</code> decision. It is not set for other decision types.
      continueAsNewWorkflowExecutionDecisionAttributes:
        $ref: '#/definitions/ContinueAsNewWorkflowExecutionDecisionAttributes'
        description: Provides details of the <code>ContinueAsNewWorkflowExecution</code> decision. It is not set for other decision types.
      recordMarkerDecisionAttributes:
        $ref: '#/definitions/RecordMarkerDecisionAttributes'
        description: Provides details of the <code>RecordMarker</code> decision. It is not set for other decision types.
      startTimerDecisionAttributes:
        $ref: '#/definitions/StartTimerDecisionAttributes'
        description: Provides details of the <code>StartTimer</code> decision. It is not set for other decision types.
      cancelTimerDecisionAttributes:
        $ref: '#/definitions/CancelTimerDecisionAttributes'
        description: Provides details of the <code>CancelTimer</code> decision. It is not set for other decision types.
      signalExternalWorkflowExecutionDecisionAttributes:
        $ref: '#/definitions/SignalExternalWorkflowExecutionDecisionAttributes'
        description: Provides details of the <code>SignalExternalWorkflowExecution</code> decision. It is not set for other decision types.
      requestCancelExternalWorkflowExecutionDecisionAttributes:
        $ref: '#/definitions/RequestCancelExternalWorkflowExecutionDecisionAttributes'
        description: Provides details of the <code>RequestCancelExternalWorkflowExecution</code> decision. It is not set for other decision types.
      startChildWorkflowExecutionDecisionAttributes:
        $ref: '#/definitions/StartChildWorkflowExecutionDecisionAttributes'
        description: Provides details of the <code>StartChildWorkflowExecution</code> decision. It is not set for other decision types.
      scheduleLambdaFunctionDecisionAttributes:
        $ref: '#/definitions/ScheduleLambdaFunctionDecisionAttributes'
    description: '<p>Specifies a decision made by the decider. A decision can be one of these types:</p> <ul> <li> <b>CancelTimer</b>: cancels a previously started timer and records a <code>TimerCanceled</code> event in the history.</li> <li> <b>CancelWorkflowExecution</b>: closes the workflow execution and records a <code>WorkflowExecutionCanceled</code> event in the history.</li> <li> <b>CompleteWorkflowExecution</b>: closes the workflow execution and records a <code>WorkflowExecutionCompleted</code> event in the history .</li> <li> <b>ContinueAsNewWorkflowExecution</b>: closes the workflow execution and starts a new workflow execution of the same type using the same workflow ID and a unique run ID. A <code>WorkflowExecutionContinuedAsNew</code> event is recorded in the history.</li> <li> <b>FailWorkflowExecution</b>: closes the workflow execution and records a <code>WorkflowExecutionFailed</code> event in the history.</li> <li> <b>RecordMarker</b>: records a <code>MarkerRecorded</code> event in the history. Markers can be used for adding custom information in the history for instance to let deciders know that they do not need to look at the history beyond the marker event.</li> <li> <b>RequestCancelActivityTask</b>: attempts to cancel a previously scheduled activity task. If the activity task was scheduled but has not been assigned to a worker, then it will be canceled. If the activity task was already assigned to a worker, then the worker will be informed that cancellation has been requested in the response to <a>RecordActivityTaskHeartbeat</a>.</li> <li> <b>RequestCancelExternalWorkflowExecution</b>: requests that a request be made to cancel the specified external workflow execution and records a <code>RequestCancelExternalWorkflowExecutionInitiated</code> event in the history.</li> <li> <b>ScheduleActivityTask</b>: schedules an activity task.</li> <li> <b>ScheduleLambdaFunction</b>: schedules a AWS Lambda function.</li> <li> <b>SignalExternalWorkflowExecution</b>: requests a signal to be delivered to the specified external workflow execution and records a <code>SignalExternalWorkflowExecutionInitiated</code> event in the history.</li> <li> <b>StartChildWorkflowExecution</b>: requests that a child workflow execution be started and records a <code>StartChildWorkflowExecutionInitiated</code> event in the history. The child workflow execution is a separate workflow execution with its own history.</li> <li> <b>StartTimer</b>: starts a timer for this workflow execution and records a <code>TimerStarted</code> event in the history. This timer will fire after the specified delay and record a <code>TimerFired</code> event.</li> </ul> <p><b>Access Control</b></p> <p>If you grant permission to use <code>RespondDecisionTaskCompleted</code>, you can use IAM policies to express permissions for the list of decisions returned by this action as if they were members of the API. Treating decisions as a pseudo API maintains a uniform conceptual model and helps keep policies readable. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p> <p><b>Decision Failure</b></p> <p>Decisions can fail for several reasons</p> <ul> <li>The ordering of decisions should follow a logical flow. Some decisions might not make sense in the current context of the workflow execution and will therefore fail.</li> <li>A limit on your account was reached.</li> <li>The decision lacks sufficient permissions.</li> </ul> <p>One of the following events might be added to the history to indicate an error. The event attribute''s <b>cause</b> parameter indicates the cause. If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p> <ul> <li> <b>ScheduleActivityTaskFailed</b>: a ScheduleActivityTask decision failed. This could happen if the activity type specified in the decision is not registered, is in a deprecated state, or the decision is not properly configured.</li> <li> <b>ScheduleLambdaFunctionFailed</b>: a ScheduleLambdaFunctionFailed decision failed. This could happen if the AWS Lambda function specified in the decision does not exist, or the AWS Lambda service''s limits are exceeded.</li> <li> <b>RequestCancelActivityTaskFailed</b>: a RequestCancelActivityTask decision failed. This could happen if there is no open activity task with the specified activityId.</li> <li> <b>StartTimerFailed</b>: a StartTimer decision failed. This could happen if there is another open timer with the same timerId.</li> <li> <b>CancelTimerFailed</b>: a CancelTimer decision failed. This could happen if there is no open timer with the specified timerId.</li> <li> <b>StartChildWorkflowExecutionFailed</b>: a StartChildWorkflowExecution decision failed. This could happen if the workflow type specified is not registered, is deprecated, or the decision is not properly configured.</li> <li> <b>SignalExternalWorkflowExecutionFailed</b>: a SignalExternalWorkflowExecution decision failed. This could happen if the <code>workflowID</code> specified in the decision was incorrect.</li> <li> <b>RequestCancelExternalWorkflowExecutionFailed</b>: a RequestCancelExternalWorkflowExecution decision failed. This could happen if the <code>workflowID</code> specified in the decision was incorrect.</li> <li> <b>CancelWorkflowExecutionFailed</b>: a CancelWorkflowExecution decision failed. This could happen if there is an unhandled decision task pending in the workflow execution.</li> <li> <b>CompleteWorkflowExecutionFailed</b>: a CompleteWorkflowExecution decision failed. This could happen if there is an unhandled decision task pending in the workflow execution.</li> <li> <b>ContinueAsNewWorkflowExecutionFailed</b>: a ContinueAsNewWorkflowExecution decision failed. This could happen if there is an unhandled decision task pending in the workflow execution or the ContinueAsNewWorkflowExecution decision was not configured correctly.</li> <li> <b>FailWorkflowExecutionFailed</b>: a FailWorkflowExecution decision failed. This could happen if there is an unhandled decision task pending in the workflow execution.</li> </ul> <p>The preceding error events might occur due to an error in the decider logic, which might put the workflow execution in an unstable state The cause field in the event structure for the error event indicates the cause of the error.</p> <note>A workflow execution may be closed by the decider by returning one of the following decisions when completing a decision task: <code>CompleteWorkflowExecution</code>, <code>FailWorkflowExecution</code>, <code>CancelWorkflowExecution</code> and <code>ContinueAsNewWorkflowExecution</code>. An UnhandledDecision fault will be returned if a workflow closing decision is specified and a signal or activity event had been added to the history while the decision task was being performed by the decider. Unlike the above situations which are logic issues, this fault is always possible because of race conditions in a distributed system. The right action here is to call <a>RespondDecisionTaskCompleted</a> without any decisions. This would result in another decision task with these new events included in the history. The decider should handle the new events and may decide to close the workflow execution.</note> <p><b>How to code a decision</b></p> <p>You code a decision by first setting the decision type field to one of the above decision values, and then set the corresponding attributes field shown below:</p> <ul> <li> <a>ScheduleActivityTaskDecisionAttributes</a> </li> <li> <a>ScheduleLambdaFunctionDecisionAttributes</a> </li> <li> <a>RequestCancelActivityTaskDecisionAttributes</a> </li> <li> <a>CompleteWorkflowExecutionDecisionAttributes</a> </li> <li> <a>FailWorkflowExecutionDecisionAttributes</a> </li> <li> <a>CancelWorkflowExecutionDecisionAttributes</a> </li> <li> <a>ContinueAsNewWorkflowExecutionDecisionAttributes</a> </li> <li> <a>RecordMarkerDecisionAttributes</a> </li> <li> <a>StartTimerDecisionAttributes</a> </li> <li> <a>CancelTimerDecisionAttributes</a> </li> <li> <a>SignalExternalWorkflowExecutionDecisionAttributes</a> </li> <li> <a>RequestCancelExternalWorkflowExecutionDecisionAttributes</a> </li> <li> <a>StartChildWorkflowExecutionDecisionAttributes</a> </li> </ul>'
  DecisionList:
    type: array
    items:
      $ref: '#/definitions/Decision'
  DecisionTask:
    type: object
    required:
      - taskToken
      - startedEventId
      - workflowExecution
      - workflowType
      - events
    properties:
      taskToken:
        $ref: '#/definitions/TaskToken'
        description: The opaque string used as a handle on the task. This token is used by workers to communicate progress and response information back to the system about the task.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskStarted</code> event recorded in the history.
      workflowExecution:
        $ref: '#/definitions/WorkflowExecution'
        description: The workflow execution for which this decision task was created.
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The type of the workflow execution for which this decision task was created.
      events:
        $ref: '#/definitions/HistoryEventList'
        description: A paginated list of history events of the workflow execution. The decider uses this during the processing of the decision task.
      nextPageToken:
        $ref: '#/definitions/PageToken'
        description: '<p>If a <code>NextPageToken</code> was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in <code>nextPageToken</code>. Keep all other arguments unchanged.</p> <p>The configured <code>maximumPageSize</code> determines how many results can be returned in a single call.</p>'
      previousStartedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the DecisionTaskStarted event of the previous decision task of this workflow execution that was processed by the decider. This can be used to determine the events in the history new since the last decision task received by the decider.
    description: A structure that represents a decision task. Decision tasks are sent to deciders in order for them to make decisions.
  DecisionTaskCompletedEventAttributes:
    type: object
    required:
      - scheduledEventId
      - startedEventId
    properties:
      executionContext:
        $ref: '#/definitions/Data'
        description: User defined context for the workflow execution.
      scheduledEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskScheduled</code> event that was recorded when this decision task was scheduled. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskStarted</code> event recorded when this decision task was started. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>DecisionTaskCompleted</code> event.
  DecisionTaskScheduledEventAttributes:
    type: object
    required:
      - taskList
    properties:
      taskList:
        $ref: '#/definitions/TaskList'
        description: The name of the task list in which the decision task was scheduled.
      taskPriority:
        $ref: '#/definitions/TaskPriority'
        description: '<p><i>Optional.</i> A task priority that, if set, specifies the priority for this decision task. Valid values are integers that range from Java''s <code>Integer.MIN_VALUE</code> (-2147483648) to <code>Integer.MAX_VALUE</code> (2147483647). Higher numbers indicate higher priority.</p> <p>For more information about setting task priority, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/programming-priority.html">Setting Task Priority</a> in the <i>Amazon Simple Workflow Developer Guide</i>.</p>'
      startToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: <p>The maximum duration for this decision task. The task is considered timed out if it does not completed within this duration.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>
    description: Provides details about the <code>DecisionTaskScheduled</code> event.
  DecisionTaskStartedEventAttributes:
    type: object
    required:
      - scheduledEventId
    properties:
      identity:
        $ref: '#/definitions/Identity'
        description: Identity of the decider making the request. This enables diagnostic tracing when problems arise. The form of this identity is user defined.
      scheduledEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskScheduled</code> event that was recorded when this decision task was scheduled. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>DecisionTaskStarted</code> event.
  DecisionTaskTimedOutEventAttributes:
    type: object
    required:
      - timeoutType
      - scheduledEventId
      - startedEventId
    properties:
      timeoutType:
        $ref: '#/definitions/DecisionTaskTimeoutType'
        description: The type of timeout that expired before the decision task could be completed.
      scheduledEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskScheduled</code> event that was recorded when this decision task was scheduled. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskStarted</code> event recorded when this decision task was started. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>DecisionTaskTimedOut</code> event.
  DecisionTaskTimeoutType:
    type: string
    enum:
      - START_TO_CLOSE
  DecisionType:
    type: string
    enum:
      - ScheduleActivityTask
      - RequestCancelActivityTask
      - CompleteWorkflowExecution
      - FailWorkflowExecution
      - CancelWorkflowExecution
      - ContinueAsNewWorkflowExecution
      - RecordMarker
      - StartTimer
      - CancelTimer
      - SignalExternalWorkflowExecution
      - RequestCancelExternalWorkflowExecution
      - StartChildWorkflowExecution
      - ScheduleLambdaFunction
  DefaultUndefinedFault:
    type: object
    properties:
      message:
        $ref: '#/definitions/ErrorMessage'
  DeprecateActivityTypeInput:
    type: object
    required:
      - domain
      - activityType
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain in which the activity type is registered.
      activityType:
        $ref: '#/definitions/ActivityType'
        description: The activity type to deprecate.
  DeprecateDomainInput:
    type: object
    required:
      - name
    properties:
      name:
        $ref: '#/definitions/DomainName'
        description: The name of the domain to deprecate.
  DeprecateWorkflowTypeInput:
    type: object
    required:
      - domain
      - workflowType
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain in which the workflow type is registered.
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The workflow type to deprecate.
  DescribeActivityTypeInput:
    type: object
    required:
      - domain
      - activityType
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain in which the activity type is registered.
      activityType:
        $ref: '#/definitions/ActivityType'
        description: The activity type to get information about. Activity types are identified by the <code>name</code> and <code>version</code> that were supplied when the activity was registered.
  DescribeDomainInput:
    type: object
    required:
      - name
    properties:
      name:
        $ref: '#/definitions/DomainName'
        description: The name of the domain to describe.
  DescribeWorkflowExecutionInput:
    type: object
    required:
      - domain
      - execution
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain containing the workflow execution.
      execution:
        $ref: '#/definitions/WorkflowExecution'
        description: The workflow execution to describe.
  DescribeWorkflowTypeInput:
    type: object
    required:
      - domain
      - workflowType
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain in which this workflow type is registered.
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The workflow type to describe.
  Description:
    type: string
    maxLength: 1024
  DomainAlreadyExistsFault:
    type: object
    properties:
      message:
        $ref: '#/definitions/ErrorMessage'
        description: A description that may help with diagnosing the cause of the fault.
    description: Returned if the specified domain already exists. You will get this fault even if the existing domain is in deprecated status.
  DomainConfiguration:
    type: object
    required:
      - workflowExecutionRetentionPeriodInDays
    properties:
      workflowExecutionRetentionPeriodInDays:
        $ref: '#/definitions/DurationInDays'
        description: The retention period for workflow executions in this domain.
    description: Contains the configuration settings of a domain.
  DomainDeprecatedFault:
    type: object
    properties:
      message:
        $ref: '#/definitions/ErrorMessage'
        description: A description that may help with diagnosing the cause of the fault.
    description: Returned when the specified domain has been deprecated.
  DomainDetail:
    type: object
    required:
      - domainInfo
      - configuration
    properties:
      domainInfo:
        $ref: '#/definitions/DomainInfo'
      configuration:
        $ref: '#/definitions/DomainConfiguration'
    description: Contains details of a domain.
  DomainInfo:
    type: object
    required:
      - name
      - status
    properties:
      name:
        $ref: '#/definitions/DomainName'
        description: The name of the domain. This name is unique within the account.
      status:
        $ref: '#/definitions/RegistrationStatus'
        description: '<p>The status of the domain:</p> <ul> <li> <b>REGISTERED</b>: The domain is properly registered and available. You can use this domain for registering types and creating new workflow executions. </li> <li> <b>DEPRECATED</b>: The domain was deprecated using <a>DeprecateDomain</a>, but is still in use. You should not create new workflow executions in this domain. </li> </ul>'
      description:
        $ref: '#/definitions/Description'
        description: The description of the domain provided through <a>RegisterDomain</a>.
    description: Contains general information about a domain.
  DomainInfoList:
    type: array
    items:
      $ref: '#/definitions/DomainInfo'
  DomainInfos:
    type: object
    required:
      - domainInfos
    properties:
      domainInfos:
        $ref: '#/definitions/DomainInfoList'
        description: A list of DomainInfo structures.
      nextPageToken:
        $ref: '#/definitions/PageToken'
        description: '<p>If a <code>NextPageToken</code> was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in <code>nextPageToken</code>. Keep all other arguments unchanged.</p> <p>The configured <code>maximumPageSize</code> determines how many results can be returned in a single call.</p>'
    description: Contains a paginated collection of DomainInfo structures.
  DomainName:
    type: string
    minLength: 1
    maxLength: 256
  DurationInDays:
    type: string
    minLength: 1
    maxLength: 8
  DurationInSeconds:
    type: string
    minLength: 1
    maxLength: 8
  DurationInSecondsOptional:
    type: string
    maxLength: 8
  ErrorMessage:
    type: string
  EventId:
    type: integer
  EventType:
    type: string
    enum:
      - WorkflowExecutionStarted
      - WorkflowExecutionCancelRequested
      - WorkflowExecutionCompleted
      - CompleteWorkflowExecutionFailed
      - WorkflowExecutionFailed
      - FailWorkflowExecutionFailed
      - WorkflowExecutionTimedOut
      - WorkflowExecutionCanceled
      - CancelWorkflowExecutionFailed
      - WorkflowExecutionContinuedAsNew
      - ContinueAsNewWorkflowExecutionFailed
      - WorkflowExecutionTerminated
      - DecisionTaskScheduled
      - DecisionTaskStarted
      - DecisionTaskCompleted
      - DecisionTaskTimedOut
      - ActivityTaskScheduled
      - ScheduleActivityTaskFailed
      - ActivityTaskStarted
      - ActivityTaskCompleted
      - ActivityTaskFailed
      - ActivityTaskTimedOut
      - ActivityTaskCanceled
      - ActivityTaskCancelRequested
      - RequestCancelActivityTaskFailed
      - WorkflowExecutionSignaled
      - MarkerRecorded
      - RecordMarkerFailed
      - TimerStarted
      - StartTimerFailed
      - TimerFired
      - TimerCanceled
      - CancelTimerFailed
      - StartChildWorkflowExecutionInitiated
      - StartChildWorkflowExecutionFailed
      - ChildWorkflowExecutionStarted
      - ChildWorkflowExecutionCompleted
      - ChildWorkflowExecutionFailed
      - ChildWorkflowExecutionTimedOut
      - ChildWorkflowExecutionCanceled
      - ChildWorkflowExecutionTerminated
      - SignalExternalWorkflowExecutionInitiated
      - SignalExternalWorkflowExecutionFailed
      - ExternalWorkflowExecutionSignaled
      - RequestCancelExternalWorkflowExecutionInitiated
      - RequestCancelExternalWorkflowExecutionFailed
      - ExternalWorkflowExecutionCancelRequested
      - LambdaFunctionScheduled
      - LambdaFunctionStarted
      - LambdaFunctionCompleted
      - LambdaFunctionFailed
      - LambdaFunctionTimedOut
      - ScheduleLambdaFunctionFailed
      - StartLambdaFunctionFailed
  ExecutionStatus:
    type: string
    enum:
      - OPEN
      - CLOSED
  ExecutionTimeFilter:
    type: object
    required:
      - oldestDate
    properties:
      oldestDate:
        $ref: '#/definitions/Timestamp'
        description: Specifies the oldest start or close date and time to return.
      latestDate:
        $ref: '#/definitions/Timestamp'
        description: Specifies the latest start or close date and time to return.
    description: 'Used to filter the workflow executions in visibility APIs by various time-based rules. Each parameter, if specified, defines a rule that must be satisfied by each returned query result. The parameter values are in the <a href="https://en.wikipedia.org/wiki/Unix_time">Unix Time format</a>. For example: <code>"oldestDate": 1325376070.</code>'
  ExternalWorkflowExecutionCancelRequestedEventAttributes:
    type: object
    required:
      - workflowExecution
      - initiatedEventId
    properties:
      workflowExecution:
        $ref: '#/definitions/WorkflowExecution'
        description: The external workflow execution to which the cancellation request was delivered.
      initiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>RequestCancelExternalWorkflowExecutionInitiated</code> event corresponding to the <code>RequestCancelExternalWorkflowExecution</code> decision to cancel this external workflow execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>ExternalWorkflowExecutionCancelRequested</code> event.
  ExternalWorkflowExecutionSignaledEventAttributes:
    type: object
    required:
      - workflowExecution
      - initiatedEventId
    properties:
      workflowExecution:
        $ref: '#/definitions/WorkflowExecution'
        description: ' The external workflow execution that the signal was delivered to.'
      initiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>SignalExternalWorkflowExecutionInitiated</code> event corresponding to the <code>SignalExternalWorkflowExecution</code> decision to request this signal. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: ' Provides details of the <code>ExternalWorkflowExecutionSignaled</code> event.'
  FailWorkflowExecutionDecisionAttributes:
    type: object
    properties:
      reason:
        $ref: '#/definitions/FailureReason'
        description: A descriptive reason for the failure that may help in diagnostics.
      details:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Details of the failure.
    description: '<p>Provides details of the <code>FailWorkflowExecution</code> decision.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this decision''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
  FailWorkflowExecutionFailedCause:
    type: string
    enum:
      - UNHANDLED_DECISION
      - OPERATION_NOT_PERMITTED
  FailWorkflowExecutionFailedEventAttributes:
    type: object
    required:
      - cause
      - decisionTaskCompletedEventId
    properties:
      cause:
        $ref: '#/definitions/FailWorkflowExecutionFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>FailWorkflowExecution</code> decision to fail this execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>FailWorkflowExecutionFailed</code> event.
  FailureReason:
    type: string
    maxLength: 256
  FunctionId:
    type: string
    minLength: 1
    maxLength: 256
  FunctionInput:
    type: string
    minLength: 1
    maxLength: 32768
  FunctionName:
    type: string
    minLength: 1
    maxLength: 64
  GetWorkflowExecutionHistoryInput:
    type: object
    required:
      - domain
      - execution
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain containing the workflow execution.
      execution:
        $ref: '#/definitions/WorkflowExecution'
        description: Specifies the workflow execution for which to return the history.
      nextPageToken:
        $ref: '#/definitions/PageToken'
        description: '<p>If a <code>NextPageToken</code> was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in <code>nextPageToken</code>. Keep all other arguments unchanged.</p> <p>The configured <code>maximumPageSize</code> determines how many results can be returned in a single call.</p>'
      maximumPageSize:
        $ref: '#/definitions/PageSize'
        description: '<p>The maximum number of results that will be returned per call. <code>nextPageToken</code> can be used to obtain futher pages of results. The default is 1000, which is the maximum allowed page size. You can, however, specify a page size <i>smaller</i> than the maximum.</p> <p>This is an upper limit only; the actual number of results returned per call may be fewer than the specified maximum.</p>'
      reverseOrder:
        $ref: '#/definitions/ReverseOrder'
        description: 'When set to <code>true</code>, returns the events in reverse order. By default the results are returned in ascending order of the <code>eventTimeStamp</code> of the events.'
  History:
    type: object
    required:
      - events
    properties:
      events:
        $ref: '#/definitions/HistoryEventList'
        description: The list of history events.
      nextPageToken:
        $ref: '#/definitions/PageToken'
        description: '<p>If a <code>NextPageToken</code> was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in <code>nextPageToken</code>. Keep all other arguments unchanged.</p> <p>The configured <code>maximumPageSize</code> determines how many results can be returned in a single call.</p>'
    description: 'Paginated representation of a workflow history for a workflow execution. This is the up to date, complete and authoritative record of the events related to all tasks and events in the life of the workflow execution.'
  HistoryEvent:
    type: object
    required:
      - eventTimestamp
      - eventType
      - eventId
    properties:
      eventTimestamp:
        $ref: '#/definitions/Timestamp'
        description: The date and time when the event occurred.
      eventType:
        $ref: '#/definitions/EventType'
        description: The type of the history event.
      eventId:
        $ref: '#/definitions/EventId'
        description: The system generated ID of the event. This ID uniquely identifies the event with in the workflow execution history.
      workflowExecutionStartedEventAttributes:
        $ref: '#/definitions/WorkflowExecutionStartedEventAttributes'
        description: If the event is of type <code>WorkflowExecutionStarted</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      workflowExecutionCompletedEventAttributes:
        $ref: '#/definitions/WorkflowExecutionCompletedEventAttributes'
        description: If the event is of type <code>WorkflowExecutionCompleted</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      completeWorkflowExecutionFailedEventAttributes:
        $ref: '#/definitions/CompleteWorkflowExecutionFailedEventAttributes'
        description: If the event is of type <code>CompleteWorkflowExecutionFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      workflowExecutionFailedEventAttributes:
        $ref: '#/definitions/WorkflowExecutionFailedEventAttributes'
        description: If the event is of type <code>WorkflowExecutionFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      failWorkflowExecutionFailedEventAttributes:
        $ref: '#/definitions/FailWorkflowExecutionFailedEventAttributes'
        description: If the event is of type <code>FailWorkflowExecutionFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      workflowExecutionTimedOutEventAttributes:
        $ref: '#/definitions/WorkflowExecutionTimedOutEventAttributes'
        description: If the event is of type <code>WorkflowExecutionTimedOut</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      workflowExecutionCanceledEventAttributes:
        $ref: '#/definitions/WorkflowExecutionCanceledEventAttributes'
        description: If the event is of type <code>WorkflowExecutionCanceled</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      cancelWorkflowExecutionFailedEventAttributes:
        $ref: '#/definitions/CancelWorkflowExecutionFailedEventAttributes'
        description: If the event is of type <code>CancelWorkflowExecutionFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      workflowExecutionContinuedAsNewEventAttributes:
        $ref: '#/definitions/WorkflowExecutionContinuedAsNewEventAttributes'
        description: If the event is of type <code>WorkflowExecutionContinuedAsNew</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      continueAsNewWorkflowExecutionFailedEventAttributes:
        $ref: '#/definitions/ContinueAsNewWorkflowExecutionFailedEventAttributes'
        description: If the event is of type <code>ContinueAsNewWorkflowExecutionFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      workflowExecutionTerminatedEventAttributes:
        $ref: '#/definitions/WorkflowExecutionTerminatedEventAttributes'
        description: If the event is of type <code>WorkflowExecutionTerminated</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      workflowExecutionCancelRequestedEventAttributes:
        $ref: '#/definitions/WorkflowExecutionCancelRequestedEventAttributes'
        description: If the event is of type <code>WorkflowExecutionCancelRequested</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      decisionTaskScheduledEventAttributes:
        $ref: '#/definitions/DecisionTaskScheduledEventAttributes'
        description: If the event is of type <code>DecisionTaskScheduled</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      decisionTaskStartedEventAttributes:
        $ref: '#/definitions/DecisionTaskStartedEventAttributes'
        description: If the event is of type <code>DecisionTaskStarted</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      decisionTaskCompletedEventAttributes:
        $ref: '#/definitions/DecisionTaskCompletedEventAttributes'
        description: If the event is of type <code>DecisionTaskCompleted</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      decisionTaskTimedOutEventAttributes:
        $ref: '#/definitions/DecisionTaskTimedOutEventAttributes'
        description: If the event is of type <code>DecisionTaskTimedOut</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      activityTaskScheduledEventAttributes:
        $ref: '#/definitions/ActivityTaskScheduledEventAttributes'
        description: If the event is of type <code>ActivityTaskScheduled</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      activityTaskStartedEventAttributes:
        $ref: '#/definitions/ActivityTaskStartedEventAttributes'
        description: If the event is of type <code>ActivityTaskStarted</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      activityTaskCompletedEventAttributes:
        $ref: '#/definitions/ActivityTaskCompletedEventAttributes'
        description: If the event is of type <code>ActivityTaskCompleted</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      activityTaskFailedEventAttributes:
        $ref: '#/definitions/ActivityTaskFailedEventAttributes'
        description: If the event is of type <code>ActivityTaskFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      activityTaskTimedOutEventAttributes:
        $ref: '#/definitions/ActivityTaskTimedOutEventAttributes'
        description: If the event is of type <code>ActivityTaskTimedOut</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      activityTaskCanceledEventAttributes:
        $ref: '#/definitions/ActivityTaskCanceledEventAttributes'
        description: If the event is of type <code>ActivityTaskCanceled</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      activityTaskCancelRequestedEventAttributes:
        $ref: '#/definitions/ActivityTaskCancelRequestedEventAttributes'
        description: If the event is of type <code>ActivityTaskcancelRequested</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      workflowExecutionSignaledEventAttributes:
        $ref: '#/definitions/WorkflowExecutionSignaledEventAttributes'
        description: If the event is of type <code>WorkflowExecutionSignaled</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      markerRecordedEventAttributes:
        $ref: '#/definitions/MarkerRecordedEventAttributes'
        description: If the event is of type <code>MarkerRecorded</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      recordMarkerFailedEventAttributes:
        $ref: '#/definitions/RecordMarkerFailedEventAttributes'
        description: If the event is of type <code>DecisionTaskFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      timerStartedEventAttributes:
        $ref: '#/definitions/TimerStartedEventAttributes'
        description: If the event is of type <code>TimerStarted</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      timerFiredEventAttributes:
        $ref: '#/definitions/TimerFiredEventAttributes'
        description: If the event is of type <code>TimerFired</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      timerCanceledEventAttributes:
        $ref: '#/definitions/TimerCanceledEventAttributes'
        description: If the event is of type <code>TimerCanceled</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      startChildWorkflowExecutionInitiatedEventAttributes:
        $ref: '#/definitions/StartChildWorkflowExecutionInitiatedEventAttributes'
        description: If the event is of type <code>StartChildWorkflowExecutionInitiated</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      childWorkflowExecutionStartedEventAttributes:
        $ref: '#/definitions/ChildWorkflowExecutionStartedEventAttributes'
        description: If the event is of type <code>ChildWorkflowExecutionStarted</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      childWorkflowExecutionCompletedEventAttributes:
        $ref: '#/definitions/ChildWorkflowExecutionCompletedEventAttributes'
        description: If the event is of type <code>ChildWorkflowExecutionCompleted</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      childWorkflowExecutionFailedEventAttributes:
        $ref: '#/definitions/ChildWorkflowExecutionFailedEventAttributes'
        description: If the event is of type <code>ChildWorkflowExecutionFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      childWorkflowExecutionTimedOutEventAttributes:
        $ref: '#/definitions/ChildWorkflowExecutionTimedOutEventAttributes'
        description: If the event is of type <code>ChildWorkflowExecutionTimedOut</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      childWorkflowExecutionCanceledEventAttributes:
        $ref: '#/definitions/ChildWorkflowExecutionCanceledEventAttributes'
        description: If the event is of type <code>ChildWorkflowExecutionCanceled</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      childWorkflowExecutionTerminatedEventAttributes:
        $ref: '#/definitions/ChildWorkflowExecutionTerminatedEventAttributes'
        description: If the event is of type <code>ChildWorkflowExecutionTerminated</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      signalExternalWorkflowExecutionInitiatedEventAttributes:
        $ref: '#/definitions/SignalExternalWorkflowExecutionInitiatedEventAttributes'
        description: If the event is of type <code>SignalExternalWorkflowExecutionInitiated</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      externalWorkflowExecutionSignaledEventAttributes:
        $ref: '#/definitions/ExternalWorkflowExecutionSignaledEventAttributes'
        description: If the event is of type <code>ExternalWorkflowExecutionSignaled</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      signalExternalWorkflowExecutionFailedEventAttributes:
        $ref: '#/definitions/SignalExternalWorkflowExecutionFailedEventAttributes'
        description: If the event is of type <code>SignalExternalWorkflowExecutionFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      externalWorkflowExecutionCancelRequestedEventAttributes:
        $ref: '#/definitions/ExternalWorkflowExecutionCancelRequestedEventAttributes'
        description: 'If the event is of type <code>ExternalWorkflowExecutionCancelRequested</code> then this member is set and provides detailed information about the event. It is not set for other event types. '
      requestCancelExternalWorkflowExecutionInitiatedEventAttributes:
        $ref: '#/definitions/RequestCancelExternalWorkflowExecutionInitiatedEventAttributes'
        description: If the event is of type <code>RequestCancelExternalWorkflowExecutionInitiated</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      requestCancelExternalWorkflowExecutionFailedEventAttributes:
        $ref: '#/definitions/RequestCancelExternalWorkflowExecutionFailedEventAttributes'
        description: If the event is of type <code>RequestCancelExternalWorkflowExecutionFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      scheduleActivityTaskFailedEventAttributes:
        $ref: '#/definitions/ScheduleActivityTaskFailedEventAttributes'
        description: If the event is of type <code>ScheduleActivityTaskFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      requestCancelActivityTaskFailedEventAttributes:
        $ref: '#/definitions/RequestCancelActivityTaskFailedEventAttributes'
        description: If the event is of type <code>RequestCancelActivityTaskFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      startTimerFailedEventAttributes:
        $ref: '#/definitions/StartTimerFailedEventAttributes'
        description: If the event is of type <code>StartTimerFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      cancelTimerFailedEventAttributes:
        $ref: '#/definitions/CancelTimerFailedEventAttributes'
        description: If the event is of type <code>CancelTimerFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      startChildWorkflowExecutionFailedEventAttributes:
        $ref: '#/definitions/StartChildWorkflowExecutionFailedEventAttributes'
        description: If the event is of type <code>StartChildWorkflowExecutionFailed</code> then this member is set and provides detailed information about the event. It is not set for other event types.
      lambdaFunctionScheduledEventAttributes:
        $ref: '#/definitions/LambdaFunctionScheduledEventAttributes'
      lambdaFunctionStartedEventAttributes:
        $ref: '#/definitions/LambdaFunctionStartedEventAttributes'
      lambdaFunctionCompletedEventAttributes:
        $ref: '#/definitions/LambdaFunctionCompletedEventAttributes'
      lambdaFunctionFailedEventAttributes:
        $ref: '#/definitions/LambdaFunctionFailedEventAttributes'
      lambdaFunctionTimedOutEventAttributes:
        $ref: '#/definitions/LambdaFunctionTimedOutEventAttributes'
      scheduleLambdaFunctionFailedEventAttributes:
        $ref: '#/definitions/ScheduleLambdaFunctionFailedEventAttributes'
      startLambdaFunctionFailedEventAttributes:
        $ref: '#/definitions/StartLambdaFunctionFailedEventAttributes'
    description: '<p>Event within a workflow execution. A history event can be one of these types:</p> <ul> <li> <b>WorkflowExecutionStarted</b>: The workflow execution was started.</li> <li> <b>WorkflowExecutionCompleted</b>: The workflow execution was closed due to successful completion.</li> <li> <b>WorkflowExecutionFailed</b>: The workflow execution closed due to a failure.</li> <li> <b>WorkflowExecutionTimedOut</b>: The workflow execution was closed because a time out was exceeded.</li> <li> <b>WorkflowExecutionCanceled</b>: The workflow execution was successfully canceled and closed.</li> <li> <b>WorkflowExecutionTerminated</b>: The workflow execution was terminated.</li> <li> <b>WorkflowExecutionContinuedAsNew</b>: The workflow execution was closed and a new execution of the same type was created with the same workflowId.</li> <li> <b>WorkflowExecutionCancelRequested</b>: A request to cancel this workflow execution was made.</li> <li> <b>DecisionTaskScheduled</b>: A decision task was scheduled for the workflow execution.</li> <li> <b>DecisionTaskStarted</b>: The decision task was dispatched to a decider.</li> <li> <b>DecisionTaskCompleted</b>: The decider successfully completed a decision task by calling <a>RespondDecisionTaskCompleted</a>.</li> <li> <b>DecisionTaskTimedOut</b>: The decision task timed out.</li> <li> <b>ActivityTaskScheduled</b>: An activity task was scheduled for execution.</li> <li> <b>ScheduleActivityTaskFailed</b>: Failed to process ScheduleActivityTask decision. This happens when the decision is not configured properly, for example the activity type specified is not registered.</li> <li> <b>ActivityTaskStarted</b>: The scheduled activity task was dispatched to a worker.</li> <li> <b>ActivityTaskCompleted</b>: An activity worker successfully completed an activity task by calling <a>RespondActivityTaskCompleted</a>.</li> <li> <b>ActivityTaskFailed</b>: An activity worker failed an activity task by calling <a>RespondActivityTaskFailed</a>.</li> <li> <b>ActivityTaskTimedOut</b>: The activity task timed out.</li> <li> <b>ActivityTaskCanceled</b>: The activity task was successfully canceled.</li> <li> <b>ActivityTaskCancelRequested</b>: A <code>RequestCancelActivityTask</code> decision was received by the system.</li> <li> <b>RequestCancelActivityTaskFailed</b>: Failed to process RequestCancelActivityTask decision. This happens when the decision is not configured properly.</li> <li> <b>WorkflowExecutionSignaled</b>: An external signal was received for the workflow execution.</li> <li> <b>MarkerRecorded</b>: A marker was recorded in the workflow history as the result of a <code>RecordMarker</code> decision.</li> <li> <b>TimerStarted</b>: A timer was started for the workflow execution due to a <code>StartTimer</code> decision.</li> <li> <b>StartTimerFailed</b>: Failed to process StartTimer decision. This happens when the decision is not configured properly, for example a timer already exists with the specified timer ID.</li> <li> <b>TimerFired</b>: A timer, previously started for this workflow execution, fired.</li> <li> <b>TimerCanceled</b>: A timer, previously started for this workflow execution, was successfully canceled.</li> <li> <b>CancelTimerFailed</b>: Failed to process CancelTimer decision. This happens when the decision is not configured properly, for example no timer exists with the specified timer ID.</li> <li> <b>StartChildWorkflowExecutionInitiated</b>: A request was made to start a child workflow execution.</li> <li> <b>StartChildWorkflowExecutionFailed</b>: Failed to process StartChildWorkflowExecution decision. This happens when the decision is not configured properly, for example the workflow type specified is not registered.</li> <li> <b>ChildWorkflowExecutionStarted</b>: A child workflow execution was successfully started.</li> <li> <b>ChildWorkflowExecutionCompleted</b>: A child workflow execution, started by this workflow execution, completed successfully and was closed.</li> <li> <b>ChildWorkflowExecutionFailed</b>: A child workflow execution, started by this workflow execution, failed to complete successfully and was closed.</li> <li> <b>ChildWorkflowExecutionTimedOut</b>: A child workflow execution, started by this workflow execution, timed out and was closed.</li> <li> <b>ChildWorkflowExecutionCanceled</b>: A child workflow execution, started by this workflow execution, was canceled and closed.</li> <li> <b>ChildWorkflowExecutionTerminated</b>: A child workflow execution, started by this workflow execution, was terminated.</li> <li> <b>SignalExternalWorkflowExecutionInitiated</b>: A request to signal an external workflow was made.</li> <li> <b>ExternalWorkflowExecutionSignaled</b>: A signal, requested by this workflow execution, was successfully delivered to the target external workflow execution.</li> <li> <b>SignalExternalWorkflowExecutionFailed</b>: The request to signal an external workflow execution failed.</li> <li> <b>RequestCancelExternalWorkflowExecutionInitiated</b>: A request was made to request the cancellation of an external workflow execution.</li> <li> <b>ExternalWorkflowExecutionCancelRequested</b>: Request to cancel an external workflow execution was successfully delivered to the target execution.</li> <li> <b>RequestCancelExternalWorkflowExecutionFailed</b>: Request to cancel an external workflow execution failed.</li> <li> <b>LambdaFunctionScheduled</b>: An AWS Lambda function was scheduled for execution.</li> <li> <b>LambdaFunctionStarted</b>: The scheduled function was invoked in the AWS Lambda service.</li> <li> <b>LambdaFunctionCompleted</b>: The AWS Lambda function successfully completed.</li> <li> <b>LambdaFunctionFailed</b>: The AWS Lambda function execution failed.</li> <li> <b>LambdaFunctionTimedOut</b>: The AWS Lambda function execution timed out.</li> <li> <b>ScheduleLambdaFunctionFailed</b>: Failed to process ScheduleLambdaFunction decision. This happens when the workflow execution does not have the proper IAM role attached to invoke AWS Lambda functions.</li> <li> <b>StartLambdaFunctionFailed</b>: Failed to invoke the scheduled function in the AWS Lambda service. This happens when the AWS Lambda service is not available in the current region, or received too many requests.</li> </ul>'
  HistoryEventList:
    type: array
    items:
      $ref: '#/definitions/HistoryEvent'
  Identity:
    type: string
    maxLength: 256
  LambdaFunctionCompletedEventAttributes:
    type: object
    required:
      - scheduledEventId
      - startedEventId
    properties:
      scheduledEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>LambdaFunctionScheduled</code> event that was recorded when this AWS Lambda function was scheduled. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>LambdaFunctionStarted</code> event recorded in the history.
      result:
        $ref: '#/definitions/Data'
        description: The result of the function execution (if any).
    description: Provides details for the <code>LambdaFunctionCompleted</code> event.
  LambdaFunctionFailedEventAttributes:
    type: object
    required:
      - scheduledEventId
      - startedEventId
    properties:
      scheduledEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>LambdaFunctionScheduled</code> event that was recorded when this AWS Lambda function was scheduled. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>LambdaFunctionStarted</code> event recorded in the history.
      reason:
        $ref: '#/definitions/FailureReason'
        description: The reason provided for the failure (if any).
      details:
        $ref: '#/definitions/Data'
        description: The details of the failure (if any).
    description: Provides details for the <code>LambdaFunctionFailed</code> event.
  LambdaFunctionScheduledEventAttributes:
    type: object
    required:
      - id
      - name
      - decisionTaskCompletedEventId
    properties:
      id:
        $ref: '#/definitions/FunctionId'
        description: The unique Amazon SWF ID for the AWS Lambda task.
      name:
        $ref: '#/definitions/FunctionName'
        description: The name of the scheduled AWS Lambda function.
      input:
        $ref: '#/definitions/FunctionInput'
        description: Input provided to the AWS Lambda function.
      startToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: 'The maximum time, in seconds, that the AWS Lambda function can take to execute from start to close before it is marked as failed.'
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event for the decision that resulted in the scheduling of this AWS Lambda function. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details for the <code>LambdaFunctionScheduled</code> event.
  LambdaFunctionStartedEventAttributes:
    type: object
    required:
      - scheduledEventId
    properties:
      scheduledEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>LambdaFunctionScheduled</code> event that was recorded when this AWS Lambda function was scheduled. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details for the <code>LambdaFunctionStarted</code> event.
  LambdaFunctionTimedOutEventAttributes:
    type: object
    required:
      - scheduledEventId
      - startedEventId
    properties:
      scheduledEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>LambdaFunctionScheduled</code> event that was recorded when this AWS Lambda function was scheduled. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>LambdaFunctionStarted</code> event recorded in the history.
      timeoutType:
        $ref: '#/definitions/LambdaFunctionTimeoutType'
        description: The type of the timeout that caused this event.
    description: Provides details for the <code>LambdaFunctionTimedOut</code> event.
  LambdaFunctionTimeoutType:
    type: string
    enum:
      - START_TO_CLOSE
  LimitExceededFault:
    type: object
    properties:
      message:
        $ref: '#/definitions/ErrorMessage'
        description: A description that may help with diagnosing the cause of the fault.
    description: Returned by any operation if a system imposed limitation has been reached. To address this fault you should either clean up unused resources or increase the limit by contacting AWS.
  LimitedData:
    type: string
    maxLength: 2048
  ListActivityTypesInput:
    type: object
    required:
      - domain
      - registrationStatus
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain in which the activity types have been registered.
      name:
        $ref: '#/definitions/Name'
        description: 'If specified, only lists the activity types that have this name.'
      registrationStatus:
        $ref: '#/definitions/RegistrationStatus'
        description: Specifies the registration status of the activity types to list.
      nextPageToken:
        $ref: '#/definitions/PageToken'
        description: '<p>If a <code>NextPageToken</code> was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in <code>nextPageToken</code>. Keep all other arguments unchanged.</p> <p>The configured <code>maximumPageSize</code> determines how many results can be returned in a single call.</p>'
      maximumPageSize:
        $ref: '#/definitions/PageSize'
        description: '<p>The maximum number of results that will be returned per call. <code>nextPageToken</code> can be used to obtain futher pages of results. The default is 1000, which is the maximum allowed page size. You can, however, specify a page size <i>smaller</i> than the maximum.</p> <p>This is an upper limit only; the actual number of results returned per call may be fewer than the specified maximum.</p>'
      reverseOrder:
        $ref: '#/definitions/ReverseOrder'
        description: 'When set to <code>true</code>, returns the results in reverse order. By default, the results are returned in ascending alphabetical order by <code>name</code> of the activity types.'
  ListClosedWorkflowExecutionsInput:
    type: object
    required:
      - domain
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain that contains the workflow executions to list.
      startTimeFilter:
        $ref: '#/definitions/ExecutionTimeFilter'
        description: '<p>If specified, the workflow executions are included in the returned results based on whether their start times are within the range specified by this filter. Also, if this parameter is specified, the returned results are ordered by their start times.</p> <note><code>startTimeFilter</code> and <code>closeTimeFilter</code> are mutually exclusive. You must specify one of these in a request but not both.</note>'
      closeTimeFilter:
        $ref: '#/definitions/ExecutionTimeFilter'
        description: '<p>If specified, the workflow executions are included in the returned results based on whether their close times are within the range specified by this filter. Also, if this parameter is specified, the returned results are ordered by their close times.</p> <note><code>startTimeFilter</code> and <code>closeTimeFilter</code> are mutually exclusive. You must specify one of these in a request but not both.</note>'
      executionFilter:
        $ref: '#/definitions/WorkflowExecutionFilter'
        description: '<p>If specified, only workflow executions matching the workflow ID specified in the filter are returned.</p> <note><code>closeStatusFilter</code>, <code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
      closeStatusFilter:
        $ref: '#/definitions/CloseStatusFilter'
        description: '<p>If specified, only workflow executions that match this <i>close status</i> are listed. For example, if TERMINATED is specified, then only TERMINATED workflow executions are listed.</p> <note><code>closeStatusFilter</code>, <code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
      typeFilter:
        $ref: '#/definitions/WorkflowTypeFilter'
        description: '<p>If specified, only executions of the type specified in the filter are returned.</p> <note><code>closeStatusFilter</code>, <code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
      tagFilter:
        $ref: '#/definitions/TagFilter'
        description: '<p>If specified, only executions that have the matching tag are listed.</p> <note><code>closeStatusFilter</code>, <code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
      nextPageToken:
        $ref: '#/definitions/PageToken'
        description: '<p>If a <code>NextPageToken</code> was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in <code>nextPageToken</code>. Keep all other arguments unchanged.</p> <p>The configured <code>maximumPageSize</code> determines how many results can be returned in a single call.</p>'
      maximumPageSize:
        $ref: '#/definitions/PageSize'
        description: '<p>The maximum number of results that will be returned per call. <code>nextPageToken</code> can be used to obtain futher pages of results. The default is 1000, which is the maximum allowed page size. You can, however, specify a page size <i>smaller</i> than the maximum.</p> <p>This is an upper limit only; the actual number of results returned per call may be fewer than the specified maximum.</p>'
      reverseOrder:
        $ref: '#/definitions/ReverseOrder'
        description: 'When set to <code>true</code>, returns the results in reverse order. By default the results are returned in descending order of the start or the close time of the executions.'
  ListDomainsInput:
    type: object
    required:
      - registrationStatus
    properties:
      nextPageToken:
        $ref: '#/definitions/PageToken'
        description: '<p>If a <code>NextPageToken</code> was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in <code>nextPageToken</code>. Keep all other arguments unchanged.</p> <p>The configured <code>maximumPageSize</code> determines how many results can be returned in a single call.</p>'
      registrationStatus:
        $ref: '#/definitions/RegistrationStatus'
        description: Specifies the registration status of the domains to list.
      maximumPageSize:
        $ref: '#/definitions/PageSize'
        description: '<p>The maximum number of results that will be returned per call. <code>nextPageToken</code> can be used to obtain futher pages of results. The default is 1000, which is the maximum allowed page size. You can, however, specify a page size <i>smaller</i> than the maximum.</p> <p>This is an upper limit only; the actual number of results returned per call may be fewer than the specified maximum.</p>'
      reverseOrder:
        $ref: '#/definitions/ReverseOrder'
        description: 'When set to <code>true</code>, returns the results in reverse order. By default, the results are returned in ascending alphabetical order by <code>name</code> of the domains.'
  ListOpenWorkflowExecutionsInput:
    type: object
    required:
      - domain
      - startTimeFilter
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain that contains the workflow executions to list.
      startTimeFilter:
        $ref: '#/definitions/ExecutionTimeFilter'
        description: Workflow executions are included in the returned results based on whether their start times are within the range specified by this filter.
      typeFilter:
        $ref: '#/definitions/WorkflowTypeFilter'
        description: '<p>If specified, only executions of the type specified in the filter are returned.</p> <note><code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
      tagFilter:
        $ref: '#/definitions/TagFilter'
        description: '<p>If specified, only executions that have the matching tag are listed.</p> <note><code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
      nextPageToken:
        $ref: '#/definitions/PageToken'
        description: '<p>If a <code>NextPageToken</code> was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in <code>nextPageToken</code>. Keep all other arguments unchanged.</p> <p>The configured <code>maximumPageSize</code> determines how many results can be returned in a single call.</p>'
      maximumPageSize:
        $ref: '#/definitions/PageSize'
        description: '<p>The maximum number of results that will be returned per call. <code>nextPageToken</code> can be used to obtain futher pages of results. The default is 1000, which is the maximum allowed page size. You can, however, specify a page size <i>smaller</i> than the maximum.</p> <p>This is an upper limit only; the actual number of results returned per call may be fewer than the specified maximum.</p>'
      reverseOrder:
        $ref: '#/definitions/ReverseOrder'
        description: 'When set to <code>true</code>, returns the results in reverse order. By default the results are returned in descending order of the start time of the executions.'
      executionFilter:
        $ref: '#/definitions/WorkflowExecutionFilter'
        description: '<p>If specified, only workflow executions matching the workflow ID specified in the filter are returned.</p> <note><code>executionFilter</code>, <code>typeFilter</code> and <code>tagFilter</code> are mutually exclusive. You can specify at most one of these in a request.</note>'
  ListWorkflowTypesInput:
    type: object
    required:
      - domain
      - registrationStatus
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain in which the workflow types have been registered.
      name:
        $ref: '#/definitions/Name'
        description: 'If specified, lists the workflow type with this name.'
      registrationStatus:
        $ref: '#/definitions/RegistrationStatus'
        description: Specifies the registration status of the workflow types to list.
      nextPageToken:
        $ref: '#/definitions/PageToken'
        description: '<p>If a <code>NextPageToken</code> was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in <code>nextPageToken</code>. Keep all other arguments unchanged.</p> <p>The configured <code>maximumPageSize</code> determines how many results can be returned in a single call.</p>'
      maximumPageSize:
        $ref: '#/definitions/PageSize'
        description: '<p>The maximum number of results that will be returned per call. <code>nextPageToken</code> can be used to obtain futher pages of results. The default is 1000, which is the maximum allowed page size. You can, however, specify a page size <i>smaller</i> than the maximum.</p> <p>This is an upper limit only; the actual number of results returned per call may be fewer than the specified maximum.</p>'
      reverseOrder:
        $ref: '#/definitions/ReverseOrder'
        description: 'When set to <code>true</code>, returns the results in reverse order. By default the results are returned in ascending alphabetical order of the <code>name</code> of the workflow types.'
  MarkerName:
    type: string
    minLength: 1
    maxLength: 256
  MarkerRecordedEventAttributes:
    type: object
    required:
      - markerName
      - decisionTaskCompletedEventId
    properties:
      markerName:
        $ref: '#/definitions/MarkerName'
        description: The name of the marker.
      details:
        $ref: '#/definitions/Data'
        description: Details of the marker (if any).
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>RecordMarker</code> decision that requested this marker. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>MarkerRecorded</code> event.
  Name:
    type: string
    minLength: 1
    maxLength: 256
  OpenDecisionTasksCount:
    type: integer
    minimum: 0
    maximum: 1
  OperationNotPermittedFault:
    type: object
    properties:
      message:
        $ref: '#/definitions/ErrorMessage'
        description: A description that may help with diagnosing the cause of the fault.
    description: Returned when the caller does not have sufficient permissions to invoke the action.
  PageSize:
    type: integer
    minimum: 0
    maximum: 1000
  PageToken:
    type: string
    maxLength: 2048
  PendingTaskCount:
    type: object
    required:
      - count
    properties:
      count:
        $ref: '#/definitions/Count'
        description: The number of tasks in the task list.
      truncated:
        $ref: '#/definitions/Truncated'
        description: 'If set to true, indicates that the actual count was more than the maximum supported by this API and the count returned is the truncated value.'
    description: Contains the count of tasks in a task list.
  PollForActivityTaskInput:
    type: object
    required:
      - domain
      - taskList
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain that contains the task lists being polled.
      taskList:
        $ref: '#/definitions/TaskList'
        description: '<p>Specifies the task list to poll for activity tasks.</p> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      identity:
        $ref: '#/definitions/Identity'
        description: 'Identity of the worker making the request, recorded in the <code>ActivityTaskStarted</code> event in the workflow history. This enables diagnostic tracing when problems arise. The form of this identity is user defined.'
  PollForDecisionTaskInput:
    type: object
    required:
      - domain
      - taskList
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain containing the task lists to poll.
      taskList:
        $ref: '#/definitions/TaskList'
        description: '<p>Specifies the task list to poll for decision tasks.</p> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      identity:
        $ref: '#/definitions/Identity'
        description: 'Identity of the decider making the request, which is recorded in the DecisionTaskStarted event in the workflow history. This enables diagnostic tracing when problems arise. The form of this identity is user defined.'
      nextPageToken:
        $ref: '#/definitions/PageToken'
        description: '<p>If a <code>NextPageToken</code> was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in <code>nextPageToken</code>. Keep all other arguments unchanged.</p> <p>The configured <code>maximumPageSize</code> determines how many results can be returned in a single call.</p> <note>The <code>nextPageToken</code> returned by this action cannot be used with <a>GetWorkflowExecutionHistory</a> to get the next page. You must call <a>PollForDecisionTask</a> again (with the <code>nextPageToken</code>) to retrieve the next page of history records. Calling <a>PollForDecisionTask</a> with a <code>nextPageToken</code> will not return a new decision task.</note>.'
      maximumPageSize:
        $ref: '#/definitions/PageSize'
        description: '<p>The maximum number of results that will be returned per call. <code>nextPageToken</code> can be used to obtain futher pages of results. The default is 1000, which is the maximum allowed page size. You can, however, specify a page size <i>smaller</i> than the maximum.</p> <p>This is an upper limit only; the actual number of results returned per call may be fewer than the specified maximum.</p>'
      reverseOrder:
        $ref: '#/definitions/ReverseOrder'
        description: 'When set to <code>true</code>, returns the events in reverse order. By default the results are returned in ascending order of the <code>eventTimestamp</code> of the events.'
  RecordActivityTaskHeartbeatInput:
    type: object
    required:
      - taskToken
    properties:
      taskToken:
        $ref: '#/definitions/TaskToken'
        description: '<p>The <code>taskToken</code> of the <a>ActivityTask</a>.</p> <important> <code>taskToken</code> is generated by the service and should be treated as an opaque value. If the task is passed to another process, its <code>taskToken</code> must also be passed. This enables it to provide its progress and respond with results. </important>'
      details:
        $ref: '#/definitions/LimitedData'
        description: 'If specified, contains details about the progress of the task.'
  RecordMarkerDecisionAttributes:
    type: object
    required:
      - markerName
    properties:
      markerName:
        $ref: '#/definitions/MarkerName'
        description: <b>Required.</b> The name of the marker.
      details:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> details of the marker.
    description: '<p>Provides details of the <code>RecordMarker</code> decision.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this decision''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
  RecordMarkerFailedCause:
    type: string
    enum:
      - OPERATION_NOT_PERMITTED
  RecordMarkerFailedEventAttributes:
    type: object
    required:
      - markerName
      - cause
      - decisionTaskCompletedEventId
    properties:
      markerName:
        $ref: '#/definitions/MarkerName'
        description: The marker's name.
      cause:
        $ref: '#/definitions/RecordMarkerFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>RecordMarkerFailed</code> decision for this cancellation request. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>RecordMarkerFailed</code> event.
  RegisterActivityTypeInput:
    type: object
    required:
      - domain
      - name
      - version
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain in which this activity is to be registered.
      name:
        $ref: '#/definitions/Name'
        description: '<p>The name of the activity type within the domain.</p> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      version:
        $ref: '#/definitions/Version'
        description: '<p>The version of the activity type.</p> <note>The activity type consists of the name and version, the combination of which must be unique within the domain.</note> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      description:
        $ref: '#/definitions/Description'
        description: A textual description of the activity type.
      defaultTaskStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p>If set, specifies the default maximum duration that a worker can take to process tasks of this activity type. This default can be overridden when scheduling an activity task using the <code>ScheduleActivityTask</code> decision.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>'
      defaultTaskHeartbeatTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p>If set, specifies the default maximum time before which a worker processing a task of this type must report progress by calling <a>RecordActivityTaskHeartbeat</a>. If the timeout is exceeded, the activity task is automatically timed out. This default can be overridden when scheduling an activity task using the <code>ScheduleActivityTask</code> decision. If the activity worker subsequently attempts to record a heartbeat or returns a result, the activity worker receives an <code>UnknownResource</code> fault. In this case, Amazon SWF no longer considers the activity task to be valid; the activity worker should clean up the activity task.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>'
      defaultTaskList:
        $ref: '#/definitions/TaskList'
        description: 'If set, specifies the default task list to use for scheduling tasks of this activity type. This default task list is used if a task list is not provided when a task is scheduled through the <code>ScheduleActivityTask</code> decision.'
      defaultTaskPriority:
        $ref: '#/definitions/TaskPriority'
        description: '<p>The default task priority to assign to the activity type. If not assigned, then "0" will be used. Valid values are integers that range from Java''s <code>Integer.MIN_VALUE</code> (-2147483648) to <code>Integer.MAX_VALUE</code> (2147483647). Higher numbers indicate higher priority.</p> <p>For more information about setting task priority, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/programming-priority.html">Setting Task Priority</a> in the <i>Amazon Simple Workflow Developer Guide</i>.</p>'
      defaultTaskScheduleToStartTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p>If set, specifies the default maximum duration that a task of this activity type can wait before being assigned to a worker. This default can be overridden when scheduling an activity task using the <code>ScheduleActivityTask</code> decision.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>'
      defaultTaskScheduleToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p>If set, specifies the default maximum duration for a task of this activity type. This default can be overridden when scheduling an activity task using the <code>ScheduleActivityTask</code> decision.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>'
  RegisterDomainInput:
    type: object
    required:
      - name
      - workflowExecutionRetentionPeriodInDays
    properties:
      name:
        $ref: '#/definitions/DomainName'
        description: '<p>Name of the domain to register. The name must be unique in the region that the domain is registered in.</p> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      description:
        $ref: '#/definitions/Description'
        description: A text description of the domain.
      workflowExecutionRetentionPeriodInDays:
        $ref: '#/definitions/DurationInDays'
        description: '<p>The duration (in days) that records and histories of workflow executions on the domain should be kept by the service. After the retention period, the workflow execution is not available in the results of visibility calls.</p> <p>If you pass the value <code>NONE</code> or <code>0</code> (zero), then the workflow execution history will not be retained. As soon as the workflow execution completes, the execution record and its history are deleted.</p> <p>The maximum workflow execution retention period is 90 days. For more information about Amazon SWF service limits, see: <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dg-limits.html">Amazon SWF Service Limits</a> in the <i>Amazon SWF Developer Guide</i>.</p>'
  RegisterWorkflowTypeInput:
    type: object
    required:
      - domain
      - name
      - version
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain in which to register the workflow type.
      name:
        $ref: '#/definitions/Name'
        description: '<p>The name of the workflow type.</p> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      version:
        $ref: '#/definitions/Version'
        description: '<p>The version of the workflow type.</p> <note>The workflow type consists of the name and version, the combination of which must be unique within the domain. To get a list of all currently registered workflow types, use the <a>ListWorkflowTypes</a> action.</note> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      description:
        $ref: '#/definitions/Description'
        description: Textual description of the workflow type.
      defaultTaskStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p>If set, specifies the default maximum duration of decision tasks for this workflow type. This default can be overridden when starting a workflow execution using the <a>StartWorkflowExecution</a> action or the <code>StartChildWorkflowExecution</code> decision.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>'
      defaultExecutionStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p>If set, specifies the default maximum duration for executions of this workflow type. You can override this default when starting an execution through the <a>StartWorkflowExecution</a> action or <code>StartChildWorkflowExecution</code> decision.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. Unlike some of the other timeout parameters in Amazon SWF, you cannot specify a value of "NONE" for <code>defaultExecutionStartToCloseTimeout</code>; there is a one-year max limit on the time that a workflow execution can run. Exceeding this limit will always cause the workflow execution to time out.</p>'
      defaultTaskList:
        $ref: '#/definitions/TaskList'
        description: 'If set, specifies the default task list to use for scheduling decision tasks for executions of this workflow type. This default is used only if a task list is not provided when starting the execution through the <a>StartWorkflowExecution</a> action or <code>StartChildWorkflowExecution</code> decision.'
      defaultTaskPriority:
        $ref: '#/definitions/TaskPriority'
        description: '<p>The default task priority to assign to the workflow type. If not assigned, then "0" will be used. Valid values are integers that range from Java''s <code>Integer.MIN_VALUE</code> (-2147483648) to <code>Integer.MAX_VALUE</code> (2147483647). Higher numbers indicate higher priority.</p> <p>For more information about setting task priority, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/programming-priority.html">Setting Task Priority</a> in the <i>Amazon Simple Workflow Developer Guide</i>.</p>'
      defaultChildPolicy:
        $ref: '#/definitions/ChildPolicy'
        description: '<p>If set, specifies the default policy to use for the child workflow executions when a workflow execution of this type is terminated, by calling the <a>TerminateWorkflowExecution</a> action explicitly or due to an expired timeout. This default can be overridden when starting a workflow execution using the <a>StartWorkflowExecution</a> action or the <code>StartChildWorkflowExecution</code> decision.</p> <p>The supported child policies are:</p> <ul> <li><b>TERMINATE:</b> the child executions will be terminated.</li> <li><b>REQUEST_CANCEL:</b> a request to cancel will be attempted for each child execution by recording a <code>WorkflowExecutionCancelRequested</code> event in its history. It is up to the decider to take appropriate actions when it receives an execution history with this event.</li> <li><b>ABANDON:</b> no action will be taken. The child executions will continue to run.</li> </ul>'
      defaultLambdaRole:
        $ref: '#/definitions/Arn'
        description: <p>The ARN of the default IAM role to use when a workflow execution of this type invokes AWS Lambda functions.</p> <p>This default can be overridden when starting a workflow execution using the <a>StartWorkflowExecution</a> action or the <code>StartChildWorkflowExecution</code> and <code>ContinueAsNewWorkflowExecution</code> decision.</p>
  RegistrationStatus:
    type: string
    enum:
      - REGISTERED
      - DEPRECATED
  RequestCancelActivityTaskDecisionAttributes:
    type: object
    required:
      - activityId
    properties:
      activityId:
        $ref: '#/definitions/ActivityId'
        description: The <code>activityId</code> of the activity task to be canceled.
    description: '<p>Provides details of the <code>RequestCancelActivityTask</code> decision.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this decision''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
  RequestCancelActivityTaskFailedCause:
    type: string
    enum:
      - ACTIVITY_ID_UNKNOWN
      - OPERATION_NOT_PERMITTED
  RequestCancelActivityTaskFailedEventAttributes:
    type: object
    required:
      - activityId
      - cause
      - decisionTaskCompletedEventId
    properties:
      activityId:
        $ref: '#/definitions/ActivityId'
        description: The activityId provided in the <code>RequestCancelActivityTask</code> decision that failed.
      cause:
        $ref: '#/definitions/RequestCancelActivityTaskFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>RequestCancelActivityTask</code> decision for this cancellation request. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>RequestCancelActivityTaskFailed</code> event.
  RequestCancelExternalWorkflowExecutionDecisionAttributes:
    type: object
    required:
      - workflowId
    properties:
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: <b>Required.</b> The <code>workflowId</code> of the external workflow execution to cancel.
      runId:
        $ref: '#/definitions/RunIdOptional'
        description: The <code>runId</code> of the external workflow execution to cancel.
      control:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Data attached to the event that can be used by the decider in subsequent workflow tasks.
    description: '<p>Provides details of the <code>RequestCancelExternalWorkflowExecution</code> decision.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this decision''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
  RequestCancelExternalWorkflowExecutionFailedCause:
    type: string
    enum:
      - UNKNOWN_EXTERNAL_WORKFLOW_EXECUTION
      - REQUEST_CANCEL_EXTERNAL_WORKFLOW_EXECUTION_RATE_EXCEEDED
      - OPERATION_NOT_PERMITTED
  RequestCancelExternalWorkflowExecutionFailedEventAttributes:
    type: object
    required:
      - workflowId
      - cause
      - initiatedEventId
      - decisionTaskCompletedEventId
    properties:
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: The <code>workflowId</code> of the external workflow to which the cancel request was to be delivered.
      runId:
        $ref: '#/definitions/RunIdOptional'
        description: The <code>runId</code> of the external workflow execution.
      cause:
        $ref: '#/definitions/RequestCancelExternalWorkflowExecutionFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      initiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>RequestCancelExternalWorkflowExecutionInitiated</code> event corresponding to the <code>RequestCancelExternalWorkflowExecution</code> decision to cancel this external workflow execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>RequestCancelExternalWorkflowExecution</code> decision for this cancellation request. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      control:
        $ref: '#/definitions/Data'
    description: Provides details of the <code>RequestCancelExternalWorkflowExecutionFailed</code> event.
  RequestCancelExternalWorkflowExecutionInitiatedEventAttributes:
    type: object
    required:
      - workflowId
      - decisionTaskCompletedEventId
    properties:
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: The <code>workflowId</code> of the external workflow execution to be canceled.
      runId:
        $ref: '#/definitions/RunIdOptional'
        description: The <code>runId</code> of the external workflow execution to be canceled.
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>RequestCancelExternalWorkflowExecution</code> decision for this cancellation request. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      control:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Data attached to the event that can be used by the decider in subsequent workflow tasks.
    description: Provides details of the <code>RequestCancelExternalWorkflowExecutionInitiated</code> event.
  RequestCancelWorkflowExecutionInput:
    type: object
    required:
      - domain
      - workflowId
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain containing the workflow execution to cancel.
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: The workflowId of the workflow execution to cancel.
      runId:
        $ref: '#/definitions/RunIdOptional'
        description: The runId of the workflow execution to cancel.
  RespondActivityTaskCanceledInput:
    type: object
    required:
      - taskToken
    properties:
      taskToken:
        $ref: '#/definitions/TaskToken'
        description: '<p>The <code>taskToken</code> of the <a>ActivityTask</a>.</p> <important><code>taskToken</code> is generated by the service and should be treated as an opaque value. If the task is passed to another process, its <code>taskToken</code> must also be passed. This enables it to provide its progress and respond with results.</important>'
      details:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Information about the cancellation.
  RespondActivityTaskCompletedInput:
    type: object
    required:
      - taskToken
    properties:
      taskToken:
        $ref: '#/definitions/TaskToken'
        description: '<p>The <code>taskToken</code> of the <a>ActivityTask</a>.</p> <important> <code>taskToken</code> is generated by the service and should be treated as an opaque value. If the task is passed to another process, its <code>taskToken</code> must also be passed. This enables it to provide its progress and respond with results.</important>'
      result:
        $ref: '#/definitions/Data'
        description: The result of the activity task. It is a free form string that is implementation specific.
  RespondActivityTaskFailedInput:
    type: object
    required:
      - taskToken
    properties:
      taskToken:
        $ref: '#/definitions/TaskToken'
        description: '<p>The <code>taskToken</code> of the <a>ActivityTask</a>.</p> <important> <code>taskToken</code> is generated by the service and should be treated as an opaque value. If the task is passed to another process, its <code>taskToken</code> must also be passed. This enables it to provide its progress and respond with results.</important>'
      reason:
        $ref: '#/definitions/FailureReason'
        description: Description of the error that may assist in diagnostics.
      details:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Detailed information about the failure.
  RespondDecisionTaskCompletedInput:
    type: object
    required:
      - taskToken
    properties:
      taskToken:
        $ref: '#/definitions/TaskToken'
        description: '<p>The <code>taskToken</code> from the <a>DecisionTask</a>.</p> <important><code>taskToken</code> is generated by the service and should be treated as an opaque value. If the task is passed to another process, its <code>taskToken</code> must also be passed. This enables it to provide its progress and respond with results.</important>'
      decisions:
        $ref: '#/definitions/DecisionList'
        description: The list of decisions (possibly empty) made by the decider while processing this decision task. See the docs for the decision structure for details.
      executionContext:
        $ref: '#/definitions/Data'
        description: User defined context to add to workflow execution.
  ReverseOrder:
    type: boolean
  Run:
    type: object
    properties:
      runId:
        $ref: '#/definitions/RunId'
        description: The <code>runId</code> of a workflow execution. This ID is generated by the service and can be used to uniquely identify the workflow execution within a domain.
    description: Specifies the <code>runId</code> of a workflow execution.
  RunId:
    type: string
    minLength: 1
    maxLength: 64
  RunIdOptional:
    type: string
    maxLength: 64
  ScheduleActivityTaskDecisionAttributes:
    type: object
    required:
      - activityType
      - activityId
    properties:
      activityType:
        $ref: '#/definitions/ActivityType'
        description: <b>Required.</b> The type of the activity task to schedule.
      activityId:
        $ref: '#/definitions/ActivityId'
        description: '<p><b>Required.</b> The <code>activityId</code> of the activity task.</p> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      control:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Data attached to the event that can be used by the decider in subsequent workflow tasks. This data is not sent to the activity.
      input:
        $ref: '#/definitions/Data'
        description: The input provided to the activity task.
      scheduleToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: <p>The maximum duration for this activity task.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p> <note>A schedule-to-close timeout for this activity task must be specified either as a default for the activity type or through this field. If neither this field is set nor a default schedule-to-close timeout was specified at registration time then a fault will be returned.</note>
      taskList:
        $ref: '#/definitions/TaskList'
        description: '<p>If set, specifies the name of the task list in which to schedule the activity task. If not specified, the <code>defaultTaskList</code> registered with the activity type will be used.</p> <note>A task list for this activity task must be specified either as a default for the activity type or through this field. If neither this field is set nor a default task list was specified at registration time then a fault will be returned.</note> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      taskPriority:
        $ref: '#/definitions/TaskPriority'
        description: '<p><i>Optional.</i> If set, specifies the priority with which the activity task is to be assigned to a worker. This overrides the defaultTaskPriority specified when registering the activity type using <a>RegisterActivityType</a>. Valid values are integers that range from Java''s <code>Integer.MIN_VALUE</code> (-2147483648) to <code>Integer.MAX_VALUE</code> (2147483647). Higher numbers indicate higher priority.</p> <p>For more information about setting task priority, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/programming-priority.html">Setting Task Priority</a> in the <i>Amazon Simple Workflow Developer Guide</i>.</p>'
      scheduleToStartTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p><i>Optional.</i> If set, specifies the maximum duration the activity task can wait to be assigned to a worker. This overrides the default schedule-to-start timeout specified when registering the activity type using <a>RegisterActivityType</a>.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p> <note>A schedule-to-start timeout for this activity task must be specified either as a default for the activity type or through this field. If neither this field is set nor a default schedule-to-start timeout was specified at registration time then a fault will be returned.</note>'
      startToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p>If set, specifies the maximum duration a worker may take to process this activity task. This overrides the default start-to-close timeout specified when registering the activity type using <a>RegisterActivityType</a>.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p> <note>A start-to-close timeout for this activity task must be specified either as a default for the activity type or through this field. If neither this field is set nor a default start-to-close timeout was specified at registration time then a fault will be returned.</note>'
      heartbeatTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p>If set, specifies the maximum time before which a worker processing a task of this type must report progress by calling <a>RecordActivityTaskHeartbeat</a>. If the timeout is exceeded, the activity task is automatically timed out. If the worker subsequently attempts to record a heartbeat or returns a result, it will be ignored. This overrides the default heartbeat timeout specified when registering the activity type using <a>RegisterActivityType</a>.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>'
    description: '<p>Provides details of the <code>ScheduleActivityTask</code> decision.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this decision''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li><code>activityType.name</code>: String constraint. The key is <code>swf:activityType.name</code>.</li> <li><code>activityType.version</code>: String constraint. The key is <code>swf:activityType.version</code>.</li> <li><code>taskList</code>: String constraint. The key is <code>swf:taskList.name</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
  ScheduleActivityTaskFailedCause:
    type: string
    enum:
      - ACTIVITY_TYPE_DEPRECATED
      - ACTIVITY_TYPE_DOES_NOT_EXIST
      - ACTIVITY_ID_ALREADY_IN_USE
      - OPEN_ACTIVITIES_LIMIT_EXCEEDED
      - ACTIVITY_CREATION_RATE_EXCEEDED
      - DEFAULT_SCHEDULE_TO_CLOSE_TIMEOUT_UNDEFINED
      - DEFAULT_TASK_LIST_UNDEFINED
      - DEFAULT_SCHEDULE_TO_START_TIMEOUT_UNDEFINED
      - DEFAULT_START_TO_CLOSE_TIMEOUT_UNDEFINED
      - DEFAULT_HEARTBEAT_TIMEOUT_UNDEFINED
      - OPERATION_NOT_PERMITTED
  ScheduleActivityTaskFailedEventAttributes:
    type: object
    required:
      - activityType
      - activityId
      - cause
      - decisionTaskCompletedEventId
    properties:
      activityType:
        $ref: '#/definitions/ActivityType'
        description: The activity type provided in the <code>ScheduleActivityTask</code> decision that failed.
      activityId:
        $ref: '#/definitions/ActivityId'
        description: The activityId provided in the <code>ScheduleActivityTask</code> decision that failed.
      cause:
        $ref: '#/definitions/ScheduleActivityTaskFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision that resulted in the scheduling of this activity task. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>ScheduleActivityTaskFailed</code> event.
  ScheduleLambdaFunctionDecisionAttributes:
    type: object
    required:
      - id
      - name
    properties:
      id:
        $ref: '#/definitions/FunctionId'
        description: '<p><b>Required.</b> The SWF <code>id</code> of the AWS Lambda task.</p> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      name:
        $ref: '#/definitions/FunctionName'
        description: <b>Required.</b> The name of the AWS Lambda function to invoke.
      input:
        $ref: '#/definitions/FunctionInput'
        description: The input provided to the AWS Lambda function.
      startToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: 'If set, specifies the maximum duration the function may take to execute.'
    description: '<p>Provides details of the <code>ScheduleLambdaFunction</code> decision.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this decision''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li><code>activityType.name</code>: String constraint. The key is <code>swf:activityType.name</code>.</li> <li><code>activityType.version</code>: String constraint. The key is <code>swf:activityType.version</code>.</li> <li><code>taskList</code>: String constraint. The key is <code>swf:taskList.name</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
  ScheduleLambdaFunctionFailedCause:
    type: string
    enum:
      - ID_ALREADY_IN_USE
      - OPEN_LAMBDA_FUNCTIONS_LIMIT_EXCEEDED
      - LAMBDA_FUNCTION_CREATION_RATE_EXCEEDED
      - LAMBDA_SERVICE_NOT_AVAILABLE_IN_REGION
  ScheduleLambdaFunctionFailedEventAttributes:
    type: object
    required:
      - id
      - name
      - cause
      - decisionTaskCompletedEventId
    properties:
      id:
        $ref: '#/definitions/FunctionId'
        description: The unique Amazon SWF ID of the AWS Lambda task.
      name:
        $ref: '#/definitions/FunctionName'
        description: The name of the scheduled AWS Lambda function.
      cause:
        $ref: '#/definitions/ScheduleLambdaFunctionFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision that resulted in the scheduling of this AWS Lambda function. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details for the <code>ScheduleLambdaFunctionFailed</code> event.
  SignalExternalWorkflowExecutionDecisionAttributes:
    type: object
    required:
      - workflowId
      - signalName
    properties:
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: <b>Required.</b> The <code>workflowId</code> of the workflow execution to be signaled.
      runId:
        $ref: '#/definitions/RunIdOptional'
        description: The <code>runId</code> of the workflow execution to be signaled.
      signalName:
        $ref: '#/definitions/SignalName'
        description: <b>Required.</b> The name of the signal.The target workflow execution will use the signal name and input to process the signal.
      input:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Input data to be provided with the signal. The target workflow execution will use the signal name and input data to process the signal.
      control:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Data attached to the event that can be used by the decider in subsequent decision tasks.
    description: '<p>Provides details of the <code>SignalExternalWorkflowExecution</code> decision.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this decision''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
  SignalExternalWorkflowExecutionFailedCause:
    type: string
    enum:
      - UNKNOWN_EXTERNAL_WORKFLOW_EXECUTION
      - SIGNAL_EXTERNAL_WORKFLOW_EXECUTION_RATE_EXCEEDED
      - OPERATION_NOT_PERMITTED
  SignalExternalWorkflowExecutionFailedEventAttributes:
    type: object
    required:
      - workflowId
      - cause
      - initiatedEventId
      - decisionTaskCompletedEventId
    properties:
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: The <code>workflowId</code> of the external workflow execution that the signal was being delivered to.
      runId:
        $ref: '#/definitions/RunIdOptional'
        description: The <code>runId</code> of the external workflow execution that the signal was being delivered to.
      cause:
        $ref: '#/definitions/SignalExternalWorkflowExecutionFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      initiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>SignalExternalWorkflowExecutionInitiated</code> event corresponding to the <code>SignalExternalWorkflowExecution</code> decision to request this signal. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>SignalExternalWorkflowExecution</code> decision for this signal. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      control:
        $ref: '#/definitions/Data'
    description: Provides details of the <code>SignalExternalWorkflowExecutionFailed</code> event.
  SignalExternalWorkflowExecutionInitiatedEventAttributes:
    type: object
    required:
      - workflowId
      - signalName
      - decisionTaskCompletedEventId
    properties:
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: The <code>workflowId</code> of the external workflow execution.
      runId:
        $ref: '#/definitions/RunIdOptional'
        description: The <code>runId</code> of the external workflow execution to send the signal to.
      signalName:
        $ref: '#/definitions/SignalName'
        description: The name of the signal.
      input:
        $ref: '#/definitions/Data'
        description: Input provided to the signal (if any).
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>SignalExternalWorkflowExecution</code> decision for this signal. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      control:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> data attached to the event that can be used by the decider in subsequent decision tasks.
    description: Provides details of the <code>SignalExternalWorkflowExecutionInitiated</code> event.
  SignalName:
    type: string
    minLength: 1
    maxLength: 256
  SignalWorkflowExecutionInput:
    type: object
    required:
      - domain
      - workflowId
      - signalName
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain containing the workflow execution to signal.
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: The workflowId of the workflow execution to signal.
      runId:
        $ref: '#/definitions/RunIdOptional'
        description: The runId of the workflow execution to signal.
      signalName:
        $ref: '#/definitions/SignalName'
        description: The name of the signal. This name must be meaningful to the target workflow.
      input:
        $ref: '#/definitions/Data'
        description: Data to attach to the <code>WorkflowExecutionSignaled</code> event in the target workflow execution's history.
  StartChildWorkflowExecutionDecisionAttributes:
    type: object
    required:
      - workflowType
      - workflowId
    properties:
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: <b>Required.</b> The type of the workflow execution to be started.
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: '<p><b>Required.</b> The <code>workflowId</code> of the workflow execution.</p> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      control:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Data attached to the event that can be used by the decider in subsequent workflow tasks. This data is not sent to the child workflow execution.
      input:
        $ref: '#/definitions/Data'
        description: The input to be provided to the workflow execution.
      executionStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: <p>The total duration for this workflow execution. This overrides the defaultExecutionStartToCloseTimeout specified when registering the workflow type.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p> <note>An execution start-to-close timeout for this workflow execution must be specified either as a default for the workflow type or through this parameter. If neither this parameter is set nor a default execution start-to-close timeout was specified at registration time then a fault will be returned.</note>
      taskList:
        $ref: '#/definitions/TaskList'
        description: '<p>The name of the task list to be used for decision tasks of the child workflow execution.</p> <note>A task list for this workflow execution must be specified either as a default for the workflow type or through this parameter. If neither this parameter is set nor a default task list was specified at registration time then a fault will be returned.</note> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      taskPriority:
        $ref: '#/definitions/TaskPriority'
        description: '<p><i>Optional.</i> A task priority that, if set, specifies the priority for a decision task of this workflow execution. This overrides the defaultTaskPriority specified when registering the workflow type. Valid values are integers that range from Java''s <code>Integer.MIN_VALUE</code> (-2147483648) to <code>Integer.MAX_VALUE</code> (2147483647). Higher numbers indicate higher priority.</p> <p>For more information about setting task priority, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/programming-priority.html">Setting Task Priority</a> in the <i>Amazon Simple Workflow Developer Guide</i>.</p>'
      taskStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: <p>Specifies the maximum duration of decision tasks for this workflow execution. This parameter overrides the <code>defaultTaskStartToCloseTimout</code> specified when registering the workflow type using <a>RegisterWorkflowType</a>.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p> <note>A task start-to-close timeout for this workflow execution must be specified either as a default for the workflow type or through this parameter. If neither this parameter is set nor a default task start-to-close timeout was specified at registration time then a fault will be returned.</note>
      childPolicy:
        $ref: '#/definitions/ChildPolicy'
        description: '<p><i>Optional.</i> If set, specifies the policy to use for the child workflow executions if the workflow execution being started is terminated by calling the <a>TerminateWorkflowExecution</a> action explicitly or due to an expired timeout. This policy overrides the default child policy specified when registering the workflow type using <a>RegisterWorkflowType</a>.</p> <p>The supported child policies are:</p> <ul> <li><b>TERMINATE:</b> the child executions will be terminated.</li> <li><b>REQUEST_CANCEL:</b> a request to cancel will be attempted for each child execution by recording a <code>WorkflowExecutionCancelRequested</code> event in its history. It is up to the decider to take appropriate actions when it receives an execution history with this event.</li> <li><b>ABANDON:</b> no action will be taken. The child executions will continue to run.</li> </ul> <note>A child policy for this workflow execution must be specified either as a default for the workflow type or through this parameter. If neither this parameter is set nor a default child policy was specified at registration time then a fault will be returned.</note>'
      tagList:
        $ref: '#/definitions/TagList'
        description: The list of tags to associate with the child workflow execution. A maximum of 5 tags can be specified. You can list workflow executions with a specific tag by calling <a>ListOpenWorkflowExecutions</a> or <a>ListClosedWorkflowExecutions</a> and specifying a <a>TagFilter</a>.
      lambdaRole:
        $ref: '#/definitions/Arn'
        description: '<p>The ARN of an IAM role that authorizes Amazon SWF to invoke AWS Lambda functions.</p> <note>In order for this workflow execution to invoke AWS Lambda functions, an appropriate IAM role must be specified either as a default for the workflow type or through this field.</note>'
    description: '<p>Provides details of the <code>StartChildWorkflowExecution</code> decision.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this decision''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>Constrain the following parameters by using a <code>Condition</code> element with the appropriate keys. <ul> <li> <code>tagList.member.N</code>: The key is "swf:tagList.N" where N is the tag number from 0 to 4, inclusive.</li> <li><code>taskList</code>: String constraint. The key is <code>swf:taskList.name</code>.</li> <li><code>workflowType.name</code>: String constraint. The key is <code>swf:workflowType.name</code>.</li> <li><code>workflowType.version</code>: String constraint. The key is <code>swf:workflowType.version</code>.</li> </ul> </li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
  StartChildWorkflowExecutionFailedCause:
    type: string
    enum:
      - WORKFLOW_TYPE_DOES_NOT_EXIST
      - WORKFLOW_TYPE_DEPRECATED
      - OPEN_CHILDREN_LIMIT_EXCEEDED
      - OPEN_WORKFLOWS_LIMIT_EXCEEDED
      - CHILD_CREATION_RATE_EXCEEDED
      - WORKFLOW_ALREADY_RUNNING
      - DEFAULT_EXECUTION_START_TO_CLOSE_TIMEOUT_UNDEFINED
      - DEFAULT_TASK_LIST_UNDEFINED
      - DEFAULT_TASK_START_TO_CLOSE_TIMEOUT_UNDEFINED
      - DEFAULT_CHILD_POLICY_UNDEFINED
      - OPERATION_NOT_PERMITTED
  StartChildWorkflowExecutionFailedEventAttributes:
    type: object
    required:
      - workflowType
      - cause
      - workflowId
      - initiatedEventId
      - decisionTaskCompletedEventId
    properties:
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The workflow type provided in the <code>StartChildWorkflowExecution</code> decision that failed.
      cause:
        $ref: '#/definitions/StartChildWorkflowExecutionFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: The <code>workflowId</code> of the child workflow execution.
      initiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>StartChildWorkflowExecutionInitiated</code> event corresponding to the <code>StartChildWorkflowExecution</code> decision to start this child workflow execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>StartChildWorkflowExecution</code> decision to request this child workflow execution. This information can be useful for diagnosing problems by tracing back the cause of events.
      control:
        $ref: '#/definitions/Data'
    description: Provides details of the <code>StartChildWorkflowExecutionFailed</code> event.
  StartChildWorkflowExecutionInitiatedEventAttributes:
    type: object
    required:
      - workflowId
      - workflowType
      - taskList
      - decisionTaskCompletedEventId
      - childPolicy
    properties:
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: The <code>workflowId</code> of the child workflow execution.
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The type of the child workflow execution.
      control:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Data attached to the event that can be used by the decider in subsequent decision tasks. This data is not sent to the activity.
      input:
        $ref: '#/definitions/Data'
        description: The inputs provided to the child workflow execution (if any).
      executionStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p>The maximum duration for the child workflow execution. If the workflow execution is not closed within this duration, it will be timed out and force terminated.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>'
      taskList:
        $ref: '#/definitions/TaskList'
        description: The name of the task list used for the decision tasks of the child workflow execution.
      taskPriority:
        $ref: '#/definitions/TaskPriority'
        description: '<p><i>Optional.</i> The priority assigned for the decision tasks for this workflow execution. Valid values are integers that range from Java''s <code>Integer.MIN_VALUE</code> (-2147483648) to <code>Integer.MAX_VALUE</code> (2147483647). Higher numbers indicate higher priority.</p> <p>For more information about setting task priority, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/programming-priority.html">Setting Task Priority</a> in the <i>Amazon Simple Workflow Developer Guide</i>.</p>'
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>StartChildWorkflowExecution</code> decision to request this child workflow execution. This information can be useful for diagnosing problems by tracing back the cause of events.
      childPolicy:
        $ref: '#/definitions/ChildPolicy'
        description: '<p>The policy to use for the child workflow executions if this execution gets terminated by explicitly calling the <a>TerminateWorkflowExecution</a> action or due to an expired timeout.</p> <p>The supported child policies are:</p> <ul> <li><b>TERMINATE:</b> the child executions will be terminated.</li> <li><b>REQUEST_CANCEL:</b> a request to cancel will be attempted for each child execution by recording a <code>WorkflowExecutionCancelRequested</code> event in its history. It is up to the decider to take appropriate actions when it receives an execution history with this event.</li> <li><b>ABANDON:</b> no action will be taken. The child executions will continue to run.</li> </ul>'
      taskStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: <p>The maximum duration allowed for the decision tasks for this workflow execution.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>
      tagList:
        $ref: '#/definitions/TagList'
        description: The list of tags to associated with the child workflow execution.
      lambdaRole:
        $ref: '#/definitions/Arn'
        description: The IAM role attached to this workflow execution to use when invoking AWS Lambda functions.
    description: Provides details of the <code>StartChildWorkflowExecutionInitiated</code> event.
  StartLambdaFunctionFailedCause:
    type: string
    enum:
      - ASSUME_ROLE_FAILED
  StartLambdaFunctionFailedEventAttributes:
    type: object
    properties:
      scheduledEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>LambdaFunctionScheduled</code> event that was recorded when this AWS Lambda function was scheduled. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      cause:
        $ref: '#/definitions/StartLambdaFunctionFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      message:
        $ref: '#/definitions/CauseMessage'
        description: The error message (if any).
    description: Provides details for the <code>StartLambdaFunctionFailed</code> event.
  StartTimerDecisionAttributes:
    type: object
    required:
      - timerId
      - startToFireTimeout
    properties:
      timerId:
        $ref: '#/definitions/TimerId'
        description: '<p><b>Required.</b> The unique ID of the timer.</p> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      control:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Data attached to the event that can be used by the decider in subsequent workflow tasks.
      startToFireTimeout:
        $ref: '#/definitions/DurationInSeconds'
        description: <p><b>Required.</b> The duration to wait before firing the timer.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0.</p>
    description: '<p>Provides details of the <code>StartTimer</code> decision.</p> <p><b>Access Control</b></p> <p>You can use IAM policies to control this decision''s access to Amazon SWF resources as follows:</p> <ul> <li>Use a <code>Resource</code> element with the domain name to limit the action to only specified domains.</li> <li>Use an <code>Action</code> element to allow or deny permission to call this action.</li> <li>You cannot use an IAM policy to constrain this action''s parameters.</li> </ul> <p>If the caller does not have sufficient permissions to invoke the action, or the parameter values fall outside the specified constraints, the action fails. The associated event attribute''s <b>cause</b> parameter will be set to OPERATION_NOT_PERMITTED. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</p>'
  StartTimerFailedCause:
    type: string
    enum:
      - TIMER_ID_ALREADY_IN_USE
      - OPEN_TIMERS_LIMIT_EXCEEDED
      - TIMER_CREATION_RATE_EXCEEDED
      - OPERATION_NOT_PERMITTED
  StartTimerFailedEventAttributes:
    type: object
    required:
      - timerId
      - cause
      - decisionTaskCompletedEventId
    properties:
      timerId:
        $ref: '#/definitions/TimerId'
        description: The timerId provided in the <code>StartTimer</code> decision that failed.
      cause:
        $ref: '#/definitions/StartTimerFailedCause'
        description: '<p>The cause of the failure. This information is generated by the system and can be useful for diagnostic purposes.</p> <note>If <b>cause</b> is set to OPERATION_NOT_PERMITTED, the decision failed because it lacked sufficient permissions. For details and example IAM policies, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/swf-dev-iam.html">Using IAM to Manage Access to Amazon SWF Workflows</a>.</note>'
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>StartTimer</code> decision for this activity task. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>StartTimerFailed</code> event.
  StartWorkflowExecutionInput:
    type: object
    required:
      - domain
      - workflowId
      - workflowType
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The name of the domain in which the workflow execution is created.
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: '<p>The user defined identifier associated with the workflow execution. You can use this to associate a custom identifier with the workflow execution. You may specify the same identifier if a workflow execution is logically a <i>restart</i> of a previous execution. You cannot have two open workflow executions with the same <code>workflowId</code> at the same time.</p> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The type of the workflow to start.
      taskList:
        $ref: '#/definitions/TaskList'
        description: '<p>The task list to use for the decision tasks generated for this workflow execution. This overrides the <code>defaultTaskList</code> specified when registering the workflow type.</p> <note>A task list for this workflow execution must be specified either as a default for the workflow type or through this parameter. If neither this parameter is set nor a default task list was specified at registration time then a fault will be returned.</note> <p>The specified string must not start or end with whitespace. It must not contain a <code>:</code> (colon), <code>/</code> (slash), <code>|</code> (vertical bar), or any control characters (\u0000-\u001f | \u007f - \u009f). Also, it must not contain the literal string quotarnquot.</p>'
      taskPriority:
        $ref: '#/definitions/TaskPriority'
        description: '<p>The task priority to use for this workflow execution. This will override any default priority that was assigned when the workflow type was registered. If not set, then the default task priority for the workflow type will be used. Valid values are integers that range from Java''s <code>Integer.MIN_VALUE</code> (-2147483648) to <code>Integer.MAX_VALUE</code> (2147483647). Higher numbers indicate higher priority.</p> <p>For more information about setting task priority, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/programming-priority.html">Setting Task Priority</a> in the <i>Amazon Simple Workflow Developer Guide</i>.</p>'
      input:
        $ref: '#/definitions/Data'
        description: The input for the workflow execution. This is a free form string which should be meaningful to the workflow you are starting. This <code>input</code> is made available to the new workflow execution in the <code>WorkflowExecutionStarted</code> history event.
      executionStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p>The total duration for this workflow execution. This overrides the defaultExecutionStartToCloseTimeout specified when registering the workflow type.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. Exceeding this limit will cause the workflow execution to time out. Unlike some of the other timeout parameters in Amazon SWF, you cannot specify a value of "NONE" for this timeout; there is a one-year max limit on the time that a workflow execution can run.</p> <note> An execution start-to-close timeout must be specified either through this parameter or as a default when the workflow type is registered. If neither this parameter nor a default execution start-to-close timeout is specified, a fault is returned.</note>'
      tagList:
        $ref: '#/definitions/TagList'
        description: The list of tags to associate with the workflow execution. You can specify a maximum of 5 tags. You can list workflow executions with a specific tag by calling <a>ListOpenWorkflowExecutions</a> or <a>ListClosedWorkflowExecutions</a> and specifying a <a>TagFilter</a>.
      taskStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: <p>Specifies the maximum duration of decision tasks for this workflow execution. This parameter overrides the <code>defaultTaskStartToCloseTimout</code> specified when registering the workflow type using <a>RegisterWorkflowType</a>.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p> <note>A task start-to-close timeout for this workflow execution must be specified either as a default for the workflow type or through this parameter. If neither this parameter is set nor a default task start-to-close timeout was specified at registration time then a fault will be returned.</note>
      childPolicy:
        $ref: '#/definitions/ChildPolicy'
        description: '<p>If set, specifies the policy to use for the child workflow executions of this workflow execution if it is terminated, by calling the <a>TerminateWorkflowExecution</a> action explicitly or due to an expired timeout. This policy overrides the default child policy specified when registering the workflow type using <a>RegisterWorkflowType</a>.</p> <p>The supported child policies are:</p> <ul> <li><b>TERMINATE:</b> the child executions will be terminated.</li> <li><b>REQUEST_CANCEL:</b> a request to cancel will be attempted for each child execution by recording a <code>WorkflowExecutionCancelRequested</code> event in its history. It is up to the decider to take appropriate actions when it receives an execution history with this event.</li> <li><b>ABANDON:</b> no action will be taken. The child executions will continue to run.</li> </ul> <note>A child policy for this workflow execution must be specified either as a default for the workflow type or through this parameter. If neither this parameter is set nor a default child policy was specified at registration time then a fault will be returned.</note>'
      lambdaRole:
        $ref: '#/definitions/Arn'
        description: '<p>The ARN of an IAM role that authorizes Amazon SWF to invoke AWS Lambda functions.</p> <note>In order for this workflow execution to invoke AWS Lambda functions, an appropriate IAM role must be specified either as a default for the workflow type or through this field.</note>'
  Tag:
    type: string
    minLength: 1
    maxLength: 256
  TagFilter:
    type: object
    required:
      - tag
    properties:
      tag:
        $ref: '#/definitions/Tag'
        description: <b>Required.</b> Specifies the tag that must be associated with the execution for it to meet the filter criteria.
    description: Used to filter the workflow executions in visibility APIs based on a tag.
  TagList:
    type: array
    items:
      $ref: '#/definitions/Tag'
    maxItems: 5
  TaskList:
    type: object
    required:
      - name
    properties:
      name:
        $ref: '#/definitions/Name'
        description: The name of the task list.
    description: Represents a task list.
  TaskPriority:
    type: string
    maxLength: 11
  TaskToken:
    type: string
    minLength: 1
    maxLength: 1024
  TerminateReason:
    type: string
    maxLength: 256
  TerminateWorkflowExecutionInput:
    type: object
    required:
      - domain
      - workflowId
    properties:
      domain:
        $ref: '#/definitions/DomainName'
        description: The domain of the workflow execution to terminate.
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: The workflowId of the workflow execution to terminate.
      runId:
        $ref: '#/definitions/RunIdOptional'
        description: The runId of the workflow execution to terminate.
      reason:
        $ref: '#/definitions/TerminateReason'
        description: <i>Optional.</i> A descriptive reason for terminating the workflow execution.
      details:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Details for terminating the workflow execution.
      childPolicy:
        $ref: '#/definitions/ChildPolicy'
        description: '<p>If set, specifies the policy to use for the child workflow executions of the workflow execution being terminated. This policy overrides the child policy specified for the workflow execution at registration time or when starting the execution.</p> <p>The supported child policies are:</p> <ul> <li><b>TERMINATE:</b> the child executions will be terminated.</li> <li><b>REQUEST_CANCEL:</b> a request to cancel will be attempted for each child execution by recording a <code>WorkflowExecutionCancelRequested</code> event in its history. It is up to the decider to take appropriate actions when it receives an execution history with this event.</li> <li><b>ABANDON:</b> no action will be taken. The child executions will continue to run.</li> </ul> <note>A child policy for this workflow execution must be specified either as a default for the workflow type or through this parameter. If neither this parameter is set nor a default child policy was specified at registration time then a fault will be returned.</note>'
  TimerCanceledEventAttributes:
    type: object
    required:
      - timerId
      - startedEventId
      - decisionTaskCompletedEventId
    properties:
      timerId:
        $ref: '#/definitions/TimerId'
        description: ' The unique ID of the timer that was canceled. '
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>TimerStarted</code> event that was recorded when this timer was started. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>CancelTimer</code> decision to cancel this timer. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: ' Provides details of the <code>TimerCanceled</code> event. '
  TimerFiredEventAttributes:
    type: object
    required:
      - timerId
      - startedEventId
    properties:
      timerId:
        $ref: '#/definitions/TimerId'
        description: The unique ID of the timer that fired.
      startedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>TimerStarted</code> event that was recorded when this timer was started. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>TimerFired</code> event.
  TimerId:
    type: string
    minLength: 1
    maxLength: 256
  TimerStartedEventAttributes:
    type: object
    required:
      - timerId
      - startToFireTimeout
      - decisionTaskCompletedEventId
    properties:
      timerId:
        $ref: '#/definitions/TimerId'
        description: The unique ID of the timer that was started.
      control:
        $ref: '#/definitions/Data'
        description: <i>Optional.</i> Data attached to the event that can be used by the decider in subsequent workflow tasks.
      startToFireTimeout:
        $ref: '#/definitions/DurationInSeconds'
        description: <p>The duration of time after which the timer will fire.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0.</p>
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>StartTimer</code> decision for this activity task. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>TimerStarted</code> event.
  Timestamp:
    type: string
    format: date-time
  Truncated:
    type: boolean
  TypeAlreadyExistsFault:
    type: object
    properties:
      message:
        $ref: '#/definitions/ErrorMessage'
        description: A description that may help with diagnosing the cause of the fault.
    description: Returned if the type already exists in the specified domain. You will get this fault even if the existing type is in deprecated status. You can specify another version if the intent is to create a new distinct version of the type.
  TypeDeprecatedFault:
    type: object
    properties:
      message:
        $ref: '#/definitions/ErrorMessage'
        description: A description that may help with diagnosing the cause of the fault.
    description: Returned when the specified activity or workflow type was already deprecated.
  UnknownResourceFault:
    type: object
    properties:
      message:
        $ref: '#/definitions/ErrorMessage'
        description: A description that may help with diagnosing the cause of the fault.
    description: Returned when the named resource cannot be found with in the scope of this operation (region or domain). This could happen if the named resource was never created or is no longer available for this operation.
  Version:
    type: string
    minLength: 1
    maxLength: 64
  VersionOptional:
    type: string
    maxLength: 64
  WorkflowExecution:
    type: object
    required:
      - workflowId
      - runId
    properties:
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: The user defined identifier associated with the workflow execution.
      runId:
        $ref: '#/definitions/RunId'
        description: A system-generated unique identifier for the workflow execution.
    description: Represents a workflow execution.
  WorkflowExecutionAlreadyStartedFault:
    type: object
    properties:
      message:
        $ref: '#/definitions/ErrorMessage'
        description: A description that may help with diagnosing the cause of the fault.
    description: Returned by <a>StartWorkflowExecution</a> when an open execution with the same workflowId is already running in the specified domain.
  WorkflowExecutionCancelRequestedCause:
    type: string
    enum:
      - CHILD_POLICY_APPLIED
  WorkflowExecutionCancelRequestedEventAttributes:
    type: object
    properties:
      externalWorkflowExecution:
        $ref: '#/definitions/WorkflowExecution'
        description: The external workflow execution for which the cancellation was requested.
      externalInitiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>RequestCancelExternalWorkflowExecutionInitiated</code> event corresponding to the <code>RequestCancelExternalWorkflowExecution</code> decision to cancel this workflow execution.The source event with this ID can be found in the history of the source workflow execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      cause:
        $ref: '#/definitions/WorkflowExecutionCancelRequestedCause'
        description: 'If set, indicates that the request to cancel the workflow execution was automatically generated, and specifies the cause. This happens if the parent workflow execution times out or is terminated, and the child policy is set to cancel child executions.'
    description: Provides details of the <code>WorkflowExecutionCancelRequested</code> event.
  WorkflowExecutionCanceledEventAttributes:
    type: object
    required:
      - decisionTaskCompletedEventId
    properties:
      details:
        $ref: '#/definitions/Data'
        description: Details for the cancellation (if any).
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>CancelWorkflowExecution</code> decision for this cancellation request. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>WorkflowExecutionCanceled</code> event.
  WorkflowExecutionCompletedEventAttributes:
    type: object
    required:
      - decisionTaskCompletedEventId
    properties:
      result:
        $ref: '#/definitions/Data'
        description: The result produced by the workflow execution upon successful completion.
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>CompleteWorkflowExecution</code> decision to complete this execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>WorkflowExecutionCompleted</code> event.
  WorkflowExecutionConfiguration:
    type: object
    required:
      - taskStartToCloseTimeout
      - executionStartToCloseTimeout
      - taskList
      - childPolicy
    properties:
      taskStartToCloseTimeout:
        $ref: '#/definitions/DurationInSeconds'
        description: <p>The maximum duration allowed for decision tasks for this workflow execution.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>
      executionStartToCloseTimeout:
        $ref: '#/definitions/DurationInSeconds'
        description: <p>The total duration for this workflow execution.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>
      taskList:
        $ref: '#/definitions/TaskList'
        description: The task list used for the decision tasks generated for this workflow execution.
      taskPriority:
        $ref: '#/definitions/TaskPriority'
        description: '<p>The priority assigned to decision tasks for this workflow execution. Valid values are integers that range from Java''s <code>Integer.MIN_VALUE</code> (-2147483648) to <code>Integer.MAX_VALUE</code> (2147483647). Higher numbers indicate higher priority.</p> <p>For more information about setting task priority, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/programming-priority.html">Setting Task Priority</a> in the <i>Amazon Simple Workflow Developer Guide</i>.</p>'
      childPolicy:
        $ref: '#/definitions/ChildPolicy'
        description: '<p>The policy to use for the child workflow executions if this workflow execution is terminated, by calling the <a>TerminateWorkflowExecution</a> action explicitly or due to an expired timeout.</p> <p>The supported child policies are:</p> <ul> <li><b>TERMINATE:</b> the child executions will be terminated.</li> <li><b>REQUEST_CANCEL:</b> a request to cancel will be attempted for each child execution by recording a <code>WorkflowExecutionCancelRequested</code> event in its history. It is up to the decider to take appropriate actions when it receives an execution history with this event.</li> <li><b>ABANDON:</b> no action will be taken. The child executions will continue to run.</li> </ul>'
      lambdaRole:
        $ref: '#/definitions/Arn'
        description: The IAM role used by this workflow execution when invoking AWS Lambda functions.
    description: 'The configuration settings for a workflow execution including timeout values, tasklist etc. These configuration settings are determined from the defaults specified when registering the workflow type and those specified when starting the workflow execution.'
  WorkflowExecutionContinuedAsNewEventAttributes:
    type: object
    required:
      - decisionTaskCompletedEventId
      - newExecutionRunId
      - taskList
      - childPolicy
      - workflowType
    properties:
      input:
        $ref: '#/definitions/Data'
        description: The input provided to the new workflow execution.
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>ContinueAsNewWorkflowExecution</code> decision that started this execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      newExecutionRunId:
        $ref: '#/definitions/RunId'
        description: The <code>runId</code> of the new workflow execution.
      executionStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: <p>The total duration allowed for the new workflow execution.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>
      taskList:
        $ref: '#/definitions/TaskList'
      taskPriority:
        $ref: '#/definitions/TaskPriority'
      taskStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: <p>The maximum duration of decision tasks for the new workflow execution.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>
      childPolicy:
        $ref: '#/definitions/ChildPolicy'
        description: '<p>The policy to use for the child workflow executions of the new execution if it is terminated by calling the <a>TerminateWorkflowExecution</a> action explicitly or due to an expired timeout.</p> <p>The supported child policies are:</p> <ul> <li><b>TERMINATE:</b> the child executions will be terminated.</li> <li><b>REQUEST_CANCEL:</b> a request to cancel will be attempted for each child execution by recording a <code>WorkflowExecutionCancelRequested</code> event in its history. It is up to the decider to take appropriate actions when it receives an execution history with this event.</li> <li><b>ABANDON:</b> no action will be taken. The child executions will continue to run.</li> </ul>'
      tagList:
        $ref: '#/definitions/TagList'
        description: The list of tags associated with the new workflow execution.
      workflowType:
        $ref: '#/definitions/WorkflowType'
      lambdaRole:
        $ref: '#/definitions/Arn'
        description: The IAM role attached to this workflow execution to use when invoking AWS Lambda functions.
    description: Provides details of the <code>WorkflowExecutionContinuedAsNew</code> event.
  WorkflowExecutionCount:
    type: object
    required:
      - count
    properties:
      count:
        $ref: '#/definitions/Count'
        description: The number of workflow executions.
      truncated:
        $ref: '#/definitions/Truncated'
        description: 'If set to true, indicates that the actual count was more than the maximum supported by this API and the count returned is the truncated value.'
    description: Contains the count of workflow executions returned from <a>CountOpenWorkflowExecutions</a> or <a>CountClosedWorkflowExecutions</a>
  WorkflowExecutionDetail:
    type: object
    required:
      - executionInfo
      - executionConfiguration
      - openCounts
    properties:
      executionInfo:
        $ref: '#/definitions/WorkflowExecutionInfo'
        description: Information about the workflow execution.
      executionConfiguration:
        $ref: '#/definitions/WorkflowExecutionConfiguration'
        description: 'The configuration settings for this workflow execution including timeout values, tasklist etc.'
      openCounts:
        $ref: '#/definitions/WorkflowExecutionOpenCounts'
        description: The number of tasks for this workflow execution. This includes open and closed tasks of all types.
      latestActivityTaskTimestamp:
        $ref: '#/definitions/Timestamp'
        description: The time when the last activity task was scheduled for this workflow execution. You can use this information to determine if the workflow has not made progress for an unusually long period of time and might require a corrective action.
      latestExecutionContext:
        $ref: '#/definitions/Data'
        description: The latest executionContext provided by the decider for this workflow execution. A decider can provide an executionContext (a free-form string) when closing a decision task using <a>RespondDecisionTaskCompleted</a>.
    description: Contains details about a workflow execution.
  WorkflowExecutionFailedEventAttributes:
    type: object
    required:
      - decisionTaskCompletedEventId
    properties:
      reason:
        $ref: '#/definitions/FailureReason'
        description: The descriptive reason provided for the failure (if any).
      details:
        $ref: '#/definitions/Data'
        description: The details of the failure (if any).
      decisionTaskCompletedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>DecisionTaskCompleted</code> event corresponding to the decision task that resulted in the <code>FailWorkflowExecution</code> decision to fail this execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
    description: Provides details of the <code>WorkflowExecutionFailed</code> event.
  WorkflowExecutionFilter:
    type: object
    required:
      - workflowId
    properties:
      workflowId:
        $ref: '#/definitions/WorkflowId'
        description: The workflowId to pass of match the criteria of this filter.
    description: Used to filter the workflow executions in visibility APIs by their <code>workflowId</code>.
  WorkflowExecutionInfo:
    type: object
    required:
      - execution
      - workflowType
      - startTimestamp
      - executionStatus
    properties:
      execution:
        $ref: '#/definitions/WorkflowExecution'
        description: The workflow execution this information is about.
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The type of the workflow execution.
      startTimestamp:
        $ref: '#/definitions/Timestamp'
        description: The time when the execution was started.
      closeTimestamp:
        $ref: '#/definitions/Timestamp'
        description: The time when the workflow execution was closed. Set only if the execution status is CLOSED.
      executionStatus:
        $ref: '#/definitions/ExecutionStatus'
        description: The current status of the execution.
      closeStatus:
        $ref: '#/definitions/CloseStatus'
        description: '<p>If the execution status is closed then this specifies how the execution was closed:</p> <ul> <li> <code>COMPLETED</code>: the execution was successfully completed.</li> <li> <code>CANCELED</code>: the execution was canceled.Cancellation allows the implementation to gracefully clean up before the execution is closed.</li> <li> <code>TERMINATED</code>: the execution was force terminated.</li> <li> <code>FAILED</code>: the execution failed to complete.</li> <li> <code>TIMED_OUT</code>: the execution did not complete in the alloted time and was automatically timed out.</li> <li> <code>CONTINUED_AS_NEW</code>: the execution is logically continued. This means the current execution was completed and a new execution was started to carry on the workflow.</li> </ul>'
      parent:
        $ref: '#/definitions/WorkflowExecution'
        description: If this workflow execution is a child of another execution then contains the workflow execution that started this execution.
      tagList:
        $ref: '#/definitions/TagList'
        description: The list of tags associated with the workflow execution. Tags can be used to identify and list workflow executions of interest through the visibility APIs. A workflow execution can have a maximum of 5 tags.
      cancelRequested:
        $ref: '#/definitions/Canceled'
        description: Set to true if a cancellation is requested for this workflow execution.
    description: 'Contains information about a workflow execution. '
  WorkflowExecutionInfoList:
    type: array
    items:
      $ref: '#/definitions/WorkflowExecutionInfo'
  WorkflowExecutionInfos:
    type: object
    required:
      - executionInfos
    properties:
      executionInfos:
        $ref: '#/definitions/WorkflowExecutionInfoList'
        description: The list of workflow information structures.
      nextPageToken:
        $ref: '#/definitions/PageToken'
        description: '<p>If a <code>NextPageToken</code> was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in <code>nextPageToken</code>. Keep all other arguments unchanged.</p> <p>The configured <code>maximumPageSize</code> determines how many results can be returned in a single call.</p>'
    description: Contains a paginated list of information about workflow executions.
  WorkflowExecutionOpenCounts:
    type: object
    required:
      - openActivityTasks
      - openDecisionTasks
      - openTimers
      - openChildWorkflowExecutions
    properties:
      openActivityTasks:
        $ref: '#/definitions/Count'
        description: The count of activity tasks whose status is OPEN.
      openDecisionTasks:
        $ref: '#/definitions/OpenDecisionTasksCount'
        description: The count of decision tasks whose status is OPEN. A workflow execution can have at most one open decision task.
      openTimers:
        $ref: '#/definitions/Count'
        description: The count of timers started by this workflow execution that have not fired yet.
      openChildWorkflowExecutions:
        $ref: '#/definitions/Count'
        description: The count of child workflow executions whose status is OPEN.
      openLambdaFunctions:
        $ref: '#/definitions/Count'
        description: The count of AWS Lambda functions that are currently executing.
    description: 'Contains the counts of open tasks, child workflow executions and timers for a workflow execution.'
  WorkflowExecutionSignaledEventAttributes:
    type: object
    required:
      - signalName
    properties:
      signalName:
        $ref: '#/definitions/SignalName'
        description: The name of the signal received. The decider can use the signal name and inputs to determine how to the process the signal.
      input:
        $ref: '#/definitions/Data'
        description: Inputs provided with the signal (if any). The decider can use the signal name and inputs to determine how to process the signal.
      externalWorkflowExecution:
        $ref: '#/definitions/WorkflowExecution'
        description: The workflow execution that sent the signal. This is set only of the signal was sent by another workflow execution.
      externalInitiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>SignalExternalWorkflowExecutionInitiated</code> event corresponding to the <code>SignalExternalWorkflow</code> decision to signal this workflow execution.The source event with this ID can be found in the history of the source workflow execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event. This field is set only if the signal was initiated by another workflow execution.
    description: Provides details of the <code>WorkflowExecutionSignaled</code> event.
  WorkflowExecutionStartedEventAttributes:
    type: object
    required:
      - childPolicy
      - taskList
      - workflowType
    properties:
      input:
        $ref: '#/definitions/Data'
        description: The input provided to the workflow execution (if any).
      executionStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: <p>The maximum duration for this workflow execution.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>
      taskStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: <p>The maximum duration of decision tasks for this workflow type.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>
      childPolicy:
        $ref: '#/definitions/ChildPolicy'
        description: '<p>The policy to use for the child workflow executions if this workflow execution is terminated, by calling the <a>TerminateWorkflowExecution</a> action explicitly or due to an expired timeout.</p> <p>The supported child policies are:</p> <ul> <li><b>TERMINATE:</b> the child executions will be terminated.</li> <li><b>REQUEST_CANCEL:</b> a request to cancel will be attempted for each child execution by recording a <code>WorkflowExecutionCancelRequested</code> event in its history. It is up to the decider to take appropriate actions when it receives an execution history with this event.</li> <li><b>ABANDON:</b> no action will be taken. The child executions will continue to run.</li> </ul>'
      taskList:
        $ref: '#/definitions/TaskList'
        description: The name of the task list for scheduling the decision tasks for this workflow execution.
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The workflow type of this execution.
      tagList:
        $ref: '#/definitions/TagList'
        description: The list of tags associated with this workflow execution. An execution can have up to 5 tags.
      taskPriority:
        $ref: '#/definitions/TaskPriority'
      continuedExecutionRunId:
        $ref: '#/definitions/RunIdOptional'
        description: 'If this workflow execution was started due to a <code>ContinueAsNewWorkflowExecution</code> decision, then it contains the <code>runId</code> of the previous workflow execution that was closed and continued as this execution.'
      parentWorkflowExecution:
        $ref: '#/definitions/WorkflowExecution'
        description: The source workflow execution that started this workflow execution. The member is not set if the workflow execution was not started by a workflow.
      parentInitiatedEventId:
        $ref: '#/definitions/EventId'
        description: The ID of the <code>StartChildWorkflowExecutionInitiated</code> event corresponding to the <code>StartChildWorkflowExecution</code> decision to start this workflow execution. The source event with this ID can be found in the history of the source workflow execution. This information can be useful for diagnosing problems by tracing back the chain of events leading up to this event.
      lambdaRole:
        $ref: '#/definitions/Arn'
        description: The IAM role attached to this workflow execution to use when invoking AWS Lambda functions.
    description: Provides details of <code>WorkflowExecutionStarted</code> event.
  WorkflowExecutionTerminatedCause:
    type: string
    enum:
      - CHILD_POLICY_APPLIED
      - EVENT_LIMIT_EXCEEDED
      - OPERATOR_INITIATED
  WorkflowExecutionTerminatedEventAttributes:
    type: object
    required:
      - childPolicy
    properties:
      reason:
        $ref: '#/definitions/TerminateReason'
        description: The reason provided for the termination (if any).
      details:
        $ref: '#/definitions/Data'
        description: The details provided for the termination (if any).
      childPolicy:
        $ref: '#/definitions/ChildPolicy'
        description: '<p>The policy used for the child workflow executions of this workflow execution.</p> <p>The supported child policies are:</p> <ul> <li><b>TERMINATE:</b> the child executions will be terminated.</li> <li><b>REQUEST_CANCEL:</b> a request to cancel will be attempted for each child execution by recording a <code>WorkflowExecutionCancelRequested</code> event in its history. It is up to the decider to take appropriate actions when it receives an execution history with this event.</li> <li><b>ABANDON:</b> no action will be taken. The child executions will continue to run.</li> </ul>'
      cause:
        $ref: '#/definitions/WorkflowExecutionTerminatedCause'
        description: 'If set, indicates that the workflow execution was automatically terminated, and specifies the cause. This happens if the parent workflow execution times out or is terminated and the child policy is set to terminate child executions.'
    description: Provides details of the <code>WorkflowExecutionTerminated</code> event.
  WorkflowExecutionTimedOutEventAttributes:
    type: object
    required:
      - timeoutType
      - childPolicy
    properties:
      timeoutType:
        $ref: '#/definitions/WorkflowExecutionTimeoutType'
        description: The type of timeout that caused this event.
      childPolicy:
        $ref: '#/definitions/ChildPolicy'
        description: '<p>The policy used for the child workflow executions of this workflow execution.</p> <p>The supported child policies are:</p> <ul> <li><b>TERMINATE:</b> the child executions will be terminated.</li> <li><b>REQUEST_CANCEL:</b> a request to cancel will be attempted for each child execution by recording a <code>WorkflowExecutionCancelRequested</code> event in its history. It is up to the decider to take appropriate actions when it receives an execution history with this event.</li> <li><b>ABANDON:</b> no action will be taken. The child executions will continue to run.</li> </ul>'
    description: Provides details of the <code>WorkflowExecutionTimedOut</code> event.
  WorkflowExecutionTimeoutType:
    type: string
    enum:
      - START_TO_CLOSE
  WorkflowId:
    type: string
    minLength: 1
    maxLength: 256
  WorkflowType:
    type: object
    required:
      - name
      - version
    properties:
      name:
        $ref: '#/definitions/Name'
        description: <p><b>Required.</b> The name of the workflow type.</p> <note>The combination of workflow type name and version must be unique with in a domain.</note>
      version:
        $ref: '#/definitions/Version'
        description: <p><b>Required.</b> The version of the workflow type.</p> <note>The combination of workflow type name and version must be unique with in a domain.</note>
    description: Represents a workflow type.
  WorkflowTypeConfiguration:
    type: object
    properties:
      defaultTaskStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p><i>Optional.</i> The default maximum duration, specified when registering the workflow type, that a decision task for executions of this workflow type might take before returning completion or failure. If the task does not close in the specified time then the task is automatically timed out and rescheduled. If the decider eventually reports a completion or failure, it is ignored. This default can be overridden when starting a workflow execution using the <a>StartWorkflowExecution</a> action or the <code>StartChildWorkflowExecution</code> decision.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>'
      defaultExecutionStartToCloseTimeout:
        $ref: '#/definitions/DurationInSecondsOptional'
        description: '<p><i>Optional.</i> The default maximum duration, specified when registering the workflow type, for executions of this workflow type. This default can be overridden when starting a workflow execution using the <a>StartWorkflowExecution</a> action or the <code>StartChildWorkflowExecution</code> decision.</p> <p>The duration is specified in seconds; an integer greater than or equal to 0. The value "NONE" can be used to specify unlimited duration.</p>'
      defaultTaskList:
        $ref: '#/definitions/TaskList'
        description: '<i>Optional.</i> The default task list, specified when registering the workflow type, for decisions tasks scheduled for workflow executions of this type. This default can be overridden when starting a workflow execution using the <a>StartWorkflowExecution</a> action or the <code>StartChildWorkflowExecution</code> decision.'
      defaultTaskPriority:
        $ref: '#/definitions/TaskPriority'
        description: '<p><i>Optional.</i> The default task priority, specified when registering the workflow type, for all decision tasks of this workflow type. This default can be overridden when starting a workflow execution using the <a>StartWorkflowExecution</a> action or the <code>StartChildWorkflowExecution</code> decision.</p> <p>Valid values are integers that range from Java''s <code>Integer.MIN_VALUE</code> (-2147483648) to <code>Integer.MAX_VALUE</code> (2147483647). Higher numbers indicate higher priority.</p> <p>For more information about setting task priority, see <a href="http://docs.aws.amazon.com/amazonswf/latest/developerguide/programming-priority.html">Setting Task Priority</a> in the <i>Amazon Simple Workflow Developer Guide</i>.</p>'
      defaultChildPolicy:
        $ref: '#/definitions/ChildPolicy'
        description: '<p><i>Optional.</i> The default policy to use for the child workflow executions when a workflow execution of this type is terminated, by calling the <a>TerminateWorkflowExecution</a> action explicitly or due to an expired timeout. This default can be overridden when starting a workflow execution using the <a>StartWorkflowExecution</a> action or the <code>StartChildWorkflowExecution</code> decision.</p> <p>The supported child policies are:</p> <ul> <li><b>TERMINATE:</b> the child executions will be terminated.</li> <li><b>REQUEST_CANCEL:</b> a request to cancel will be attempted for each child execution by recording a <code>WorkflowExecutionCancelRequested</code> event in its history. It is up to the decider to take appropriate actions when it receives an execution history with this event.</li> <li><b>ABANDON:</b> no action will be taken. The child executions will continue to run.</li> </ul>'
      defaultLambdaRole:
        $ref: '#/definitions/Arn'
        description: The default IAM role to use when a workflow execution invokes a AWS Lambda function.
    description: The configuration settings of a workflow type.
  WorkflowTypeDetail:
    type: object
    required:
      - typeInfo
      - configuration
    properties:
      typeInfo:
        $ref: '#/definitions/WorkflowTypeInfo'
        description: '<p>General information about the workflow type.</p> <p>The status of the workflow type (returned in the WorkflowTypeInfo structure) can be one of the following.</p> <ul> <li> <b>REGISTERED</b>: The type is registered and available. Workers supporting this type should be running.</li> <li> <b>DEPRECATED</b>: The type was deprecated using <a>DeprecateWorkflowType</a>, but is still in use. You should keep workers supporting this type running. You cannot create new workflow executions of this type.</li> </ul>'
      configuration:
        $ref: '#/definitions/WorkflowTypeConfiguration'
        description: Configuration settings of the workflow type registered through <a>RegisterWorkflowType</a>
    description: Contains details about a workflow type.
  WorkflowTypeFilter:
    type: object
    required:
      - name
    properties:
      name:
        $ref: '#/definitions/Name'
        description: <b>Required.</b> Name of the workflow type.
      version:
        $ref: '#/definitions/VersionOptional'
        description: Version of the workflow type.
    description: 'Used to filter workflow execution query results by type. Each parameter, if specified, defines a rule that must be satisfied by each returned result.'
  WorkflowTypeInfo:
    type: object
    required:
      - workflowType
      - status
      - creationDate
    properties:
      workflowType:
        $ref: '#/definitions/WorkflowType'
        description: The workflow type this information is about.
      status:
        $ref: '#/definitions/RegistrationStatus'
        description: The current status of the workflow type.
      description:
        $ref: '#/definitions/Description'
        description: The description of the type registered through <a>RegisterWorkflowType</a>.
      creationDate:
        $ref: '#/definitions/Timestamp'
        description: The date when this type was registered.
      deprecationDate:
        $ref: '#/definitions/Timestamp'
        description: 'If the type is in deprecated state, then it is set to the date when the type was deprecated.'
    description: Contains information about a workflow type.
  WorkflowTypeInfoList:
    type: array
    items:
      $ref: '#/definitions/WorkflowTypeInfo'
  WorkflowTypeInfos:
    type: object
    required:
      - typeInfos
    properties:
      typeInfos:
        $ref: '#/definitions/WorkflowTypeInfoList'
        description: The list of workflow type information.
      nextPageToken:
        $ref: '#/definitions/PageToken'
        description: '<p>If a <code>NextPageToken</code> was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in <code>nextPageToken</code>. Keep all other arguments unchanged.</p> <p>The configured <code>maximumPageSize</code> determines how many results can be returned in a single call.</p>'
    description: Contains a paginated list of information structures about workflow types.
