# coding: utf-8

"""
    App Center Client

    Microsoft Visual Studio App Center API

    The version of the OpenAPI document: v0.1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ApiTokensPrivateCreateRequest(BaseModel):
    """
    ApiTokensPrivateCreateRequest
    """ # noqa: E501
    description: Optional[StrictStr] = Field(default=None, description="The description of the token")
    principal_id: StrictStr = Field(description="The principal ID assigned to the API token")
    principal_type: StrictStr = Field(description="The principal type assigned to the API token")
    scope: Optional[List[StrictStr]] = Field(default=None, description="The scope for this token (default \"all\").")
    token_type: Optional[StrictStr] = Field(default=None, description="The token's type (default \"public\")   public: managed by the user   in_app_update: special token for in-app update scenario   buid: dedicated for CI usage for now   session: for CLI session management   tester_app: used for tester mobile app")
    __properties: ClassVar[List[str]] = ["description", "principal_id", "principal_type", "scope", "token_type"]

    @field_validator('principal_type')
    def principal_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['app', 'user']):
            raise ValueError("must be one of enum values ('app', 'user')")
        return value

    @field_validator('scope')
    def scope_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['all', 'in_app_update', 'viewer']):
                raise ValueError("each list item must be one of ('all', 'in_app_update', 'viewer')")
        return value

    @field_validator('token_type')
    def token_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['public', 'in_app_update', 'build', 'session', 'tester_app']):
            raise ValueError("must be one of enum values ('public', 'in_app_update', 'build', 'session', 'tester_app')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ApiTokensPrivateCreateRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ApiTokensPrivateCreateRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "description": obj.get("description"),
            "principal_id": obj.get("principal_id"),
            "principal_type": obj.get("principal_type"),
            "scope": obj.get("scope"),
            "token_type": obj.get("token_type")
        })
        return _obj


