openapi: 3.0.0
servers:
  - url: https://art19.com
info:
  contact:
    email: support@art19.com
    name: ART19 Engineering Team
    x-twitter: art19co
  description: >
    The ART19 Content API conforms to the [JSON:API specification](http://jsonapi.org).


    API requests **MUST** use the HTTP Accept header:


    `Accept: application/vnd.api+json`


    API requests **MUST** be authenticated using the HTTP Authorization header:


    `Authorization: Token token="your-token", credential="your-credential"`


    ## General Notes


    Some query parameters use unencoded [ and ] characters simply for readability. Defaults, examples, and

    possible values are additionally rendered in double quotes for readability. In practice, query parameters should

    not have quotes around the values (e.g., `foo=bar` is valid, not `foo="bar"`), and both query parameter keys

    and values must be percent-encoded, per the requirements in [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).


    ## Rate Limiting


    In order to provide a fair distribution of available resources, all API calls are subject to rate limits.

    If you exceed the number of API calls per minute granted to your credential, a `429 Too Many Requests`

    error response will be returned.


    In that case, a `Retry-After` header MAY be included in the response, describing the number of seconds

    after which a request can be retried.


    If you run into a high number of 429 errors, please reach out to ART19 Support to adjust your rate limit.


    ### Example


    In the following example the request can be retried after waiting for 21 seconds:

        HTTP/1.1 429 Too Many Requests
        Content-Type: text/html
        Retry-After: 21

    ## Pagination


    Requests to collection endpoints **SHOULD** provide pagination parameters.

    Some endpoints **REQUIRE** pagination parameters to be provided.

    Whenever pagination is provided, it **MUST** be valid.

    Failing to provide pagination when it is required or providing wrong or incomplete pagination

    always results in a `400 Bad Request` error response.


    The page numbering starts with `1` and the maximum page size (if not otherwise documented

    on an endpoint) is `100`. Pagination **MUST NOT** be specified if requesting a list of IDs (using an `ids[]` parameter).


    Providing invalid values for page number or page size, as well as providing only a page number or only a page size,

    is considered an error. Pagination is provided like this:


    `page[number]=1&page[size]=25`


    Responses conform to the [JSON:API specification's pagination section](https://jsonapi.org/format/#fetching-pagination)

    by including pagination links. Your requested page size will be carried into the pagination links.


    ## Sorting


    Requests to collection endpoints usually accept a `sort` parameter. Please refer to the

    [JSON:API Specification's sorting section](https://jsonapi.org/format/#fetching-sorting) for further details.


    ## Relationship Linking


    Currently, resources return all of their relationships, in no particular order, pursuant to how relationships

    should be returned [according to the JSON:API specification](https://jsonapi.org/format/#document-resource-object-relationships). Consumers of this API

    **MUST NOT** make assumptions about the order of these collections. Even though this data is not currently paginated, consumers **MUST** support

    paginating relationships per the JSON:API specification if this data is important for their application.
  termsOfService: https://art19.com/business-terms
  title: ART19 Content API Documentation
  version: 1.0.0
  x-apisguru-categories:
    - media
  x-logo:
    url: https://twitter.com/art19co/profile_image?size=original
  x-origin:
    - format: openapi
      url: https://art19.com/swagger_json/external/content.json
      version: "3.0"
  x-providerName: art19.com
security:
  - api_key: []
paths:
  /classification_inclusions:
    get:
      description: >
        Classification Inclusions connect classifications with entities like series, episodes, or campaigns, amongst others.


        In order to retrieve a set of classification inclusions, at least one of the following filter parameters must

        be provided. Failing to do so renders a `400 Bad Request` response.


        - `ids[]`

        - `classified_id` and `classified_type`

        - `classified_id` and `classification_type`

        - `classification_id` and `classified_type`
      parameters:
        - description: >
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.

            The brackets *MUST* be percent-encoded, per the requirements in

            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: >+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: >+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: Limit the result to classification inclusions linked to a classification with this ID.
          example: a90609c0-9f0c-48fd-8a41-83eb8414bedb
          in: query
          name: classification_id
          schema:
            format: uuid
            type: string
        - description: >
            The type of classification that the classification inclusions are linked to.

            Depending on the permissions of your credential, only a subset of these types are usable.
          in: query
          name: classification_type
          schema:
            enum:
              - AlternateFeedType
              - Genre
              - Industry
              - Language
              - MediaRating
            type: string
        - description: >
            Limit the result to classification inclusions linked to a resource with this ID.

            You will have to use this filter together with either `classified_type` or `classification_type`.
          example: fee1976e-f1bc-48a9-9ee8-58800f4d3462
          in: query
          name: classified_id
          schema:
            format: uuid
            type: string
        - description: Limit the result to classification inclusions linked to this type of entity.
          in: query
          name: classified_type
          schema:
            enum:
              - Series
              - Season
              - Episode
            type: string
        - description: >
            Limit the result to classification inclusions linked to a classification containing

            the parameter value disregarding case.
          in: query
          name: q
          schema:
            type: string
        - description: >
            Specify how to sort the result. Please refer to either the top section or the

            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: created_at
            enum:
              - classification_value
              - created_at
              - position
            items:
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/ClassificationInclusion"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          description: >
            Bad Request


            Parameters requiring a specific format must adhere to that format.

            Pagination must have a positive page number and a page size no larger than 100. At least one filter parameter is required.
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get ClassificationInclusion records
      tags:
        - ClassificationInclusion
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/classification_inclusions",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="ab919d2b-1ffd-42bf-98d7-ae959e612ff6"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/classification_inclusions?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="cce84b1f-220c-4d74-976a-41c8711da647"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/classification_inclusions');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="d8eba514-2364-46c6-8b37-58203492b586"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/classification_inclusions?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="68df9275-0c4e-4ed4-b595-f9d92b848879"' \
              --compressed
  "/classification_inclusions/{id}":
    get:
      parameters:
        - description: The ID of the classification inclusion resource to load.
          example: dd115519-888c-4b1d-b4be-26b72b05a38f
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/ClassificationInclusion"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a specific classification inclusion
      tags:
        - ClassificationInclusion
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/classification_inclusions/0366ea9e-04c8-4b37-8dec-34df4e8ff3b1",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="a32794e7-d809-4606-afb9-db88211d7b96"'
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/classification_inclusions/0366ea9e-04c8-4b37-8dec-34df4e8ff3b1', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="e0685a69-6c5e-4ddf-8837-6dee85ac3beb"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/classification_inclusions/0366ea9e-04c8-4b37-8dec-34df4e8ff3b1');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="28f5c55b-236c-49a3-b24c-5d032b2d65b0"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/classification_inclusions/0366ea9e-04c8-4b37-8dec-34df4e8ff3b1' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="174aedf5-d90e-44a4-808c-ffbcacb9cf62"' \
              --compressed
  /classifications:
    get:
      parameters:
        - description: >
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.

            The brackets *MUST* be percent-encoded, per the requirements in

            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: >+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: >+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: >
            Limit the result to classifications of this type. The possible values for types

            depends on the privileges of the provided credential.
          example: Genre
          in: query
          name: type
          schema:
            enum:
              - AlternateFeedType
              - Genre
              - Industry
              - Language
              - MediaRating
            type: string
        - description: >
            Limit the result to classifications with a value containing the parameter value

            disregarding case.
          in: query
          name: q
          schema:
            type: string
        - description: >
            If this parameter is provided and not empty, limit the result to classifications

            of type `Geography` representing countries. The credential must have at least one

            privilege with a network, series, or advertiser profile.
          example: "true"
          in: query
          name: is_country
          schema:
            type: string
        - description: >
            Specify how to sort the result. Please refer to either the top section or the

            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: value
            enum:
              - created_at
              - updated_at
              - value
            items:
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Classification"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          description: >
            Bad Request


            Parameters requiring a specific format must adhere to that format.

            Pagination must have a positive page number and a page size no larger than 100. Filtering by type for AudienceSegment and AudienceTag is no longer supported.
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a list of classifications
      tags:
        - Classification
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/classifications",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="80e36191-ddf5-48c9-9396-f0d360787d74"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/classifications?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="f7894f04-684a-432e-b64b-36a6300a3d5e"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/classifications');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="af3c523c-ab05-466c-94cd-fc49bff6e185"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/classifications?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="913fe29d-f945-48a8-bdba-85ffebd03765"' \
              --compressed
  "/classifications/{id}":
    get:
      parameters:
        - description: The ID of the classification resource to load.
          example: e5a6a42f-3784-44b7-956c-79d29d42fb48
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Classification"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a specific classification
      tags:
        - Classification
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/classifications/eda0cf0c-8e74-4963-ba98-70f5ec973e70",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="cd1def2c-f6c5-4963-b084-aeee5f3f7849"'
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/classifications/eda0cf0c-8e74-4963-ba98-70f5ec973e70', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="f82d6cea-84da-4c37-a127-7912c68c9fe2"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/classifications/eda0cf0c-8e74-4963-ba98-70f5ec973e70');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="3773941b-d1ba-4030-b4ad-5cea237587c0"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/classifications/eda0cf0c-8e74-4963-ba98-70f5ec973e70' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="cb295f1e-b350-4c29-9791-299316ece1fe"' \
              --compressed
  /credits:
    get:
      parameters:
        - description: >
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.

            The brackets *MUST* be percent-encoded, per the requirements in

            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: >+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: >+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: >
            Limit the result to credits for the entity with this ID only. If the entity is not

            published, the credential needs to have the right privilege to list the credits for it.
          example: 76654e16-76ce-4945-92e9-e0a381917853
          in: query
          name: creditable_id
          schema:
            format: uuid
            type: string
        - description: >
            Limit the result to credits linked to this type of entity only.

            Depending on the privileges of the credential, this list might be further reduced to

            only published entities.
          in: query
          name: creditable_type
          schema:
            enum:
              - Series
              - Season
              - Episode
            type: string
        - description: >
            Specify how to sort the result. Please refer to either the top section or the

            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: position
            enum:
              - created_at
              - position
              - updated_at
            items:
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Credit"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          description: >
            Bad Request


            Parameters requiring a specific format must adhere to that format.

            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a list of credits
      tags:
        - Credit
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/credits",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="b3bd07ba-6fc9-48ac-9ab3-19c7d8dd2942"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/credits?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="77f1438d-f19a-48d8-ad18-527043095e5d"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/credits');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="eac603fd-0492-4463-9bc1-c3393a16a100"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/credits?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="421382c3-1bd9-4bb6-be9f-2a63d3eb6aa1"' \
              --compressed
  "/credits/{id}":
    get:
      parameters:
        - description: The ID of the credit resource to load.
          example: abc85597-4d58-4c25-a92e-7f5f3becb120
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Credit"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a specific credit
      tags:
        - Credit
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/credits/86ad8f59-07a7-4aee-92b5-2aec3db45f95",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="502cdf59-9362-4f63-a4f1-f1bc6e6df626"'
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/credits/86ad8f59-07a7-4aee-92b5-2aec3db45f95', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="5105c4c2-fc8f-436f-b4ef-1b2386cd532a"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/credits/86ad8f59-07a7-4aee-92b5-2aec3db45f95');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="54f258b6-13a1-4c21-aaf0-3315baf5862a"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/credits/86ad8f59-07a7-4aee-92b5-2aec3db45f95' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="1c12f1b2-934c-4c4f-a0e8-2a6b85645cce"' \
              --compressed
  /episodes:
    get:
      description: >
        One initial filter must be provided (`ids`, `series_id`, or `season_id`), otherwise a `400 Bad Request` response

        will be returned. Additional filters are allowed.


        This API will only return episodes that your credential has permission to access, which may not be exclusive to

        your account, depending on the filter(s) being used. Be careful to filter the results as needed.
      parameters:
        - description: >
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.

            The brackets *MUST* be percent-encoded, per the requirements in

            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: >+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          required: true
          schema:
            minimum: 1
            type: integer
        - description: >+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          required: true
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: Limit the result to episodes usable with an inventory management calendar for the provided AdDeal ID.
          example: b97239ef-1776-489a-a1df-5f70a8407148
          in: query
          name: included_in_inventory_calendar
          schema:
            format: uuid
            type: string
        - description: Limit the result to episodes released in this month.
          in: query
          name: month
          schema:
            type: string
        - description: >
            If `true`, limit the result to published episodes only.

            If `false`, limit the result to _unpublished_ episodes only.
          in: query
          name: published
          schema:
            type: boolean
        - description: Limit the result to episodes with a title containing this parameter in a case-insensitive way.
          in: query
          name: q
          schema:
            type: string
        - description: Limit the result to episodes released after this timestamp.
          in: query
          name: released_after
          schema:
            format: date-time
            type: string
        - description: Limit the result to episodes released before this timestamp.
          in: query
          name: released_before
          schema:
            format: date-time
            type: string
        - description: >
            If `true`, limit the result to published and released episodes holding an available media file.

            If this is `true`, it also changes the default sort order to `-released_at`.
          in: query
          name: rss
          schema:
            type: boolean
        - description: Limit result to episodes linked to this season.
          example: 30e7b568-d099-486b-9a8e-51b7594cab7d
          in: query
          name: season_id
          schema:
            format: uuid
            type: string
        - description: Limit the result to episodes linked to this series.
          example: d0ceb1ff-b95b-4c1e-a7e5-a36ed7b4d3b7
          in: query
          name: series_id
          schema:
            format: uuid
            type: string
        - description: >
            Specify how to sort the result. Please refer to either the top section or the

            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: sort_title
            enum:
              - created_at
              - earliest_released_at
              - released_at
              - released_or_created_at
              - sort_title
              - title
              - updated_at
            items:
              type: string
            type: array
          style: form
        - description: Limit the result to episodes released in this year.
          in: query
          name: year
          schema:
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Episode"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          description: >
            Bad Request


            Parameters requiring a specific format must adhere to that format.

            Pagination must be present and have a positive page number and a page size no larger than 100. One initial filter must be provided (`ids`, `series_id`, or `season_id`).
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a list of episodes
      tags:
        - Episode
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/episodes",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="d6dc87a1-ad6b-4651-bf4e-977f05a43993"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/episodes?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="37376a3c-5cfc-4dad-bd93-43387833883e"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/episodes');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="00245eb4-f9ce-4616-83d2-85246bf14993"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/episodes?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="9ae49e51-0740-4808-84ee-8a419ab738af"' \
              --compressed
  "/episodes/{id}":
    get:
      parameters:
        - description: The ID of the episode resource to load.
          example: 9dcdf083-bcc6-436e-b1f5-fa9627b94868
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Episode"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a specific episode
      tags:
        - Episode
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/episodes/e1ab05fa-1a30-4f27-bb24-138c04f49225",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="6752b5a5-b9c0-4f9f-8b07-6b993ded513a"'
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/episodes/e1ab05fa-1a30-4f27-bb24-138c04f49225', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="b90d744f-a307-424c-995f-f8125e68652b"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/episodes/e1ab05fa-1a30-4f27-bb24-138c04f49225');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="d93df845-e5f4-46d2-b1cd-11544fd08655"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/episodes/e1ab05fa-1a30-4f27-bb24-138c04f49225' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="8d36161d-cfab-4e5f-a7f7-8c45e4ea5e80"' \
              --compressed
  "/episodes/{id}/next_sibling":
    get:
      parameters:
        - description: The ID of the episode to find the one episode released right after it.
          example: 02fc63a3-4811-4df2-a24e-2d976d476941
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
        - description: >
            If `true`, get the next released and published episode with an available media file.
          in: query
          name: rss
          schema:
            type: boolean
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Episode"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get the episode released right after the specified one
      tags:
        - Episode
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/episodes/f2868a01-a56e-4367-adac-be4d1e9bd252/next_sibling",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="338cf9b5-c0d4-4526-9f2c-24137c19cf95"'
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/episodes/f2868a01-a56e-4367-adac-be4d1e9bd252/next_sibling', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="0be68e5a-30bb-4025-9c7e-9bf78ffb115b"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/episodes/f2868a01-a56e-4367-adac-be4d1e9bd252/next_sibling');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="25cb389e-3433-4167-937b-f6ec3817b35a"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/episodes/f2868a01-a56e-4367-adac-be4d1e9bd252/next_sibling' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="6fc1a59f-405a-4b40-8ce3-7453fdd41f5d"' \
              --compressed
  "/episodes/{id}/previous_sibling":
    get:
      parameters:
        - description: The ID of the episode to find the one episode released right before it.
          example: ad75db44-1fc7-4197-8949-cea3da755521
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
        - description: >
            If `true`, get the previously released and published episode with an available media file.
          in: query
          name: rss
          schema:
            type: boolean
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Episode"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get the episode released right before the specified one
      tags:
        - Episode
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/episodes/6faec9c1-0dd9-4dbe-8cac-8022ef1c0846/previous_sibling",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="4db174ba-42b6-49e1-8198-ae915a76e966"'
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/episodes/6faec9c1-0dd9-4dbe-8cac-8022ef1c0846/previous_sibling', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="75045986-2929-4be9-af22-c4dec83d977e"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/episodes/6faec9c1-0dd9-4dbe-8cac-8022ef1c0846/previous_sibling');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="2c51ad11-2281-421c-a576-b8f7fe6bfb64"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/episodes/6faec9c1-0dd9-4dbe-8cac-8022ef1c0846/previous_sibling' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="7e4e0593-6f6e-48c4-95b1-85f75e48b00c"' \
              --compressed
  /images:
    get:
      description: >+
        An image represents a piece of artwork attached to some entity like a series, season, or episode,

        and is _owned_ by an entity called the `bucket`.

        An image is also a container for several `MediaAssets` representing the physical files for various

        styles used.


        ## Media Asset Styles for Images


        Most media assets use square images. You may upload and use a square image, or upload an image of any

        shape and crop it to a square by specifying the cropping area. This area – identified by a coordinate

        `x, y` and a `width` & `height` – is the portion of the image used for all cover art. If an image has

        cropping defined, the cropped version of the image will be used in any `regular` or `square` style of media

        asset. If the original file is rectangular and does not have cropping, then the system will use a squared

        version of the original file with the smaller of width or height as the square size.


        The original image as uploaded into the system is always retained unmodified and available through

        the style `original`. All media asset styles except `stripped-original` consist of the cropped image.


        An image has media assets with the following styles:


        - `original`: This is the original file provided. May not be available, depending on permissions
          and file type.
        - `stripped-original`: The original file with all metadata (EXIF, XMP, PNG metadata, etc.) removed.
          This should be used for any application needing the original, uncropped, image.
        - `regular`: If the image has cropping defined, this is the cropped image.
          If not, this is a square version of the original.
        - `thumb`: A square thumbnail of the image with a size of `100x100` pixels.

        - `square-400`: A square version of the image with a size of `400x400` pixels.

        - `square-640`: A square version of the image with a size of `640x640` pixels.

        - `square-888`: A square version of the image with a size of `888x888` pixels.

        - `square-3000`: A square version of the image with a size of `3000x3000` pixels.
          This variant is only created if the cropped width & height are each at least 3000.
        - `itunes`: A square version of the image with a size of `1400x1400` pixels.


        ### Preferred image used in feeds


        For the main series image used in feeds, it is ideal to use the `square-3000` version.

        If that is not available, the `itunes` version should be used instead.

      parameters:
        - description: >
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.

            The brackets *MUST* be percent-encoded, per the requirements in

            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: true
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Image"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          description: >
            Bad Request


            Parameters requiring a specific format must adhere to that format.

            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a list of images
      tags:
        - Image
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/images",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="bd3d4b46-95c6-4a9d-81e7-f790d9931bf4"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/images?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="088f6491-a523-461f-b2b9-742c3a2bd631"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/images');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="66696863-b5a5-4c4f-a76a-6e78550c403c"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/images?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="f1f49e8e-34cf-42a6-84b4-24f036033330"' \
              --compressed
  "/images/{id}":
    get:
      description: >+
        An image represents a piece of artwork attached to some entity like a series, season, or episode,

        and is _owned_ by an entity called the `bucket`.

        An image is also a container for several `MediaAssets` representing the physical files for various

        styles used.


        ## Media Asset Styles for Images


        Most media assets use square images. You may upload and use a square image, or upload an image of any

        shape and crop it to a square by specifying the cropping area. This area – identified by a coordinate

        `x, y` and a `width` & `height` – is the portion of the image used for all cover art. If an image has

        cropping defined, the cropped version of the image will be used in any `regular` or `square` style of media

        asset. If the original file is rectangular and does not have cropping, then the system will use a squared

        version of the original file with the smaller of width or height as the square size.


        The original image as uploaded into the system is always retained unmodified and available through

        the style `original`. All media asset styles except `stripped-original` consist of the cropped image.


        An image has media assets with the following styles:


        - `original`: This is the original file provided. May not be available, depending on permissions
          and file type.
        - `stripped-original`: The original file with all metadata (EXIF, XMP, PNG metadata, etc.) removed.
          This should be used for any application needing the original, uncropped, image.
        - `regular`: If the image has cropping defined, this is the cropped image.
          If not, this is a square version of the original.
        - `thumb`: A square thumbnail of the image with a size of `100x100` pixels.

        - `square-400`: A square version of the image with a size of `400x400` pixels.

        - `square-640`: A square version of the image with a size of `640x640` pixels.

        - `square-888`: A square version of the image with a size of `888x888` pixels.

        - `square-3000`: A square version of the image with a size of `3000x3000` pixels.
          This variant is only created if the cropped width & height are each at least 3000.
        - `itunes`: A square version of the image with a size of `1400x1400` pixels.


        ### Preferred image used in feeds


        For the main series image used in feeds, it is ideal to use the `square-3000` version.

        If that is not available, the `itunes` version should be used instead.

      parameters:
        - description: The ID of the image resource to load.
          example: 987200b9-afd0-4355-a299-58b7473dcd19
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Image"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a specific image
      tags:
        - Image
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/images/06b52b06-ecd3-4817-8b72-a735d3208593",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="082409ed-5c00-433f-8dda-541f97a40d6c"'
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/images/06b52b06-ecd3-4817-8b72-a735d3208593', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="a42e3cfb-5a0c-427b-a647-52d8d317902a"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/images/06b52b06-ecd3-4817-8b72-a735d3208593');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="eb1905fe-b329-47f9-8b93-f46689cbbcd7"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/images/06b52b06-ecd3-4817-8b72-a735d3208593' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="4831d34a-d1d9-4942-afbc-4b25d0021ab1"' \
              --compressed
  /media_assets:
    get:
      description: >+
        A media asset is part of a collection of assets or files representing an image or a piece of audio content

        like an episode or an ad. Images, for example, have differently sized versions for better rendering performance,

        and a piece of audio content usually comes in different encoding formats.


        The `style` attribute of a media asset describes the role an asset plays in the context of the collection.

        The collection is what an asset is attached to (`attachment_id|type`).


        ## Styles for Images


        Most media assets use square images. You may upload and use a square image, or upload an image of any

        shape and crop it to a square by specifying the cropping area. This area – identified by a coordinate

        `x, y` and a `width` & `height` – is the portion of the image used for all cover art. If an image has

        cropping defined, the cropped version of the image will be used in any `regular` or `square` style of media

        asset. If the original file is rectangular and does not have cropping, then the system will use a squared

        version of the original file with the smaller of width or height as the square size.


        The original image as uploaded into the system is always retained unmodified and available through

        the style `original`. All media asset styles except `stripped-original` consist of the cropped image.


        An image has media assets with the following styles:


        - `original`: This is the original file provided. May not be available, depending on permissions
          and file type.
        - `stripped-original`: The original file with all metadata (EXIF, XMP, PNG metadata, etc.) removed.
          This should be used for any application needing the original, uncropped, image.
        - `regular`: If the image has cropping defined, this is the cropped image.
          If not, this is a square version of the original.
        - `thumb`: A square thumbnail of the image with a size of `100x100` pixels.

        - `square-400`: A square version of the image with a size of `400x400` pixels.

        - `square-640`: A square version of the image with a size of `640x640` pixels.

        - `square-888`: A square version of the image with a size of `888x888` pixels.

        - `square-3000`: A square version of the image with a size of `3000x3000` pixels.
          This variant is only created if the cropped width & height are each at least 3000.
        - `itunes`: A square version of the image with a size of `1400x1400` pixels.


        ### Preferred image used in feeds


        For the main series image used in feeds, it is ideal to use the `square-3000` version.

        If that is not available, the `itunes` version should be used instead.



        ## Styles for Audio


        - `medium`: A medium-quality version of the audio asset in various formats.

        - `original`: This is the original file provided. May not be available, depending on permissions
          and file type.
        - `waveform_data`: The generated BBC Audiowaveform data in JSON or binary format.

      parameters:
        - description: >
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.

            The brackets *MUST* be percent-encoded, per the requirements in

            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: true
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/MediaAsset"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          description: >
            Bad Request


            Parameters requiring a specific format must adhere to that format.

            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a list of media assets
      tags:
        - MediaAsset
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/media_assets",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="6bd7c158-cd31-4044-b76e-bc49efa83ba3"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/media_assets?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="203f0830-2e7c-412d-b37e-cb8d355ba957"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/media_assets');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="6575ba0a-4c6e-4c92-aab0-3b44096c4f92"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/media_assets?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="046910d5-8aec-4fbe-a17b-c13ec9d29957"' \
              --compressed
  "/media_assets/{id}":
    get:
      description: >+
        A media asset is part of a collection of assets or files representing an image or a piece of audio content

        like an episode or an ad. Images, for example, have differently sized versions for better rendering performance,

        and a piece of audio content usually comes in different encoding formats.


        The `style` attribute of a media asset describes the role an asset plays in the context of the collection.

        The collection is what an asset is attached to (`attachment_id|type`).


        ## Styles for Images


        Most media assets use square images. You may upload and use a square image, or upload an image of any

        shape and crop it to a square by specifying the cropping area. This area – identified by a coordinate

        `x, y` and a `width` & `height` – is the portion of the image used for all cover art. If an image has

        cropping defined, the cropped version of the image will be used in any `regular` or `square` style of media

        asset. If the original file is rectangular and does not have cropping, then the system will use a squared

        version of the original file with the smaller of width or height as the square size.


        The original image as uploaded into the system is always retained unmodified and available through

        the style `original`. All media asset styles except `stripped-original` consist of the cropped image.


        An image has media assets with the following styles:


        - `original`: This is the original file provided. May not be available, depending on permissions
          and file type.
        - `stripped-original`: The original file with all metadata (EXIF, XMP, PNG metadata, etc.) removed.
          This should be used for any application needing the original, uncropped, image.
        - `regular`: If the image has cropping defined, this is the cropped image.
          If not, this is a square version of the original.
        - `thumb`: A square thumbnail of the image with a size of `100x100` pixels.

        - `square-400`: A square version of the image with a size of `400x400` pixels.

        - `square-640`: A square version of the image with a size of `640x640` pixels.

        - `square-888`: A square version of the image with a size of `888x888` pixels.

        - `square-3000`: A square version of the image with a size of `3000x3000` pixels.
          This variant is only created if the cropped width & height are each at least 3000.
        - `itunes`: A square version of the image with a size of `1400x1400` pixels.


        ### Preferred image used in feeds


        For the main series image used in feeds, it is ideal to use the `square-3000` version.

        If that is not available, the `itunes` version should be used instead.



        ## Styles for Audio


        - `medium`: A medium-quality version of the audio asset in various formats.

        - `original`: This is the original file provided. May not be available, depending on permissions
          and file type.
        - `waveform_data`: The generated BBC Audiowaveform data in JSON or binary format.

      parameters:
        - description: The ID of the media asset resource to load.
          example: ad980196-52c1-4b9d-83d8-25d914d78095
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/MediaAsset"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a specific media asset
      tags:
        - MediaAsset
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/media_assets/e103cd0d-ad13-4226-a41d-eb08af5dd5bf",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="9935c076-8ba3-48e3-9dc5-62f2da882639"'
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/media_assets/e103cd0d-ad13-4226-a41d-eb08af5dd5bf', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="5d562adc-968c-400a-8c13-45139642ff9a"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/media_assets/e103cd0d-ad13-4226-a41d-eb08af5dd5bf');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="76f6f089-9b2b-4149-a07c-1cf48ea24248"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/media_assets/e103cd0d-ad13-4226-a41d-eb08af5dd5bf' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="e50667d2-2e37-4394-b1ac-350b5e889963"' \
              --compressed
  /networks:
    get:
      parameters:
        - description: >
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.

            The brackets *MUST* be percent-encoded, per the requirements in

            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: >+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: >+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: Limit the result to networks with a name containing this parameter in a case-insensitive way.
          in: query
          name: q
          schema:
            type: string
        - description: >
            Limit the result to networks containing at least one series with an ad deal of this AdOps profile.
          in: query
          name: ad_rep_account_id
          schema:
            format: uuid
            type: string
        - description: >
            Limit the result to networks containing at least one series with an ad deal matching one of the

            values in this parameter.
          explode: false
          in: query
          name: ad_deal_status
          schema:
            items:
              enum:
                - active
                - inactive
                - pending_active
                - pending_inactive
              type: string
            type: array
          style: form
        - description: >
            Specify how to sort the result. Please refer to either the top section or the

            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: name
            enum:
              - created_at
              - name
              - updated_at
            items:
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Network"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          description: >
            Bad Request


            Parameters requiring a specific format must adhere to that format.

            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a list of networks
      tags:
        - Network
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/networks",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="81902963-db83-4e35-b9e8-7d29089158c3"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/networks?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="69e60a04-33ab-4001-9a5e-1b26f5cfd24b"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/networks');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="b2a4e79f-cb8b-44c5-a4e0-a1adfb0ee6b5"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/networks?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="d242906e-0d02-4cc1-b29d-efa1d3b7d17b"' \
              --compressed
  "/networks/{id}":
    get:
      parameters:
        - description: The ID of the network resource to load.
          example: f10a2249-848c-4817-93e0-659d23701d6e
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Network"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a specific network
      tags:
        - Network
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/networks/7f1892a4-92b0-4515-887d-9914048236c8",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="67251b60-6c6a-44f1-9703-f35ee4c69a6c"'
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/networks/7f1892a4-92b0-4515-887d-9914048236c8', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="db187230-e015-46f4-885a-528705d75dc0"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/networks/7f1892a4-92b0-4515-887d-9914048236c8');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="456639fb-b755-4cad-93a7-d2183d8d3dd8"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/networks/7f1892a4-92b0-4515-887d-9914048236c8' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="07472f45-3587-4bc8-bb95-ad242afbe70b"' \
              --compressed
  /people:
    get:
      description: >
        Each series, season, and episode has a Credits section where you may add people and roles.

        This is an internal tool to recognize contributors. It is not related to ART19 users or account permissions.

        Each Person added will have no additional access or permissions granted as a result of being included in the Credits section.
      parameters:
        - description: >
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.

            The brackets *MUST* be percent-encoded, per the requirements in

            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: >+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: >+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: Limit the result to people with a full name containing this parameter in a case-insensitive way.
          in: query
          name: q
          schema:
            type: string
        - description: >
            Specify how to sort the result. Please refer to either the top section or the

            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: last_name,first_name
            enum:
              - created_at
              - first_name
              - last_name
              - updated_at
            items:
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Person"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          description: >
            Bad Request


            Parameters requiring a specific format must adhere to that format.

            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a list of people
      tags:
        - Person
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/people",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="a7760a0b-9520-4254-9dd0-a1672d91851f"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/people?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="e03b9134-986f-4349-be21-7ed2e1e7de1b"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/people');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="9d725a37-0450-46fe-9722-af0583ba0c1b"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/people?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="569fc805-5b6e-414d-b84a-e16394c81945"' \
              --compressed
  "/people/{id}":
    get:
      description: >
        Each series, season, and episode has a Credits section where you may add people and roles.

        This is an internal tool to recognize contributors. It is not related to ART19 users or account permissions.

        Each Person added will have no additional access or permissions granted as a result of being included in the Credits section.
      parameters:
        - description: The ID of the person resource to load.
          example: 7b87e1f1-69a3-407e-b8b8-625fd6cc2103
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Person"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a specific person
      tags:
        - Person
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/people/099f0e66-c757-4ceb-972a-957a6d400a7c",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="1dd452e4-5981-4db4-a1c3-1acf2869b994"'
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/people/099f0e66-c757-4ceb-972a-957a6d400a7c', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="d9644c73-a14e-481b-883d-0bbd3f94f3b5"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/people/099f0e66-c757-4ceb-972a-957a6d400a7c');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="6c53f43b-e388-426f-b490-248647747fec"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/people/099f0e66-c757-4ceb-972a-957a6d400a7c' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="75c286ed-f9d8-45a2-a756-7ae7b79ed719"' \
              --compressed
  /seasons:
    get:
      description: >
        When retrieving a list of seasons, the result is automatically filtered depending on the

        privileges the used credential holds. If there are no specific privileges to a series or network,

        only active seasons for active series are included.
      parameters:
        - description: >
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.

            The brackets *MUST* be percent-encoded, per the requirements in

            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: >+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: >+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: Limit the result to seasons owned by this series.
          example: 7ebd702d-07c8-4da9-a7a8-cf18ec414d5c
          in: query
          name: series_id
          schema:
            format: uuid
            type: string
        - description: Limit the result to seasons with a title containing this parameter in a case-insensitive way.
          in: query
          name: q
          schema:
            type: string
        - description: >
            Specify how to sort the result. Please refer to either the top section or the

            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: sort_title
            enum:
              - created_at
              - sort_title
              - title
              - updated_at
            items:
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Season"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          description: >
            Bad Request


            Parameters requiring a specific format must adhere to that format.

            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a list of seasons
      tags:
        - Season
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/seasons",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="d0cd71cf-de71-4232-8e99-67f69cd00034"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/seasons?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="6d5e655a-eb50-47f3-9dd2-96132714f05b"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/seasons');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="ff2dc7e0-3586-48e2-9ad5-67138e1a9078"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/seasons?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="574ddb35-ce3e-49db-a7d4-062cb13855ce"' \
              --compressed
  "/seasons/{id}":
    get:
      parameters:
        - description: The ID of the season resource to load.
          example: 7038aab0-ea52-4c69-9433-afe705e2215b
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Season"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a specific season
      tags:
        - Season
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/seasons/911ff79f-2a2b-49a7-b372-03c942f84ab2",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="53867281-b74a-49d6-a750-38f033ca9106"'
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/seasons/911ff79f-2a2b-49a7-b372-03c942f84ab2', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="640f8add-4644-47c2-8242-a265796a34aa"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/seasons/911ff79f-2a2b-49a7-b372-03c942f84ab2');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="963a74db-cd12-495b-ac80-773da3ed3af0"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/seasons/911ff79f-2a2b-49a7-b372-03c942f84ab2' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="71ce5b3d-a9f8-4952-bfd1-adc29c7e2a30"' \
              --compressed
  /series:
    get:
      description: >
        When retrieving a list of series, the result is automatically filtered depending on the

        privileges the used credential holds. All credentials will have access to active series

        with a public page enabled (on ART19). Utilizing a filter to limit the result to series

        associated with your account is recommended.
      parameters:
        - description: >
            The list of IDs to filter by. Repeat this parameter for each ID you want to include in the filter.

            The brackets *MUST* be percent-encoded, per the requirements in

            [RFC 3986 § 3.4](https://tools.ietf.org/html/rfc3986#section-3.4).
          explode: true
          in: query
          name: ids[]
          required: false
          schema:
            items:
              format: uuid
              type: string
            type: array
            uniqueItems: true
        - description: >+
            Select which page number to receive results for. Pages are numbered starting at 1.

          in: query
          name: page[number]
          schema:
            minimum: 1
            type: integer
        - description: >+
            Indicate how many records to return per page. The maximum is 100.

          in: query
          name: page[size]
          schema:
            maximum: 100
            minimum: 1
            type: integer
        - description: >
            Limit the result to series with an ad deal of this AdOps profile.
          example: 35eb02fc-54db-4650-98e0-4695b101b71d
          in: query
          name: ad_rep_account_id
          schema:
            format: uuid
            type: string
        - description: Limit the result to series owned by this network.
          example: 5317358a-527e-4365-a343-361854286cc7
          in: query
          name: network_id
          schema:
            format: uuid
            type: string
        - description: Limit the result to series with a title or slug containing this parameter in a case-insensitive way.
          in: query
          name: q
          schema:
            type: string
        - description: >
            Specify how to sort the result. Please refer to either the top section or the

            [JSON:API specification](https://jsonapi.org/format/#fetching-sorting) on how sorting works in general.
          explode: false
          in: query
          name: sort
          schema:
            default: sort_title
            enum:
              - created_at
              - episode_released_at
              - sort_title
              - title
              - updated_at
            items:
              type: string
            type: array
          style: form
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    items:
                      $ref: "#/components/schemas/Series"
                    type: array
                  links:
                    $ref: "#/components/schemas/Links"
                type: object
          description: Success
        "400":
          description: >
            Bad Request


            Parameters requiring a specific format must adhere to that format.

            Pagination must have a positive page number and a page size no larger than 100. 
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a list of series
      tags:
        - Series
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/series",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="9053a236-9bd4-4f47-8a16-315effdfd846"'
              },
              params: {
                # add filters if desired
                page: { number: 1, size: 25 }
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/series?page%5Bnumber%5D=1&page%5Bsize%5D=25', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="5b01f49e-bfb8-48fd-b7dd-b14d78e99d32"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // The API endpoint to request data from
              $url = array('https://art19.com/series');

              // Provide extra parameters like filters or pagination to the request
              $url[] = http_build_query(array(
                'page' => array('number' => 1, 'size' => 25)
              ));

              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, implode('?', $url));
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="2555d736-bc6d-4de4-8a38-3f424d5387e2"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/series?page%5Bnumber%5D=1&page%5Bsize%5D=25' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="413a0ced-b4ae-44dd-9dc9-33a65a6411c8"' \
              --compressed
  "/series/{id}":
    get:
      parameters:
        - description: The ID of the series resource to load.
          example: ebeb50be-b6e4-48c6-8b88-925f7ee2d0de
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/vnd.api+json:
              schema:
                properties:
                  data:
                    $ref: "#/components/schemas/Series"
                  links:
                    $ref: "#/components/schemas/ResourceLink"
                type: object
          description: Success
        "401":
          $ref: "#/components/responses/unauthorized"
        "403":
          $ref: "#/components/responses/forbidden"
        "404":
          $ref: "#/components/responses/not_found"
        "406":
          $ref: "#/components/responses/not_acceptable"
        "429":
          $ref: "#/components/responses/rate_limit_exceeded"
      summary: Get a specific series
      tags:
        - Series
      x-codeSamples:
        - label: Ruby - Typhoeus
          lang: ruby
          source: >
            response = Typhoeus.get(
              "https://art19.com/series/6838134a-6807-48cf-bfd2-65ec6830e3fc",
              accept_encoding: 'gzip',
              headers: {
                Accept:        'application/vnd.api+json',
                Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="1845d26a-4e2c-4dea-88b9-02e1013b05ce"'
              },
              timeout: 10
            )

            if response.success?
              # HTTP 2xx - JSON:API available to be parsed in response.body
            elsif response.timed_out?
              # Request timed out
            elsif response.code.zero?
              # cURL didn't get back an HTTP response, the reason is in response.return_message
            else
              # Other HTTP status, check response.code for which
            end
        - label: JavaScript - Fetch
          lang: javascript
          source: >
            try {
              const response = await fetch('https://art19.com/series/6838134a-6807-48cf-bfd2-65ec6830e3fc', {
                headers: {
                  Accept:        'application/vnd.api+json',
                  Authorization: 'Token token="UVd4c0lFTnZjSKbElFSm", credential="2f61a7e1-fa60-4b2f-b873-b942fe477d1c"'
                }
              });

              if (response.ok) {
                // Get the response body if HTTP-status is 200-299
                const json = await response.json();
              } else {
                // HTTP request failed, handle error
              }
            } catch(err) {
              // A network error occured
            }
        - label: PHP - cURL
          lang: php
          source: >
            <?php

            // Initialize a cURL resource for a request

            $request = curl_init();


            try {
              // Configure the cURL options
              curl_setopt($request, CURLOPT_URL, 'https://art19.com/series/6838134a-6807-48cf-bfd2-65ec6830e3fc');
              curl_setopt($request, CURLOPT_FOLLOWLOCATION, true);
              curl_setopt($request, CURLOPT_RETURNTRANSFER, true);
              curl_setopt($request, CURLOPT_ENCODING, '');
              curl_setopt($request, CURLOPT_HTTPHEADER, array(
                'Accept: application/vnd.api+json',
                'Authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="3ff244b0-b80e-4f76-ba01-8720a8dcd629"'
              ));

              // Execute the request and grab all status information
              $output = curl_exec($request);
              $info   = curl_getinfo($request);
              $code   = $info['http_code'];

              if ($code >= 200 && $code < 300) {
                // HTTP 2xx - JSON:API response available to be parsed in $output
                $json = json_decode($output);
              } else if ($code >= 400 && $code < 500) {
                // HTTP 4xx - Client error; JSON:API response evt. available to be parsed in $output
                if (strlen($output) > 0) {
                  $errors = json_decode($output);
                }
              } else {
                // Other HTTP status, treat according to workflow
              }
            } finally {
              // Free up the resource to clear out memory
              curl_close($request);
            }

            ?>
        - label: cURL
          lang: shell
          source: >
            curl 'https://art19.com/series/6838134a-6807-48cf-bfd2-65ec6830e3fc' \
              -H 'accept: application/vnd.api+json' \
              -H 'authorization: Token token="UVd4c0lFTnZjSKbElFSm", credential="d5c95813-bd5e-46ee-a2e0-664e05c88ba1"' \
              --compressed
components:
  responses:
    forbidden:
      description: >
        Forbidden


        The provided credential is not authorized to access the requested resource or collection.

        [See RFC 7231 § 6.5.3](https://tools.ietf.org/html/rfc7231#section-6.5.3)
    not_acceptable:
      description: >
        Not Acceptable


        The client requested a response format which cannot be satisfied. Ensure that you specify the

        correct `Accept` header. [See RFC 7231 § 6.5.6](https://tools.ietf.org/html/rfc7231#section-6.5.6).
    not_found:
      description: >
        Not Found


        The requested resource/collection does not exist. [See RFC 7231 § 6.5.4](https://tools.ietf.org/html/rfc7231#section-6.5.4).
    rate_limit_exceeded:
      description: Rate limit exceeded
      headers:
        Retry-After:
          description: >
            The number of seconds after which the request can be retried.

            Since the rate limit is per minute, this value ranges from 1 to 59.

            Conforms to [RFC 7231 § 7.1.3](https://tools.ietf.org/html/rfc7231#section-7.1.3).
          schema:
            type: integer
    unauthorized:
      description: >
        Unauthorized


        A valid credential was not supplied, but one is required.

        [See RFC 7235 § 3.1](https://tools.ietf.org/html/rfc7235#section-3.1).

        A `WWW-Authenticate` header is not currently returned.
  schemas:
    Classification:
      description: >
        A classification, in general, is something that can describe other resources, such as countries, genres or

        languages. How they're described (included or excluded, and whether they're the primary classification

        of this type) is handled using other resources. Different functions of ART19 rely on classifications differently,

        and could have an effect only on directory information, or may have an effect on ad decisioning.
      properties:
        attributes:
          properties:
            created_at:
              format: date-time
              type: string
            display_name:
              description: Human readable representation of the classification
              type: string
            metadata:
              description: >
                An object with more detailed information about the classification.

                The possible members are different per type of classification.
              type: object
            parent_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `parent` in a future update.<br/>

                For hierarchical classifications, this is the ID of the parent classification
              format: uuid
              type: string
            tree_path:
              description: >
                For hierarchical classifications this is the list of IDs reflecting the hierarchy starting

                with the root level node.
              example:
                - bcb0f462-e809-40e3-abd2-1ca8b595ebed
                - dc23001f-c88c-41d7-baad-7924f5af1225
              items:
                format: uuid
                type: string
              type: array
            type:
              description: >
                The type of classification. The type of retrievable classifications depends on the privileges

                of the credential being used.
              enum:
                - AlternateFeedType
                - Genre
                - Industry
                - Language
                - MediaRating
              example: Genre
              type: string
            updated_at:
              format: date-time
              type: string
            value:
              description: >
                The value the classification represents. The meaning of this attribute depends on the type of classification.
              type: string
            value_path:
              description: >
                For hierarchical classifications this is a concatenated string of all the classification

                values from the root to this classification using a `: ` as separator.
              example: "Comedy: Standup"
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        type:
          description: The type of resource. This is always `classifications`
          example: classifications
          type: string
      type: object
    ClassificationInclusion:
      description: >
        Connects a Classification with a classified item like a Series, Season or Episode.
      properties:
        attributes:
          properties:
            classification_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `classification` in a future update.<br/>

                The ID of the classification linked to the entity
              example: 497aad13-1c96-41d8-80e2-9a5aa7bc96fa
              format: uuid
              type: string
            classification_type:
              deprecated: true
              description: This will be replaced by the relationship `classification` in a future update.<br/>
              enum:
                - AlternateFeedType
                - Genre
                - Industry
                - Language
                - MediaRating
              type: string
            classified_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `classified` in a future update.<br/>

                The ID of the entity linked to the classification
              example: 8e9dc5d8-8a16-424e-ba96-6a7261a9a23b
              format: uuid
              type: string
            classified_type:
              deprecated: true
              description: This will be replaced by the relationship `classified` in a future update.<br/>
              enum:
                - Series
                - Season
                - Episode
              type: string
            created_at:
              format: date-time
              type: string
            position:
              description: The position or order of the linked classification within the classified resource
              type: integer
            primary:
              description: >
                When `true` it describes the best classification of a specific type for a given

                classified item
              type: boolean
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            classification:
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
          type: object
        type:
          description: The type of resource. This is always `classification_inclusion`
          example: classification_inclusion
          type: string
      type: object
    Credit:
      description: >
        A credit links a specific person to a series, season, or episode in a specific role.
      properties:
        attributes:
          properties:
            created_at:
              format: date-time
              type: string
            creditable_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `creditable` in a future update.<br/>

                The ID of the entity this credit is for
              example: c71c6734-ab34-4464-ae03-d7f677e3b658
              format: uuid
              type: string
            creditable_type:
              deprecated: true
              description: >
                This will be replaced by the relationship `creditable` in a future update.<br/>

                The type of entitiy this credit is for
              enum:
                - Series
                - Season
                - Episode
              type: string
            position:
              description: The order of this credit within all credits of the referenced creditable entity
              type: integer
            type:
              description: The type of credit the linked person has on the referenced entity
              enum:
                - AnchorCredit
                - AssociateProducerCredit
                - AuthorCredit
                - CastCredit
                - CoHostCredit
                - ComposerCredit
                - CreatorCredit
                - Credit
                - DirectorCredit
                - EditorCredit
                - EngineerCredit
                - ExecutiveProducerCredit
                - GuestCoHostCredit
                - GuestCredit
                - GuestHostCredit
                - HeadWriterCredit
                - HostCredit
                - ProducerCredit
                - ReporterCredit
                - SeniorProducerCredit
                - SidekickCredit
                - VideoProducerCredit
                - WriterCredit
              type: string
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            person:
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              type: object
          type: object
        type:
          description: The type of resource. This is always `credits`
          example: credits
          type: string
      type: object
    Episode:
      properties:
        attributes:
          properties:
            allow_user_comments:
              description: >
                Only visible to credentials having write privileges for the series.
              type: boolean
            cascaded_cover_image_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `cascaded_cover_image` in a future update.<br/>

                The image ID of the cover art for this episode. This is the preferred image ID to use.

                If there is no specific cover for the episode, the image ID of the season or the

                series will be returned, whichever has one defined.

                The `/images` endpoint provides details to the image.
              example: ec8f13bf-77c0-40fe-933a-8c088c16bc17
              format: uuid
              type: string
            cover_image_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `cover_image` in a future update.<br/>

                The image ID of the cover art for this episode, if an episode-specific image has been provided.

                The `/images` endpoint provideds details to the image.
              example: ec8f13bf-77c0-40fe-933a-8c088c16bc17
              format: uuid
              type: string
            created_at:
              format: date-time
              type: string
            description:
              description: The description of the episode, possibly in HTML format
              example: <p><strong>Some</strong> funny example.</p>
              type: string
            description_is_html:
              description: Determines whether `description` is in HTML format or plain.
              type: boolean
            description_plain:
              description: >
                The description with all HTML markup removed. If the description does not contain markup,

                this is identitcal to `description`.
              example: Some funny example.
              type: string
            file_name:
              description: The file name, without extension, to present users when downloading the episode
              type: string
            itunes_type:
              description: The type of the episode according to Apple's type categorization
              enum:
                - full
                - bonus
                - trailer
              type: string
            listen_count:
              description: >
                The number of total downloads all time for this episode.

                Only visible if the credential has the right privilege on the series.
              type: integer
            premium_status:
              description: The current premium status of the episode
              enum:
                - active
                - inactive
                - force-active
                - force-inactive
              type: string
            published:
              description: >
                If true, the episode will be available for download and included in the feed as soon as its

                release time is reached. Aside from that, it also needs to have valid media file associated.
              type: boolean
            release_immediately:
              description: >
                If true, the episode's release time is set in a way it is considered released right away.

                Only visible if the credential has write privileges on the series.
              type: boolean
            released_at:
              description: >
                The date & time when an episode shall be released. It will be available for download and

                included in the feed, if this time is reached and `published` is set to `true` and the episode

                has an available media file associated.
              format: date-time
              type: string
            rss_guid:
              description: >
                A globally unique identifier describing the episode. This ID is commonly used by podcast applications

                to determine whether an episode has already been downloaded or not.
              type: string
            season_id:
              deprecated: true
              description: >
                Use the relationship `season` instead.<br />

                The ID of the season an episode belongs to. This can be `null`. A series can have episodes with and

                without seasons at the same time.
              example: eba5523d-e73a-43ec-bfd9-140ce9db76ac
              format: uuid
              type: string
            series_id:
              deprecated: true
              description: >
                Use the relationship `series` instead.<br />

                The ID of the series an episode belongs to.
              example: 9897f8cb-ffa7-4674-9529-c6037826c738
              format: uuid
              type: string
            sort_title:
              description: >
                The title of the episode modified for sorting. This converts all characters to

                lower case, removes an eventually leading `The` as well as leading and trailling whitespace characters.
              example: big hit
              type: string
            status:
              description: The status of an episode
              enum:
                - active
                - inactive
              type: string
            title:
              description: The title of the episode
              example: The Big Hit
              type: string
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            classification_inclusions:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of classifications associated with the episode
              type: object
            credits:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of credits given to people in the episode
              type: object
            default_marker_points:
              description: Only visible if the credential has write privileges on the series
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of default (WarpFeed) marker points
              type: object
            episode_versions:
              description: Only visible if the credential has write privileges on the series
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of episode versions for this episode
              type: object
            images:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: Both the cover image and the cascaded cover image for the season
              type: object
            season:
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              title: The season the episode belongs to
              type: object
            segment_lists:
              description: Only visible if the credential has write privileges on the series
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of segment lists directly attached to the episode
              type: object
            series:
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              title: The series an episode belongs to
              type: object
            taggings:
              description: "`DEPRECATED`"
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of associations linking tags to the episodes.
              type: object
            tags:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of tags associated with the episodes
              type: object
          type: object
        type:
          description: The type of resource. This is always `episodes`
          example: episodes
          type: string
      type: object
    Image:
      properties:
        attributes:
          properties:
            bucket_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `bucket` in a future update.<br/>

                The ID of the owning entity
              example: 687282a1-faf7-4d37-8f4a-cbddedf6db8f
              format: uuid
              type: string
            bucket_type:
              deprecated: true
              description: >
                This will be replaced by the relationship `bucket` in a future update.<br/>

                The type of the owning entity
              enum:
                - AdRepAccount
                - Agency
                - Network
                - Series
                - User
              type: string
            created_at:
              format: date-time
              type: string
            crop_data:
              properties:
                height:
                  description: The height of the crop area
                  example: 3000
                  format: float
                  type: number
                left:
                  description: The left coordinate of the crop area
                  example: 100
                  format: float
                  type: number
                top:
                  description: The top coordinate of the crop area
                  example: 150
                  format: float
                  type: number
                width:
                  description: The width of the crop area
                  example: 3000
                  format: float
                  type: number
              title: The crop area within the original media asset
              type: object
            status:
              description: >
                The current value of the image state machine. It starts with `uploaded` and

                transitions to `processing` once a worker has picked it up. If all variations and

                styles of an image have been successfully created, the status becomes `valid`. In

                case of a problem during the processing it changes to `invalid`.
              enum:
                - invalid
                - processing
                - uploaded
                - valid
              type: string
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            media_assets:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: All media assets available for this image
              type: object
          type: object
        type:
          description: The type of resource. This is always `images`
          example: images
          type: string
      type: object
    Links:
      properties:
        first:
          description: A URL to retrieve the first page of data keeping the provided page size
          type: string
        last:
          description: A URL to retrieve the last page of data keeping the provided page size
          type: string
        next:
          description: A URL to retrieve the next page of data keeping the provided page size
          type: string
        prev:
          description: A URL to retrieve the previous page of keeping using the provided page size
          type: string
        self:
          description: A URL to retrieve the collection as the primary data
          type: string
      type: object
    MediaAsset:
      properties:
        attributes:
          properties:
            attachment_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `attachment` in a future update.<br/>

                The ID of the entity this media asset is attached to
              example: 09a93847-d5bf-47b4-90d6-854e95011951
              format: uuid
              type: string
            attachment_type:
              deprecated: true
              description: >
                This will be replaced by the relationship `attachment` in a future update.<br/>

                The type of the entity this media asset is attached to
              enum:
                - Image
                - Segment
                - SegmentList
              type: string
            cdn_url:
              description: The CDN-URL where the media asset is available
              type: string
            content_type:
              description: The MIME type of the media asset
              example: image/jpeg
              type: string
            created_at:
              format: date-time
              type: string
            file_name:
              description: >
                The filename of the media asset without any path components.
              type: string
            file_size:
              description: The size in bytes of the media asset
              example: 2544307
              type: integer
            size_height:
              description: The height in pixels of the media asset, if the media asset is an image
              example: 1839
              type: integer
            size_width:
              description: The width in pixels of the media asset, if the media asset is an image
              example: 1839
              type: integer
            style:
              description: >
                The style of the media asset. A media asset is part of a collection of related media files,

                for example an image or an episode (audio). Images usually have differently sized versions,

                and episodes can have different audio formats.
              enum:
                - info
                - itunes
                - medium
                - original
                - regular
                - square-400
                - square-640
                - square-888
                - square-3000
                - stripped-original
                - thumb
                - waveform_data
              example: regular
              type: string
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        type:
          description: The type of resource. This is always `media_assets`
          example: media_assets
          type: string
      type: object
    Network:
      properties:
        attributes:
          properties:
            copyright:
              description: The copyright of the network
              type: string
            cover_image_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `cover_image` in a future update.<br/>

                The image ID of the cover art for this network. The `/images` endpoint provideds details to the image.
              example: 1fcae36a-0ff5-46ea-aa14-c1f378d28d2f
              format: uuid
              type: string
            created_at:
              format: date-time
              type: string
            description:
              description: The description of the network eventually in HTML format
              example: <p><strong>Some</strong> funny example.</p>
              type: string
            description_is_html:
              description: Determines whether `description` is in HTML format or plain.
              type: boolean
            description_plain:
              description: >
                The description with all HTML markup removed. If the description does not contain markup

                this is identitcal to `description`.
              example: Some funny example.
              type: string
            facebook_url:
              description: Where to find this network on Facebook
              type: string
            linkedin_url:
              description: Where to find the network on LinkedIn
              type: string
            name:
              description: The name of the network
              type: string
            public_email:
              description: A public point of contact through email
              type: string
            public_page_enabled:
              description: >
                If this is `true`, a public web page for this network is available under

                `https://art19.com/networks/{slug}`, where `slug` is the value of the corresponding attribute.

                This public page provides details about the network as well as descriptive links to all its

                series' public pages. If this is `false`, no such public page is available.
              type: boolean
            slug:
              description: >
                The unique slug for the network also used on its public page for example.

                This value is unique system-wide amongst all networks _and_ series.
              type: string
            tumblr_url:
              description: Where to find the network on Tumblr
              type: string
            twitter_url:
              description: Where to find the network on Twitter
              type: string
            updated_at:
              format: date-time
              type: string
            website_url:
              description: A public website for the network
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            images:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: >
                A collection of images linked to the network. This currently only includes

                the cover image for the network.
              type: object
            series:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: List of currently active series within the network
              type: object
          type: object
        type:
          description: The type of resource. This is always `networks`
          example: networks
          type: string
      type: object
    Person:
      properties:
        attributes:
          properties:
            avatar_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `avatar` in a future update.<br/>

                The image ID of an avatar or head shot of the person. Details can be

                retrieved using the `/images` endpoint.
              example: 686ae786-6bbe-42f4-9a51-9e1cf207fc8b
              format: uuid
              type: string
            biography:
              description: The biography of the person without markup.
              type: string
            born:
              description: The birthday of the person
              format: date
              type: string
            created_at:
              format: date-time
              type: string
            died:
              description: The day of death of the person
              format: date
              type: string
            first_name:
              description: The first name of the person
              type: string
            from_country:
              description: The country the person is from
              type: string
            from_locality:
              description: The locality or city the person is from
              type: string
            from_region:
              description: The region/state/province the person is from
              type: string
            last_name:
              description: The last name of the person
              type: string
            public_email:
              description: The public email contact for the person
              type: string
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        type:
          description: The type of resource. This is always `people`
          example: people
          type: string
      type: object
    ResourceIdentifier:
      properties:
        id:
          description: The ID of the resource
          example: 1a6be34f-5f8f-4275-ac10-d8102fb3af22
          format: uuid
          type: string
        type:
          description: The type of the resource
          type: string
      type: object
    ResourceLink:
      description: Links related to the retrieved resource
      properties:
        self:
          description: A URL to retrieve the resource as the primary data
          type: string
      title: Links
      type: object
    Season:
      properties:
        attributes:
          properties:
            cascaded_cover_image_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `cascaded_cover_image` in a future update.<br/>

                The image ID of the cover art for this season. If there is no specific cover for the season,

                the image ID of the series' cover art will be returned.

                The `/images` endpoint provideds details to the image.
              example: ec8f13bf-77c0-40fe-933a-8c088c16bc17
              format: uuid
              type: string
            cover_image_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `cover_image` in a future update.<br/>

                The image ID of the cover art for this season. The `/images` endpoint provideds details to the image.
              example: ec8f13bf-77c0-40fe-933a-8c088c16bc17
              format: uuid
              type: string
            created_at:
              format: date-time
              type: string
            description:
              description: The description of the season eventually in HTML format
              example: <p><strong>Some</strong> funny example.</p>
              type: string
            description_is_html:
              description: Determines whether `description` is in HTML format or plain.
              type: boolean
            description_plain:
              description: >
                The description with all HTML markup removed. If the description does not contain markup

                this is identitcal to `description`.
              example: Some funny example.
              type: string
            first_released_episode_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `first_released_episode` in a future update.<br/>

                The ID of the first released episode within the season
              example: 73a97f1a-5c3c-4226-8ab3-1d7f9e486e4d
              format: uuid
              type: string
            last_released_episode_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `last_released_episode` in a future update.<br/>

                The ID of the most recently released episode within the season
              example: a50b7a22-2128-46d7-b3df-2b308df21bf0
              format: uuid
              type: string
            season_number:
              description: The number of the season. This could be `null` as well.
              example: 1
              type: integer
            sort_title:
              description: >
                The title of the season modified for sorting. This converts all characters to

                lower case, removes an eventually leading `The` as well as leading and trailling whitespace characters.
              example: awesome season
              type: string
            status:
              description: The current status of the season
              enum:
                - active
                - inactive
              type: string
            title:
              description: The title of the season
              example: The Aweseom Season
              type: string
            updated_at:
              format: date-time
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            classification_inclusions:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of classifications associated with the season
              type: object
            credits:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of credits given to people in the season
              type: object
            episodes:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of published and released episodes with an actual media file within the season
              type: object
            images:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: Both the cover image and the cascaded cover image for the season
              type: object
            series:
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              title: The series the season belongs to
              type: object
            taggings:
              description: "`DEPRECATED`"
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of associations linking tags to the season.
              type: object
            tags:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of tags associated with the season
              type: object
          type: object
        type:
          description: The type of resource. This is always `seasons`
          example: seasons
          type: string
      type: object
    Series:
      properties:
        attributes:
          properties:
            amazon_subscription_url:
              description: The Amazon Music subscription URL
              type: string
            castbox_subscription_url:
              description: The CastBox subscription URL
              type: string
            cover_image_id:
              deprecated: true
              description: >
                This will be replaced by the relationship `cover_image` in a future update.<br/>

                The image ID of the cover art for this series. The `/images` endpoint provideds details to the image.
              example: ec8f13bf-77c0-40fe-933a-8c088c16bc17
              format: uuid
              type: string
            created_at:
              format: date-time
              type: string
            description:
              description: The description of the series eventually in HTML format
              example: <p><strong>Some</strong> funny example.</p>
              type: string
            description_is_html:
              description: Determines whether `description` is in HTML format or plain.
              type: boolean
            description_plain:
              description: >
                The description with all HTML markup removed. If the description does not contain markup

                this is identitcal to `description`.
              example: Some funny example.
              type: string
            facebook_url:
              description: The URL of the series on Facebook
              type: string
            google_subscription_url:
              description: The Google subscription URL
              type: string
            iheart_subscription_url:
              description: The iHeartRadio subscription URL
              type: string
            itunes_subscription_badge_enabled:
              description: If `null` or `true`, the ART19 web player will render an iTunes subscription badge
              type: boolean
            itunes_subscription_url:
              description: The Apple iTunes subscription URL
              type: string
            linkedin_url:
              description: The URL of the series on LinkedIn
              type: string
            pocket_casts_subscription_url:
              description: The Pocket Casts subscription URL
              type: string
            podcast_subscription_url:
              description: The URL of the RSS feed
              example: https://rss.art19.com/greatest-show-ever
              type: string
            public_page_enabled:
              description: >
                If this is `true`, a public web page for this series is available under

                `https://art19.com/shows/{slug}`, where `slug` is the value of the corresponding attribute.

                This public page provides details about the series as well as descriptive links to all its

                episodes. If this is `false`, no such public page is available.
              type: boolean
            public_title:
              description: If present, this is the preferred title for the series in the RSS Feed
              type: string
            radio_public_subscription_url:
              description: The RadioPublic subscription URL
              type: string
            rss_author:
              description: The author responsible for the RSS feed
              type: string
            rss_copyright:
              description: The copyright disclaimer for the contents of the show, appearing after a © symbol, e.g. `2020 ART19, Inc.`
              type: string
            rss_email:
              description: The pubic point of contact for the show
              type: string
            rss_owner:
              description: The name of the contact for the show
              type: string
            slug:
              description: >
                This slug is unique over all series and networks system wide and is used to access the series' feed

                as well as its eventually enabled public page on ART19.
              example: greatest-show-ever
              type: string
            sort_title:
              description: >
                The title of the series modified for sorting. This converts all characters to

                lower case, removes an eventually leading `The` as well as leading and trailling whitespace characters.
              example: greatest show ever
              type: string
            spotify_subscription_badge_enabled:
              description: If `null` or `true`, the ART19 web player will render a Spotify subscription badge
              type: boolean
            spotify_subscription_url:
              description: The Spotify subscription URL
              type: string
            status:
              description: The status of the series
              enum:
                - active
                - inactive
              type: string
            stitcher_subscription_badge_enabled:
              description: If `null` or `true`, the ART19 web player will render a Stitcher subscription badge
              type: boolean
            stitcher_subscription_url:
              description: The Stitcher subscription URL
              type: string
            title:
              description: The title of the series
              example: The greatest show ever
              type: string
            tumblr_url:
              description: The URL of the series on Tumblr
              type: string
            tune_in_subscription_url:
              description: The TuneIn subscription URL
              type: string
            twitter_url:
              description: The URL of the series on Twitter
              type: string
            type:
              description: The type of the series
              enum:
                - Series
              type: string
            updated_at:
              format: date-time
              type: string
            website_url:
              description: The custom website URL for the series
              type: string
          type: object
        id:
          example: c1fbb1bf-45b1-4803-986e-123166f7f427
          format: uuid
          type: string
        relationships:
          properties:
            classification_inclusions:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of classifications associated with the series
              type: object
            credits:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of credits given to people in the series
              type: object
            episodes:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of published and released episodes with an actual media file
              type: object
            images:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: The cover image for the series
              type: object
            network:
              properties:
                data:
                  $ref: "#/components/schemas/ResourceIdentifier"
              title: The network the series belongs to
              type: object
            seasons:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of seasons in the series
              type: object
            taggings:
              description: "`DEPRECATED`"
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of associations linking tags to the series
              type: object
            tags:
              properties:
                data:
                  items:
                    $ref: "#/components/schemas/ResourceIdentifier"
                  type: array
              title: A list of tags associated with the series
              type: object
          type: object
        type:
          description: The type of resource. This is always `series`
          example: series
          type: string
      type: object
  securitySchemes:
    api_key:
      description: >
        Authentication is **REQUIRED** for all APIs using an API token, which is placed into an HTTP Authorization header.


        This token is generated by ART19 Support and consists of a shared secret placed in the `token`

        parameter and a credential ID placed in the `credential` parameter.


        The final Authorization header will look like this:


        ```

        Authorization: Token token="V2h5LCBoZWxsbyB0aGVyZSE=", credential="8f71320c-fdbc-4934-b3b9-f1eda4fb39a9"

        ```
      in: header
      name: Authorization
      type: apiKey
