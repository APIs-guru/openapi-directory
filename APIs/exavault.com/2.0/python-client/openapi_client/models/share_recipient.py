# coding: utf-8

"""
    ExaVault

    ExaVaults API allows you to incorporate ExaVaults suite of file transfer and user management tools into your own application.\\nExaVault supports both POST (recommended when requesting large data sets) and GET operations, and requires an API key in order to use.

    The version of the OpenAPI document: 2.0
    Contact: support@exavault.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ShareRecipient(BaseModel):
    """
    ShareRecipient
    """ # noqa: E501
    created: Optional[datetime] = Field(default=None, description="Timestamp of adding recipient to the share.")
    email: Optional[StrictStr] = Field(default=None, description="Recipient email address.")
    hash: Optional[StrictStr] = Field(default=None, description="Share hash.")
    id: Optional[StrictInt] = Field(default=None, description="ID of the recipient.")
    received: Optional[StrictBool] = Field(default=None, description="Set to true if recipient has accessed the share. Note this is set to true when the recipient clicks the link to access the share; not when they download a file.")
    sent: Optional[StrictBool] = Field(default=None, description="Set to true if invite email was sent; false otherwise.")
    share_id: Optional[StrictStr] = Field(default=None, description="ID of the share that the recipoient belongs to.", alias="shareId")
    type: Optional[StrictStr] = Field(default=None, description="Type of the recipient.")
    __properties: ClassVar[List[str]] = ["created", "email", "hash", "id", "received", "sent", "shareId", "type"]

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['owner', 'direct']):
            raise ValueError("must be one of enum values ('owner', 'direct')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ShareRecipient from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ShareRecipient from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "created": obj.get("created"),
            "email": obj.get("email"),
            "hash": obj.get("hash"),
            "id": obj.get("id"),
            "received": obj.get("received"),
            "sent": obj.get("sent"),
            "shareId": obj.get("shareId"),
            "type": obj.get("type")
        })
        return _obj


