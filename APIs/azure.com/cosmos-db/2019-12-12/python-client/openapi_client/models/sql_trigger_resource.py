# coding: utf-8

"""
    Cosmos DB

    Azure Cosmos DB Database Service Resource Provider REST API

    The version of the OpenAPI document: 2019-12-12
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class SqlTriggerResource(BaseModel):
    """
    Cosmos DB SQL trigger resource object
    """ # noqa: E501
    body: Optional[StrictStr] = Field(default=None, description="Body of the Trigger")
    id: StrictStr = Field(description="Name of the Cosmos DB SQL trigger")
    trigger_operation: Optional[StrictStr] = Field(default=None, description="The operation the trigger is associated with", alias="triggerOperation")
    trigger_type: Optional[StrictStr] = Field(default=None, description="Type of the Trigger", alias="triggerType")
    __properties: ClassVar[List[str]] = ["body", "id", "triggerOperation", "triggerType"]

    @field_validator('trigger_operation')
    def trigger_operation_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['All', 'Create', 'Update', 'Delete', 'Replace']):
            raise ValueError("must be one of enum values ('All', 'Create', 'Update', 'Delete', 'Replace')")
        return value

    @field_validator('trigger_type')
    def trigger_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Pre', 'Post']):
            raise ValueError("must be one of enum values ('Pre', 'Post')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SqlTriggerResource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SqlTriggerResource from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "body": obj.get("body"),
            "id": obj.get("id"),
            "triggerOperation": obj.get("triggerOperation"),
            "triggerType": obj.get("triggerType")
        })
        return _obj


