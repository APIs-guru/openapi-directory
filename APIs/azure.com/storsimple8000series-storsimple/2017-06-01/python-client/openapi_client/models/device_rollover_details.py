# coding: utf-8

"""
    StorSimple8000SeriesManagementClient

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: 2017-06-01
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class DeviceRolloverDetails(BaseModel):
    """
    The additional device details for the service data encryption key rollover.
    """ # noqa: E501
    authorization_eligibility: Optional[StrictStr] = Field(default=None, description="The eligibility status of device for service data encryption key rollover.", alias="authorizationEligibility")
    authorization_status: Optional[StrictStr] = Field(default=None, description="The authorization status of the device for service data encryption key rollover.", alias="authorizationStatus")
    in_eligibility_reason: Optional[StrictStr] = Field(default=None, description="The reason for inEligibility of device, in case it's not eligible for service data encryption key rollover.", alias="inEligibilityReason")
    __properties: ClassVar[List[str]] = ["authorizationEligibility", "authorizationStatus", "inEligibilityReason"]

    @field_validator('authorization_eligibility')
    def authorization_eligibility_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['InEligible', 'Eligible']):
            raise ValueError("must be one of enum values ('InEligible', 'Eligible')")
        return value

    @field_validator('authorization_status')
    def authorization_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Disabled', 'Enabled']):
            raise ValueError("must be one of enum values ('Disabled', 'Enabled')")
        return value

    @field_validator('in_eligibility_reason')
    def in_eligibility_reason_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['DeviceNotOnline', 'NotSupportedAppliance', 'RolloverPending']):
            raise ValueError("must be one of enum values ('DeviceNotOnline', 'NotSupportedAppliance', 'RolloverPending')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DeviceRolloverDetails from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DeviceRolloverDetails from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "authorizationEligibility": obj.get("authorizationEligibility"),
            "authorizationStatus": obj.get("authorizationStatus"),
            "inEligibilityReason": obj.get("inEligibilityReason")
        })
        return _obj


