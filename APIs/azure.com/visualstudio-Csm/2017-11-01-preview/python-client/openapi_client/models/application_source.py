# coding: utf-8

"""
    Visual Studio Resource Provider Client

    Use these APIs to manage Visual Studio Team Services resources through the Azure Resource Manager. All task operations conform to the HTTP/1.1 protocol specification and each operation returns an x-ms-request-id header that can be used to obtain information about the request. You must make sure that requests made to these resources are secure. For more information, see https://docs.microsoft.com/en-us/rest/api/index.

    The version of the OpenAPI document: 2017-11-01-preview
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from importlib import import_module
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

from typing import TYPE_CHECKING
if TYPE_CHECKING:
    from openapi_client.models.code_repository_application_source import CodeRepositoryApplicationSource
    from openapi_client.models.code_template_application_source import CodeTemplateApplicationSource

class ApplicationSource(BaseModel):
    """
    Defines the source application for a VSTS pipeline.
    """ # noqa: E501
    application_configuration: Optional[Dict[str, StrictStr]] = Field(default=None, description="Application specific properties.", alias="applicationConfiguration")
    application_type: StrictStr = Field(description="Type of application.", alias="applicationType")
    source_type: StrictStr = Field(description="Type of application source.", alias="sourceType")
    __properties: ClassVar[List[str]] = ["applicationConfiguration", "applicationType", "sourceType"]

    @field_validator('application_type')
    def application_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['AspDotNet', 'AspDotNetCore', 'NodeJs']):
            raise ValueError("must be one of enum values ('AspDotNet', 'AspDotNetCore', 'NodeJs')")
        return value

    @field_validator('source_type')
    def source_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['CodeTemplate', 'CodeRepository']):
            raise ValueError("must be one of enum values ('CodeTemplate', 'CodeRepository')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    # JSON field name that stores the object type
    __discriminator_property_name: ClassVar[str] = 'sourceType'

    # discriminator mappings
    __discriminator_value_class_map: ClassVar[Dict[str, str]] = {
        'CodeRepositoryApplicationSource': 'CodeRepositoryApplicationSource','CodeTemplateApplicationSource': 'CodeTemplateApplicationSource'
    }

    @classmethod
    def get_discriminator_value(cls, obj: Dict[str, Any]) -> Optional[str]:
        """Returns the discriminator value (object type) of the data"""
        discriminator_value = obj[cls.__discriminator_property_name]
        if discriminator_value:
            return cls.__discriminator_value_class_map.get(discriminator_value)
        else:
            return None

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Union[CodeRepositoryApplicationSource, CodeTemplateApplicationSource]]:
        """Create an instance of ApplicationSource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Dict[str, Any]) -> Optional[Union[CodeRepositoryApplicationSource, CodeTemplateApplicationSource]]:
        """Create an instance of ApplicationSource from a dict"""
        # look up the object type based on discriminator mapping
        object_type = cls.get_discriminator_value(obj)
        if object_type ==  'CodeRepositoryApplicationSource':
            return import_module("openapi_client.models.code_repository_application_source").CodeRepositoryApplicationSource.from_dict(obj)
        if object_type ==  'CodeTemplateApplicationSource':
            return import_module("openapi_client.models.code_template_application_source").CodeTemplateApplicationSource.from_dict(obj)

        raise ValueError("ApplicationSource failed to lookup discriminator value from " +
                            json.dumps(obj) + ". Discriminator property name: " + cls.__discriminator_property_name +
                            ", mapping: " + json.dumps(cls.__discriminator_value_class_map))


