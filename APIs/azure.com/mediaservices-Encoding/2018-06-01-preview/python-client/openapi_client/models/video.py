# coding: utf-8

"""
    Azure Media Services

    This Swagger was generated by the API Framework.

    The version of the OpenAPI document: 2018-06-01-preview
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from importlib import import_module
from pydantic import ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from openapi_client.models.codec import Codec
from typing import Optional, Set
from typing_extensions import Self

from typing import TYPE_CHECKING
if TYPE_CHECKING:
    from openapi_client.models.h264_video import H264Video
    from openapi_client.models.image import Image
    from openapi_client.models.jpg_image import JpgImage
    from openapi_client.models.png_image import PngImage

class Video(Codec):
    """
    Describes the basic properties for encoding the input video.
    """ # noqa: E501
    key_frame_interval: Optional[StrictStr] = Field(default=None, description="The distance between two key frames, thereby defining a group of pictures (GOP). The value should be a non-zero integer in the range [1, 30] seconds, specified in ISO 8601 format. The default is 2 seconds (PT2S).", alias="keyFrameInterval")
    stretch_mode: Optional[StrictStr] = Field(default=None, description="The resizing mode - how the input video will be resized to fit the desired output resolution(s). Default is AutoSize", alias="stretchMode")
    __properties: ClassVar[List[str]] = ["@odata.type", "label"]

    @field_validator('stretch_mode')
    def stretch_mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['None', 'AutoSize', 'AutoFit']):
            raise ValueError("must be one of enum values ('None', 'AutoSize', 'AutoFit')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    # JSON field name that stores the object type
    __discriminator_property_name: ClassVar[str] = '@odata.type'

    # discriminator mappings
    __discriminator_value_class_map: ClassVar[Dict[str, str]] = {
        'H264Video': 'H264Video','Image': 'Image','JpgImage': 'JpgImage','PngImage': 'PngImage'
    }

    @classmethod
    def get_discriminator_value(cls, obj: Dict[str, Any]) -> Optional[str]:
        """Returns the discriminator value (object type) of the data"""
        discriminator_value = obj[cls.__discriminator_property_name]
        if discriminator_value:
            return cls.__discriminator_value_class_map.get(discriminator_value)
        else:
            return None

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Union[H264Video, Image, JpgImage, PngImage]]:
        """Create an instance of Video from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Dict[str, Any]) -> Optional[Union[H264Video, Image, JpgImage, PngImage]]:
        """Create an instance of Video from a dict"""
        # look up the object type based on discriminator mapping
        object_type = cls.get_discriminator_value(obj)
        if object_type ==  'H264Video':
            return import_module("openapi_client.models.h264_video").H264Video.from_dict(obj)
        if object_type ==  'Image':
            return import_module("openapi_client.models.image").Image.from_dict(obj)
        if object_type ==  'JpgImage':
            return import_module("openapi_client.models.jpg_image").JpgImage.from_dict(obj)
        if object_type ==  'PngImage':
            return import_module("openapi_client.models.png_image").PngImage.from_dict(obj)

        raise ValueError("Video failed to lookup discriminator value from " +
                            json.dumps(obj) + ". Discriminator property name: " + cls.__discriminator_property_name +
                            ", mapping: " + json.dumps(cls.__discriminator_value_class_map))


