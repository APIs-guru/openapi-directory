# coding: utf-8

"""
    SqlManagementClient

    The Azure SQL Database management API provides a RESTful set of web APIs that interact with Azure SQL Database services to manage your databases. The API enables users to create, retrieve, update, and delete databases, servers, and other entities.

    The version of the OpenAPI document: 2015-05-01-preview
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from openapi_client.models.automatic_tuning_options import AutomaticTuningOptions
from typing import Optional, Set
from typing_extensions import Self

class DatabaseAutomaticTuningProperties(BaseModel):
    """
    Database-level Automatic Tuning properties.
    """ # noqa: E501
    actual_state: Optional[StrictStr] = Field(default=None, description="Automatic tuning actual state.", alias="actualState")
    desired_state: Optional[StrictStr] = Field(default=None, description="Automatic tuning desired state.", alias="desiredState")
    options: Optional[Dict[str, AutomaticTuningOptions]] = Field(default=None, description="Automatic tuning options definition.")
    __properties: ClassVar[List[str]] = ["actualState", "desiredState", "options"]

    @field_validator('actual_state')
    def actual_state_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Inherit', 'Custom', 'Auto', 'Unspecified']):
            raise ValueError("must be one of enum values ('Inherit', 'Custom', 'Auto', 'Unspecified')")
        return value

    @field_validator('desired_state')
    def desired_state_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Inherit', 'Custom', 'Auto', 'Unspecified']):
            raise ValueError("must be one of enum values ('Inherit', 'Custom', 'Auto', 'Unspecified')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DatabaseAutomaticTuningProperties from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set([
            "actual_state",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each value in options (dict)
        _field_dict = {}
        if self.options:
            for _key_options in self.options:
                if self.options[_key_options]:
                    _field_dict[_key_options] = self.options[_key_options].to_dict()
            _dict['options'] = _field_dict
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DatabaseAutomaticTuningProperties from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "actualState": obj.get("actualState"),
            "desiredState": obj.get("desiredState"),
            "options": dict(
                (_k, AutomaticTuningOptions.from_dict(_v))
                for _k, _v in obj["options"].items()
            )
            if obj.get("options") is not None
            else None
        })
        return _obj


