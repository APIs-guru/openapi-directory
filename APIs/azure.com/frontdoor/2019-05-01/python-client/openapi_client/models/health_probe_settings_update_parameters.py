# coding: utf-8

"""
    FrontDoorManagementClient

    Use these APIs to manage Azure Front Door resources through the Azure Resource Manager. You must make sure that requests made to these resources are secure.

    The version of the OpenAPI document: 2019-05-01
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class HealthProbeSettingsUpdateParameters(BaseModel):
    """
    L7 health probe settings for a backend pool
    """ # noqa: E501
    enabled_state: Optional[StrictStr] = Field(default=None, description="Whether to enable health probes to be made against backends defined under backendPools. Health probes can only be disabled if there is a single enabled backend in single enabled backend pool.", alias="enabledState")
    health_probe_method: Optional[StrictStr] = Field(default='HEAD', description="Configures which HTTP method to use to probe the backends defined under backendPools.", alias="healthProbeMethod")
    interval_in_seconds: Optional[StrictInt] = Field(default=None, description="The number of seconds between health probes.", alias="intervalInSeconds")
    path: Optional[StrictStr] = Field(default=None, description="The path to use for the health probe. Default is /")
    protocol: Optional[StrictStr] = Field(default=None, description="Protocol scheme to use for this probe")
    __properties: ClassVar[List[str]] = ["enabledState", "healthProbeMethod", "intervalInSeconds", "path", "protocol"]

    @field_validator('enabled_state')
    def enabled_state_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Enabled', 'Disabled']):
            raise ValueError("must be one of enum values ('Enabled', 'Disabled')")
        return value

    @field_validator('health_probe_method')
    def health_probe_method_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['GET', 'HEAD']):
            raise ValueError("must be one of enum values ('GET', 'HEAD')")
        return value

    @field_validator('protocol')
    def protocol_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Http', 'Https']):
            raise ValueError("must be one of enum values ('Http', 'Https')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of HealthProbeSettingsUpdateParameters from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of HealthProbeSettingsUpdateParameters from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "enabledState": obj.get("enabledState"),
            "healthProbeMethod": obj.get("healthProbeMethod") if obj.get("healthProbeMethod") is not None else 'HEAD',
            "intervalInSeconds": obj.get("intervalInSeconds"),
            "path": obj.get("path"),
            "protocol": obj.get("protocol")
        })
        return _obj


