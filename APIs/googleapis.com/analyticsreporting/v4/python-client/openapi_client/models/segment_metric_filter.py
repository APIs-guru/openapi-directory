# coding: utf-8

"""
    Analytics Reporting API

    Accesses Analytics report data.

    The version of the OpenAPI document: v4
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class SegmentMetricFilter(BaseModel):
    """
    Metric filter to be used in a segment filter clause.
    """ # noqa: E501
    comparison_value: Optional[StrictStr] = Field(default=None, description="The value to compare against. If the operator is `BETWEEN`, this value is treated as minimum comparison value.", alias="comparisonValue")
    max_comparison_value: Optional[StrictStr] = Field(default=None, description="Max comparison value is only used for `BETWEEN` operator.", alias="maxComparisonValue")
    metric_name: Optional[StrictStr] = Field(default=None, description="The metric that will be filtered on. A `metricFilter` must contain a metric name.", alias="metricName")
    operator: Optional[StrictStr] = Field(default=None, description="Specifies is the operation to perform to compare the metric. The default is `EQUAL`.")
    scope: Optional[StrictStr] = Field(default=None, description="Scope for a metric defines the level at which that metric is defined. The specified metric scope must be equal to or greater than its primary scope as defined in the data model. The primary scope is defined by if the segment is selecting users or sessions.")
    __properties: ClassVar[List[str]] = ["comparisonValue", "maxComparisonValue", "metricName", "operator", "scope"]

    @field_validator('operator')
    def operator_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['UNSPECIFIED_OPERATOR', 'LESS_THAN', 'GREATER_THAN', 'EQUAL', 'BETWEEN']):
            raise ValueError("must be one of enum values ('UNSPECIFIED_OPERATOR', 'LESS_THAN', 'GREATER_THAN', 'EQUAL', 'BETWEEN')")
        return value

    @field_validator('scope')
    def scope_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['UNSPECIFIED_SCOPE', 'PRODUCT', 'HIT', 'SESSION', 'USER']):
            raise ValueError("must be one of enum values ('UNSPECIFIED_SCOPE', 'PRODUCT', 'HIT', 'SESSION', 'USER')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SegmentMetricFilter from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SegmentMetricFilter from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "comparisonValue": obj.get("comparisonValue"),
            "maxComparisonValue": obj.get("maxComparisonValue"),
            "metricName": obj.get("metricName"),
            "operator": obj.get("operator"),
            "scope": obj.get("scope")
        })
        return _obj


