# coding: utf-8

"""
    Google Cloud Memorystore for Redis API

    Creates and manages Redis instances on the Google Cloud Platform.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class AvailabilityConfiguration(BaseModel):
    """
    Configuration for availability of database instance
    """ # noqa: E501
    availability_type: Optional[StrictStr] = Field(default=None, description="Availability type. Potential values: * `ZONAL`: The instance serves data from only one zone. Outages in that zone affect data accessibility. * `REGIONAL`: The instance can serve data from more than one zone in a region (it is highly available).", alias="availabilityType")
    external_replica_configured: Optional[StrictBool] = Field(default=None, alias="externalReplicaConfigured")
    promotable_replica_configured: Optional[StrictBool] = Field(default=None, alias="promotableReplicaConfigured")
    __properties: ClassVar[List[str]] = ["availabilityType", "externalReplicaConfigured", "promotableReplicaConfigured"]

    @field_validator('availability_type')
    def availability_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['AVAILABILITY_TYPE_UNSPECIFIED', 'ZONAL', 'REGIONAL', 'MULTI_REGIONAL', 'AVAILABILITY_TYPE_OTHER']):
            raise ValueError("must be one of enum values ('AVAILABILITY_TYPE_UNSPECIFIED', 'ZONAL', 'REGIONAL', 'MULTI_REGIONAL', 'AVAILABILITY_TYPE_OTHER')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AvailabilityConfiguration from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AvailabilityConfiguration from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "availabilityType": obj.get("availabilityType"),
            "externalReplicaConfigured": obj.get("externalReplicaConfigured"),
            "promotableReplicaConfigured": obj.get("promotableReplicaConfigured")
        })
        return _obj


