# coding: utf-8

"""
    Cloud Search API

    Cloud Search provides cloud-based search capabilities over Google Workspace data. The Cloud Search API allows indexing of non-Google Workspace data into Cloud Search.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class CompositeFilter(BaseModel):
    """
    CompositeFilter
    """ # noqa: E501
    logic_operator: Optional[StrictStr] = Field(default=None, description="The logic operator of the sub filter.", alias="logicOperator")
    sub_filters: Optional[List[Filter]] = Field(default=None, description="Sub filters.", alias="subFilters")
    __properties: ClassVar[List[str]] = ["logicOperator", "subFilters"]

    @field_validator('logic_operator')
    def logic_operator_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['AND', 'OR', 'NOT']):
            raise ValueError("must be one of enum values ('AND', 'OR', 'NOT')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CompositeFilter from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in sub_filters (list)
        _items = []
        if self.sub_filters:
            for _item_sub_filters in self.sub_filters:
                if _item_sub_filters:
                    _items.append(_item_sub_filters.to_dict())
            _dict['subFilters'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CompositeFilter from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "logicOperator": obj.get("logicOperator"),
            "subFilters": [Filter.from_dict(_item) for _item in obj["subFilters"]] if obj.get("subFilters") is not None else None
        })
        return _obj

from openapi_client.models.filter import Filter
# TODO: Rewrite to not use raise_errors
CompositeFilter.model_rebuild(raise_errors=False)

