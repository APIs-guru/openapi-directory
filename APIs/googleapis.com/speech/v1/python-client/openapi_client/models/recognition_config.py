# coding: utf-8

"""
    Cloud Speech-to-Text API

    Converts audio to text by applying powerful neural network models.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from openapi_client.models.recognition_metadata import RecognitionMetadata
from openapi_client.models.speaker_diarization_config import SpeakerDiarizationConfig
from openapi_client.models.speech_adaptation import SpeechAdaptation
from openapi_client.models.speech_context import SpeechContext
from openapi_client.models.transcript_normalization import TranscriptNormalization
from typing import Optional, Set
from typing_extensions import Self

class RecognitionConfig(BaseModel):
    """
    Provides information to the recognizer that specifies how to process the request.
    """ # noqa: E501
    adaptation: Optional[SpeechAdaptation] = None
    alternative_language_codes: Optional[List[StrictStr]] = Field(default=None, description="A list of up to 3 additional [BCP-47](https://www.rfc-editor.org/rfc/bcp/bcp47.txt) language tags, listing possible alternative languages of the supplied audio. See [Language Support](https://cloud.google.com/speech-to-text/docs/languages) for a list of the currently supported language codes. If alternative languages are listed, recognition result will contain recognition in the most likely language detected including the main language_code. The recognition result will include the language tag of the language detected in the audio. Note: This feature is only supported for Voice Command and Voice Search use cases and performance may vary for other use cases (e.g., phone call transcription).", alias="alternativeLanguageCodes")
    audio_channel_count: Optional[StrictInt] = Field(default=None, description="The number of channels in the input audio data. ONLY set this for MULTI-CHANNEL recognition. Valid values for LINEAR16, OGG_OPUS and FLAC are `1`-`8`. Valid value for MULAW, AMR, AMR_WB and SPEEX_WITH_HEADER_BYTE is only `1`. If `0` or omitted, defaults to one channel (mono). Note: We only recognize the first channel by default. To perform independent recognition on each channel set `enable_separate_recognition_per_channel` to 'true'.", alias="audioChannelCount")
    diarization_config: Optional[SpeakerDiarizationConfig] = Field(default=None, alias="diarizationConfig")
    enable_automatic_punctuation: Optional[StrictBool] = Field(default=None, description="If 'true', adds punctuation to recognition result hypotheses. This feature is only available in select languages. Setting this for requests in other languages has no effect at all. The default 'false' value does not add punctuation to result hypotheses.", alias="enableAutomaticPunctuation")
    enable_separate_recognition_per_channel: Optional[StrictBool] = Field(default=None, description="This needs to be set to `true` explicitly and `audio_channel_count` > 1 to get each channel recognized separately. The recognition result will contain a `channel_tag` field to state which channel that result belongs to. If this is not true, we will only recognize the first channel. The request is billed cumulatively for all channels recognized: `audio_channel_count` multiplied by the length of the audio.", alias="enableSeparateRecognitionPerChannel")
    enable_spoken_emojis: Optional[StrictBool] = Field(default=None, description="The spoken emoji behavior for the call If not set, uses default behavior based on model of choice If 'true', adds spoken emoji formatting for the request. This will replace spoken emojis with the corresponding Unicode symbols in the final transcript. If 'false', spoken emojis are not replaced.", alias="enableSpokenEmojis")
    enable_spoken_punctuation: Optional[StrictBool] = Field(default=None, description="The spoken punctuation behavior for the call If not set, uses default behavior based on model of choice e.g. command_and_search will enable spoken punctuation by default If 'true', replaces spoken punctuation with the corresponding symbols in the request. For example, \"how are you question mark\" becomes \"how are you?\". See https://cloud.google.com/speech-to-text/docs/spoken-punctuation for support. If 'false', spoken punctuation is not replaced.", alias="enableSpokenPunctuation")
    enable_word_confidence: Optional[StrictBool] = Field(default=None, description="If `true`, the top result includes a list of words and the confidence for those words. If `false`, no word-level confidence information is returned. The default is `false`.", alias="enableWordConfidence")
    enable_word_time_offsets: Optional[StrictBool] = Field(default=None, description="If `true`, the top result includes a list of words and the start and end time offsets (timestamps) for those words. If `false`, no word-level time offset information is returned. The default is `false`.", alias="enableWordTimeOffsets")
    encoding: Optional[StrictStr] = Field(default=None, description="Encoding of audio data sent in all `RecognitionAudio` messages. This field is optional for `FLAC` and `WAV` audio files and required for all other audio formats. For details, see AudioEncoding.")
    language_code: Optional[StrictStr] = Field(default=None, description="Required. The language of the supplied audio as a [BCP-47](https://www.rfc-editor.org/rfc/bcp/bcp47.txt) language tag. Example: \"en-US\". See [Language Support](https://cloud.google.com/speech-to-text/docs/languages) for a list of the currently supported language codes.", alias="languageCode")
    max_alternatives: Optional[StrictInt] = Field(default=None, description="Maximum number of recognition hypotheses to be returned. Specifically, the maximum number of `SpeechRecognitionAlternative` messages within each `SpeechRecognitionResult`. The server may return fewer than `max_alternatives`. Valid values are `0`-`30`. A value of `0` or `1` will return a maximum of one. If omitted, will return a maximum of one.", alias="maxAlternatives")
    metadata: Optional[RecognitionMetadata] = None
    model: Optional[StrictStr] = Field(default=None, description="Which model to select for the given request. Select the model best suited to your domain to get best results. If a model is not explicitly specified, then we auto-select a model based on the parameters in the RecognitionConfig. *Model* *Description* latest_long Best for long form content like media or conversation. latest_short Best for short form content like commands or single shot directed speech. command_and_search Best for short queries such as voice commands or voice search. phone_call Best for audio that originated from a phone call (typically recorded at an 8khz sampling rate). video Best for audio that originated from video or includes multiple speakers. Ideally the audio is recorded at a 16khz or greater sampling rate. This is a premium model that costs more than the standard rate. default Best for audio that is not one of the specific audio models. For example, long-form audio. Ideally the audio is high-fidelity, recorded at a 16khz or greater sampling rate. medical_conversation Best for audio that originated from a conversation between a medical provider and patient. medical_dictation Best for audio that originated from dictation notes by a medical provider. ")
    profanity_filter: Optional[StrictBool] = Field(default=None, description="If set to `true`, the server will attempt to filter out profanities, replacing all but the initial character in each filtered word with asterisks, e.g. \"f***\". If set to `false` or omitted, profanities won't be filtered out.", alias="profanityFilter")
    sample_rate_hertz: Optional[StrictInt] = Field(default=None, description="Sample rate in Hertz of the audio data sent in all `RecognitionAudio` messages. Valid values are: 8000-48000. 16000 is optimal. For best results, set the sampling rate of the audio source to 16000 Hz. If that's not possible, use the native sample rate of the audio source (instead of re-sampling). This field is optional for FLAC and WAV audio files, but is required for all other audio formats. For details, see AudioEncoding.", alias="sampleRateHertz")
    speech_contexts: Optional[List[SpeechContext]] = Field(default=None, description="Array of SpeechContext. A means to provide context to assist the speech recognition. For more information, see [speech adaptation](https://cloud.google.com/speech-to-text/docs/adaptation).", alias="speechContexts")
    transcript_normalization: Optional[TranscriptNormalization] = Field(default=None, alias="transcriptNormalization")
    use_enhanced: Optional[StrictBool] = Field(default=None, description="Set to true to use an enhanced model for speech recognition. If `use_enhanced` is set to true and the `model` field is not set, then an appropriate enhanced model is chosen if an enhanced model exists for the audio. If `use_enhanced` is true and an enhanced version of the specified model does not exist, then the speech is recognized using the standard version of the specified model.", alias="useEnhanced")
    __properties: ClassVar[List[str]] = ["adaptation", "alternativeLanguageCodes", "audioChannelCount", "diarizationConfig", "enableAutomaticPunctuation", "enableSeparateRecognitionPerChannel", "enableSpokenEmojis", "enableSpokenPunctuation", "enableWordConfidence", "enableWordTimeOffsets", "encoding", "languageCode", "maxAlternatives", "metadata", "model", "profanityFilter", "sampleRateHertz", "speechContexts", "transcriptNormalization", "useEnhanced"]

    @field_validator('encoding')
    def encoding_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ENCODING_UNSPECIFIED', 'LINEAR16', 'FLAC', 'MULAW', 'AMR', 'AMR_WB', 'OGG_OPUS', 'SPEEX_WITH_HEADER_BYTE', 'MP3', 'WEBM_OPUS']):
            raise ValueError("must be one of enum values ('ENCODING_UNSPECIFIED', 'LINEAR16', 'FLAC', 'MULAW', 'AMR', 'AMR_WB', 'OGG_OPUS', 'SPEEX_WITH_HEADER_BYTE', 'MP3', 'WEBM_OPUS')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RecognitionConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of adaptation
        if self.adaptation:
            _dict['adaptation'] = self.adaptation.to_dict()
        # override the default output from pydantic by calling `to_dict()` of diarization_config
        if self.diarization_config:
            _dict['diarizationConfig'] = self.diarization_config.to_dict()
        # override the default output from pydantic by calling `to_dict()` of metadata
        if self.metadata:
            _dict['metadata'] = self.metadata.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in speech_contexts (list)
        _items = []
        if self.speech_contexts:
            for _item_speech_contexts in self.speech_contexts:
                if _item_speech_contexts:
                    _items.append(_item_speech_contexts.to_dict())
            _dict['speechContexts'] = _items
        # override the default output from pydantic by calling `to_dict()` of transcript_normalization
        if self.transcript_normalization:
            _dict['transcriptNormalization'] = self.transcript_normalization.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RecognitionConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "adaptation": SpeechAdaptation.from_dict(obj["adaptation"]) if obj.get("adaptation") is not None else None,
            "alternativeLanguageCodes": obj.get("alternativeLanguageCodes"),
            "audioChannelCount": obj.get("audioChannelCount"),
            "diarizationConfig": SpeakerDiarizationConfig.from_dict(obj["diarizationConfig"]) if obj.get("diarizationConfig") is not None else None,
            "enableAutomaticPunctuation": obj.get("enableAutomaticPunctuation"),
            "enableSeparateRecognitionPerChannel": obj.get("enableSeparateRecognitionPerChannel"),
            "enableSpokenEmojis": obj.get("enableSpokenEmojis"),
            "enableSpokenPunctuation": obj.get("enableSpokenPunctuation"),
            "enableWordConfidence": obj.get("enableWordConfidence"),
            "enableWordTimeOffsets": obj.get("enableWordTimeOffsets"),
            "encoding": obj.get("encoding"),
            "languageCode": obj.get("languageCode"),
            "maxAlternatives": obj.get("maxAlternatives"),
            "metadata": RecognitionMetadata.from_dict(obj["metadata"]) if obj.get("metadata") is not None else None,
            "model": obj.get("model"),
            "profanityFilter": obj.get("profanityFilter"),
            "sampleRateHertz": obj.get("sampleRateHertz"),
            "speechContexts": [SpeechContext.from_dict(_item) for _item in obj["speechContexts"]] if obj.get("speechContexts") is not None else None,
            "transcriptNormalization": TranscriptNormalization.from_dict(obj["transcriptNormalization"]) if obj.get("transcriptNormalization") is not None else None,
            "useEnhanced": obj.get("useEnhanced")
        })
        return _obj


