# coding: utf-8

"""
    Cloud Datastore API

    Accesses the schemaless NoSQL database to provide fully managed, robust, scalable storage for your application. 

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBytes, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from openapi_client.models.entity_result import EntityResult
from typing import Optional, Set
from typing_extensions import Self

class QueryResultBatch(BaseModel):
    """
    A batch of results produced by a query.
    """ # noqa: E501
    end_cursor: Optional[Union[StrictBytes, StrictStr]] = Field(default=None, description="A cursor that points to the position after the last result in the batch.", alias="endCursor")
    entity_result_type: Optional[StrictStr] = Field(default=None, description="The result type for every entity in `entity_results`.", alias="entityResultType")
    entity_results: Optional[List[EntityResult]] = Field(default=None, description="The results for this batch.", alias="entityResults")
    more_results: Optional[StrictStr] = Field(default=None, description="The state of the query after the current batch.", alias="moreResults")
    read_time: Optional[StrictStr] = Field(default=None, description="Read timestamp this batch was returned from. This applies to the range of results from the query's `start_cursor` (or the beginning of the query if no cursor was given) to this batch's `end_cursor` (not the query's `end_cursor`). In a single transaction, subsequent query result batches for the same query can have a greater timestamp. Each batch's read timestamp is valid for all preceding batches. This value will not be set for eventually consistent queries in Cloud Datastore.", alias="readTime")
    skipped_cursor: Optional[Union[StrictBytes, StrictStr]] = Field(default=None, description="A cursor that points to the position after the last skipped result. Will be set when `skipped_results` != 0.", alias="skippedCursor")
    skipped_results: Optional[StrictInt] = Field(default=None, description="The number of results skipped, typically because of an offset.", alias="skippedResults")
    snapshot_version: Optional[StrictStr] = Field(default=None, description="The version number of the snapshot this batch was returned from. This applies to the range of results from the query's `start_cursor` (or the beginning of the query if no cursor was given) to this batch's `end_cursor` (not the query's `end_cursor`). In a single transaction, subsequent query result batches for the same query can have a greater snapshot version number. Each batch's snapshot version is valid for all preceding batches. The value will be zero for eventually consistent queries.", alias="snapshotVersion")
    __properties: ClassVar[List[str]] = ["endCursor", "entityResultType", "entityResults", "moreResults", "readTime", "skippedCursor", "skippedResults", "snapshotVersion"]

    @field_validator('entity_result_type')
    def entity_result_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['RESULT_TYPE_UNSPECIFIED', 'FULL', 'PROJECTION', 'KEY_ONLY']):
            raise ValueError("must be one of enum values ('RESULT_TYPE_UNSPECIFIED', 'FULL', 'PROJECTION', 'KEY_ONLY')")
        return value

    @field_validator('more_results')
    def more_results_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['MORE_RESULTS_TYPE_UNSPECIFIED', 'NOT_FINISHED', 'MORE_RESULTS_AFTER_LIMIT', 'MORE_RESULTS_AFTER_CURSOR', 'NO_MORE_RESULTS']):
            raise ValueError("must be one of enum values ('MORE_RESULTS_TYPE_UNSPECIFIED', 'NOT_FINISHED', 'MORE_RESULTS_AFTER_LIMIT', 'MORE_RESULTS_AFTER_CURSOR', 'NO_MORE_RESULTS')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of QueryResultBatch from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in entity_results (list)
        _items = []
        if self.entity_results:
            for _item_entity_results in self.entity_results:
                if _item_entity_results:
                    _items.append(_item_entity_results.to_dict())
            _dict['entityResults'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of QueryResultBatch from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "endCursor": obj.get("endCursor"),
            "entityResultType": obj.get("entityResultType"),
            "entityResults": [EntityResult.from_dict(_item) for _item in obj["entityResults"]] if obj.get("entityResults") is not None else None,
            "moreResults": obj.get("moreResults"),
            "readTime": obj.get("readTime"),
            "skippedCursor": obj.get("skippedCursor"),
            "skippedResults": obj.get("skippedResults"),
            "snapshotVersion": obj.get("snapshotVersion")
        })
        return _obj


