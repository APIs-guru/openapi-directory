# coding: utf-8

"""
    Cloud Dataproc API

    Manages Hadoop-based clusters and jobs on Google Cloud Platform.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from openapi_client.models.repository_config import RepositoryConfig
from typing import Optional, Set
from typing_extensions import Self

class RuntimeConfig(BaseModel):
    """
    Runtime configuration for a workload.
    """ # noqa: E501
    container_image: Optional[StrictStr] = Field(default=None, description="Optional. Optional custom container image for the job runtime environment. If not specified, a default container image will be used.", alias="containerImage")
    properties: Optional[Dict[str, StrictStr]] = Field(default=None, description="Optional. A mapping of property names to values, which are used to configure workload execution.")
    repository_config: Optional[RepositoryConfig] = Field(default=None, alias="repositoryConfig")
    version: Optional[StrictStr] = Field(default=None, description="Optional. Version of the batch runtime.")
    __properties: ClassVar[List[str]] = ["containerImage", "properties", "repositoryConfig", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RuntimeConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of repository_config
        if self.repository_config:
            _dict['repositoryConfig'] = self.repository_config.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RuntimeConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "containerImage": obj.get("containerImage"),
            "properties": obj.get("properties"),
            "repositoryConfig": RepositoryConfig.from_dict(obj["repositoryConfig"]) if obj.get("repositoryConfig") is not None else None,
            "version": obj.get("version")
        })
        return _obj


