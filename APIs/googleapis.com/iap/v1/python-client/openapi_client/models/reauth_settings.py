# coding: utf-8

"""
    Cloud Identity-Aware Proxy API

    Controls access to cloud applications running on Google Cloud Platform.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ReauthSettings(BaseModel):
    """
    Configuration for IAP reauthentication policies.
    """ # noqa: E501
    max_age: Optional[StrictStr] = Field(default=None, description="Reauth session lifetime, how long before a user has to reauthenticate again.", alias="maxAge")
    method: Optional[StrictStr] = Field(default=None, description="Reauth method requested.")
    policy_type: Optional[StrictStr] = Field(default=None, description="How IAP determines the effective policy in cases of hierarchical policies. Policies are merged from higher in the hierarchy to lower in the hierarchy.", alias="policyType")
    __properties: ClassVar[List[str]] = ["maxAge", "method", "policyType"]

    @field_validator('method')
    def method_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['METHOD_UNSPECIFIED', 'LOGIN', 'PASSWORD', 'SECURE_KEY', 'ENROLLED_SECOND_FACTORS']):
            raise ValueError("must be one of enum values ('METHOD_UNSPECIFIED', 'LOGIN', 'PASSWORD', 'SECURE_KEY', 'ENROLLED_SECOND_FACTORS')")
        return value

    @field_validator('policy_type')
    def policy_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['POLICY_TYPE_UNSPECIFIED', 'MINIMUM', 'DEFAULT']):
            raise ValueError("must be one of enum values ('POLICY_TYPE_UNSPECIFIED', 'MINIMUM', 'DEFAULT')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ReauthSettings from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ReauthSettings from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "maxAge": obj.get("maxAge"),
            "method": obj.get("method"),
            "policyType": obj.get("policyType")
        })
        return _obj


