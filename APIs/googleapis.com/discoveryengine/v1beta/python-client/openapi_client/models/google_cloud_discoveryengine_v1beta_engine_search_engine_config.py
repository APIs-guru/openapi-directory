# coding: utf-8

"""
    Discovery Engine API

    Discovery Engine API.

    The version of the OpenAPI document: v1beta
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class GoogleCloudDiscoveryengineV1betaEngineSearchEngineConfig(BaseModel):
    """
    Configurations for a Search Engine.
    """ # noqa: E501
    search_add_ons: Optional[List[StrictStr]] = Field(default=None, description="The add-on that this search engine enables.", alias="searchAddOns")
    search_tier: Optional[StrictStr] = Field(default=None, description="The search feature tier of this engine. Different tiers might have different pricing. To learn more, please check the pricing documentation. Defaults to SearchTier.SEARCH_TIER_STANDARD if not specified.", alias="searchTier")
    __properties: ClassVar[List[str]] = ["searchAddOns", "searchTier"]

    @field_validator('search_add_ons')
    def search_add_ons_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['SEARCH_ADD_ON_UNSPECIFIED', 'SEARCH_ADD_ON_LLM']):
                raise ValueError("each list item must be one of ('SEARCH_ADD_ON_UNSPECIFIED', 'SEARCH_ADD_ON_LLM')")
        return value

    @field_validator('search_tier')
    def search_tier_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['SEARCH_TIER_UNSPECIFIED', 'SEARCH_TIER_STANDARD', 'SEARCH_TIER_ENTERPRISE']):
            raise ValueError("must be one of enum values ('SEARCH_TIER_UNSPECIFIED', 'SEARCH_TIER_STANDARD', 'SEARCH_TIER_ENTERPRISE')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GoogleCloudDiscoveryengineV1betaEngineSearchEngineConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GoogleCloudDiscoveryengineV1betaEngineSearchEngineConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "searchAddOns": obj.get("searchAddOns"),
            "searchTier": obj.get("searchTier")
        })
        return _obj


