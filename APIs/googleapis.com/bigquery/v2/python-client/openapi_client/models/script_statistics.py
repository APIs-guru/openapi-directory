# coding: utf-8

"""
    BigQuery API

    A data platform for customers to create, manage, share and query data.

    The version of the OpenAPI document: v2
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from openapi_client.models.script_stack_frame import ScriptStackFrame
from typing import Optional, Set
from typing_extensions import Self

class ScriptStatistics(BaseModel):
    """
    Job statistics specific to the child job of a script.
    """ # noqa: E501
    evaluation_kind: Optional[StrictStr] = Field(default=None, description="Whether this child job was a statement or expression.", alias="evaluationKind")
    stack_frames: Optional[List[ScriptStackFrame]] = Field(default=None, description="Stack trace showing the line/column/procedure name of each frame on the stack at the point where the current evaluation happened. The leaf frame is first, the primary script is last. Never empty.", alias="stackFrames")
    __properties: ClassVar[List[str]] = ["evaluationKind", "stackFrames"]

    @field_validator('evaluation_kind')
    def evaluation_kind_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['EVALUATION_KIND_UNSPECIFIED', 'STATEMENT', 'EXPRESSION']):
            raise ValueError("must be one of enum values ('EVALUATION_KIND_UNSPECIFIED', 'STATEMENT', 'EXPRESSION')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ScriptStatistics from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in stack_frames (list)
        _items = []
        if self.stack_frames:
            for _item_stack_frames in self.stack_frames:
                if _item_stack_frames:
                    _items.append(_item_stack_frames.to_dict())
            _dict['stackFrames'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ScriptStatistics from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "evaluationKind": obj.get("evaluationKind"),
            "stackFrames": [ScriptStackFrame.from_dict(_item) for _item in obj["stackFrames"]] if obj.get("stackFrames") is not None else None
        })
        return _obj


