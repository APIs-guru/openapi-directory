# coding: utf-8

"""
    Kubernetes Engine API

    Builds and manages container-based applications, powered by the open source Kubernetes technology.

    The version of the OpenAPI document: v1beta1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from openapi_client.models.workload_config import WorkloadConfig
from typing import Optional, Set
from typing_extensions import Self

class ProtectConfig(BaseModel):
    """
    ProtectConfig defines the flags needed to enable/disable features for the Protect API.
    """ # noqa: E501
    workload_config: Optional[WorkloadConfig] = Field(default=None, alias="workloadConfig")
    workload_vulnerability_mode: Optional[StrictStr] = Field(default=None, description="Sets which mode to use for Protect workload vulnerability scanning feature.", alias="workloadVulnerabilityMode")
    __properties: ClassVar[List[str]] = ["workloadConfig", "workloadVulnerabilityMode"]

    @field_validator('workload_vulnerability_mode')
    def workload_vulnerability_mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['WORKLOAD_VULNERABILITY_MODE_UNSPECIFIED', 'DISABLED', 'BASIC']):
            raise ValueError("must be one of enum values ('WORKLOAD_VULNERABILITY_MODE_UNSPECIFIED', 'DISABLED', 'BASIC')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProtectConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of workload_config
        if self.workload_config:
            _dict['workloadConfig'] = self.workload_config.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProtectConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "workloadConfig": WorkloadConfig.from_dict(obj["workloadConfig"]) if obj.get("workloadConfig") is not None else None,
            "workloadVulnerabilityMode": obj.get("workloadVulnerabilityMode")
        })
        return _obj


