# coding: utf-8

"""
    Kubernetes Engine API

    Builds and manages container-based applications, powered by the open source Kubernetes technology.

    The version of the OpenAPI document: v1beta1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class NetworkPerformanceConfig(BaseModel):
    """
    Configuration of all network bandwidth tiers
    """ # noqa: E501
    external_ip_egress_bandwidth_tier: Optional[StrictStr] = Field(default=None, description="Specifies the network bandwidth tier for the NodePool for traffic to external/public IP addresses.", alias="externalIpEgressBandwidthTier")
    total_egress_bandwidth_tier: Optional[StrictStr] = Field(default=None, description="Specifies the total network bandwidth tier for the NodePool.", alias="totalEgressBandwidthTier")
    __properties: ClassVar[List[str]] = ["externalIpEgressBandwidthTier", "totalEgressBandwidthTier"]

    @field_validator('external_ip_egress_bandwidth_tier')
    def external_ip_egress_bandwidth_tier_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['TIER_UNSPECIFIED', 'TIER_1']):
            raise ValueError("must be one of enum values ('TIER_UNSPECIFIED', 'TIER_1')")
        return value

    @field_validator('total_egress_bandwidth_tier')
    def total_egress_bandwidth_tier_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['TIER_UNSPECIFIED', 'TIER_1']):
            raise ValueError("must be one of enum values ('TIER_UNSPECIFIED', 'TIER_1')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of NetworkPerformanceConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of NetworkPerformanceConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "externalIpEgressBandwidthTier": obj.get("externalIpEgressBandwidthTier"),
            "totalEgressBandwidthTier": obj.get("totalEgressBandwidthTier")
        })
        return _obj


