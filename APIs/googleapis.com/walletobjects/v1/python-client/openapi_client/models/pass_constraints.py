# coding: utf-8

"""
    Google Wallet API

    API for issuers to save and manage Google Wallet Objects.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class PassConstraints(BaseModel):
    """
    Container for any constraints that may be placed on passes.
    """ # noqa: E501
    nfc_constraint: Optional[List[StrictStr]] = Field(default=None, description="The NFC constraints for the pass.", alias="nfcConstraint")
    screenshot_eligibility: Optional[StrictStr] = Field(default=None, description="The screenshot eligibility for the pass.", alias="screenshotEligibility")
    __properties: ClassVar[List[str]] = ["nfcConstraint", "screenshotEligibility"]

    @field_validator('nfc_constraint')
    def nfc_constraint_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['NFC_CONSTRAINT_UNSPECIFIED', 'BLOCK_PAYMENT', 'BLOCK_CLOSED_LOOP_TRANSIT']):
                raise ValueError("each list item must be one of ('NFC_CONSTRAINT_UNSPECIFIED', 'BLOCK_PAYMENT', 'BLOCK_CLOSED_LOOP_TRANSIT')")
        return value

    @field_validator('screenshot_eligibility')
    def screenshot_eligibility_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['SCREENSHOT_ELIGIBILITY_UNSPECIFIED', 'ELIGIBLE', 'INELIGIBLE']):
            raise ValueError("must be one of enum values ('SCREENSHOT_ELIGIBILITY_UNSPECIFIED', 'ELIGIBLE', 'INELIGIBLE')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PassConstraints from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PassConstraints from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "nfcConstraint": obj.get("nfcConstraint"),
            "screenshotEligibility": obj.get("screenshotEligibility")
        })
        return _obj


