# coding: utf-8

"""
    Cloud Life Sciences API

    Cloud Life Sciences is a suite of services and tools for managing, processing, and transforming life sciences data.

    The version of the OpenAPI document: v2beta
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from openapi_client.models.mount import Mount
from openapi_client.models.secret import Secret
from typing import Optional, Set
from typing_extensions import Self

class Action(BaseModel):
    """
    Specifies a single action that runs a Docker container.
    """ # noqa: E501
    always_run: Optional[StrictBool] = Field(default=None, description="By default, after an action fails, no further actions are run. This flag indicates that this action must be run even if the pipeline has already failed. This is useful for actions that copy output files off of the VM or for debugging. Note that no actions will be run if image prefetching fails.", alias="alwaysRun")
    block_external_network: Optional[StrictBool] = Field(default=None, description="Prevents the container from accessing the external network.", alias="blockExternalNetwork")
    commands: Optional[List[StrictStr]] = Field(default=None, description="If specified, overrides the `CMD` specified in the container. If the container also has an `ENTRYPOINT` the values are used as entrypoint arguments. Otherwise, they are used as a command and arguments to run inside the container.")
    container_name: Optional[StrictStr] = Field(default=None, description="An optional name for the container. The container hostname will be set to this name, making it useful for inter-container communication. The name must contain only upper and lowercase alphanumeric characters and hyphens and cannot start with a hyphen.", alias="containerName")
    credentials: Optional[Secret] = None
    disable_image_prefetch: Optional[StrictBool] = Field(default=None, description="All container images are typically downloaded before any actions are executed. This helps prevent typos in URIs or issues like lack of disk space from wasting large amounts of compute resources. If set, this flag prevents the worker from downloading the image until just before the action is executed.", alias="disableImagePrefetch")
    disable_standard_error_capture: Optional[StrictBool] = Field(default=None, description="A small portion of the container's standard error stream is typically captured and returned inside the `ContainerStoppedEvent`. Setting this flag disables this functionality.", alias="disableStandardErrorCapture")
    enable_fuse: Optional[StrictBool] = Field(default=None, description="Enable access to the FUSE device for this action. Filesystems can then be mounted into disks shared with other actions. The other actions do not need the `enable_fuse` flag to access the mounted filesystem. This has the effect of causing the container to be executed with `CAP_SYS_ADMIN` and exposes `/dev/fuse` to the container, so use it only for containers you trust.", alias="enableFuse")
    encrypted_environment: Optional[Secret] = Field(default=None, alias="encryptedEnvironment")
    entrypoint: Optional[StrictStr] = Field(default=None, description="If specified, overrides the `ENTRYPOINT` specified in the container.")
    environment: Optional[Dict[str, StrictStr]] = Field(default=None, description="The environment to pass into the container. This environment is merged with values specified in the google.cloud.lifesciences.v2beta.Pipeline message, overwriting any duplicate values. In addition to the values passed here, a few other values are automatically injected into the environment. These cannot be hidden or overwritten. `GOOGLE_PIPELINE_FAILED` will be set to \"1\" if the pipeline failed because an action has exited with a non-zero status (and did not have the `IGNORE_EXIT_STATUS` flag set). This can be used to determine if additional debug or logging actions should execute. `GOOGLE_LAST_EXIT_STATUS` will be set to the exit status of the last non-background action that executed. This can be used by workflow engine authors to determine whether an individual action has succeeded or failed.")
    ignore_exit_status: Optional[StrictBool] = Field(default=None, description="Normally, a non-zero exit status causes the pipeline to fail. This flag allows execution of other actions to continue instead.", alias="ignoreExitStatus")
    image_uri: Optional[StrictStr] = Field(default=None, description="Required. The URI to pull the container image from. Note that all images referenced by actions in the pipeline are pulled before the first action runs. If multiple actions reference the same image, it is only pulled once, ensuring that the same image is used for all actions in a single pipeline. The image URI can be either a complete host and image specification (e.g., quay.io/biocontainers/samtools), a library and image name (e.g., google/cloud-sdk) or a bare image name ('bash') to pull from the default library. No schema is required in any of these cases. If the specified image is not public, the service account specified for the Virtual Machine must have access to pull the images from GCR, or appropriate credentials must be specified in the google.cloud.lifesciences.v2beta.Action.credentials field.", alias="imageUri")
    labels: Optional[Dict[str, StrictStr]] = Field(default=None, description="Labels to associate with the action. This field is provided to assist workflow engine authors in identifying actions (for example, to indicate what sort of action they perform, such as localization or debugging). They are returned in the operation metadata, but are otherwise ignored.")
    mounts: Optional[List[Mount]] = Field(default=None, description="A list of mounts to make available to the action. In addition to the values specified here, every action has a special virtual disk mounted under `/google` that contains log files and other operational components. - /google/logs All logs written during the pipeline execution. - /google/logs/output The combined standard output and standard error of all actions run as part of the pipeline execution. - /google/logs/action/*/stdout The complete contents of each individual action's standard output. - /google/logs/action/*/stderr The complete contents of each individual action's standard error output. ")
    pid_namespace: Optional[StrictStr] = Field(default=None, description="An optional identifier for a PID namespace to run the action inside. Multiple actions should use the same string to share a namespace. If unspecified, a separate isolated namespace is used.", alias="pidNamespace")
    port_mappings: Optional[Dict[str, StrictInt]] = Field(default=None, description="A map of containers to host port mappings for this container. If the container already specifies exposed ports, use the `PUBLISH_EXPOSED_PORTS` flag instead. The host port number must be less than 65536. If it is zero, an unused random port is assigned. To determine the resulting port number, consult the `ContainerStartedEvent` in the operation metadata.", alias="portMappings")
    publish_exposed_ports: Optional[StrictBool] = Field(default=None, description="Exposes all ports specified by `EXPOSE` statements in the container. To discover the host side port numbers, consult the `ACTION_STARTED` event in the operation metadata.", alias="publishExposedPorts")
    run_in_background: Optional[StrictBool] = Field(default=None, description="This flag allows an action to continue running in the background while executing subsequent actions. This is useful to provide services to other actions (or to provide debugging support tools like SSH servers).", alias="runInBackground")
    timeout: Optional[StrictStr] = Field(default=None, description="The maximum amount of time to give the action to complete. If the action fails to complete before the timeout, it will be terminated and the exit status will be non-zero. The pipeline will continue or terminate based on the rules defined by the `ALWAYS_RUN` and `IGNORE_EXIT_STATUS` flags.")
    __properties: ClassVar[List[str]] = ["alwaysRun", "blockExternalNetwork", "commands", "containerName", "credentials", "disableImagePrefetch", "disableStandardErrorCapture", "enableFuse", "encryptedEnvironment", "entrypoint", "environment", "ignoreExitStatus", "imageUri", "labels", "mounts", "pidNamespace", "portMappings", "publishExposedPorts", "runInBackground", "timeout"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Action from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of credentials
        if self.credentials:
            _dict['credentials'] = self.credentials.to_dict()
        # override the default output from pydantic by calling `to_dict()` of encrypted_environment
        if self.encrypted_environment:
            _dict['encryptedEnvironment'] = self.encrypted_environment.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in mounts (list)
        _items = []
        if self.mounts:
            for _item_mounts in self.mounts:
                if _item_mounts:
                    _items.append(_item_mounts.to_dict())
            _dict['mounts'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Action from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "alwaysRun": obj.get("alwaysRun"),
            "blockExternalNetwork": obj.get("blockExternalNetwork"),
            "commands": obj.get("commands"),
            "containerName": obj.get("containerName"),
            "credentials": Secret.from_dict(obj["credentials"]) if obj.get("credentials") is not None else None,
            "disableImagePrefetch": obj.get("disableImagePrefetch"),
            "disableStandardErrorCapture": obj.get("disableStandardErrorCapture"),
            "enableFuse": obj.get("enableFuse"),
            "encryptedEnvironment": Secret.from_dict(obj["encryptedEnvironment"]) if obj.get("encryptedEnvironment") is not None else None,
            "entrypoint": obj.get("entrypoint"),
            "environment": obj.get("environment"),
            "ignoreExitStatus": obj.get("ignoreExitStatus"),
            "imageUri": obj.get("imageUri"),
            "labels": obj.get("labels"),
            "mounts": [Mount.from_dict(_item) for _item in obj["mounts"]] if obj.get("mounts") is not None else None,
            "pidNamespace": obj.get("pidNamespace"),
            "portMappings": obj.get("portMappings"),
            "publishExposedPorts": obj.get("publishExposedPorts"),
            "runInBackground": obj.get("runInBackground"),
            "timeout": obj.get("timeout")
        })
        return _obj


