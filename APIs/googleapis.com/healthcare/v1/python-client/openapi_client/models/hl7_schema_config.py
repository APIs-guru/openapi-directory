# coding: utf-8

"""
    Cloud Healthcare API

    Manage, store, and access healthcare data in Google Cloud Platform.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from openapi_client.models.schema_group import SchemaGroup
from openapi_client.models.version_source import VersionSource
from typing import Optional, Set
from typing_extensions import Self

class Hl7SchemaConfig(BaseModel):
    """
    Root config message for HL7v2 schema. This contains a schema structure of groups and segments, and filters that determine which messages to apply the schema structure to.
    """ # noqa: E501
    message_schema_configs: Optional[Dict[str, SchemaGroup]] = Field(default=None, description="Map from each HL7v2 message type and trigger event pair, such as ADT_A04, to its schema configuration root group.", alias="messageSchemaConfigs")
    version: Optional[List[VersionSource]] = Field(default=None, description="Each VersionSource is tested and only if they all match is the schema used for the message.")
    __properties: ClassVar[List[str]] = ["messageSchemaConfigs", "version"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Hl7SchemaConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each value in message_schema_configs (dict)
        _field_dict = {}
        if self.message_schema_configs:
            for _key_message_schema_configs in self.message_schema_configs:
                if self.message_schema_configs[_key_message_schema_configs]:
                    _field_dict[_key_message_schema_configs] = self.message_schema_configs[_key_message_schema_configs].to_dict()
            _dict['messageSchemaConfigs'] = _field_dict
        # override the default output from pydantic by calling `to_dict()` of each item in version (list)
        _items = []
        if self.version:
            for _item_version in self.version:
                if _item_version:
                    _items.append(_item_version.to_dict())
            _dict['version'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Hl7SchemaConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "messageSchemaConfigs": dict(
                (_k, SchemaGroup.from_dict(_v))
                for _k, _v in obj["messageSchemaConfigs"].items()
            )
            if obj.get("messageSchemaConfigs") is not None
            else None,
            "version": [VersionSource.from_dict(_item) for _item in obj["version"]] if obj.get("version") is not None else None
        })
        return _obj


