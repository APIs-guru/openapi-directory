# coding: utf-8

"""
    Cloud Healthcare API

    Manage, store, and access healthcare data in Google Cloud Platform.

    The version of the OpenAPI document: v1beta1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from openapi_client.models.time_partitioning import TimePartitioning
from typing import Optional, Set
from typing_extensions import Self

class SchemaConfig(BaseModel):
    """
    Configuration for the FHIR BigQuery schema. Determines how the server generates the schema.
    """ # noqa: E501
    last_updated_partition_config: Optional[TimePartitioning] = Field(default=None, alias="lastUpdatedPartitionConfig")
    recursive_structure_depth: Optional[StrictStr] = Field(default=None, description="The depth for all recursive structures in the output analytics schema. For example, `concept` in the CodeSystem resource is a recursive structure; when the depth is 2, the CodeSystem table will have a column called `concept.concept` but not `concept.concept.concept`. If not specified or set to 0, the server will use the default value 2. The maximum depth allowed is 5.", alias="recursiveStructureDepth")
    schema_type: Optional[StrictStr] = Field(default=None, description="Specifies the output schema type. Schema type is required.", alias="schemaType")
    __properties: ClassVar[List[str]] = ["lastUpdatedPartitionConfig", "recursiveStructureDepth", "schemaType"]

    @field_validator('schema_type')
    def schema_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['SCHEMA_TYPE_UNSPECIFIED', 'LOSSLESS', 'ANALYTICS', 'ANALYTICS_V2']):
            raise ValueError("must be one of enum values ('SCHEMA_TYPE_UNSPECIFIED', 'LOSSLESS', 'ANALYTICS', 'ANALYTICS_V2')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SchemaConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of last_updated_partition_config
        if self.last_updated_partition_config:
            _dict['lastUpdatedPartitionConfig'] = self.last_updated_partition_config.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SchemaConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "lastUpdatedPartitionConfig": TimePartitioning.from_dict(obj["lastUpdatedPartitionConfig"]) if obj.get("lastUpdatedPartitionConfig") is not None else None,
            "recursiveStructureDepth": obj.get("recursiveStructureDepth"),
            "schemaType": obj.get("schemaType")
        })
        return _obj


