# coding: utf-8

"""
    Cloud Healthcare API

    Manage, store, and access healthcare data in Google Cloud Platform.

    The version of the OpenAPI document: v1beta1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class GcsDestination(BaseModel):
    """
    The Cloud Storage output destination. The Cloud Healthcare Service Agent requires the `roles/storage.objectAdmin` Cloud IAM roles on the Cloud Storage location.
    """ # noqa: E501
    content_structure: Optional[StrictStr] = Field(default=None, description="The format of the exported HL7v2 message files.", alias="contentStructure")
    message_view: Optional[StrictStr] = Field(default=None, description="Specifies the parts of the Message resource to include in the export. If not specified, FULL is used.", alias="messageView")
    uri_prefix: Optional[StrictStr] = Field(default=None, description="URI of an existing Cloud Storage directory where the server writes result files, in the format `gs://{bucket-id}/{path/to/destination/dir}`. If there is no trailing slash, the service appends one when composing the object path.", alias="uriPrefix")
    __properties: ClassVar[List[str]] = ["contentStructure", "messageView", "uriPrefix"]

    @field_validator('content_structure')
    def content_structure_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['CONTENT_STRUCTURE_UNSPECIFIED', 'MESSAGE_JSON']):
            raise ValueError("must be one of enum values ('CONTENT_STRUCTURE_UNSPECIFIED', 'MESSAGE_JSON')")
        return value

    @field_validator('message_view')
    def message_view_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['MESSAGE_VIEW_UNSPECIFIED', 'RAW_ONLY', 'PARSED_ONLY', 'FULL', 'SCHEMATIZED_ONLY', 'BASIC']):
            raise ValueError("must be one of enum values ('MESSAGE_VIEW_UNSPECIFIED', 'RAW_ONLY', 'PARSED_ONLY', 'FULL', 'SCHEMATIZED_ONLY', 'BASIC')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GcsDestination from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GcsDestination from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "contentStructure": obj.get("contentStructure"),
            "messageView": obj.get("messageView"),
            "uriPrefix": obj.get("uriPrefix")
        })
        return _obj


