# coding: utf-8

"""
    Dataflow API

    Manages Google Cloud Dataflow projects on Google Cloud Platform.

    The version of the OpenAPI document: v1b3
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class FlexTemplateRuntimeEnvironment(BaseModel):
    """
    The environment values to be set at runtime for flex template. LINT.IfChange
    """ # noqa: E501
    additional_experiments: Optional[List[StrictStr]] = Field(default=None, description="Additional experiment flags for the job.", alias="additionalExperiments")
    additional_user_labels: Optional[Dict[str, StrictStr]] = Field(default=None, description="Additional user labels to be specified for the job. Keys and values must follow the restrictions specified in the [labeling restrictions](https://cloud.google.com/compute/docs/labeling-resources#restrictions) page. An object containing a list of \"key\": value pairs. Example: { \"name\": \"wrench\", \"mass\": \"1kg\", \"count\": \"3\" }.", alias="additionalUserLabels")
    autoscaling_algorithm: Optional[StrictStr] = Field(default=None, description="The algorithm to use for autoscaling", alias="autoscalingAlgorithm")
    disk_size_gb: Optional[StrictInt] = Field(default=None, description="Worker disk size, in gigabytes.", alias="diskSizeGb")
    dump_heap_on_oom: Optional[StrictBool] = Field(default=None, description="If true, when processing time is spent almost entirely on garbage collection (GC), saves a heap dump before ending the thread or process. If false, ends the thread or process without saving a heap dump. Does not save a heap dump when the Java Virtual Machine (JVM) has an out of memory error during processing. The location of the heap file is either echoed back to the user, or the user is given the opportunity to download the heap file.", alias="dumpHeapOnOom")
    enable_launcher_vm_serial_port_logging: Optional[StrictBool] = Field(default=None, description="If true serial port logging will be enabled for the launcher VM.", alias="enableLauncherVmSerialPortLogging")
    enable_streaming_engine: Optional[StrictBool] = Field(default=None, description="Whether to enable Streaming Engine for the job.", alias="enableStreamingEngine")
    flexrs_goal: Optional[StrictStr] = Field(default=None, description="Set FlexRS goal for the job. https://cloud.google.com/dataflow/docs/guides/flexrs", alias="flexrsGoal")
    ip_configuration: Optional[StrictStr] = Field(default=None, description="Configuration for VM IPs.", alias="ipConfiguration")
    kms_key_name: Optional[StrictStr] = Field(default=None, description="Name for the Cloud KMS key for the job. Key format is: projects//locations//keyRings//cryptoKeys/", alias="kmsKeyName")
    launcher_machine_type: Optional[StrictStr] = Field(default=None, description="The machine type to use for launching the job. The default is n1-standard-1.", alias="launcherMachineType")
    machine_type: Optional[StrictStr] = Field(default=None, description="The machine type to use for the job. Defaults to the value from the template if not specified.", alias="machineType")
    max_workers: Optional[StrictInt] = Field(default=None, description="The maximum number of Google Compute Engine instances to be made available to your pipeline during execution, from 1 to 1000.", alias="maxWorkers")
    network: Optional[StrictStr] = Field(default=None, description="Network to which VMs will be assigned. If empty or unspecified, the service will use the network \"default\".")
    num_workers: Optional[StrictInt] = Field(default=None, description="The initial number of Google Compute Engine instances for the job.", alias="numWorkers")
    save_heap_dumps_to_gcs_path: Optional[StrictStr] = Field(default=None, description="Cloud Storage bucket (directory) to upload heap dumps to. Enabling this field implies that `dump_heap_on_oom` is set to true.", alias="saveHeapDumpsToGcsPath")
    sdk_container_image: Optional[StrictStr] = Field(default=None, description="Docker registry location of container image to use for the 'worker harness. Default is the container for the version of the SDK. Note this field is only valid for portable pipelines.", alias="sdkContainerImage")
    service_account_email: Optional[StrictStr] = Field(default=None, description="The email address of the service account to run the job as.", alias="serviceAccountEmail")
    staging_location: Optional[StrictStr] = Field(default=None, description="The Cloud Storage path for staging local files. Must be a valid Cloud Storage URL, beginning with `gs://`.", alias="stagingLocation")
    streaming_mode: Optional[StrictStr] = Field(default=None, description="Optional. Specifies the Streaming Engine message processing guarantees. Reduces cost and latency but might result in duplicate messages committed to storage. Designed to run simple mapping streaming ETL jobs at the lowest cost. For example, Change Data Capture (CDC) to BigQuery is a canonical use case.", alias="streamingMode")
    subnetwork: Optional[StrictStr] = Field(default=None, description="Subnetwork to which VMs will be assigned, if desired. You can specify a subnetwork using either a complete URL or an abbreviated path. Expected to be of the form \"https://www.googleapis.com/compute/v1/projects/HOST_PROJECT_ID/regions/REGION/subnetworks/SUBNETWORK\" or \"regions/REGION/subnetworks/SUBNETWORK\". If the subnetwork is located in a Shared VPC network, you must use the complete URL.")
    temp_location: Optional[StrictStr] = Field(default=None, description="The Cloud Storage path to use for temporary files. Must be a valid Cloud Storage URL, beginning with `gs://`.", alias="tempLocation")
    worker_region: Optional[StrictStr] = Field(default=None, description="The Compute Engine region (https://cloud.google.com/compute/docs/regions-zones/regions-zones) in which worker processing should occur, e.g. \"us-west1\". Mutually exclusive with worker_zone. If neither worker_region nor worker_zone is specified, default to the control plane's region.", alias="workerRegion")
    worker_zone: Optional[StrictStr] = Field(default=None, description="The Compute Engine zone (https://cloud.google.com/compute/docs/regions-zones/regions-zones) in which worker processing should occur, e.g. \"us-west1-a\". Mutually exclusive with worker_region. If neither worker_region nor worker_zone is specified, a zone in the control plane's region is chosen based on available capacity. If both `worker_zone` and `zone` are set, `worker_zone` takes precedence.", alias="workerZone")
    zone: Optional[StrictStr] = Field(default=None, description="The Compute Engine [availability zone](https://cloud.google.com/compute/docs/regions-zones/regions-zones) for launching worker instances to run your pipeline. In the future, worker_zone will take precedence.")
    __properties: ClassVar[List[str]] = ["additionalExperiments", "additionalUserLabels", "autoscalingAlgorithm", "diskSizeGb", "dumpHeapOnOom", "enableLauncherVmSerialPortLogging", "enableStreamingEngine", "flexrsGoal", "ipConfiguration", "kmsKeyName", "launcherMachineType", "machineType", "maxWorkers", "network", "numWorkers", "saveHeapDumpsToGcsPath", "sdkContainerImage", "serviceAccountEmail", "stagingLocation", "streamingMode", "subnetwork", "tempLocation", "workerRegion", "workerZone", "zone"]

    @field_validator('autoscaling_algorithm')
    def autoscaling_algorithm_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['AUTOSCALING_ALGORITHM_UNKNOWN', 'AUTOSCALING_ALGORITHM_NONE', 'AUTOSCALING_ALGORITHM_BASIC']):
            raise ValueError("must be one of enum values ('AUTOSCALING_ALGORITHM_UNKNOWN', 'AUTOSCALING_ALGORITHM_NONE', 'AUTOSCALING_ALGORITHM_BASIC')")
        return value

    @field_validator('flexrs_goal')
    def flexrs_goal_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['FLEXRS_UNSPECIFIED', 'FLEXRS_SPEED_OPTIMIZED', 'FLEXRS_COST_OPTIMIZED']):
            raise ValueError("must be one of enum values ('FLEXRS_UNSPECIFIED', 'FLEXRS_SPEED_OPTIMIZED', 'FLEXRS_COST_OPTIMIZED')")
        return value

    @field_validator('ip_configuration')
    def ip_configuration_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['WORKER_IP_UNSPECIFIED', 'WORKER_IP_PUBLIC', 'WORKER_IP_PRIVATE']):
            raise ValueError("must be one of enum values ('WORKER_IP_UNSPECIFIED', 'WORKER_IP_PUBLIC', 'WORKER_IP_PRIVATE')")
        return value

    @field_validator('streaming_mode')
    def streaming_mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['STREAMING_MODE_UNSPECIFIED', 'STREAMING_MODE_EXACTLY_ONCE', 'STREAMING_MODE_AT_LEAST_ONCE']):
            raise ValueError("must be one of enum values ('STREAMING_MODE_UNSPECIFIED', 'STREAMING_MODE_EXACTLY_ONCE', 'STREAMING_MODE_AT_LEAST_ONCE')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of FlexTemplateRuntimeEnvironment from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of FlexTemplateRuntimeEnvironment from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "additionalExperiments": obj.get("additionalExperiments"),
            "additionalUserLabels": obj.get("additionalUserLabels"),
            "autoscalingAlgorithm": obj.get("autoscalingAlgorithm"),
            "diskSizeGb": obj.get("diskSizeGb"),
            "dumpHeapOnOom": obj.get("dumpHeapOnOom"),
            "enableLauncherVmSerialPortLogging": obj.get("enableLauncherVmSerialPortLogging"),
            "enableStreamingEngine": obj.get("enableStreamingEngine"),
            "flexrsGoal": obj.get("flexrsGoal"),
            "ipConfiguration": obj.get("ipConfiguration"),
            "kmsKeyName": obj.get("kmsKeyName"),
            "launcherMachineType": obj.get("launcherMachineType"),
            "machineType": obj.get("machineType"),
            "maxWorkers": obj.get("maxWorkers"),
            "network": obj.get("network"),
            "numWorkers": obj.get("numWorkers"),
            "saveHeapDumpsToGcsPath": obj.get("saveHeapDumpsToGcsPath"),
            "sdkContainerImage": obj.get("sdkContainerImage"),
            "serviceAccountEmail": obj.get("serviceAccountEmail"),
            "stagingLocation": obj.get("stagingLocation"),
            "streamingMode": obj.get("streamingMode"),
            "subnetwork": obj.get("subnetwork"),
            "tempLocation": obj.get("tempLocation"),
            "workerRegion": obj.get("workerRegion"),
            "workerZone": obj.get("workerZone"),
            "zone": obj.get("zone")
        })
        return _obj


