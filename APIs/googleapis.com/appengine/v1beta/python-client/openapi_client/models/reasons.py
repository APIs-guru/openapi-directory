# coding: utf-8

"""
    App Engine Admin API

    Provisions and manages developers' App Engine applications.

    The version of the OpenAPI document: v1beta
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Reasons(BaseModel):
    """
    Containers transition between and within states based on reasons sent from various systems. CCFE will provide the CLH with reasons for the current state per system.The current systems that CCFE supports are: Service Management (Inception) Data Governance (Wipeout) Abuse (Ares) Billing (Internal Cloud Billing API) Service Activation (Service Controller)
    """ # noqa: E501
    abuse: Optional[StrictStr] = None
    billing: Optional[StrictStr] = None
    data_governance: Optional[StrictStr] = Field(default=None, alias="dataGovernance")
    service_activation: Optional[StrictStr] = Field(default=None, description="Consumer Container denotes if the service is active within a project or not. This information could be used to clean up resources in case service in DISABLED_FULL i.e. Service is inactive > 30 days.", alias="serviceActivation")
    service_management: Optional[StrictStr] = Field(default=None, alias="serviceManagement")
    __properties: ClassVar[List[str]] = ["abuse", "billing", "dataGovernance", "serviceActivation", "serviceManagement"]

    @field_validator('abuse')
    def abuse_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ABUSE_UNKNOWN_REASON', 'ABUSE_CONTROL_PLANE_SYNC', 'SUSPEND', 'REINSTATE']):
            raise ValueError("must be one of enum values ('ABUSE_UNKNOWN_REASON', 'ABUSE_CONTROL_PLANE_SYNC', 'SUSPEND', 'REINSTATE')")
        return value

    @field_validator('billing')
    def billing_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['BILLING_UNKNOWN_REASON', 'BILLING_CONTROL_PLANE_SYNC', 'PROBATION', 'CLOSE', 'OPEN']):
            raise ValueError("must be one of enum values ('BILLING_UNKNOWN_REASON', 'BILLING_CONTROL_PLANE_SYNC', 'PROBATION', 'CLOSE', 'OPEN')")
        return value

    @field_validator('data_governance')
    def data_governance_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['DATA_GOVERNANCE_UNKNOWN_REASON', 'DATA_GOVERNANCE_CONTROL_PLANE_SYNC', 'HIDE', 'UNHIDE', 'PURGE']):
            raise ValueError("must be one of enum values ('DATA_GOVERNANCE_UNKNOWN_REASON', 'DATA_GOVERNANCE_CONTROL_PLANE_SYNC', 'HIDE', 'UNHIDE', 'PURGE')")
        return value

    @field_validator('service_activation')
    def service_activation_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['SERVICE_ACTIVATION_STATUS_UNSPECIFIED', 'SERVICE_ACTIVATION_ENABLED', 'SERVICE_ACTIVATION_DISABLED', 'SERVICE_ACTIVATION_DISABLED_FULL', 'SERVICE_ACTIVATION_UNKNOWN_REASON']):
            raise ValueError("must be one of enum values ('SERVICE_ACTIVATION_STATUS_UNSPECIFIED', 'SERVICE_ACTIVATION_ENABLED', 'SERVICE_ACTIVATION_DISABLED', 'SERVICE_ACTIVATION_DISABLED_FULL', 'SERVICE_ACTIVATION_UNKNOWN_REASON')")
        return value

    @field_validator('service_management')
    def service_management_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['SERVICE_MANAGEMENT_UNKNOWN_REASON', 'SERVICE_MANAGEMENT_CONTROL_PLANE_SYNC', 'ACTIVATION', 'PREPARE_DEACTIVATION', 'ABORT_DEACTIVATION', 'COMMIT_DEACTIVATION']):
            raise ValueError("must be one of enum values ('SERVICE_MANAGEMENT_UNKNOWN_REASON', 'SERVICE_MANAGEMENT_CONTROL_PLANE_SYNC', 'ACTIVATION', 'PREPARE_DEACTIVATION', 'ABORT_DEACTIVATION', 'COMMIT_DEACTIVATION')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Reasons from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Reasons from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "abuse": obj.get("abuse"),
            "billing": obj.get("billing"),
            "dataGovernance": obj.get("dataGovernance"),
            "serviceActivation": obj.get("serviceActivation"),
            "serviceManagement": obj.get("serviceManagement")
        })
        return _obj


