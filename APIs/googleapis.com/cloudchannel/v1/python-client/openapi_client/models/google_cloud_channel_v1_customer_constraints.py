# coding: utf-8

"""
    Cloud Channel API

    The Cloud Channel API enables Google Cloud partners to have a single unified resale platform and APIs across all of Google Cloud including GCP, Workspace, Maps and Chrome.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class GoogleCloudChannelV1CustomerConstraints(BaseModel):
    """
    Represents constraints required to purchase the Offer for a customer.
    """ # noqa: E501
    allowed_customer_types: Optional[List[StrictStr]] = Field(default=None, description="Allowed Customer Type.", alias="allowedCustomerTypes")
    allowed_regions: Optional[List[StrictStr]] = Field(default=None, description="Allowed geographical regions of the customer.", alias="allowedRegions")
    promotional_order_types: Optional[List[StrictStr]] = Field(default=None, description="Allowed Promotional Order Type. Present for Promotional offers.", alias="promotionalOrderTypes")
    __properties: ClassVar[List[str]] = ["allowedCustomerTypes", "allowedRegions", "promotionalOrderTypes"]

    @field_validator('allowed_customer_types')
    def allowed_customer_types_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['CUSTOMER_TYPE_UNSPECIFIED', 'DOMAIN', 'TEAM']):
                raise ValueError("each list item must be one of ('CUSTOMER_TYPE_UNSPECIFIED', 'DOMAIN', 'TEAM')")
        return value

    @field_validator('promotional_order_types')
    def promotional_order_types_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['PROMOTIONAL_TYPE_UNSPECIFIED', 'NEW_UPGRADE', 'TRANSFER', 'PROMOTION_SWITCH']):
                raise ValueError("each list item must be one of ('PROMOTIONAL_TYPE_UNSPECIFIED', 'NEW_UPGRADE', 'TRANSFER', 'PROMOTION_SWITCH')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GoogleCloudChannelV1CustomerConstraints from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GoogleCloudChannelV1CustomerConstraints from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "allowedCustomerTypes": obj.get("allowedCustomerTypes"),
            "allowedRegions": obj.get("allowedRegions"),
            "promotionalOrderTypes": obj.get("promotionalOrderTypes")
        })
        return _obj


