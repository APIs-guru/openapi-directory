# coding: utf-8

"""
    Application Integration API

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class EnterpriseCrmLoggingGwsSanitizeOptions(BaseModel):
    """
    Identifies whether a field contains, or may contain, PII or sensitive data, and how to sanitize the field if it does. If a field's privacy type cannot be determined then it is sanitized (e.g., scrubbed). The specific sanitizer implementation is determined by run-time configuration and environment options (e.g., prod vs. qa). next_id: 5
    """ # noqa: E501
    is_already_sanitized: Optional[StrictBool] = Field(default=None, description="If true, the value has already been sanitized and needs no further sanitization. For instance, a D3 customer id is already an obfuscated entity and *might not* need further sanitization.", alias="isAlreadySanitized")
    log_type: Optional[List[StrictStr]] = Field(default=None, description="To which type(s) of logs the sanitize options apply.", alias="logType")
    privacy: Optional[StrictStr] = None
    sanitize_type: Optional[StrictStr] = Field(default=None, alias="sanitizeType")
    __properties: ClassVar[List[str]] = ["isAlreadySanitized", "logType", "privacy", "sanitizeType"]

    @field_validator('log_type')
    def log_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['LOG_TYPE_UNSPECIFIED', 'GWS', 'GTS', 'ALL']):
                raise ValueError("each list item must be one of ('LOG_TYPE_UNSPECIFIED', 'GWS', 'GTS', 'ALL')")
        return value

    @field_validator('privacy')
    def privacy_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['PRIVACY_TYPE_UNSPECIFIED', 'NOT_PII', 'PII', 'SPII', 'UNSURE']):
            raise ValueError("must be one of enum values ('PRIVACY_TYPE_UNSPECIFIED', 'NOT_PII', 'PII', 'SPII', 'UNSURE')")
        return value

    @field_validator('sanitize_type')
    def sanitize_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['SANITIZE_TYPE_UNSPECIFIED', 'SCRUB', 'ANONYMIZE', 'ANONYMIZE_LIMITED_REPEATABLE', 'OBFUSCATE', 'ENCRYPT', 'DO_NOT_SANITIZE']):
            raise ValueError("must be one of enum values ('SANITIZE_TYPE_UNSPECIFIED', 'SCRUB', 'ANONYMIZE', 'ANONYMIZE_LIMITED_REPEATABLE', 'OBFUSCATE', 'ENCRYPT', 'DO_NOT_SANITIZE')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EnterpriseCrmLoggingGwsSanitizeOptions from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EnterpriseCrmLoggingGwsSanitizeOptions from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "isAlreadySanitized": obj.get("isAlreadySanitized"),
            "logType": obj.get("logType"),
            "privacy": obj.get("privacy"),
            "sanitizeType": obj.get("sanitizeType")
        })
        return _obj


