
__doc__ = """ SDK Documentation: https://docs.github.com/enterprise-server@2.22/rest/ - GitHub Enterprise Developer Docs"""
import requests

from . import utils

from .actions import Actions
from .activity import Activity
from .apps import Apps
from .checks import Checks
from .code_scanning import CodeScanning
from .codes_of_conduct import CodesOfConduct
from .emojis import Emojis
from .enterprise_admin import EnterpriseAdmin
from .gists import Gists
from .git import Git
from .gitignore import Gitignore
from .issues import Issues
from .licenses import Licenses
from .markdown import Markdown
from .meta import Meta
from .oauth_authorizations import OauthAuthorizations
from .orgs import Orgs
from .projects import Projects
from .pulls import Pulls
from .rate_limit import RateLimit
from .reactions import Reactions
from .repos import Repos
from .search import Search
from .teams import Teams
from .users import Users


SERVERS = [
	"https://github.com/",
	"https://{protocol}://{hostname}/api/v3",
]


class SDK:
    r"""SDK Documentation: https://docs.github.com/enterprise-server@2.22/rest/ - GitHub Enterprise Developer Docs"""
    actions: Actions
    activity: Activity
    apps: Apps
    checks: Checks
    code_scanning: CodeScanning
    codes_of_conduct: CodesOfConduct
    emojis: Emojis
    enterprise_admin: EnterpriseAdmin
    gists: Gists
    git: Git
    gitignore: Gitignore
    issues: Issues
    licenses: Licenses
    markdown: Markdown
    meta: Meta
    oauth_authorizations: OauthAuthorizations
    orgs: Orgs
    projects: Projects
    pulls: Pulls
    rate_limit: RateLimit
    reactions: Reactions
    repos: Repos
    search: Search
    teams: Teams
    users: Users

    _client: requests.Session
    _security_client: requests.Session
    
    _server_url: str = SERVERS[0]
    _language: str = "python"
    _sdk_version: str = "0.0.1"
    _gen_version: str = "internal"

    def __init__(self) -> None:
        self._client = requests.Session()
        self._security_client = requests.Session()
        self._init_sdks()


    def config_server_url(self, server_url: str, params: dict[str, str]):
        if params is not None:
            self._server_url = utils.replace_parameters(server_url, params)
        else:
            self._server_url = server_url

        self._init_sdks()
    

    def config_client(self, client: requests.Session):
        self._client = client
        self._init_sdks()
    
    
    def _init_sdks(self):
        
        self.actions = Actions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.activity = Activity(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.apps = Apps(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.checks = Checks(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.code_scanning = CodeScanning(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.codes_of_conduct = CodesOfConduct(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.emojis = Emojis(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.enterprise_admin = EnterpriseAdmin(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.gists = Gists(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.git = Git(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.gitignore = Gitignore(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.issues = Issues(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.licenses = Licenses(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.markdown = Markdown(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.meta = Meta(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.oauth_authorizations = OauthAuthorizations(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.orgs = Orgs(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.projects = Projects(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.pulls = Pulls(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.rate_limit = RateLimit(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.reactions = Reactions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.repos = Repos(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.search = Search(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.teams = Teams(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.users = Users(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
    
    