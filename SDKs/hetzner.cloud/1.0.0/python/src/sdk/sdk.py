

import requests

from . import utils

from .actions import Actions
from .certificate_actions import CertificateActions
from .certificates import Certificates
from .datacenters import Datacenters
from .firewall_actions import FirewallActions
from .firewalls import Firewalls
from .floating_ip_actions import FloatingIPActions
from .floating_ips import FloatingIPs
from .isos import IsOs
from .image_actions import ImageActions
from .images import Images
from .load_balancer_actions import LoadBalancerActions
from .load_balancer_types import LoadBalancerTypes
from .load_balancers import LoadBalancers
from .locations import Locations
from .network_actions import NetworkActions
from .networks import Networks
from .placement_groups import PlacementGroups
from .pricing import Pricing
from .ssh_keys import SSHKeys
from .server_actions import ServerActions
from .server_types import ServerTypes
from .servers import Servers
from .volume_actions import VolumeActions
from .volumes import Volumes


SERVERS = [
	"https://api.hetzner.cloud/v1",
]


class SDK:
    
    actions: Actions
    certificate_actions: CertificateActions
    certificates: Certificates
    datacenters: Datacenters
    firewall_actions: FirewallActions
    firewalls: Firewalls
    floating_ip_actions: FloatingIPActions
    floating_i_ps: FloatingIPs
    is_os: IsOs
    image_actions: ImageActions
    images: Images
    load_balancer_actions: LoadBalancerActions
    load_balancer_types: LoadBalancerTypes
    load_balancers: LoadBalancers
    locations: Locations
    network_actions: NetworkActions
    networks: Networks
    placement_groups: PlacementGroups
    pricing: Pricing
    ssh_keys: SSHKeys
    server_actions: ServerActions
    server_types: ServerTypes
    servers: Servers
    volume_actions: VolumeActions
    volumes: Volumes

    _client: requests.Session
    _security_client: requests.Session
    
    _server_url: str = SERVERS[0]
    _language: str = "python"
    _sdk_version: str = "0.0.1"
    _gen_version: str = "internal"

    def __init__(self) -> None:
        self._client = requests.Session()
        self._security_client = requests.Session()
        self._init_sdks()


    def config_server_url(self, server_url: str, params: dict[str, str]):
        if params is not None:
            self._server_url = utils.replace_parameters(server_url, params)
        else:
            self._server_url = server_url

        self._init_sdks()
    

    def config_client(self, client: requests.Session):
        self._client = client
        self._init_sdks()
    
    
    def _init_sdks(self):
        
        self.actions = Actions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.certificate_actions = CertificateActions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.certificates = Certificates(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.datacenters = Datacenters(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.firewall_actions = FirewallActions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.firewalls = Firewalls(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.floating_ip_actions = FloatingIPActions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.floating_i_ps = FloatingIPs(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.is_os = IsOs(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.image_actions = ImageActions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.images = Images(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.load_balancer_actions = LoadBalancerActions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.load_balancer_types = LoadBalancerTypes(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.load_balancers = LoadBalancers(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.locations = Locations(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.network_actions = NetworkActions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.networks = Networks(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.placement_groups = PlacementGroups(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.pricing = Pricing(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.ssh_keys = SSHKeys(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.server_actions = ServerActions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.server_types = ServerTypes(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.servers = Servers(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.volume_actions = VolumeActions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.volumes = Volumes(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
    
    