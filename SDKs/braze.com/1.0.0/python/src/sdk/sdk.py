

import requests

from . import utils

from .campaign import Campaign
from .canvas import Canvas
from .content_blocks import ContentBlocks
from .custom_events import CustomEvents
from .email_lists_and_addresses import EmailListsAndAddresses
from .email_templates import EmailTemplates
from .export import Export
from .kpi import Kpi
from .messaging import Messaging
from .news_feed import NewsFeed
from .sms import Sms
from .schedule_mesages import ScheduleMesages
from .segment import Segment
from .session_analytics import SessionAnalytics
from .subscription_groups import SubscriptionGroups
from .templates import Templates


SERVERS = [
	"https://rest.iad-01.braze.com",
]


class SDK:
    
    campaign: Campaign
    canvas: Canvas
    content_blocks: ContentBlocks
    custom_events: CustomEvents
    email_lists_and_addresses: EmailListsAndAddresses
    email_templates: EmailTemplates
    export: Export
    kpi: Kpi
    messaging: Messaging
    news_feed: NewsFeed
    sms: Sms
    schedule_mesages: ScheduleMesages
    segment: Segment
    session_analytics: SessionAnalytics
    subscription_groups: SubscriptionGroups
    templates: Templates

    _client: requests.Session
    _security_client: requests.Session
    
    _server_url: str = SERVERS[0]
    _language: str = "python"
    _sdk_version: str = "0.0.1"
    _gen_version: str = "internal"

    def __init__(self) -> None:
        self._client = requests.Session()
        self._security_client = requests.Session()
        self._init_sdks()


    def config_server_url(self, server_url: str, params: dict[str, str]):
        if params is not None:
            self._server_url = utils.replace_parameters(server_url, params)
        else:
            self._server_url = server_url

        self._init_sdks()
    

    def config_client(self, client: requests.Session):
        self._client = client
        self._init_sdks()
    
    
    def _init_sdks(self):
        
        self.campaign = Campaign(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.canvas = Canvas(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.content_blocks = ContentBlocks(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.custom_events = CustomEvents(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.email_lists_and_addresses = EmailListsAndAddresses(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.email_templates = EmailTemplates(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.export = Export(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.kpi = Kpi(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.messaging = Messaging(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.news_feed = NewsFeed(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.sms = Sms(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.schedule_mesages = ScheduleMesages(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.segment = Segment(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.session_analytics = SessionAnalytics(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.subscription_groups = SubscriptionGroups(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.templates = Templates(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
    
    