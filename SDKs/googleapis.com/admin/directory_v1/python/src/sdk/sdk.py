
__doc__ = """ SDK Documentation: https://developers.google.com/admin-sdk/"""
import requests

from . import utils

from .asps import Asps
from .channels import Channels
from .chromeosdevices import Chromeosdevices
from .customer import Customer
from .customers import Customers
from .domainaliases import DomainAliases
from .domains import Domains
from .groups import Groups
from .members import Members
from .mobiledevices import Mobiledevices
from .orgunits import Orgunits
from .privileges import Privileges
from .resources import Resources
from .roleassignments import RoleAssignments
from .roles import Roles
from .schemas import Schemas
from .tokens import Tokens
from .twostepverification import TwoStepVerification
from .users import Users
from .verificationcodes import VerificationCodes


SERVERS = [
	"https://admin.googleapis.com/",
]


class SDK:
    r"""SDK Documentation: https://developers.google.com/admin-sdk/"""
    asps: Asps
    channels: Channels
    chromeosdevices: Chromeosdevices
    customer: Customer
    customers: Customers
    domain_aliases: DomainAliases
    domains: Domains
    groups: Groups
    members: Members
    mobiledevices: Mobiledevices
    orgunits: Orgunits
    privileges: Privileges
    resources: Resources
    role_assignments: RoleAssignments
    roles: Roles
    schemas: Schemas
    tokens: Tokens
    two_step_verification: TwoStepVerification
    users: Users
    verification_codes: VerificationCodes

    _client: requests.Session
    _security_client: requests.Session
    
    _server_url: str = SERVERS[0]
    _language: str = "python"
    _sdk_version: str = "0.0.1"
    _gen_version: str = "internal"

    def __init__(self) -> None:
        self._client = requests.Session()
        self._security_client = requests.Session()
        self._init_sdks()


    def config_server_url(self, server_url: str, params: dict[str, str]):
        if params is not None:
            self._server_url = utils.replace_parameters(server_url, params)
        else:
            self._server_url = server_url

        self._init_sdks()
    

    def config_client(self, client: requests.Session):
        self._client = client
        self._init_sdks()
    
    
    def _init_sdks(self):
        
        self.asps = Asps(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.channels = Channels(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.chromeosdevices = Chromeosdevices(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.customer = Customer(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.customers = Customers(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.domain_aliases = DomainAliases(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.domains = Domains(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.groups = Groups(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.members = Members(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.mobiledevices = Mobiledevices(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.orgunits = Orgunits(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.privileges = Privileges(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.resources = Resources(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.role_assignments = RoleAssignments(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.roles = Roles(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.schemas = Schemas(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.tokens = Tokens(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.two_step_verification = TwoStepVerification(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.users = Users(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.verification_codes = VerificationCodes(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
    
    