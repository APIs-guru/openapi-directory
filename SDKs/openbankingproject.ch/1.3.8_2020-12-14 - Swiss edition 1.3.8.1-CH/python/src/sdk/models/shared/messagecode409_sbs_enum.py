from dataclasses import dataclass, field
from enum import Enum

class MessageCode409SbsEnum(str, Enum):
    REFERENCE_STATUS_INVALID = "REFERENCE_STATUS_INVALID"
    STATUS_INVALID = "STATUS_INVALID"

