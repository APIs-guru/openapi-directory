

import requests
from sdk.models import shared
from . import utils

from .audit import Audit
from .candidate import Candidate
from .committee import Committee
from .communication_cost import CommunicationCost
from .dates import Dates
from .debts import Debts
from .disbursements import Disbursements
from .efiling import Efiling
from .electioneering import Electioneering
from .filer_resources import FilerResources
from .filings import Filings
from .financial import Financial
from .independent_expenditures import IndependentExpenditures
from .legal import Legal
from .loans import Loans
from .party_coordinated_expenditures import PartyCoordinatedExpenditures
from .presidential import Presidential
from .receipts import Receipts
from .search import Search


SERVERS = [
	"https://fec.gov/v1",
]


class SDK:
    
    audit: Audit
    candidate: Candidate
    committee: Committee
    communication_cost: CommunicationCost
    dates: Dates
    debts: Debts
    disbursements: Disbursements
    efiling: Efiling
    electioneering: Electioneering
    filer_resources: FilerResources
    filings: Filings
    financial: Financial
    independent_expenditures: IndependentExpenditures
    legal: Legal
    loans: Loans
    party_coordinated_expenditures: PartyCoordinatedExpenditures
    presidential: Presidential
    receipts: Receipts
    search: Search

    _client: requests.Session
    _security_client: requests.Session
    _security: shared.Security
    _server_url: str = SERVERS[0]
    _language: str = "python"
    _sdk_version: str = "0.0.1"
    _gen_version: str = "internal"

    def __init__(self) -> None:
        self._client = requests.Session()
        self._security_client = requests.Session()
        self._init_sdks()


    def config_server_url(self, server_url: str, params: dict[str, str]):
        if params is not None:
            self._server_url = utils.replace_parameters(server_url, params)
        else:
            self._server_url = server_url

        self._init_sdks()
    

    def config_client(self, client: requests.Session):
        self._client = client
        
        if self._security is not None:
            self._security_client = utils.configure_security_client(self._client, self._security)
        self._init_sdks()
    

    def config_security(self, security: shared.Security):
        self._security = security
        self._security_client = utils.configure_security_client(self._client, security)
        self._init_sdks()
    
    
    def _init_sdks(self):
        
        self.audit = Audit(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.candidate = Candidate(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.committee = Committee(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.communication_cost = CommunicationCost(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.dates = Dates(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.debts = Debts(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.disbursements = Disbursements(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.efiling = Efiling(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.electioneering = Electioneering(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.filer_resources = FilerResources(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.filings = Filings(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.financial = Financial(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.independent_expenditures = IndependentExpenditures(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.legal = Legal(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.loans = Loans(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.party_coordinated_expenditures = PartyCoordinatedExpenditures(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.presidential = Presidential(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.receipts = Receipts(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.search = Search(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
    
    