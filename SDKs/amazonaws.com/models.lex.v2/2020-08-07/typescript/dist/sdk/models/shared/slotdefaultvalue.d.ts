import { SpeakeasyBase } from "../../../internal/utils";
/**
 * Specifies the default value to use when a user doesn't provide a value for a slot.
**/
export declare class SlotDefaultValue extends SpeakeasyBase {
    defaultValue: string;
}
