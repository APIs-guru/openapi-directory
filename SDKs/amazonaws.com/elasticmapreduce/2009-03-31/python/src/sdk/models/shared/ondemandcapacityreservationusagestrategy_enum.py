from dataclasses import dataclass, field
from typing import Enum

class OnDemandCapacityReservationUsageStrategyEnum(str, Enum):
    USE_CAPACITY_RESERVATIONS_FIRST = "use-capacity-reservations-first"

