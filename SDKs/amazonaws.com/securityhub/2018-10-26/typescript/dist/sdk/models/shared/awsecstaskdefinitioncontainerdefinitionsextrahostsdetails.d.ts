import { SpeakeasyBase } from "../../../internal/utils";
/**
 * A hostname and IP address mapping to append to the <b>/etc/hosts</b> file on the container.
**/
export declare class AwsEcsTaskDefinitionContainerDefinitionsExtraHostsDetails extends SpeakeasyBase {
    hostname?: string;
    ipAddress?: string;
}
