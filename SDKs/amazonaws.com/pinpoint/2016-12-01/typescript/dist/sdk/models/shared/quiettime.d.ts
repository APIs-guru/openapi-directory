import { SpeakeasyBase } from "../../../internal/utils";
/**
 * Specifies the start and end times that define a time range when messages aren't sent to endpoints.
**/
export declare class QuietTime extends SpeakeasyBase {
    end?: string;
    start?: string;
}
