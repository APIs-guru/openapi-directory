import { SpeakeasyBase } from "../../../internal/utils";
/**
 * Information about the build badge for the build project.
**/
export declare class ProjectBadge extends SpeakeasyBase {
    badgeEnabled?: boolean;
    badgeRequestUrl?: string;
}
